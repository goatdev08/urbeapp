-- Rollback: 20260819000002_resolve_ad_zone.sql (subtarea #170.6)
-- Función 100% nueva (sin versión previa que restaurar) — drop basta.
-- Re-ejecutable (if exists).

drop function if exists public.resolve_ad_zone(double precision, double precision);

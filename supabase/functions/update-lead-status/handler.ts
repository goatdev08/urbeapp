// supabase/functions/update-lead-status/handler.ts
// STUB — implementación pendiente (fase RED, subtarea 15.6).
// Signatures correctas para que los tests importen sin error;
// la función lanza 'not_implemented' para que los tests fallen por excepción, no por import.

import type { UpdateLeadStatusDeps } from "./types.ts";

export async function handler(
  _req: Request,
  _deps?: UpdateLeadStatusDeps,
): Promise<Response> {
  throw new Error("not_implemented");
}

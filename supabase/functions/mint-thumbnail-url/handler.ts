// supabase/functions/mint-thumbnail-url/handler.ts
// STUB — subtarea 68.14 (fase RED). Sin lógica de negocio: únicamente hace que
// handler.test.ts falle por aserción/excepción en vez de por import roto.
// La implementación real (orquestación descrita en types.ts) llega en GREEN.

import type { MintThumbnailUrlDeps } from "./types.ts";

export function handler(_req: Request, _deps?: MintThumbnailUrlDeps): Promise<Response> {
  throw new Error("not_implemented");
}

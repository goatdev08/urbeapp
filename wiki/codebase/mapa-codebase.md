---
tipo: codebase
actualizado: 2026-08-16
---

# Mapa del codebase

**Pieza clave del vault:** dominio/concepto → archivos exactos. Consultar ANTES de escribir código (¿ya existe algo reutilizable?). Reemplaza `grep`.

## Backend — Supabase (existe)
Detalle de tablas/migraciones en [[db-schema-map]].

| Dominio | Archivos | Concepto |
|---------|----------|----------|
| Identidad | `supabase/migrations/..._identity_users.sql` | [[roles-y-permisos]] |
| Inmobiliarias/Agentes | `..._agencies_and_agents.sql` | [[inmobiliarias-y-agentes]] |
| Roles/Multi-tenant Ola 1 (#71) | **`20260805000001`** helper `private.is_premium(uid)` (premium derivado de properties vivas; rama compras → #76) · **`000002`+`000003`** enum `agency_member_role` a 4 niveles (admin/viewer) + matriz RLS (`private.agency_role_of`/`can_manage_agency_member`; admin gestiona todo EXCEPTO fila owner; owner edita properties de sus agentes) · **`000004`** RPC `upgrade_to_agent_atomic` (espejo de redeem para usuario existente) · **`000005`** `agent_applications.reason` (tabla 0003 REUSADA para el Camino B) · **`000006`** RPC `register_agency_atomic` (self-service → pending_approval, sin membresía) · **`000007`** triggers de máquina de estados de aprobaciones + auditoría `admin_actions` inmutable (actor JWT>GUC) · **`000008`+`000009`** enum `agency_member_status` +suspended, RPC `switch_agency_atomic` (desactivar ANTES de activar) y `properties_insert` exige membresía activa (suspendido no publica; independiente sí) · **`000010`** fix #99 (review PR #41): `MEMBER_OF_OTHER_AGENCY` (antes ALREADY_ACTIVE_MEMBER sin guía) + guard `role='admin'` nunca degrada en los 2 triggers de aprobación de 000007 · **`000011`** fix #100 (review PR #41): `publish_property_atomic` bloquea con `AGENCY_MEMBERSHIP_SUSPENDED` (antes bypaseaba la RLS de `properties_insert` por ser SECURITY DEFINER) + denormaliza `properties.agency_id` al publicar (antes letra muerta, con desempate ACTIVE-primero ante doble membresía) + backfill de properties ya publicadas + `properties_select`/`properties_update` reemplazan `is_agency_owner_of` por `agency_role_of(agency_id)` (cerraba un leak Y un falso-bloqueo cross-agencia tras `switch_agency_atomic`). Todos con rollback; pgTAP `20`–`26` (~360 asserts, guardian PASS ×7). Deuda registrada: #96 (EXECUTE PUBLIC en private.*), #97 (INSERT directo agencies), #98 (agencia sin owner activo — pendiente spec de baja de inmobiliaria, ver notas en la tarea), #101–#102 (hardening/polish, PR #41) | [[roles-y-permisos]], [[inmobiliarias-y-agentes]], [[rls-seguridad]] |
| Capacidad de publicidad + invitación por correo (#168, 2026-08-16, sin desplegar) | **`20260815000001`** `agencies` gana `can_publish_properties boolean default true`/`can_advertise boolean default false`/`advertiser_category` (enum nuevo `credito_hipotecario\|seguros\|mudanzas\|limpieza\|notaria\|avaluos\|otro`, nullable) + CHECK `agencies_al_menos_una_capacidad`; helper `private.org_can_advertise(agency_id)` (false para inexistente/deleted/inactiva/sin capacidad) — **sin backfill**, toda fila preexistente queda `(true,false,null)` = el statu quo exacto. **`20260815000002`** RPC **`set_org_advertising_atomic`** (solo `service_role`, revoke explícito a anon/authenticated/public — enciende Y apaga la capacidad, escribe `admin_actions` en la MISMA transacción; fault-injection pgTAP confirma rollback total). **`20260816000001`** `admin_create_agency_atomic` 9→11 params (`p_can_publish_properties`/`p_can_advertise`, DEFAULT al final — drop+create-or-replace del overload viejo, patrón calcado de `20260604000016`, mismo mecanismo con el que ya se blindó `admin_create_agency_atomic` una vez). **`20260816000002`** `publish_property_atomic` gana el guard `AGENCY_CANNOT_PUBLISH_PROPERTIES` (una organización solo-publicidad no puede publicar) **partiendo del cuerpo VIGENTE tras #167** (`20260815000004`, 20 params con `built_square_meters`/`half_bathrooms`/`currency`) — **NO** del cuerpo anterior a #167, que habría borrado en silencio esos 3 campos ya con backfill real en producción (167 mergeó y desplegó a medio camino de esta tarea; hubo que rebasear y re-anclar). **`20260816000003`** CHECK `agencies_categoria_requerida_para_anunciar` (`can_advertise=true` exige `advertiser_category` no nula — cierra un hueco que el guardian de `20260815000002` encontró: la RPC dejaba `can=true,cat=NULL` alcanzable en silencio) + guard `P0001 ADVERTISER_CATEGORY_REQUIRED` en `set_org_advertising_atomic`. **`20260816000004`** `admin_create_agency_atomic` 11→12 params (`p_advertiser_category`, DEFAULT al final — para que una organización solo-publicidad se pueda dar de alta en un solo paso sin violar el CHECK nuevo). Las 6 con rollback probado (`docker exec psql`); pgTAP `46_org_advertising_test.sql` (71 asserts, guardian PASS ×3 con mutation testing). **EF `admin-create-agency`**: pasa de `generateLink` (solo genera el link) a **`auth.admin.inviteUserByEmail`** (`_shared/auth_user.ts`+`_shared/clients.ts`, feature-detection sin romper los fakes viejos) — el correo se ENVÍA de verdad (verificado E2E contra Mailpit local), `invite_action_link` se conserva como respaldo (contrato publicado, builds instalados de `mobile/app/admin/agencies/[id].tsx` lo siguen leyendo), respuesta gana `email_sent`/`advertiser_category` aditivos; `_shared/agency.ts` (`AgencyCreateParams`) y `handler.ts` reenvían `can_publish_properties`/`can_advertise`/`advertiser_category` del payload SOLO si vienen definidos (para no pisar los DEFAULTs de la RPC con `null` explícito). **Mobile** (168.5, OTA): `mobile/app/admin/agencies/create.tsx` + `[id].tsx` — el copy pasa de "copia este link" a "le enviamos un correo a…", `invite_action_link` degrada a respaldo visual (nunca se oculta), error `AUTH_INVITE_FAILED` con mensaje accionable (reintentar funciona: GoTrue ya revirtió el usuario). 🔴 **BLOQUEANTE pendiente antes del flip de Resend en remoto** (ver `docs/TODO-pendientes.md` §1.1): `OWNER_INVITE_REDIRECT_TO='urbea://'` (`_shared/clients.ts:173`) apunta a la raíz, donde nadie monta `useSessionFromDeepLink` — el destino correcto ya existe en `reset-password.tsx`. Derivadas: **#173** (hardening: mapeo de códigos de error de constraints de `agencies`), **#174** (hardening: cobertura del passthrough condicional de `advertiser_category` en `_shared/clients.ts`), **#176**/**#177** (hallazgos de guardian en 168.2/168.4). | [[inmobiliarias-y-agentes]], [[rls-seguridad]] |
| Anuncios: creativo, campaña por zona y moderación (#169, 2026-08-17, **sin desplegar**) | **`20260816000005`** 4 tablas + 4 enums: `ad_creatives` (video, `duration_seconds` CHECK 6–30, `uploading→processing→ready|failed`), `ads` (`ad_status` **sin `approved`** — un intermedio entre `pending_review` y `active` es el deadlock que #153 cortó en propiedades), `ad_zones` (municipio **XOR** colonia contra el catálogo de #157; `neighborhood_id` **bigint**, `municipality_id` **text**/cvegeo), `ad_prices` (3 alcances sembrados, PRD §17.2). 🔒 **Cero filas en `ad_zones` = inventario nacional, NO error**. **`20260816000006`** trigger de estados (`draft→active` directo **falla**; `rejected`/`expired` terminales; admin obligatorio vía `private.resolve_admin_actor()`; auditoría en `admin_actions` en la MISMA transacción o la transición se revierte) + **extiende `handle_agency_status_change` con `active↔suspended`** (la decisión "suspender pausa el reloj" presuponía un estado **inalcanzable**: la máquina de #71.5 solo admitía `pending_approval→active|rejected`) + cascada que pausa los anuncios vigentes conservando los días, con `paused_by_suspension` como discriminador (sin él, reactivar la organización **resucitaría un anuncio apagado por moderación**) + CHECK `ads_paused_at_matches_status` (reemplazó un `coalesce` que era código de producción moldeado por un fixture incoherente: habría devuelto **cero días en silencio**). **`20260816000007`** RPC **`grant_ad_slot_atomic`** (solo `service_role`; ad + zonas + vigencia + auditoría en una transacción; guard `AGENCY_CANNOT_ADVERTISE` delegado en `private.org_can_advertise`) + siembra `app_config.ads_free`. **`20260816000008`** wrapper `public.org_can_advertise` que **delega** en la private — 🔴 el adapter la llamaba por `client.rpc()` y **PostgREST no expone `private`**, así que la EF devolvía **403 a todo anunciante legítimo** con los 32 tests en verde; solo lo cazó una sonda real. Grants revocados a public/anon/authenticated. Las 4 con rollback probado; pgTAP `47`–`49` + `WRAP1-9` en el `46` (**1319 asserts en 50 archivos**, guardián PASS en las 5 subtareas de backend). **EFs**: `mint-ad-upload-url/` (`maxDurationSeconds=30`, concurrencia scoped por `agency_id` sobre `ad_creatives` — la separación con el 409 de propiedades sale **por tabla, sin un solo condicional de dominio**; reaper `STALE_UPLOAD_MS` heredado de #103) · `stream-webhook` **extendido de forma ADITIVA** (la rama de anuncios solo se alcanza si el UPDATE sobre `property_videos` afecta 0 filas; **la suite existente pasa sin modificarse** — criterio de aceptación, no recomendación; valida la duración **cruda** y redondea después, porque al revés un video de 5.7 s entra violando el mínimo; re-lanza en vez de tragarse el error porque `processing` no tiene ventana de expiración) · `mint-ad-urls/` (calco de `mint-poster-urls`: autz fail-closed **por item**, token en el PATH, vigencia además de status). **Mobile**: `features/ads/lib/validation.ts` (fronteras 6–30 inclusive sobre el valor crudo, `AD_DURATION_INVALID` **literal compartido con el servidor**, CTA por **allowlist** de esquema con `normalized_value` — se validaba una cadena y se guardaba otra) · `hooks/useAdUpload.ts` (recupera el **verify-before-failing** de #103, que la copia había perdido) · `hooks/useCanAdvertise.ts` + `app/(protected)/ads/_layout.tsx` (🔒 **sin capacidad la RUTA no existe**; **falla cerrado ante 42703/42P01** para que un OTA anticipado deje la feature apagada, no rota) · `app/(protected)/ads/new/` (wizard de 5 pasos; reusa SelectionCard, StepIndicator, usePlaceSearch y MapSearchSuggestions contra `search_places` de #157). ⚠️ **No hay ruta para que el anunciante cree su campaña** (`ads` solo tiene `select` para `authenticated`): el wizard solo persiste el creativo → **#191**. Derivadas: **#183** (ventana del reaper duplicada), **#188** (`processing` sin expiración), **#189** (fail-closed vs pickers Android viejos), **#190** (`database.types.ts` 10 migraciones atrás), **#181**/**#182** (asserts de #169.3). | [[publicidad-anuncios]], [[inmobiliarias-y-agentes]], [[moderacion]] |
| Propiedades/Video | `..._properties_and_videos.sql` | [[propiedades-y-video]] |
| Publicación completa Ola 1 (#73) | **`20260809000002`** `property_status` +10 estados operativos (aditivo puro, migración SOLA por el gotcha `ADD VALUE`: `uploading_media`,`media_failed`,`pending_payment`,`approved`,`expired`,`rented`,`sold`,`rejected`,`deleted_soft`,`deleted_hard` — enum final 17 valores; `closed` se queda vivo para filas históricas, código nuevo ya no lo escribe) · **`000003`** tabla `property_revisions` (doble versión §15.6: snapshot `changed_fields` jsonb + `status` `pending`\|`needs_changes`\|`rejected`\|`approved`; índice único parcial "máx 1 activa pending/needs_changes por propiedad"; escritura exclusiva `service_role`, RLS SELECT owner/admin) · **`000004`** tabla `property_video_slots` (abstracción de vigencia del slot consumido al publicar, PRD §2.2/§17.1 — semilla mínima para #76/pagos: `property_id` unique, `is_free` default true, `started_at`; escritura exclusiva de la RPC) · **`000005`** `publish_property_atomic` extendida a 16 params (`p_property_status` reemplaza el `'active'` hardcodeado, default `'active'` backward-compat) + inserta el slot atómicamente (fault-injection pgTAP confirma rollback total si el INSERT del slot falla). **EF `publish-property` reescrita**: pipeline `videoStatusChecker`(ready+60-120s exacto)→`duplicatePropertyChecker`(mismo owner+dirección normalizada, excluye rejected/deleted_soft/deleted_hard)→publisher, códigos `VIDEO_NOT_FOUND`=404/`VIDEO_NOT_READY`=409/`VIDEO_DURATION_INVALID`=400/`DUPLICATE_PROPERTY`=409; **TODA publicación va a `pending_review`** (PRD §14.2 — ya no hay auto-`active`). **EF nueva `edit-property/`** (73.6, PRD §15.5/§15.6 — reemplaza el UPDATE directo por RLS que usaba `usePublish.ts` en editMode desde #53): diff campo-a-campo contra el snapshot actual — dirección/coords/operación/tipo/precio/descripción cambian → `revision_upserter.ts` crea/actualiza `property_revisions` (current_published intacta); el resto aplica directo. `location.ts` parsea EWKB hex (NDR+XDR) de PostgREST vs EWKT del cliente para el diff de coordenadas. **EF nueva `moderate-property/`** (73.9, PRD §15.6, 1 EF unificada — decisión: NO 4 EFs separadas): `action` parametrizada `approve`\|`needs_changes`\|`reject`\|`suspend`; con revisión activa → approve aplica `changed_fields` sobre `properties` vía whitelist `project_property_snapshot_fields` (properties.status NO cambia) + revisión→resuelta; sin revisión → transiciona `properties.status` directo desde `pending_review` (si no, `NOTHING_TO_MODERATE` 400); `suspend` SIEMPRE directo a `properties.status='suspended'`, nunca toca `property_revisions`, bloqueado en estados terminales `{sold,rented,deleted_hard,deleted_soft}`; TODAS las ramas registran en `admin_actions` (fallo de auditoría también 500, no best-effort). **`update-property-status` extendida** (73.8, PRD §16.1): `VALID_TRANSITIONS` gana `rented`/`sold` como destino directo (sin `closed_reason`, autodescriptivo) desde `active`/`paused`/`approved`, ambos terminales sin reapertura. **Wizard 5 pasos** (73.3, antes 3): `mobile/app/(protected)/publish/step{1..5}.tsx` (1 operación+tipo, 2-3 obligatorios con dirección/mapa/precio/visibilidad, 4 opcionales, 5 video+publicar) + `hooks/useDraftAutosave.ts` (autoguardado a `status='draft'` con debounce 1500ms, gate = quinteto NOT NULL operation_type/property_type/price/address/location). **Publicación rápida** (73.7, PRD §15.3): sin código nuevo en el hook — 73.4 ya manda TODA publicación a `pending_review`; el gap real era UI (`PropertyListItem.tsx` no traducía los estados nuevos) — cerrado con `STATUS_BADGE`+`CONTEXTUAL_SUBROW` para pending_review/needs_changes/rejected + fallback `humanize_status()`. 🔴 **3 defectos críticos encontrados contra la DB real** (ver [[moderacion]] y `wiki/log.md` 2026-08-09): (1) `index.ts` de `publish-property` nunca reenviaba `property_status` a la RPC — ninguna propiedad habría llegado jamás a `pending_review` en producción pese a 47 tests DI en verde; (2) `location.ts` de `edit-property` solo se había ejercitado con EWKT mockeado en ambos lados — mutation testing expuso el hueco (sin bug genuino, cerrado con 11 tests EWKB reales); (3) `apply_revision_snapshot` de `moderate-property` hacía spread crudo de `changed_fields` (incluye `property_id`) → approve con revisión SIEMPRE fallaba 500 real ("column property_id does not exist") — corregido con whitelist explícito. Los 3 solo aparecieron al verificar contra el stack local real, nunca contra mocks. pgTAP `36`-`38` (~90 asserts) + Deno 968/968 + Jest 988/988, guardian PASS en las 3 subtareas críticas. E2E completo del ciclo (73.10) verificado contra stack local real: publish→pending_review→approve→active→edit(precio)→revision pending→approve→snapshot aplicado→close(rented)→sin reapertura, + bonus needs_changes/reject/suspend — sin hallazgos nuevos (las 3 correcciones de arriba ya sostenían el flujo completo) 🆕 **Quick fixes wizard paso 3 (sesión 2026-08-15, sin tarea de Taskmaster — detalle completo en `wiki/log.md`):** 3 columnas aditivas en `properties` (`20260815000003`): `built_square_meters` (superficie construida, `square_meters` pasa a significar terreno), `half_bathrooms`, `currency` (`MXN`\|`USD` default `MXN`, solo etiqueta sin conversión de tipo de cambio). `publish_property_atomic` extendida a 20 params (`20260815000004`, mismo patrón DROP+CREATE OR REPLACE); `moderate_property_atomic` snapshot-apply gana los 3 campos como no-críticos (`20260815000005`, misma firma). En `publish-property` los 3 viajan con default (currency valida enum); en `edit-property` son **OPCIONALES** — a diferencia del resto de campos (#142, clave siempre requerida) — porque son nuevos sobre un contrato YA publicado: un build instalado sin el OTA de este cambio nunca los manda, y tratarlos como body-parcial-rechazado rompería la edición para todo cliente viejo (§0.5.2). Wizard: `step1.tsx` retira 'Ambos' del picker (enum/DB conservan `'both'` por compat con propiedades existentes — filtros/tarjetas lo siguen mostrando); `step3.tsx` gana selector MXN/USD junto al precio, formateo con comas de miles mientras se teclea (solo dígitos), fila Terreno/Construida lado a lado, stepper de medios baños junto a baños, y errores inline por campo (borde rojo + mensaje) que solo se pintan tras un intento de avanzar inválido — se quitó la caja resumen fija sobre el botón, y el botón ya no usa `disabled` (debe poder recibir el press que dispara `attempted_submit`); `MapPicker.tsx` cambia de tap-to-place+marker arrastrable a **modo mira central estilo Uber/Airbnb** (pin fijo `position:absolute` al centro del viewport, `onRegionChangeComplete` con `details.isGesture` filtra recentrados programáticos); `step4.tsx` reemplaza los 3 `Switch` de características por `FilterChipGroup` (promovido de `features/search/components/` a `src/components/FilterChipGroup.tsx` como componente cross-feature — cierra tarea #167; ⚠️ #167 solo existe en la rama sin mergear `tarea/168-org-can-advertise`, no en `main` — reconciliar el `set-status done` cuando esa rama mergee). Verificación: 41+17 asserts pgTAP nuevos + suite completa (0 fallos), 68+72 Deno tests nuevos + suite completa 1026/1026, Jest 1072/1072 (93 suites), tsc/lint 0. 🆕 **2ª ronda (mismo día, tras smoke manual en Android+iOS):** `CompactStepper.tsx` nuevo (label+hint FUERA de la caja con borde, controles en `space-between` proporcional al ancho de columna) reemplaza a `NumericStepper` en Recámaras/Baños/Medios baños — `NumericStepper.tsx` quedó huérfano y **se borró**. **Feed** (`PropertyOverlay.tsx`): 2 chips sobre la dirección (operación con el `op-badge` canónico salvia/arcilla + tipo en glass oscuro), precio con divisa como **sufijo siempre** (`$15,000 MXN`), y `price_visible=false` → "Precio a consultar" (se capturaba desde #73 pero el feed lo ignoraba — bug latente); `FEED_SELECT` gana `operation_type,property_type,currency,price_visible` con fail-open al mapear. **`mobile/src/lib/formatPrice.ts` nuevo** = formateador ÚNICO `format_price(price, currency?)` consumido por feed, detalle, `PropertyGridCard` (perfil+guardados) y `shareProperty` — antes había 4 con MXN hardcodeado (⚠️ NO confundir con `src/features/map/lib/formatPrice.ts`, que es el compacto del mapa: `format_compact_price`/`format_full_price`; **deuda conocida: el mini-card del mapa aún no muestra divisa**). El detalle muestra además `2 baños + 1 medio` y `120 m² terreno · 85 m² const.`. **"Cambiar video" cancela la subida en curso:** `useVideoUpload` gana `cancel()` + supersede por token de generación (`upload_seq_ref` + `AbortSignal`; un upload cancelado/superado nunca vuelve a escribir status/error/form) y `mint-upload-url` acepta `{ replace: true }` → soft-borra los pendientes del caller **con `property_id IS NULL`** antes del chequeo de concurrencia (sin el flag, contrato idéntico → builds pre-OTA sin cambio de comportamiento). Antes, tocar "Cambiar video" durante una subida daba 409 `UPLOAD_IN_PROGRESS` hasta que el reaper de 15 min (#103) liberaba la fila. 🚀 **Desplegado al remoto `mvpvqmyhrrkwbnpctpuq`** (2026-08-15): 3 migraciones vía `apply_migration` (el `default 'MXN'` hizo el backfill de las 20 propiedades sin UPDATE manual) + EFs `publish-property`/`edit-property`/`mint-upload-url`. **Orden obligado: migración ANTES del OTA** — `FEED_SELECT` pide `currency` y PostgREST falla ante columna inexistente, así que un OTA adelantado rompería el feed entero de los builds instalados. | [[moderacion]], [[propiedades-y-video]] |
| Fixes bloqueantes del review de #73 (#126–#130, #142) | **`20260809000006`** `publish_property_atomic` a 17 params: `p_price_visible` (default true = default de columna) llega a la fila real (#129 — antes el toggle del wizard moría en la EF) · **`000007`** RPC **`moderate_property_atomic`** (#130): snapshot de revisión POR PRESENCIA de clave jsonb (`?` — ausente=no tocar, null=borrar, misma distinción que #142), transición de status, resolución de `property_revisions` y auditoría `admin_actions` en UNA transacción (patrón `publish_property_atomic`; fault-injection pgTAP 39 demuestra rollback total) — reemplaza los 3 seams de escritura sueltos de `moderate-property` (`make_property_updater`/`make_revision_resolver`/`make_admin_action_recorder` → `make_moderation_writer`); la state machine sigue en el handler · **`000008`** `properties_insert` exige `status='draft'` (#127.7 — el único INSERT de cliente legítimo es el autosave; antes cualquier anon key + JWT agente creaba `active` sin moderación; pgTAP 40 con impersonación). **EF `update-property-status`** (#128): `VALID_TRANSITIONS` = Record exhaustivo sobre los 17 estados (compile-break al agregar valores; contrato 17×6 en tests; `PropertyStatusTarget` separa lo que el cliente puede pedir) · **EF `edit-property`** (#142): seam `AgencyRoleResolver` restaura la rama `agency_role_of(agency_id) in (owner,admin)` de #71 perdida al migrar del UPDATE directo, y el parser exige body COMPLETO (parcial → 400; antes coaccionaba ausentes a falsy y borraba datos en silencio) · **EF `publish-property`/`_shared`** (#126): checker alineado con la RPC (`processing\|ready`), duración validada SOLO cuando se conoce (ready+null ya no es 400 sin salida) — hallazgo: **nadie escribe `processing` en DB jamás** (uploading→webhook→ready) · **EF `moderate-property`** (#130): approve-con-revisión desde `pending_review`/`needs_changes` TAMBIÉN activa (antes quedaba invisible para siempre; needs_changes reenviada era un deadlock sin salida) y la respuesta reporta el status RESULTANTE. **Móvil**: `useVideoReady` (pollea el `ready` real de `property_videos`; step5 bloquea Publicar con "Procesando video…" hasta entonces), `validate_video_duration_ms` (60–120s al ELEGIR el video, fail-open si el picker no reporta duración), `useDraftAutosave` reescrito (#127: `owner_user_id` de sesión — sin él NINGÚN insert pasaba; in-flight flag; errores logueados; flush al desmontar — EC-9 INVERTIDO, el test viejo fijaba el bug; draft id a nivel de módulo `get_current_draft_id`/`clear_current_draft`, `usePublish` lo soft-deletea al publicar). Todo desplegado al remoto 2026-08-09 (migraciones vía MCP, EFs `--use-api`, OTA runtimes `06760f36…`/`aeb9a9b3…` = builds 1.0.4). Abiertas: #131–#141 (no bloqueantes; #141 = UI de moderación, mientras tanto curl/Studio) | [[moderacion]], [[propiedades-y-video]], [[rls-seguridad]] |
| Storage (video) | `..._storage_property_videos.sql` (0011: bucket `property-videos` + col `storage_path` + RLS INSERT/SELECT en `storage.objects`) · `..._property_videos_ready_requires_storage.sql` (0012: CHECK `ready` exige `storage_path` o `cloudflare_uid`) · `20260710000001_increase_video_size_limit.sql` (#49: `file_size_limit` 100→**500 MB**, idempotente con WHERE guard) · `20260710000002_fix_property_videos_select_rls.sql` (#52: policy SELECT **solo dueño** — rama pública muerta `[2]::uuid` eliminada, cerraba hueco con paths fabricados de 3 seg; lectura pública = solo EF `mint-video-url`; pgTAP `11`. ⚠️ el **límite GLOBAL de subida del proyecto** — 50 MB en plan Free — capea al bucket: subir >50 MB al remoto da 413) · `20260720000001_stream_schema.sql` (#68.2, Épica B: **aditivo** — enum `property_video_status` +`archived`; `property_videos` +4 cols nullable `tus_upload_url`/`thumbnail_pct`(check 0-100)/`archived_at`/`r2_archive_key`; NO duplica `ready_at`/`failure_reason` que ya existían en 0005; +rollback; pgTAP `12` 17 asserts. Aplicado a local+remoto, paridad verificada) · `20260720000002_property_videos_upload_first.sql` (#68.3, Épica B **upload-first**: `property_id` → **NULLABLE** (video se sube a Stream antes de existir la propiedad); +col `agent_id` uuid→`users` (atribuye el video en vuelo → invariante de concurrencia por agente); índice parcial `(agent_id,status)`; CHECK `property_videos_attributable` (property_id **o** agent_id no nulo); trigger `enforce_max_videos` tolera property_id NULL; +rollback; pgTAP `13` 9 asserts. Local+remoto, paridad verificada) · **`20260807000001_video_in_flight_owner_select.sql`** (#103.1-A, causa raíz del falso negativo del upload: la policy `videos_select` gana una **tercera rama** `(property_id is null and agent_id = (select auth.uid()) and deleted_at is null)` — las dos ramas previas dependen de `property_id` y con el upload-first (`property_id=NULL`) `private.property_is_public(NULL)`/`private.can_manage_property(NULL)` son SIEMPRE false → **el agente no podía leer su propio video en vuelo**, así que el cliente no tenía forma de verificar nada. Acotada ESTRICTAMENTE al dueño, **sin helpers de agencia/membresía a propósito** (precedente vivo: la #100 tuvo que reemplazar un helper de "membresía compartida" que filtraba filas de otra agencia); `deleted_at is null` = fail-closed explícito. +rollback; pgTAP `27_video_in_flight_select_test.sql` `plan(8)` — 2 DELTA + 6 invariantes/no-regresión, incluido el ancla de que el dueño **NO** ve su propia fila soft-deleted. Guardian con mutación real: quitar la rama → 2 rojos, sin `deleted_at` → 1 rojo, sin scoping por agente → 2 rojos. **Desplegada al remoto** como `20260807234556`) | [[propiedades-y-video]], [[rls-seguridad]] |
| Config de negocio (`app_config`) | `20260720000001_stream_schema.sql` (#68.2: tabla `public.app_config(key text pk, value jsonb, updated_at)` **fail-closed** — RLS ON, policy SELECT solo `private.is_admin()`, `service_role` bypassa/escribe; seed `video_slot_free=true`, `archived_retention_days=7`, `signed_url_ttl_seconds=14400`) | [[propiedades-y-video]], [[rls-seguridad]] |
| Storage (foto perfil) | `..._profile_photos_storage.sql` (0015, #6: bucket **público** `profile-photos` + RLS por path `auth.uid()` en `storage.objects` + cols `full_name`/`profile_photo_url` en `user_preferences`) | [[onboarding-y-preferencias]] |
| Publicación (RPC) | `20260625000001_publish_property_rpc.sql` (#8: RPC `publish_property_atomic` SECURITY DEFINER search_path fijo — inserta `properties` status=active + `property_videos` status=ready en 1 tx atómica sin EXCEPTION; `location=ST_Point(lng,lat,4326)`) + rollback | [[propiedades-y-video]] |
| Cercanía (RPC PostGIS) | `20260706000001_properties_within_radius_rpc.sql` (#40, **desplegada a urbea-app**: RPC `properties_within_radius(p_lat,p_lng,p_radius_m float8)` SECURITY DEFINER `search_path=public,extensions` — enfoque **A1 "flaco"**: devuelve SOLO `{id, distance_m}` de props `active`+`deleted_at null` dentro del radio, ordenadas por distancia asc; `ST_DWithin`+`ST_Distance`, reusa GiST `properties_location_gix` (0005), PostGIS schema-qualificado `extensions.ST_*`; ⚠️ `ST_Point(p_lng,p_lat)` lng primero; **`revoke execute from public,anon` + grant a `authenticated`** [advisor 0028, defense-in-depth]) + rollback + pgTAP `09` (8 asserts, guardian PASS). Lo consumirá el feed/mapa en #42 (Fase C) | [[mapa-y-ubicacion]], [[propiedades-y-video]] |
| Edge Functions | `supabase/functions/publish-property/` (#8: handler DI + `index.ts` impls reales `CallerVerifier`[agent/admin]+`PropertyPublisher`; valida payload, 401/403, llama RPC) · `update-property-status/` (#17.5: handler DI + `make_property_status_updater(client)` testeable — transiciones/ownership/closed_reason; 54 Deno tests) · `mint-video-url/` (#21: minter PURO de signed URLs, `make_video_url_minter`, auth=verify_jwt gateway, 34 Deno tests) · `contact-agent/` (#14: crea lead+origin+contact_count idempotente desde el detalle, handler DI + 4 resolvers reales en index.ts, **79 Deno tests**, desplegada) · `update-lead-status/` (#15.6: cambia estado del lead por el embudo, valida transición+ownership doble-query, note opcional, **55 Deno tests**) · `update-lead-note/` (#29: **edita `internal_notes` SIN tocar el status** — mirror de update-lead-status sin transiciones; `make_note_updater` doble-query ownership solo `agent_id`/admin, `note=""`→limpia a null, UPDATE nunca incluye `status`; **24 Deno tests**, guardian PASS) · `admin-create-agency/` (#7) · `create-invitation/` (#34: owner genera códigos para su agencia, agencia derivada del JWT, 24 Deno tests, **pendiente deploy**) · `mint-r2-url/` (#69: presigned PUT/GET a Cloudflare R2 para assets — avatares/logos/archive; autz fail-closed en handler, firma aws4fetch, **40 Deno tests**, desplegada `--use-api`; ver [[storage-hibrido]]) · `mint-upload-url/` (#68.3, Épica B: **mintea URL de subida directa a Cloudflare Stream** (Direct Creator Upload, `creator=uid`, `maxDurationSeconds=120`, `requireSignedURLs=true`); handler DI puro + `index.ts` (uid del JWT vía `client.auth.getUser`) + factories `make_active_upload_checker`/`make_stream_upload_creator`/`make_video_registrar` en `_shared/clients.ts`; **invariante concurrencia §13.2**: 409 si el agente ya tiene 1 video `uploading`/`processing` (`agent_id`, fail-closed: query con error → asume ocupado); inserta `property_videos(agent_id, property_id=NULL, status=uploading, position=1, cloudflare_uid, tus_upload_url)`; 502 sin fila huérfana si Stream falla; **18 Deno tests**, guardian PASS; **desplegada al remoto** (`--import-map --use-api`) con secrets `STREAM_*` rotados — el **402** del gateway se levantó en el deploy-day #90 y la EF ya se invoca en E2E real; **#103.1-B — reaper de subidas colgadas**: `count_active_uploads` contaba TODA fila en `('uploading','processing')` **sin ventana de expiración** → si el binario nunca llegaba, la fila quedaba `uploading` para siempre y el agente **no podía volver a publicar jamás** (409 eterno). Ahora `types.ts` exporta `STALE_UPLOAD_MS`=**15 min**, el handler calcula `stale_before` y lo pasa como 2º argumento, y el adapter filtra `.or('status.eq.processing,and(status.eq.uploading,created_at.gt.<iso>)')`: `processing` bloquea SIEMPRE (Stream ya recibió los bytes, lo resuelve el webhook), `uploading` colgado libera. El fail-closed ante error de red/DB (`return 1`) se conserva y **ahora se loguea** (`console.error`) — sin eso un `PGRST100` del `.or()` interpolado sería un 409 permanente para todos los agentes e invisible en los logs. Deuda #107: `make_active_upload_checker` sigue **sin ningún test** (revertir el filtro deja la suite entera en verde)) · `stream-smoke/` (#68.1: EF de humo DESECHABLE que valida los secrets `STREAM_*` contra la API de Stream — longitudes + `stream_api_ok` + `jwk_kid_matches_key_id`; borrar tras el smoke) · `stream-webhook/` (#68.5, Épica B: **webhook de Cloudflare Stream**, EF **pública** `--no-verify-jwt`; verifica firma HMAC-SHA256(`STREAM_WEBHOOK_SECRET`, `<time>.<rawBody>`) del header `Webhook-Signature` — rawBody leído una vez sin re-serializar, comparación tiempo constante, fail-closed 401 antes de tocar DB; `state=ready`→`mark_ready(cloudflare_uid, thumbnail_url)` status=ready+ready_at, `state=error`→`mark_failed(cloudflare_uid, failure_reason)`, inprogress/uid desconocido/0 filas→200 idempotente; UPDATE filtra **solo por cloudflare_uid** (upload-first, nunca property_id); `notify` = gancho Ola 0 (push real en Ola 2); **15 Deno tests**, guardian PASS; #68.13: `mark_ready` ahora también escribe `duration_seconds` desde `payload.duration`; ⚠️ deploy pendiente — registrar webhook en Cloudflare (PUT /stream/webhook) genera el `STREAM_WEBHOOK_SECRET` + 402 remoto) · `mint-thumbnail-url/` (#68.14, Épica B: **mintea token firmado para thumbnails de Stream** — dado `cloudflare_uid` del propio agente → `{baseUrl `.../thumbnails/thumbnail.jpg`, token RS256 sub=uid, durationSeconds, expiresIn}`; **un token cubre todos los `?time=`**; fail-closed ownership agent/owner→403, no-ready/inexistente→404, sin JWK→500; DI-pura, 21 handler + 7 firma Deno tests, guardian PASS; deploy diferido 402) · `mint-poster-urls/` (#89, Épica B: **mintea portadas firmadas EN BATCH para los grids** — POST `{property_ids}` con JWT del caller → `{posters:[{property_id,posterUrl}]}`; auth combinada **POR ITEM** (`owner_user_id=caller` cualquier status **O** `status=active`); fail-closed por item, nunca URL sin firmar; primer video ready por `position`; reusa `build_poster_url`/`sign_stream_token`; factory `make_poster_url_minter`; 19 handler + 24 firma Deno tests, guardian PASS; cliente vía `mobile/src/lib/gridPosters.ts` `fetch_grid_posters` fail-soft, batch por página) · `_shared/` (cors/response/auth/validation/clients + `make_r2_url_minter`/`make_agency_ownership_verifier` #69 + `make_active_upload_checker`/`make_stream_upload_creator`/`make_video_registrar` #68.3 + `make_video_status_updater`/`make_video_event_notifier` #68.5 + `make_thumbnail_url_signer`/`sign_stream_token` #68.14/68.15 — `make_video_url_minter` devuelve `posterUrl` firmado por video #68.15) · `edit-property/` y `moderate-property/` (#73.6/#73.9 — detalle completo en la fila "Publicación completa Ola 1 (#73)" arriba) | [[crm-leads]], [[propiedades-y-video]], [[feed-vertical-video]], [[inmobiliarias-y-agentes]] |
| Engagement/CRM | `..._engagement_crm.sql` · `20260701000001_engagement_count_triggers.sql` (#13.2/13.4: triggers `AFTER INSERT/DELETE` en `likes`/`saves` → `properties.like_count`/`save_count`, funcs `update_like_count`/`update_save_count` **SECURITY DEFINER** `search_path=''`, `GREATEST(0,-1)` guard, **+ backfill**; desplegada a `urbea-app` 2026-07-01) + rollback | [[crm-leads]], [[feed-vertical-video]] |
| CRM Ola 1 — embudo + scoring (#75, tramo urgente) | **`20260807000002`** enum `lead_status` +`whatsapp_opened`/`interested`/`closed_won_rent`/`closed_won_sale` — migración **SOLA** por el gotcha `ALTER TYPE ADD VALUE` (el valor nuevo no se puede USAR hasta el commit; precedente `20260805000002`); los 7 legacy se conservan (Postgres no vacía enums + apps v1.0.3 en la calle) · **`000003`** data-fix `new→whatsapp_opened` (los `closed_won` legacy NO se parten: adivinar rent/sale es peor) + `leads.is_follow_up` **bandera ortogonal al estado** + tabla `lead_status_history` append-only (patrón `admin_actions`) poblada por `trg_lead_status_history`/`public.log_lead_status_change` — **ninguna EF escribe el historial**, así ninguna ruta futura se olvida de loguear; ⚠️ el guard de no-op va DENTRO del cuerpo con `TG_OP` porque Postgres prohíbe `OLD` en el `WHEN` de un trigger que incluye INSERT; RLS **solo lectura** (`can_view_lead`) + `revoke all … from anon,authenticated` · **`000004`** enum `lead_temperature` + `leads.score`/`leads.level`; fórmula **10 (contacto) + 4×saves + 1×likes** (reorientada: `events_raw` **no tiene un solo escritor**, 0 filas en el remoto → el pipeline original no existía); `private.compute_lead_level` (único lugar de la fórmula, umbrales `app_config.lead_score_threshold_tibio/_caliente` con **fallback COALESCE 15/30 — filas NO sembradas** porque el RED las inserta crudas y `12_stream_schema_test.sql` fija `count(*)=3`), `private.init_lead_score` (**BEFORE INSERT**, score **retroactivo**), `private.adjust_lead_score` (AFTER INSERT/DELETE en likes/saves, calco de `20260701000001`; 🔒 el aislamiento por agente sale del JOIN a `properties.owner_user_id`), `private.recompute_lead_levels` (AFTER UPDATE en `app_config` → recalibrar sin publicar app es real) + backfill · **`000005`** `private.is_agency_admin_of` (calco de `is_agency_owner_of` con `member_role='admin'`, ambos `active` en la MISMA agencia) + cláusula OR en los **3 puntos de LECTURA** (`leads_select`, `can_view_lead`, `can_view_user_as_lead_searcher`) → el **admin** de inmobiliaria ve el pipeline del equipo; 🔴 **`leads_update` NO se toca** (escritura sigue del agente dueño; editar = #31). Los 4 con rollback; pgTAP `28`–`30`. Deuda: **#108** (`score`/`level` escribibles por REST + `changed_by` NULL porque las EFs usan `service_client`) | [[crm-leads]], [[rls-seguridad]], [[roles-y-permisos]] |
| Moderación/Analítica | `..._analytics_moderation_audit.sql` | [[moderacion]], [[notificaciones]] |
| RLS/Seguridad | `..._rls_helpers_and_policies.sql`, `..._security_perf_hardening.sql` | [[rls-seguridad]] |
| Legal | `..._user_profile_legal.sql` (0004: `terms_versions` con índice único parcial `terms_versions_one_current_per_doctype` = **a lo más una vigente por doc_type**; `user_consents` append-only; enum `consent_type` ya incluía `whatsapp` y `age`, y el constraint `consent_version_presence` los EXIME de `terms_version_id`) · `..._seed_terms.sql` (0009: siembra terms 1.0 y privacy 1.0 vigentes) · **`20260727000003_legal_gate.sql` (#72.6)**: RPC `pending_legal_consents()` **security INVOKER** (la RLS de `user_consents` ya acota a las filas propias → menos privilegio para el mismo resultado) que devuelve los documentos vigentes no aceptados; ⚠️ `doc_type` y `consent_type` son enums DISTINTOS con etiquetas que se solapan → hay que **castear a text** para compararlos; `revoke execute from public,anon` + grant a `authenticated`; `revoke update,delete` y `revoke truncate` sobre `user_consents` (TRUNCATE no pasa por RLS y Supabase lo concede de fábrica) + rollback + pgTAP `18` (23 asserts, guardian) | [[legal-consentimientos]] |
| Catálogo MX (INEGI) | **`20260727000001_mx_catalog.sql` (#72.1)**: `mx_states` (32) + `mx_municipalities` (**2478**, no 2469) sembrados del web service OFICIAL `gaia.inegi.org.mx/wscatgeo`; PK = clave INEGI literal en `text` (une con cualquier dataset de INEGI sin traducción; `'09' != 9`); constraint `mx_muni_cvegeo_coherente` `left(id,2)=state_id` (la FK sola dejaría pasar un municipio con state_id válido pero ajeno); RLS lectura pública — **`anon` NECESITA select** porque el registro elige estado/ciudad antes de autenticarse; SIN policy de escritura a propósito (sería letra muerta: `authenticated` nunca recibe grant de escritura); `revoke truncate` + rollback + pgTAP `16` (42 asserts, guardian PASS) | [[legal-consentimientos]], [[mapa-y-ubicacion]] |
| Colonias con polígono (DCAH/INEGI) | **`20260813000001_mx_neighborhoods.sql` (#157)**: `mx_neighborhoods` (`source_key`=CVEGEO unique — ancla del upsert, `municipality_id` FK, `name`, `name_normalized` GENERADA con **`private.normalize_search_text`** (lower+translate español, sin unaccent), `postal_code`, **`geom geography(MultiPolygon,4326)`** — geography a propósito: `ST_Intersects` contra `properties.location` usa el GiST existente sin casts); índices GiST(geom)+GIN trgm+btree; `mx_municipalities` ganó `name_normalized` generada + GIN trgm + `bbox_min/max_lat/lng` precalculados por el import (NULL = sin colonias cargadas). **`000002`**: RPCs `search_places` (UNION colonias+municipios, prefijo>similarity>municipio, clamp [1,20], guard <2 chars), `get_neighborhood_geojson` (ST_AsGeoJSON 5 decimales + bbox), `properties_within_neighborhood` (A1 flaco, ST_Intersects, filtros de visibilidad en el cuerpo) — todas patrón radio: security definer + revoke anon + grant authenticated. **`000003`**: `import_neighborhoods_batch(jsonb)` SOLO service_role — canal de import sin contraseña de DB vía EF desechable `functions/import-neighborhoods/` (gate `x-import-secret` fail-closed, TDD Deno 12/12). Pipeline: `supabase/scripts/prepare-neighborhoods.mjs` (DCAH shapefile→WGS84 con mapshaper simplify 15m, title_case_es, CVEGEO) + `import-neighborhoods.sh` (`state NN`\|`all`\|`bboxes`\|`verify` local psql · `remote state/all` vía EF) + `upload-neighborhoods.mjs` (lotes 500). Dataset: **DCAH 2024 INEGI** (75,516 polígonos, corte dic-2023, cobertura por entrega municipal — NO completa; CP '00000' en varios estados; nombres MAYÚSCULAS→title case heurístico). pgTAP 42/43/44/45. | [[mapa-y-ubicacion]], [[busqueda-y-filtros]] |
| Registro (§5.1) | **`20260727000002_registro_constraints.sql` (#72.2)**: aditiva — `users.state_id`/`municipality_id` FK al catálogo + backfill best-effort desde `city`/`state` (que quedan **DEPRECADAS** por comment, no se dropean); CHECK `users_municipio_del_estado`; CHECK `users_mayoria_de_edad` (reemplaza `users_dob_not_future`); CHECK `users_phone_e164_mx` + índice único parcial `users_phone_unique_active`; `handle_new_user` extendida para persistir `date_of_birth`/`state_id`/`municipality_id` desde `raw_user_meta_data`. ⚠️ **SIN `not null`** a propósito (las filas existentes tienen `date_of_birth` NULL: forzarlo obligaría a fabricar datos personales o rompería todo UPDATE sobre ellas). De-dup defensivo de teléfonos antes del índice + rollback + pgTAP `17` (32 asserts, guardian PASS). **`20260729000001_register_user_atomic_rpc.sql` (#93.1)**: RPC `register_user_atomic(p_user_id uuid, p_ip inet default null)` SECURITY DEFINER, espejo de `redeem_invitation_atomic` — valida defensivamente que `phone`/`date_of_birth`/`state_id`/`municipality_id` no quedaron NULL (`FIELDS_INCOMPLETE` si sí, catch del hueco de arriba a nivel de flujo) e inserta atómicamente los **4 consentimientos** (terms/privacy/age/whatsapp) en `user_consents`; append-only, NO idempotente (responsabilidad de exactamente-una-llamada es de la EF); `revoke all` + `grant execute` solo a `service_role`; rollback + pgTAP `19` (24 asserts, guardian PASS). **EF `supabase/functions/register/`** (#93.2, pública sin JWT — sustituye `auth.signUp` directo, cierra el hueco de la tarea **#93**: cuenta incompleta desde curl + errores crudos de Postgres filtrados a `anon`): `authAdmin.createUser` (metadata EXACTA a las 6 claves que lee `handle_new_user`; **`email_confirm:false` desde #72.3** — el correo de confirmación lo dispara el CLIENTE vía `auth.resend` [admin.createUser no envía correo] y GoTrue rechaza el login de no-confirmados) → `register_user_atomic` → compensación `deleteUser` si la RPC falla → `200 {user_id}` (sin sesión; el cliente navega a `/verify-email`). Validación >=18 y `phone_exists` pre-check ANTES de tocar la DB (GoTrue colapsa toda violación del trigger en el mismo mensaje genérico "Database error creating new user" — no hay señal que mapear después del hecho). Errores **siempre sanitizados**: `PHONE_TAKEN`/`EMAIL_ALREADY_EXISTS` (409), `UNDERAGE`/`FIELDS_INCOMPLETE` (422), nunca el índice/constraint ni el detail crudo de la fila. 61+ Deno tests, guardian PASS. `/auth/v1/signup` público cerrado a `anon` vía `config.toml` `[auth] enable_signup=false` (#93.4, local; remoto pendiente de flip post-deploy, ver runbook en la bitácora de 93.4) · `[auth.email] enable_confirmations=true` + `additional_redirect_urls=["urbea://*"]` (#72.3: paridad local del flujo de verificación — Mailpit :54324 captura los correos sin SMTP; el remoto espera Resend) | [[roles-y-permisos]], [[legal-consentimientos]] |
| Tests | `supabase/tests/01_constraints_test.sql`, `02_rls_test.sql`, `03_storage_test.sql`, `03_redeem_invitation_test.sql`, `04_profile_photos_test.sql` (#6: 17 asserts bucket+RLS+cols), `05_admin_create_agency_test.sql` (#7: RPC 0016, plan 20), `06_publish_property_rpc_test.sql` (#8: RPC publish_property_atomic, plan 8), `07_engagement_counts_test.sql` (#13.2/13.4: triggers like_count/save_count + backfill, plan 14, verde en tx revertida contra remoto), `09_properties_within_radius_test.sql` (#40: RPC PostGIS por radio, plan 8 — existe/security-definer/incluye-dentro/excluye-fuera/orden-asc/status-active/deleted-null/orden-lng-lat con `ST_Project` a distancia conocida ±100m), **`28_lead_status_reconcile_test.sql`** (#75.1: enum gana los 4 y conserva los 7 legacy · `is_follow_up` ortogonal · 1 fila de historial al crear + 1 por cambio REAL, ninguna si el UPDATE no toca status o repite el mismo · reabrir un cerrado se permite y queda registrado · RLS: dueño ve, otro agente no, NADIE escribe directo · no quedan leads en `new`), **`29_lead_scoring_test.sql`** (#75.2, 22 asserts: lead sin actividad nace en 10 · retroactividad · +1 like/+4 save y el DELETE resta · like repetido no infla · **aislamiento por agente** · actividad de quien no tiene lead no crea lead ni truena · cruce de umbrales inclusivo · cambiar `app_config` recalcula el nivel de leads EXISTENTES sin tocar el score · el status nunca cambia por actividad · no-regresión de `like_count`/`save_count`), **`30_leads_admin_visibility_test.sql`** (#75.5, plan 15 = 5 DELTA + 10 INVARIANTE, con 2 asserts que custodian que la ESCRITURA no se amplió), **`36_property_status_operational_test.sql`**/**`37_property_revisions_test.sql`**/**`38_property_video_slots_test.sql`** (#73.1/73.2/73.4: shape+invariantes de los 10 estados nuevos, único-activa-por-propiedad de `property_revisions`, FK cascade+UNIQUE+fault-injection atómica de `property_video_slots`) | [[rls-seguridad]], [[mapa-y-ubicacion]], [[crm-leads]], [[moderacion]] |
| Tests EF/hooks (mobile+deno) | `supabase/functions/publish-property/handler.test.ts` (#8: 43 Deno tests, DI; **57 con #73.4** — pipeline video+duplicado) · `supabase/functions/update-property-status/{handler,property_status_updater}.test.ts` (#17.5: 44+10 Deno tests) · `mobile/src/features/publish/__tests__/{useVideoUpload,usePublish,usePublish.edit,useLoadProperty}.test.tsx` (#8+#17.8: 14+10+9+8 Jest; **`useVideoUpload` 14→26 con #103.2** — EC12–EC23 del falso negativo) · `supabase/functions/mint-upload-url/handler.test.ts` (#68.3+#103.1-B: el `stale_before` que el handler pasa al checker) · `mobile/src/features/profile/__tests__/usePropertyActions.test.tsx` (#17.7: 22 Jest) · `supabase/functions/edit-property/{handler,revision_upserter,location,property_updater}.test.ts` (#73.6: 44+7+11 Deno tests) · `supabase/functions/moderate-property/{handler,property_updater}.test.ts` (#73.9: 39+6 Deno tests) · `mobile/src/features/publish/__tests__/{validation,useDraftAutosave}.test.ts(x)` (#73.3: 40+10 Jest — wizard 5 pasos + autoguardado) | [[propiedades-y-video]], [[moderacion]] |

## Documentación de producto
| Concepto | Fuente |
|----------|--------|
| [[feed-vertical-video]] | `docs/PRD.md` §9 |
| [[propiedades-y-video]] | `docs/PRD.md` §12-13 |
| [[roles-y-permisos]] | `docs/PRD.md` §4 |
| [[crm-leads]] | `docs/PRD.md` §19 |
| [[monetizacion-pago-por-video]] | `docs/PRD.md` §17 |
| Alcance demo | `docs/PRD-MVP-demo.md` |
| Lineamientos | `docs/lineamientos-desarrollo.md` |

## App móvil — `mobile/` (inicializada · tareas #1, #2)
Base **existe** (Expo SDK 56, dev build, Expo Router, TS strict, standalone). Lo ya construido:
- `mobile/app.config.ts` — config dinámica: `com.urbea.app` (iOS+Android), slug/scheme `urbea`, owner EAS `deabratech`, Google Maps vía `process.env.GOOGLE_MAPS_API_KEY`, plugins (expo-dev-client/router/video). projectId EAS `85c7157a-…`.
- `mobile/eas.json` — perfiles `development`/`preview`/`production`. Proyecto EAS: `@deabratech/urbea`.
- `mobile/.npmrc` — `node-linker=hoisted` (gotcha Metro+pnpm). `mobile/.env.local` (gitignored) con credenciales Supabase; `.env.example` con nombres.
- `mobile/app/_layout.tsx` — root: `SafeAreaProvider > AuthProvider > Stack` (headerShown:false). `mobile/app/login.tsx` — pantalla de login (fuera del grupo protegido); **enlace "¿Tienes un código de invitación? Regístrate" → `/register` (#20.13** — antes el registro era inalcanzable desde el login). `mobile/app/register.tsx` — **dos modos (#15/#20/#50/#93):** modo `user` (registro libre → EF `register` vía `register_user` [`src/features/auth/api.ts`, #93.2/93.3] → **#72.3: `send_verification_email(email)` + `router.replace('/verify-email?email=…')` — ya NO hay auto-login, GoTrue rechaza no-confirmados**; el server crea la cuenta + perfil + **4 consentimientos atómicamente**, ya NO pasa por `supabase.auth.signUp` — ver [[legal-consentimientos]]) y modo `agent` (código de invitación, 2 fases: `validate-invitation` → `redeem-invitation` → auto-login → `/onboarding` → `role='agent'`). Feature `src/features/registration/`. El default de rol lo pone el trigger, NO el cliente — revertido a `user` por #50 (migración `20260708000001_signup_default_buscador`; ver [[roles-y-permisos]]). **branding espejo del login (#20.13):** fondo `brand.carnita` + `UrbeaLockup` + botón `brand.green` + tokens theme (antes grises hardcodeados "en pausa hasta #19", ya cancelada).
- `mobile/app/(protected)/_layout.tsx` — guard de rutas: `FilterProvider > LocationProvider > ProtectedLayoutWithLocation` (auth gate → **location gate** #41: `useLocation().status` → `LocationWall` por variante o el contenido) · `mobile/app/(protected)/index.tsx` — home protegida (placeholder "Urbea"; `/` resuelve aquí, el grupo es transparente a la URL).
- `mobile/src/lib/supabase/client.ts` — **cliente Supabase tipado** `createClient<Database>` + AsyncStorage (persistSession, autoRefreshToken) + listener `AppState` start/stopAutoRefresh (hotspot global; smoke test 200 OK contra remoto) → [[rls-seguridad]].
- `mobile/src/types/database.ts` — re-export de `supabase/types/database.types.ts`.
- `mobile/tsconfig.json` — strict (+ noUncheckedIndexedAccess, exactOptionalPropertyTypes), alias `@/*`→`src/*`.
- **Tests** (tarea #2): `jest-expo` + `@testing-library/react-native`; `mobile/jest.config.js` (preset, `moduleNameMapper @/`, `transformIgnorePatterns` con fix `.pnpm`), `mobile/jest.setup.js` (mock AsyncStorage). Correr: `pnpm test`. Ver [[comandos]].
- **Lint** (tarea #24, vivo): `mobile/eslint.config.js` — flat config CJS, **preset Expo mínimo** (`eslint-config-expo/flat`, sin reglas custom; `eslint@9.39.4`+`eslint-config-expo@56.0.4` alineado a SDK 56), `ignores` de artefactos (`android/`,`.expo/`,`dist/`,`ios/`,`node_modules/`). Script `pnpm lint` (`eslint .`) = **gate válido, exit 0** (baseline 17 errores→0; ~9 warnings documentados no bloqueantes). Solo cubre `mobile/` (backend Deno = `deno check`). Gotcha: dependencia nativa transitiva `unrs-resolver` requiere `pnpm approve-builds` (→ `mobile/pnpm-workspace.yaml`). El preset trajo `react-hooks/set-state-in-effect` (regla nueva): los 13 hits del patrón fetch-on-mount+ignore-flag llevan `eslint-disable` puntual justificado por línea. Correr: `cd mobile && pnpm lint`.

Estructura prevista por feature (carpetas `src/{features,components,theme,hooks}` ya creadas, vacías):
- `src/features/auth/` — **Auth email/password (tarea #2, vivo)**: `context.tsx` (AuthProvider + `useAuth()` → {session,user(perfil public.users),isLoading,signIn,signOut}; carga perfil por `id=auth.uid()`, listener onAuthStateChange), `validation.ts` (validación pura de form), `auth-errors.ts` (`map_auth_error` → mensajes ES), `protected-layout.tsx` (guard: loading→Redirect→Slot), `components/form-field.tsx`. Login email/password; el **registro** (libre→buscador / código→agente) vive en `app/register.tsx` (ver arriba, #15/#20/#50). → [[roles-y-permisos]], [[inmobiliarias-y-agentes]] **#72 (Ola 1, vivo)**: `validation.ts` crece con los validadores de §5.1 (`validate_phone`, `validate_birthdate` con boundary de mayoría de edad en UTC, `validate_state`, `validate_municipality` con coherencia por prefijo, `validate_full_name`, `validate_register_form`) y **`to_e164_mx`** — ⚠️ el índice `users_phone_unique_active` compara TEXTO CRUDO, así que sin forma canónica '3312345678' y '+523312345678' serían cuentas distintas de la misma persona. **#93 (registro server-side atómico, vivo)**: `context.tsx` retiró `signUp`/`SignUpProfile` — el registro libre YA NO llama `supabase.auth.signUp` directo (cerraba el hueco 1/2 de la tarea #93: cuenta incompleta + errores crudos de Postgres filtrados a `anon`, la anon key vive en el bundle). `api.ts` (`register_user`, nuevo): cliente de la EF pública `register` (`supabase.functions.invoke('register', {body})`), mismo patrón que `registration/api.ts` (+ `extract_error_code` de `edge-errors.ts`); `app/register.tsx` modo `user` llama `register_user(...)` y, si `ok`, hace `signIn(email,password)` — la EF NO autologuea. `record-consents.ts` **se retiró** (93.3): grabar los 4 consentimientos (terms/privacy/age/whatsapp) ahora es responsabilidad atómica del servidor (RPC `register_user_atomic`, migración `20260729000001`, invocada DENTRO de la EF `register`), no del cliente — ver [[legal-consentimientos]]. `hooks/useLegalGate.ts` (crítica, 7 tests): consume la RPC `pending_legal_consents`; patrón async-dentro-del-efecto + `tick` como en `useAgentLeads` — el linter de React marca como ERROR mutar refs en render y el setState síncrono en efecto. `components/legal-gate-boundary.tsx` (**UN gate, TRES consumidores**: `protected-layout.tsx`, `admin/admin-layout.tsx` y `app/onboarding.tsx` — el gate vivía solo en el primero y `urbea://admin` lo esquivaba), `components/legal-wall.tsx` (**MURO inline, no ruta**: una ruta se esquiva con `router.replace`/deep link/botón atrás; sin el texto del documento en pantalla NO se puede aceptar), `components/consent-checkbox.tsx`, `components/select-field.tsx`, `components/location-fields.tsx` (carga 32 estados una vez, municipios filtrados por estado, limpia el municipio al cambiar de estado, desambigua homónimos de INEGI), `feature-flags.ts` (Google/Apple en `false`; Apple = gate de release iOS por la guía 4.8 de App Store). **#72.3/72.4/72.5 (verificación de email + reset + Google OAuth — vivo en código, testeado E2E local vía Mailpit; solo falta config remota Resend/Google, ver `docs/TODO-pendientes.md`)**: `deep-link-session.ts` (`parse_session_from_url` — tokens de GoTrue en fragment `#access_token=…&refresh_token=…` o query → `{kind:'tokens'|'error'}` | null; `should_redirect_to_verify_email(session)`; parseo con `URLSearchParams` sobre el substring, NO `new URL()` — Hermes no parsea schemes custom) · `hooks/useSessionFromDeepLink.ts` (hook parametrizado por `on_success`: `Linking.useURL` → parse → `setSession`; error → mensaje inline; lo montan `verify-email.tsx` [→ `replace('/')`] y `reset-password.tsx` [no-op, se queda a capturar la nueva contraseña]) · `api.ts` + `send_verification_email(email): Promise<boolean>` fail-soft (`auth.resend` type signup, `emailRedirectTo` urbea://verify-email) · `hooks/useGoogleOAuth.ts` (`sign_in_with_google`: `signInWithOAuth` flow implícito `skipBrowserRedirect` → `WebBrowser.openAuthSessionAsync` → **REUSO de `parse_session_from_url`** → `setSession`; cancel/dismiss silencioso; `GOOGLE_OAUTH_ENABLED` sigue `false` hasta el client de Google Cloud; candado EC-B5: flags apagados → botones sociales no se renderizan [gate App Store 4.8]; ⚠️ trajo **`expo-web-browser` = módulo nativo nuevo → fingerprint cambia → rebuild del dev client y de testers, NO viaja por OTA**). `protected-layout.tsx` orden de gates: isLoading → sin sesión `/login` → `email_confirmed_at` null `/verify-email` → gate legal. Pantallas: `verify-email.tsx` (email por route param sin sesión, reenvío único vía api con cooldown 60s, deep link → sesión directa), `forgot-password.tsx` (anti-enumeración), `reset-password.tsx` (deep link recovery → `setSession` → `updatePassword` → `replace('/')`, CTA "Pedir un nuevo enlace" si el link expiró).
- `src/features/onboarding/` — **Onboarding agente (tarea #6, vivo)**: ruta `mobile/app/onboarding.tsx` → `OnboardingScreen.tsx` (nombre + foto, validación, skip foto, progreso, nav a `/(protected)`, guard re-show), `validation.ts` (`is_valid_full_name`), `components/AvatarPicker.tsx`, `hooks/useImagePicker.ts` (cámara/galería, permisos ES). Servicios en `src/lib/`: `profileService.ts` (`saveProfile`: **#52: upload por streaming `createSignedUploadUrl({upsert:true})` + `createUploadTask`** + upsert `user_preferences`), `imageUtils.ts` (`processProfileImage`: resize 512 + compresión ≤1MB; **#52: `new File(uri).exists/.size` — el `getInfoAsync` del entry principal LANZA en runtime en expo-file-system v56**). El thumbnail de video (`features/publish/lib/videoThumbnail.ts`) usa el mismo patrón streaming (#52). **#69: `saveProfile` ya NO sube a Storage `profile-photos` — va a R2 presigned y guarda el key (ver [[storage-hibrido]]); el bucket queda legacy solo-lectura.** → [[onboarding-y-preferencias]]
- `src/features/feed/` — **feed vertical de video (tarea #9, vivo)**: ruta `app/(protected)/(tabs)/index.tsx` delega a `FeedScreen.tsx` (FlashList v2 vertical paginada — `pagingEnabled`+`snapToInterval(height)`+`drawDistance(height)` para precarga; skeleton/empty/error/pull-to-refresh/scroll-infinito). **#54: un solo return con contenido condicional (`is_skeleton`/`is_error`/`is_empty`/feed) + botón de filtros y `<FilterSheet>` renderizados UNA vez bajo `show_filters = !is_skeleton && !is_error` (alcanzables desde el empty state; ocultos en skeleton/error). El empty state se ramifica por `active_filter_count`: >0 = filtered-empty (icon MagnifyingGlass, "No hay propiedades con estos filtros" + CTA "Limpiar filtros"→`clear_filters()`, re-query automático por identidad de `filters`); =0 = BD-vacía (copy/CTA original). Fix de la trampa de UX: filtrar a 0 resultados ya no atrapa al usuario. NO crítica (pantalla).** `types.ts` (`FeedProperty`/`FeedPropertyWithUrl`=+signed_url+video_id). `lib/feedProperties.ts` (**crítica TDD, 15 tests**: `fetchFeedProperties(cursor)` → query properties active+video ready, llama EF `mint-video-url` con batch property_ids → `{videos:[{property_id,video_id,signed_url}]}`, **merge fail-closed reconciliando el video embebido con el video_id minteado**, nextCursor). Hooks: `useFeedProperties.ts` (estado loading/error/paginación/refetch/loadMore), `useFeedActiveIndex.ts` (viewability 70% + AppState + `useFocusEffect` de `expo-router` → isActive), `useLikeProperty.ts`/`useSaveProperty.ts` (**crítica TDD, 16 tests**: persistencia optimista en `likes`[por video]/`saves`[por propiedad], rollback, conflicto único 23505, user de `useAuth`; `likeOnly` idempotente para doble-tap). Componentes: `VideoFeedItem.tsx` (expo-video `useVideoPlayer`+`VideoView` cover, play/pause por isActive, fallback de error, spinner, doble-tap `GestureDetector`→likeOnly+`Haptics`), `PropertyOverlay.tsx` (precio MXN+specs+rail like/guardar glass+avatar, mockup identidad pantalla feed; **#27: bloque de info tappable `onPropertyPress`→`router.push(/property/[id])`, separado del doble-tap=like y del avatar=onAgentPress**), `HeartAnimation.tsx` (Reanimated), `FeedSkeleton.tsx` (shimmer). **#42 (Fase C proximidad, crítica TDD+guardian PASS)**: `fetchFeedProperties` ahora llama la RPC `properties_within_radius` (coords por DI `deps.coords` — producción las inyecta `useFeedProperties` desde `useLocation()`, fallback GDL, refetch al llegar — y `p_radius_m=filters.radius_m`) → `{id,distance_m}[]`; expansión ×2 máx 3 reintentos si vacía (agotada → vacío SIN tocar PostgREST; error → throw); **paginación rediseñada a offset sobre los ids de la RPC** (cursor=offset string, `ids.slice(offset,offset+10)`→`.in('id',…)`, nextCursor sobre ids → páginas cortas post-filtro aceptadas `// ponytail:`); re-sort cliente por `distance_map`; `build_filter_query` y merge fail-closed intactos. 20+15 tests (EC-16..19, EC-F14/F15). **#58→#62**: `filters.radius_m === null` (default) = "Sin límite" — ⭐ #62 corrigió el path plano de #58.3: el feed **SIEMPRE va por la RPC** (carga contextual por cercanía con todo radio); null → `p_radius_m = UNLIMITED_RADIUS_M` (21,000 km, exportada del lib) **sin expansión ×2** (vacío con radio infinito = no hay props); el `.range()` plano quedó eliminado del feed (helper `mint_and_build_feed_data` sigue compartido); ⚠️ el chequeo `=== null` va ANTES del `?? DEFAULT_RADIUS_M` (`??` trataría null como ausente — candados EC-NULL-RPC-1..7); `undefined` sigue cayendo al default 5 km. **#61**: `VideoFeedItem` con **player estable por instancia** — `useVideoPlayer` nace UNA vez con la fuente inicial; cambios de fuente (reciclaje FlashList / URL re-firmada en refetch) entran por `player.replaceAsync()` + reset `has_error`/`is_paused` + resume si activo (fix del error Android "Cannot set prop 'player'… shared object already released": `useReleasingSharedObject` liberaba/recreaba el player por cambio de fuente y el view nativo alcanzaba a recibir el liberado). Stack nativo (9.10): `@shopify/flash-list`+`react-native-reanimated`(v4 +`react-native-worklets`)+`react-native-gesture-handler`+`expo-haptics`+`phosphor-react-native` (#43, antes @expo/vector-icons) (babel reanimated plugin + `GestureHandlerRootView` en `app/_layout.tsx`). ⚠️ **modo oscuro** con `ink_feed`/hardcodes `// ponytail:` (dual-mode formal en theme pendiente). ⚠️ **pendiente: nuevo dev build** (módulos nativos nuevos) antes de probar en device. **#56 (buscar en esta zona, vivo)**: `fetchFeedProperties` gana rama **aditiva** `filters.area != null` que overridea coords→`area.center`, base_radius→`area.radius_m`, attempts→MAX (sin expansión, reusa el mecanismo `is_unlimited`); zona gana sobre `radius_m` (EC-Z1..Z7, guardian PASS); con `area==null` bit-idéntico a #42/#58/#62. `FeedScreen` empty state de zona ("No hay publicaciones en esta zona" + "Limpiar zona") como **PRIMERA** rama del `is_empty` (area no cuenta en `active_filter_count`) + `ZoneActiveChip` dark. **#59 (fix flash GDL, crítica TDD+guardian PASS)**: `useFeedProperties` gana guard `_can_load = coords !== null` + early-return en `load_initial` (dep `_can_load`) → NO hace fetch hasta que llega la coord real; `isLoading` arranca y se queda en true (skeleton) hasta entonces, y al llegar la coord `load_initial` cambia de identidad → el `useEffect` de FeedScreen re-dispara la carga UNA vez (una sola RPC con coords reales, sin el orden GDL intermedio que saltaba). Fallback GDL de `feedProperties.ts` intacto (red de seguridad). Tests `gate-EC-1..5` (default `useLocation`→granted en beforeEach para no romper los de borrado #55.2). Smoke en device diferido (#59.4). → [[feed-vertical-video]]
- `src/features/property-detail/` — **pantalla de detalle de propiedad (tarea #10, vivo)**: ruta `app/(protected)/property/[id].tsx` (bajo guard, necesita auth para like/save) → `PropertyDetailScreen.tsx` (ensamblaje: hero video oscuro full-bleed + `ScrollView` paper claro con `layout.screen_inset`; loading→`DetailSkeleton`, error→UI con back; back flotante + `ActionButtons` flotante sobre el hero; CTA sticky "Contactar por WhatsApp"). **Tema híbrido fiel al mockup #5: hero oscuro + contenido paper claro** (NO todo oscuro). `types.ts` (`PropertyDetail` con `agent`/`agency`/`videos[]` anidados). `hooks/usePropertyDetail.ts` (query `properties` active+deleted_at null con embeds `users!properties_owner_user_id_fkey`+`agencies!properties_agency_id_fkey`+`property_videos`; **2ª query a `user_preferences`** para nombre/foto del agente — gotcha #16; filtro/sort de videos en cliente; **llama EF `mint-video-url` fail-soft** por el signed_url, igual que el feed #9). Componentes: `PropertyVideoPlayer.tsx` (hero `useVideoPlayer`+`VideoView` patrón VideoFeedItem, video position 1, badge "N videos", sin carrusel, cleanup-safe), `PropertyInfoHeader.tsx` (badge operación + property_type outline, precio-héroe MXN `Intl.NumberFormat` + tick salvia patrón `PropertyGridCard`, specs bed/water/resize Ionicons null-safe con hairlines silver), `AmenityChips.tsx` (chips de flags `pet_friendly`/`allows_no_guarantor`/`student_friendly` + array `amenities` con diccionario 13 valores; oculta si vacío), `PropertyMap.tsx` (`react-native-maps` patrón `MapPicker`, oculta si null), `AgentCard.tsx` (sheet paper, avatar patrón ProfileHeader, icono WhatsApp — **#75.4: pasa por `useContactAgent`, ya no por `open_whatsapp` directo**; recibe `property_id`, perdió `address`), **`ActionButtons.tsx` (CRÍTICA TDD, 10 tests, guardian PASS): reusa `useLikeProperty`/`useSaveProperty` de feed SIN tocarlos** (like por property_video_id oculto si null, save por property_id; accessibilityLabels), `DetailSkeleton.tsx` (shimmer patrón FeedSkeleton). Utils: `utils/parseLocation.ts` (**decodifica EWKB hex `0101000020…` Y WKT `POINT(lng lat)`; lng primero, 18 tests** — invertir = bug silencioso; #27: PostgREST devuelve geography como EWKB hex, no WKT) + `utils/whatsapp.ts` (`open_whatsapp(phone,address)` directo SIN CRM — **#75.4: su único consumidor legítimo que queda es `LeadExpandedView`, donde el AGENTE le escribe al lead (dirección inversa, no genera lead)** + **`open_whatsapp_ef(phone,message)` #14**: deep link `whatsapp://send` → fallback `wa.me` → `Alert` "✓ Contacto enviado"). **CTA sticky CRM (#14): `mobile/src/components/ContactAgentButton.tsx`** (reusa `PrimaryButton`; **#75.4: la llamada, el mapa de errores y la apertura se movieron a `useContactAgent` — perdió `onContactReady`**; gate de phone oculta el CTA). Share OMITIDO (decisión usuario). **#27: el feed (`PropertyOverlay`) y el perfil (`ProfileScreen`) navegan aquí vía `router.push(/property/[id])`.** → [[crm-leads]], [[propiedades-y-video]], [[feed-vertical-video]], [[design-system]]
- `src/features/search/` — **filtros básicos del feed + mapa (tarea #12, vivo)**: `FilterProvider` montado en `app/(protected)/_layout.tsx` (un solo `FilterState` compartido por feed y mapa). `components/FilterSheet.tsx` (**RN Modal nativo, NO @gorhom/bottom-sheet** — decisión para evitar z-index del autocomplete de zona dentro de un bottom-sheet; tema gestión-claro slide-up; secciones Operación/Tipo/Precio/Zona/Recámaras/Extras; footer Limpiar→`clear_filters()` / Aplicar→`onClose`). Componentes reutilizables: `FilterChipGroup.tsx` (multi-select de pills `{options,selected,onChange}`, usado por Operación=rent/sale y Tipo=5 enums), `BedroomsSelector.tsx` (segmented any/1+/2+/3+ → `bedrooms>=value`), `ZoneAutocomplete.tsx` (TextInput + dropdown inline sin portal). **`lib/filterQuery.ts` (CRÍTICA TDD, 27 tests, guardian PASS)**: `build_filter_query(query, filters)` puro aplica solo filtros activos sobre el query builder de supabase-js **ADEMÁS de** status/deleted_at base (NO en su lugar) — ⚠️ **sutileza `operation_type='both'`**: selección no vacía S → `.in('operation_type', [...S,'both'])` (una propiedad 'both' matchea cualquier modalidad); booleanos `true`→`.eq(col,true)`, `false`→sin filtro; zone→`.eq` exacto; `EMPTY_FILTERS`→cero llamadas. `get_active_filter_count` cuenta por grupo (precio=1) para el badge. **`lib/zones.ts` (CRÍTICA TDD, 15 tests, guardian PASS)**: `fetch_distinct_zones` (query `select('zone')` active/deleted_at null, dedup+orden client-side, **SIN ILIKE a DB** → honra el gotcha 🔒 no-ILIKE-sin-índice) + `filter_zones` (pura, substring case-insensitive, normaliza acentos NFD). **`lib/filterStorage.ts` (CRÍTICA TDD, 7 tests, guardian PASS)**: `save_filters`/`load_filters` AsyncStorage (key `urbea_filters`, **fail-safe**→`EMPTY_FILTERS` en null/JSON corrupto/shape inválida). `filterStore.tsx` (`FilterProvider` useReducer + `useFilters()` {filters, set_filter, clear_filters, active_filter_count}; hidrata en mount con `load_filters`, persiste debounced 500ms). `hooks/useZoneAutocomplete.ts` (carga única + debounce 300ms). `types.ts` (`FilterState`). **Integración**: `fetchFeedProperties(cursor?,deps?,filters?)` y `fetchMapProperties(deps?,filters?)` ganaron `filters` último param opcional (backward-compat: 25 tests preexistentes intactos); badge `active_filter_count` en `FeedScreen` y `MapSearchBar` (prop nueva); refetch por identidad de `filters` en useEffect. **Alcance: filtros demo completos incl. recámaras + toggles (pet_friendly/allows_no_guarantor/student_friendly)** — decisión usuario de ir más allá del demo-lean del concepto. **#42→#58→#62**: `radius_m: number | null` en `FilterState` — **default `null` = "Sin límite"** (#62: quita el TOPE pero el feed conserva el orden por cercanía vía RPC con `UNLIMITED_RADIUS_M`; solo el MAPA salta la RPC con null; 🔒 SOLO parámetro de la RPC de proximidad, JAMÁS entra a `build_filter_query` — candados EC-26/EC-F14/EC-M13/EC-30) + `components/RadiusSelector.tsx` (**#58: slider continuo custom PanResponder** 0–50 km step 1 km + toggle "Sin límite"→`onChange(null)`/reactivar→5 km, a11y adjustable increment/decrement, OTA-safe sin deps nuevas — descartados @react-native-community/slider [módulo nativo] y gesture-handler/reanimated [worklets no mockeables en jest]; antes pills 5/10/20/50) como **PRIMERA sección** del sheet; badge NO cuenta `radius_m` (`// ponytail:` en `get_active_filter_count`); `load_filters` hidrata pre-#42/pre-#58 con merge `{...EMPTY_FILTERS,...parsed}` (probado sin cambio de código, EC-STORAGE-1..7 en `filterStorage.migration.test.ts`). **#56 (buscar en esta zona, vivo)**: `FilterState` gana `area:{center:{lat,lng},radius_m}|null` (2º eje geoespacial, override de `radius_m`, gana sobre él); 🔒 igual que `radius_m`: fuera de `build_filter_query` y NO cuenta en `get_active_filter_count` (EC-32/33/34). **Efímera**: `filterStorage` excluye `area` (`save_filters` destructura fuera; `load_filters` fuerza `area:null` tras merge, defensivo vs JSON legacy; EC-AREA-1..4, + EC-S2/S6 actualizados al contrato area-excluida). `clear_filters` **preserva** `area` (ejes independientes). Chip `components/ZoneActiveChip.tsx` (prop `dark`, "Zona activa · Quitar"→`set_filter('area',null)`) reusado en feed+mapa. → [[busqueda-y-filtros]], [[feed-vertical-video]], [[mapa-y-ubicacion]]
- `src/features/map/` — **mapa global de propiedades (tarea #11, vivo)**: ruta `app/(protected)/(tabs)/map.tsx` (fina → `MapScreen`; tab "Mapa" añadido en `(tabs)/_layout.tsx` entre Inicio y Perfil). `MapScreen.tsx` (`MapContent` con hooks + `MapErrorBoundary` clase; MapView centrado en `GDL_REGION`, `onRegionChangeComplete`→region state, `cluster_properties(filtered,region)` memo, render `PropertyMarker`/`ClusterMarker`, selección→`PropertyMiniCard` overlay, cluster tap→`animateToRegion` delta/2 zoom-in, mini-card tap→`router.push(/property/[id])`, overlays loading/error). `constants.ts` (`GDL_REGION` Guadalajara). `types.ts` (`MapProperty`). **`lib/mapProperties.ts` + `hooks/useMapProperties.ts` (CRÍTICA TDD, 10 tests, guardian PASS)**: query `properties` active+deleted_at null (select +`location`, sin paginar) → `parse_location` (reusado de property-detail) → **fail-closed** omite location null/no-parseable. **`lib/clusterMarkers.ts` `cluster_properties()` (CRÍTICA TDD, 14 tests, guardian PASS)**: clustering **custom puro sin dependencia** (decisión grilling) — grid absoluto `cellSize=delta/divisions(8)`, 1→point/>1→cluster (id `cluster_<x>_<y>`, centroide media), guard `delta<=0`. `lib/formatPrice.ts` (`format_compact_price` $15k/$2.4M, `format_full_price` es-MX). Componentes: `PropertyMarker.tsx` (pin canónico `MapPinIcon` — Phosphor `MapPinSimple` fill desde #185 (2026-08-16, era `MapPin`), solo el icono, sin price tag ni isotipo desde el flash 2026-07-06; **rent=primary salvia / sale|both=accent arcilla**; `tracksViewChanges` arranca `true`→congela a `false` a los 300ms del mount, #64 — ver abajo), `ClusterMarker.tsx` (círculo `ink` + count, tono distinto, mismo fix #64), `PropertyMiniCard.tsx` (card inferior flotante, **liquid glass** BlurView, NO Callout nativo por bug Android), `MapSearchBar.tsx` (barra top **liquid glass** + filtro **cliente** address/property_type, `useSafeAreaInsets`). **Sin lib de clustering** (custom). **#42 (Fase C proximidad, crítica TDD+guardian PASS)**: `fetchMapProperties` llama la RPC `properties_within_radius` (coords por DI `deps.coords`, fallback GDL) + expansión ×2 máx 3 reintentos → `.in('id', TODOS los ids)` **sin paginar ni re-ordenar** + `build_filter_query` intacto (loop de expansión duplicado del feed a propósito, `// ponytail:` techo = 3er consumidor → extraer helper); `useMapProperties` inyecta coords de `useLocation()` con refetch al llegar. **#58**: con `filters.radius_m === null` (default desde #58) el mapa **salta la RPC** → todas las activas sin `.in('id',…)` + `build_filter_query` (helper `build_map_result`; candados EC-MAP-NULL-1..3; `// ponytail:` techo = sin radio puede traer muchas props, aceptable escala demo). (#62 NO tocó el mapa: los pins no tienen orden — solo el FEED dejó de saltar la RPC con null.) **42.4**: `MapScreen` se recentra con `animateToRegion` UNA vez cuando las coords llegan post-mount (`coords_used_ref`; el gate deja pasar `loading`) — `GDL_REGION` queda como fallback (el "sin expo-location" de #11 quedó superado); ⚠️ punto azul `showsUserLocation` SE MANTIENE, pendiente confirmar con cliente. 14+14 tests (EC-MAP-1..3, EC-M11..M14). ⚠️ requiere dev build (react-native-maps nativo); `GOOGLE_MAPS_API_KEY` ya en `app.config.js`. Deuda: filtros sliders reales + guard NaN-delta = futuro. (#32→#43.4: el pin usa `IsotipoMark`, ahora **react-native-svg** con la geometría del logo final `urbea-logo-final.html`.) **#56 (buscar en esta zona, vivo)**: `lib/viewportToArea.ts` (**crítica TDD, 7 tests, guardian PASS**: `viewport_to_area(region)→{center,radius_m}` Haversine a mano radio=diagonal/2, clamp 100–50000, pura); `fetchMapProperties` gana rama **aditiva** `filters.area != null` (ANTES del check `radius_m===null`) que llama la RPC con center/radio de la zona sin expansión (zona gana sobre `radius_m`; EC-ZM1..ZM5); `MapScreen` gana `AreaSearchPill.tsx` (pill flotante, debounce 500ms en `onRegionChangeComplete`→`viewport_to_area`→`set_filter('area')`→`router.push('/')`) + `ZoneActiveChip` (claro). Aditivo: con `area==null` #42/#58/#62 bit-idénticos (verificado guardian). **#64 (fix pin rojo duplicado Android, vivo)**: causa raíz `tracksViewChanges={false}` fijo desde el 1er render + contenido SVG (`MapPinIcon`) que no pinta síncrono en Android → el snapshot nativo se congela antes del paint y Google Maps deja su pin default visible, duplicado. Fix: arranca en `true`, congela a `false` a los 300ms (`useState`+`useEffect`+`setTimeout`, sin deps nuevas) en `PropertyMarker.tsx`/`ClusterMarker.tsx`. No crítico (`components/**`); descartada causa de datos (RPC sin joins, `cluster_properties` sin duplicación posible). **#157 (búsqueda de lugares estilo Google Maps, vivo)**: el filtro cliente de `MapSearchBar` (#11.7) quedó **SUSTITUIDO** por autocomplete server-side — `lib/placeSearch.ts` (`search_places(query)` → RPC `search_places`, guard <2 chars sin round-trip, bbox fail-closed a null), `lib/neighborhoodPolygon.ts` (`geojson_to_polygons` pura [lng,lat]→{latitude,longitude} anillos exteriores/holes ignorados + `fetch_neighborhood_polygon` → RPC `get_neighborhood_geojson`), `lib/bboxRegion.ts` (`bbox_to_region` pura, MIN_DELTA 0.005), `hooks/usePlaceSearch.ts` (debounce 300ms + **guard anti-stale** por contador de request — cada keystroke es red), `components/MapSearchSuggestions.tsx` (dropdown, MapPin=colonia/Buildings=municipio). `fetchMapProperties`/`useMapProperties` ganan 3er parámetro opcional `neighborhood_id` — rama de MÁXIMA prioridad (antes de `area`): RPC `properties_within_neighborhood` → `.in('id',ids)`, sin expansión, colonia vacía = `[]` honesto (EC-N1..N7, invariante A1 intacta). La colonia es estado LOCAL de MapScreen (decisión D6 — NO vive en FilterState; el feed no la consume, follow-up aparte); colonia y `filters.area` mutuamente excluyentes (D9, el pill limpia la colonia); municipio reusa `filters.area` vía `bbox_to_region`+`viewport_to_area` (D5, sin RPC nueva); `<Polygon>` de react-native-maps (primary, fill 10%) persiste al panear; chip = `ZoneActiveChip` con prop nueva `label` (nombre de la colonia). Todos críticos TDD (33 tests nuevos). ⚠️ RNTL 14: `renderHook` es async y las interacciones piden `await act(async…)`. → [[mapa-y-ubicacion]], [[propiedades-y-video]], [[busqueda-y-filtros]]
- `src/features/location/` — **permiso + ubicación cliente, muro bloqueante (tarea #41 / Fase B, vivo)**: **`lib/permissionDecision.ts` (CRÍTICA TDD, 4 EC, guardian PASS)** `decide_permission_action({granted,canAskAgain})`→`'granted'|'request'|'open_settings'` (pura, sin `expo-location`). **`LocationProvider.tsx` + `useLocation()` (crítica de facto, 12 tests, guardian PASS)**: `{status:'loading'|'permission_denied'|'gps_off'|'granted', coords, request, refresh}`; `Accuracy.Balanced`, coord cacheada por sesión, `is_mounted` ref (patrón `AuthContext`). 3 estados en mount (`getForegroundPermissionsAsync`→`hasServicesEnabledAsync`→`getCurrentPositionAsync`). ⚠️ **`getCurrentPositionAsync` LANZA** "Current location is unavailable" en emulador sin fix → try/catch → `gps_off` (EC-8, cazado en smoke). **Listener `AppState 'active'`→`refresh()`** (EC-9): re-evalúa al volver de Ajustes sin re-pedir permiso. **`LocationWall.tsx`** muro full-screen funcional (tokens `theme.ts` + `PrimaryButton`, NO gate branding #19), 2 variantes `permission_denied`/`gps_off`. Gate en `app/(protected)/_layout.tsx` (uniforme por rol). Config: `expo-location@56.0.19` + plugin en `app.config.js` (⚠️ instalar con `pnpm dlx expo install`, NO `pnpm add` que trae 57.x incompatible SDK56) → **nuevo dev build**. E2E: `helpers/launch.yaml` concede `permissions.location: allow` + `setLocation` GDL → 6/6 verde. Fase C (#42, vivo): `useFeedProperties`/`useMapProperties` consumen `useLocation().coords` para feed/mapa por cercanía. **#59 (timeout GPS, vivo):** `fetch_current_coords` envuelve `getCurrentPositionAsync` en `Promise.race` con `GPS_TIMEOUT_MS=10s` (+`clearTimeout` en finally) → si el GPS cuelga sin fix, el timeout rechaza y el catch cae a `gps_off` (mismo destino que EC-8), evitando que el skeleton del feed (gate de #59) quede colgado indefinido; test `EC-10` (fake timers + `advanceTimersByTimeAsync`, spia `AppState` self-contained). → [[mapa-y-ubicacion]]
- `src/features/publish/` — **wizard 3 pasos + subida video (tarea #8, vivo)**: rutas `app/(protected)/publish/_layout.tsx` (`PublishFormProvider` envuelve Stack + `WizardHeader` con `useSegments()`→StepIndicator), `step1.tsx` (operación rent/sale/both + tipo casa/departamento/local/oficina/terreno), `step2.tsx` (price/recámaras/baños/m²/descripción + `AddressAutocomplete` + `MapPicker` + toggles nicho + `validate_step2`), `step3.tsx` (expo-image-picker video → preview expo-video → upload → botón Publicar; **#103.2: estado `verifying` con etiqueta "Verificando que el video llegó…"** — recibido en vivo por `on_status_change`, bloquea "Cambiar video" igual que `uploading`). `store/PublishFormContext.tsx` (`usePublishForm`→{state,update,reset}), `store/types.ts` (`PublishFormState`/`PublishFormPayload`), `validation.ts` (`validate_step1/2/3`, `get_property_payload`, **`validate_video_size`+`MAX_VIDEO_SIZE_BYTES`=524288000 #49** — ⚠️ techo LEGADO de Storage; el vivo es `MAX_STREAM_UPLOAD_BYTES`=200 MB, el que aplica `useVideoUpload` desde #68). Componentes: `SelectionCard`, `NumericStepper`, `AddressAutocomplete` (Places API New REST+fetch, key `EXPO_PUBLIC_GOOGLE_PLACES_API_KEY`, degrada sin key), `MapPicker` (react-native-maps, marker draggable, no escribe coords falsas). Hooks (críticos, TDD): `hooks/useVideoUpload.ts` (**vigente desde #68.4 = Direct Creator Upload a Cloudflare Stream**, ya NO Supabase Storage: invoca la EF `mint-upload-url` → `new File(uri).createUploadTask(uploadURL, {POST, `UploadType.MULTIPART`, fieldName:'file', onProgress cap 0.99})` → deja el form en `processing` con `cloudflare_uid` (el `ready` lo pone el webhook 68.5); techo `MAX_STREAM_UPLOAD_BYTES`=**200 MB** validado con `File.size` síncrono ANTES de mintear; 409 de la EF → "ya tienes un video en proceso". **#103.2 — "verificar antes de fallar"**: una excepción de `uploadAsync()` (`"Unable to upload a file: Failed to read response"`) **o** un status fuera de 2xx ya NO marcan error — pasan por `verify_before_failing`, que expone `status='verifying'`+`progress=0.99` y consulta `property_videos.status` por `cloudflare_uid` hasta **10× cada 3 s** (⚠️ **consulta primero, duerme ENTRE intentos** — el estado real ya suele estar ahí al primer poll): `ready`/`processing` → **ÉXITO** (mismo desenlace que el 2xx: escribe `video_id`/`cloudflare_uid` al form), `failed` → corta de inmediato con mensaje propio, `uploading`/`missing` agotados los intentos o checker que lanza → error neutro fail-closed. El camino feliz 2xx **nunca** invoca el checker. El estado viaja a la pantalla por callback **`on_status_change`** (sin él la etiqueta "Verificando que el video llegó…" de `step3.tsx` era rama muerta: el screen solo leía el status DESPUÉS del `await`). 14→**26 tests Jest** (EC12–EC23), guardian PASS. _Historia: hasta #68 subía a `{user_id}/{video_id}.mp4` del bucket `property-videos` con `createSignedUploadUrl`+PUT `BINARY_CONTENT` y guard de 500 MB (#52, que cerró el OOM de Hermes de #49 — ese patrón de streaming sobrevive en `profileService`/`imageUtils`)_), `hooks/usePublish.ts` (**#17.8**: ramifica create/edit — create invoca EF `publish-property`; **editMode+propertyId → `from('properties').update({...}).eq('id')` UPDATE directo con RLS**, sin EF), `hooks/useLoadProperty.ts` (**#17.8**: carga propiedad+videos por id → mapea a `PublishFormState` para pre-fill; `FormPrefiller` en `_layout.tsx`). **#53 (fix duplicación al editar, vivo):** el "modo edición" ya no viaja por URL param (se perdía entre `router.push` step1→step2→step3 → step3 caía en create mode → duplicaba property+video). Ahora `PublishFormState` lleva **`edit_mode:boolean`+`property_id:string|null`** (default false/null en `INITIAL`); `FormPrefiller` (en `_layout.tsx`) hace `update({edit_mode:true, property_id})` de inmediato **y prellena una sola vez** (guard `useRef` — no pisa ediciones al re-montar step1); `step3.tsx` lee `is_edit_mode`/`property_id` del **contexto** (`state.edit_mode`), NO de `useLocalSearchParams` → inmune a la pérdida del param; en edit mode **oculta el picker de video** (placeholder informativo "El video no se puede cambiar en modo edición" — evita uploads huérfanos, video no reemplazable en v1). `CleanupOnUnmount` resetea el form al salir del wizard (defensivo: el Provider vive en `_layout` y ya se desmonta al salir). Sin migraciones (policy `properties_update` ya desplegada). RED 5 EC-CTX-* + guardian PASS (67/67 publish). Componente reutilizado fuera del feature: `src/components/StepIndicator.tsx`. → [[propiedades-y-video]]
- `src/features/profile/` (gestión, **tarea #17, vivo**) — **Mis publicaciones**: ruta `app/(protected)/profile/my-listings.tsx` (Stack bajo profile, NO tab; entrada desde `ProfileScreen`). `hooks/useMyProperties.ts` (todos los status del agente + video_count + contadores, `refetch`), `hooks/usePropertyActions.ts` (changeStatus/pause/close[guard closed_reason]/delete[soft-delete], invoca EF `update-property-status`; 22 tests TDD), `components/{PropertyListItem,PropertyActionMenu,FilterTabs,ClosePropertyDialog,DeletePropertyDialog}.tsx`, tipo `MyProperty` en `types.ts`. Token nuevo `colors.danger`. **#55 (vivo): refresco cross-pantalla al eliminar** — `src/lib/propertyEvents.ts` (mini pub/sub sin deps: `emitPropertyDeleted(id)`/`onPropertyDeleted→unsubscribe`, `Set` interno; tests `src/lib/__tests__/propertyEvents.test.ts`); `usePropertyActions.deleteProperty` **emite** en la rama de éxito; los 3 hooks de datos `useFeedProperties`/`usePropertiesGrid`/`useSavedProperties` **se suscriben** (`useEffect([])`, cleanup=unsubscribe) y quitan solo ese id del estado local (**remoción optimista, sin refetch** → feed no reinicia video ni pierde scroll; no montadas cargan limpias por `deleted_at`/RLS → sin `useFocusEffect`). Ambas subtareas CRÍTICAS TDD+guardian PASS (`__tests__/{useFeedProperties,usePropertiesGrid,useSavedProperties}.test.tsx`). → [[propiedades-y-video]], [[perfil-agente]]
- `src/features/agency/` — **invitaciones del owner (tarea #34, vivo)**: ruta `app/(protected)/agency/invitations.tsx` (guard `useAgencyRole().isOwner` — spinner en loading, `<Redirect>` a perfil si no es owner; form máx-usos opcional entero≥1, vacío=ilimitado; éxito → bloque one-time con aviso + `CopyCard` + "Generar otro"; `// ponytail:` sin campo de expiración — la EF ya soporta `expires_at`). `api.ts` (`create_invitation` invoca EF `create-invitation`, patrón `registration/api.ts`), **`hooks/useCreateInvitation.ts` (CRÍTICA TDD, 6 tests)**: `{create, invitation, loading, error_code, reset}` — `error_code` del backend o `'UNKNOWN'`, `create` limpia estado previo (gotcha: `renderHook` de RNTL es async → `await renderHook(...)`). `components/CopyCard.tsx` (theme-based, `expo-clipboard`, estado "Copiado ✓" 2s; el CopyCard del admin sigue pre-theme — migrarlo aquí es deuda del audit). Entrada: botón "Invitar agentes" en `ProfileScreen` solo `is_own_profile && isOwner`. **`src/lib/supabase/edge-errors.ts`** (#34.2): `extract_error_code` extraído de `registration/api.ts` para reuso cross-feature (5 tests). Código vigente en remoto: `DEMO2026` (GDL Premium, ilimitado). → [[inmobiliarias-y-agentes]], [[design-system]]
- `src/features/leads/` — **CRM del agente (tarea #15, vivo)**: ruta `app/(protected)/(tabs)/crm.tsx` (tab oculto a no-agentes vía `user.role==='agent'` + `<Redirect>`; tab declarado en `(tabs)/_layout.tsx` con `href:null` condicional — RLS/EF = capa real de seguridad). `screens/CRMScreen.tsx` (ensamblaje: `useAgentLeads` → `FilterTabs` + búsqueda `TextInput` + `FlatList<LeadCard>` + Modal `LeadExpandedView`; filtros y búsqueda **client-side** con `useMemo` en cascada, sin re-query; loading/error/empty states). Hooks: `hooks/useAgentLeads.ts` (query `from('leads')` con embeds `users!leads_user_id_fkey(first_name,last_name,avatar_url,phone)` (#30: identidad del buscador desde `users`, `full_name`=`build_full_name(...)`, `profile_photo_url`=`avatar_url`; ya NO `user_preferences`)+`lead_origin_properties→properties→property_videos`; RLS filtra `agent_id`; `.is('deleted_at',null)`; orden `updated_at` DESC — **hoy primario `score` DESC, ver #75.6 abajo**; transforma raw→`AgentLead` null-safe; **10 tests**), **`hooks/useUpdateLeadStatus.ts` (CRÍTICA TDD, 13 tests, guardian PASS)**: invoca EF `update-lead-status` vía `functions.invoke` (DI del cliente, patrón `usePropertyActions`: `is_working_ref`+force-update síncrono, spread condicional de `note`, `onSuccess` solo en éxito; retorna `{ok,error}`). Componentes: `components/LeadCard.tsx` (avatar+nombre, badge de estado, thumbnail, tiempo relativo), `components/LeadExpandedView.tsx` (Modal bottom-sheet RN nativo: selector de estados + textarea de notas + "Ver propiedad" `router.push('/property/[id]')` + WhatsApp `open_whatsapp(lead.phone,...)`). `lead_status_meta.ts` (**mapa compartido** estado→etiqueta/color, fuente única para Card+ExpandedView), `types.ts` (`AgentLead`/`LeadStatus`/**`Agent`** #28). **Owner-view de la agencia (tarea #28, vivo):** el dueño de la inmobiliaria ve los leads de todos sus agentes vía un selector — SIN cambios de backend (la policy RLS `leads_select` + helper `private.is_agency_owner_of(agent_id)` de migración 0010 ya lo permiten). Hooks nuevos (ambos CRÍTICOS TDD+guardian): **`hooks/useAgencyRole.ts`** (6 tests; query `agency_members` por `user_id=auth.uid()` + `status='active'` → `{isOwner,agencyId,memberRole}` — **hoy +`isAdmin`/`canViewTeam`/`error`, ver #75.5 abajo**; el rol de agencia vive SOLO en `agency_members.member_role`, NUNCA en `users.role` que es 'agent' incluso para owners) y **`hooks/useAgencyAgents.ts`** (8 tests; solo ejecuta si `isOwner`; embed `agency_members→users(id,first_name,last_name,avatar_url)` **SIN cast** — ⚠️ el nombre/foto se leen de `users`, NO de `user_preferences`: el owner NO puede leer el `user_preferences` de sus agentes por RLS `user_prefs_select` (fila propia), pero SÍ sus filas `users` vía `is_agency_owner_of`; `full_name`=first+last, `profile_photo_url`=avatar_url; filtra `member_role='agent'`+`status='active'`, orden client-side nulls al final; el owner NO aparece como opción). **`hooks/useAgentLeads.ts` extendido (#28, 15 tests):** firma `(agentId?: string | null)` retrocompatible — string añade `.eq('agent_id',id)`, null/ausente = sin filtro (RLS decide: agente ve los suyos, owner ve TODOS los de la agencia = semántica "Todos" agregada). ⚠️ **fix embed:** el buscador se embebe con FK explícita `users!leads_user_id_fkey(...)` — `leads` tiene DOS FKs a `users` (agent_id/user_id) y el embed bare rompía en runtime ("Could not embed... more than one relationship"); bug latente de #15 que los mocks no atrapaban (+test de regresión). Componente **`components/AgentSelector.tsx`** (no crítico; rail `ScrollView` horizontal de chips: "Todos" + un chip por agente con avatar/iniciales, activo en `colors.primary`; presentacional puro). Wiring en `CRMScreen.tsx`: `useAgencyRole`+`useAgencyAgents` incondicionales, selector renderizado solo si `isOwner && agents.length>0`, subtítulo "Leads de tu equipo" para owner. Demo: `owner.oeste@urbea.demo`/`urbea2026` (2 agentes). **Guardar nota sin cambiar estado (tarea #29, vivo):** **`hooks/useUpdateLeadNote.ts` (CRÍTICA TDD, 13 tests, guardian PASS)** invoca EF `update-lead-note` vía `functions.invoke` (mirror de `useUpdateLeadStatus`: `is_working_ref`+force-update síncrono que **bloquea doble-submit**, DI del cliente, `onSuccess` solo en éxito, `note=""` se pasa tal cual); en `LeadExpandedView` un botón **"Guardar nota"** (pill `colors.primary`, visible solo si la nota cambió, deshabilitado durante guardado, error inline separado del de status) persiste la nota sin forzar transición — **respeta `readOnly`** (owner solo-lectura #28: no se muestra). El status-change SIGUE mandando la nota (retrocompat #15). ⚠️ el GREEN tocó `mobile/jest.setup.js` (fix legítimo de drenado `act()` FIFO→LIFO por contaminación cruzada entre tests, verificado por guardian; NO enmascara bug del hook). Deuda menor: `#FFFFFF` hardcodeado (sin token `on_primary` en theme.ts, sigue convención preexistente del archivo). **Identidad del buscador (tarea #30, vivo):** el CRM mostraba "Usuario sin nombre" porque el agente/owner no podía leer la fila `users` del buscador de su lead. Fix RLS: helper `private.can_view_user_as_lead_searcher(user_id)` + cláusula OR en `users_select` (migración `supabase/migrations/20260702000001_rls_lead_searcher_identity.sql` + rollback; pgTAP `supabase/tests/08_rls_lead_searcher_test.sql` plan 12). Cliente: `useAgentLeads` lee identidad desde `users` (embed `users!leads_user_id_fkey(first_name,last_name,avatar_url,phone)`); `build_full_name` **extraída a `utils/full_name.ts`** (util compartido) e importada por `useAgentLeads` **y** `useAgencyAgents` (antes local en este último). `user_prefs_select` NO se toca. Suite leads 26/26 en esos 2 hooks, tsc 0, guardian PASS. Desplegada al remoto vía `apply_migration`. **Tramo urgente del CRM (#75.1/75.5/75.6, vivo 2026-08-07):** el cliente se realineó **en la MISMA subtarea que la migración** para que nunca existiera una ventana con los estados rotos — `types.ts` con los **11** `LeadStatus` (7 legacy + 4 vigentes) + `LeadSortMode` + `LeadStatusHistoryEntry`; `lead_status_meta.ts` con labels de los 4 nuevos, **`ALL_LEAD_STATUSES` recortado a los 8 vigentes** (los 3 legacy se ven pero no se pueden elegir) y **`LEVEL_META`/`get_level_meta`** (escala Salvia/Arcilla, deliberadamente distinta del badge de estado); `screens/CRMScreen.tsx` con los **grupos de tabs realineados** (⚠️ sin esto TODO lead nuevo caía en el tab "Cerrados"), `canViewTeam` en vez de `isOwner`, y **sección fija "En seguimiento"** (`is_follow_up`, independiente del tab, respeta el buscador, deduplicada de la lista de abajo vía `ListHeaderComponent`). Nuevos: **`lead_error_messages.ts`** (código EF → español, patrón `ContactAgentButton`) cableado con **`extract_error_code`** en `useUpdateLeadStatus`/`useUpdateLeadNote` — se acabó el *"Edge Function returned a non-2xx status code"* en pantalla, que era el 3er defecto del usuario; **`hooks/useLeadStatusHistory.ts`** (timeline solo-lectura de `lead_status_history`, RLS ya filtra por `can_view_lead`, lista vacía ≠ error); **`utils/relative_time.ts`** (extraída de `LeadCard`, compartida con el timeline). Modificados: `hooks/useAgencyRole.ts` (**4 roles** owner/admin/agent/viewer + `isAdmin` + **`canViewTeam`** + `error` booleano que distingue "falló la query" de "sin membresía"), `hooks/useAgentLeads.ts` (pide `score,level,is_follow_up`; **orden por puntaje**: `.order('score',desc)` — o `last_contact_at` con `nullsFirst:false` en modo `last_contact` — y desempate SIEMPRE `updated_at` DESC; error → mensaje neutro en español, ya no `query_error.message`), `components/LeadCard.tsx` (fila de nivel + puntaje), `components/LeadExpandedView.tsx` (toggle bookmark de seguimiento + sección **Historial**), y fuera del feature `features/profile/hooks/useAgentStats.ts` (cuenta los **dos** `closed_won_*`; RED confirmado antes del fix). ⚠️ De paso se **regeneró `supabase/types/database.types.ts`** contra el stack local, saldando el **drift arrastrado desde la #71** (`agency_member_role`/`status` + 3 RPCs). Verificación del tramo: pgTAP 680 · Deno 795 · Jest 881 · tsc/lint 0; **E2E de UI bloqueado por el muro de ubicación (#109)** → se verificó contra el remoto con JWT real. **Estadísticas tangibles — fuera puntaje y temperatura (#112, vivo 2026-08-08):** decisión del dueño, el agente ve HECHOS y no un índice opaco. Nuevos: **`hooks/useLeadStats.ts`** (CRÍTICO, 7 tests; **una sola** llamada `supabase.rpc('get_lead_stats', {p_lead_ids})` para toda la lista — nunca N+1; ⚠️ la dependencia del efecto es `leadIds.join(',')` por CONTENIDO y no por referencia: con `[leadIds]` un array literal del caller re-dispara el efecto → setState → render → array nuevo → **loop infinito**, visto en los tests) y **`components/ActionStatsBar.tsx`** (4 tramos que se rellenan: like · video completo · guardó · volvió a ver; lleno `colors.primary`, vacío `colors.paper_3`; en `LeadCard` solo la barra, en `LeadExpandedView` con etiquetas y "hace X"). Modificados: `LeadCard.tsx` y `LeadExpandedView.tsx` (fuera la pill de nivel + puntaje), `lead_status_meta.ts` (**`LEVEL_META`/`get_level_meta` ELIMINADOS** al quedarse sin consumidores; `leads.score`/`level` siguen en base y en tipos — expand·migrate·contract), `CRMScreen.tsx` (dueño de la llamada batch), y el **banner FIJO de solo lectura** en `LeadExpandedView` (antes era texto gris enterrado en el scroll: causa literal del reporte *"no me aparece ninguna opción"* — los permisos NO se ampliaron, solo se explica). Captura en el feed: **`features/feed/lib/videoEngagementDedupe.ts`**, **`lib/appSession.ts`**, **`hooks/useVideoEngagementEvents.ts`** y el cableado en `VideoFeedItem.tsx` (ver [[crm-leads]] §Estadísticas tangibles para los 3 gotchas que costaron dos rondas de guardian: `playToEnd` muerto por `loop`, `timeUpdateEventInterval` que si es 0 mata la feature entera, y el closure obsoleto de `property_id` al reciclar FlashList).  **Privacidad del lead (#75.3, vivo 2026-08-09):** SIN cambios de cliente — todo es RLS. `supabase/migrations/20260809000001_events_raw_lead_gate.sql` (+rollback) sustituye la condición de `events_raw_select`: de `can_manage_property(property_id)` (ser dueño de la propiedad bastaba) a **`private.can_view_user_events(user_id, property_id)`**, que exige **lead ACTIVO** entre esa persona y el agente dueño de la propiedad. Cierra la fuga que abrió #112 — medida en producción con un JWT real: un agente leía el historial de video de gente que nunca lo contactó (PRD §19.1/§19.2). De regalo corrige un hueco de #75.5: el **admin de inmobiliaria no veía NINGÚN evento** del equipo (`can_manage_property` no lo contempla). pgTAP `supabase/tests/35_lead_privacy_test.sql` (plan 15, 5 DELTA) + 3 asserts de `33_` reencuadrados con leads de fixture. Docs: `docs/aviso-privacidad.md` (borrador, NO vigente) y [[privacidad-datos]] (inventario dato→quién lo lee). → [[privacidad-datos]] → [[crm-leads]], [[inmobiliarias-y-agentes]], [[rls-seguridad]], [[design-system]]
- `src/features/saved/` — **propiedades guardadas (tarea #13, vivo)**: ruta `app/(protected)/(tabs)/saved.tsx` (**slot 4 de la tab bar COMPARTIDO por rol** desde #65: Guardados es tab visible para NO-agentes — usuarios normales y admins — y `href:null` para agentes, que lo abren desde Perfil→menú ⋯ "Guardados"; misma ruta/pantalla para ambos) → `SavedScreen.tsx` (**#179: FlatList 3-col borde a borde con el tile** `PropertyGridCard`, `ListHeaderComponent` con `insets.top` porque la pantalla no tiene header de navegación propio y el primer tile tocaba el status bar, RefreshControl, empty state inline; **#187: el `ListHeaderComponent` pinta el título "Guardados" (h1) cuando `has_header=false`** — el tab directo no tiene Stack header (a diferencia de `/profile/saved`, que sí trae el suyo), mismo patrón que el título de tab de `CRMScreen.tsx`; `hidden_ids:Set` para quitado optimista + rollback; **#155: `useFocusEffect`→`refetch()` saltando el primer foco** — como tab queda montada con `freezeOnBlur` y el hook solo consultaba al montar: guardar/quitar desde feed/detalle dejaba la lista desfasada). **`hooks/useSavedProperties.ts` (CRÍTICA TDD, 11 tests, guardian PASS)**: query `from('saves')` embed `properties(+property_videos)` **+ `.eq('user_id', user.id)` EXPLÍCITO (#155)** — ⚠️ NO basta RLS: `saves_select` incluye `OR is_admin()` y las cuentas admin veían los saves de TODOS (ajenos imborrables — `saves_delete` solo filas propias, DELETE de 0 filas SIN error → el item "revivía" — y keys de fila duplicadas en la FlatList `numColumns` si 2 usuarios guardan la misma propiedad), orden `created_at` DESC, transform → `GridProperty` (thumbnail por menor `position`, null-safe; descarta `properties=null`), DI del cliente + lazy require; **`saves` es DELETE duro (sin `deleted_at`)**. `components/SavedGridItem.tsx` (por celda: `useSaveProperty({property_id, initialSaved:true})`; long-press → `Alert` confirmar → unsave + `on_removed`/`on_synced`). Botones globales de engagement: **`src/components/LikeButton.tsx`/`SaveButton.tsx` (#13.1/13.3)** (Reanimated bounce, presentacionales, en `ActionButtons`); `PropertyGridCard` ganó prop opcional `onLongPress`. Contadores reales vía triggers DB (#13.2/13.4, ver migraciones). → [[feed-vertical-video]], [[design-system]]
- `src/features/profile/` — **perfil de agente (tareas #16 vivo, #22 vivo, #23 vivo, #179 vivo, #180 vivo)**: `ProfileScreen.tsx` (ensamblaje compartido `{agent_id, is_own_profile}`; **#23: llama `useAgentStats` y pasa `stats`/`loading` a `ProfileHeader`; #179: le pasa además `is_own_profile` y usa `useAgentStats(agent_id,{include_leads:is_own_profile})`; se eliminó el divider header/grilla**), `hooks/useAgentProfile.ts` (identidad del agente: nombre+foto de `user_preferences`, bio/created_at de `users`, agencia de `agencies` — 2 queries `Promise.all`; **#22.5: re-fetch on focus vía `useFocusEffect`** para anti-stale tras editar), `hooks/useEditProfile.ts` (**#22, vivo**: `save({fullName,imageUri,bio,removePhoto})` dual-write híbrido → `profileService` user_preferences + `users.bio`; error independiente; devuelve `{ok,error}`), `hooks/usePropertiesGrid.ts` (propiedades active/paused del agente + primer video), **`hooks/useAgentStats.ts` (#23.1, CRÍTICA TDD, 5 tests, guardian PASS)**: 3 counts en `Promise.all` (publicaciones=`properties` owner_user_id+status in active/paused+deleted_at null; leads=`leads` agent_id+deleted_at null; cerrados=`leads` agent_id+status in closed_won/closed_lost+deleted_at null), todas `.select('id',{count:'exact',head:true})` — **orden real supabase-js `.select().eq().in().is()` (el guardian atrapó un test que codificaba el orden ficticio `.eq()...select()` + cast que lo ocultaba → habría dado `TypeError` en runtime real)**; error handling degrada a ceros sin throw; patrón `usePropertiesGrid`, `useEffect`+flag `ignore`, `AgentStats` exportado, `firma useAgentStats(agent_id)→{loading,stats|null}`, `components/ProfileHeader.tsx` (avatar/nombre display/badge agencia/miembro-desde/bio; **#23: props `stats?`/`loading?` → renderiza `ProfessionalStats` tras la bio; badge de isotipo `absolute` en esquina del avatar solo con foto (no en iniciales), vía `avatar_wrapper` sin `overflow:hidden`**), **`components/ProfessionalStats.tsx` (#23.2, no crítico)**: profstats sheet (3 columnas iguales publicaciones/leads/cerrados con dividers `colors.paper_3`; número `type_scale.h1` Space Grotesk `colors.ink`, label `type_scale.caption` uppercase `colors.gray_2`; `shadows.sm`+`radii.r_12`; loading→'—'; **`return null` si `stats` null o los 3 counts en 0**), `components/PropertiesGrid.tsx` (**#179: `FlatList numColumns={layout.grid_cols}`=3 borde a borde, `columnWrapperStyle` con `layout.grid_tile_gap`=2, sin padding horizontal; el ancho de celda sale de `grid_tile_width(useWindowDimensions().width)` y se pasa por prop — con `gap`+`flex:1` la última fila PARCIAL estiraba sus celdas**), celdas `PropertyGridCard`, `components/EmptyState.tsx` (`ListEmptyComponent`, copy por is_own_profile; **flash 2026-07-06: íconos Phosphor + CTA opcional + variante `dark` — lo reusan feed vacío, Saved y CRM**), `types.ts` (`AgentProfile`/`GridProperty`), `__tests__/{useEditProfile,useAgentProfile,useAgentStats}.test.tsx` (#22/#23, TDD). Rutas: `app/(protected)/(tabs)/profile.tsx` (propio) + `app/(protected)/profile/[id].tsx` (ajeno, bajo guard) + **`app/(protected)/profile/edit.tsx` (#22: editar avatar/nombre/bio, validación + char counter)**. → [[perfil-agente]], [[design-system]] **#179 (2026-08-16, perfil estilo Instagram):** `hooks/useAgentStats.ts` reescrito — `closed` FUERA (nadie lo consumía; el CRM usa el RPC `get_lead_stats` de `20260808000002`) y ENTRAN `saves`/`likes` como suma en cliente de `properties.save_count`/`like_count` (los mantiene el trigger de `20260701000001`, cero backend nuevo); firma `useAgentStats(agent_id, {include_leads})` — en perfil AJENO no se consulta `leads` porque la RLS devuelve 0 y el header pintaba "0 Leads" como si el agente no tuviera ninguno ([[privacidad-datos]]); 8 tests. `ProfessionalStats.tsx` deja de ser sheet: columnas inline con dos juegos según `is_own_profile` (propio Publicaciones/Leads/Guardados · ajeno Publicaciones/Guardados/Me gusta), labels capitalizados (en MAYÚSCULAS no caben en ~80px) y **ya no se oculta con todo en 0**. `ProfileHeader.tsx` al layout de Instagram: fila avatar 80px + stats, identidad a la izquierda y **bio sin `numberOfLines`** (admitía 280 chars y se cortaba a 3 líneas). **#180 (2026-08-16):** `components/ProfileActions.tsx` NUEVO — la fila de acciones entre bio y grilla (propio: Editar perfil + Guardados; ajeno: Contactar por WhatsApp, oculto sin `users.phone`), con texto a la izquierda e ícono Phosphor a la derecha; esas 2 entradas salieron del `ProfileMenu`. `whatsapp.ts` gana `open_whatsapp_text(phone, texto)` (extraída de `open_whatsapp`, que ahora la llama) — **NO** se usa `open_whatsapp_ef` porque su Alert "✓ Contacto enviado" mentiría: el contacto desde el perfil no crea lead (el CRM exige `property_id`). `useAgentProfile` + `AgentProfile` traen `users.phone`. `useAgentStats` pierde `leads` e `include_leads` (el conteo se consulta en el CRM y solo el dueño) → stats propio Publicaciones/Guardados/Me gusta, ajeno Publicaciones/Me gusta. ⚠️ `ProfileHeader.container.paddingTop` = `s_8 + 40 + s_8` = la banda EXACTA del botón flotante "⋯": con un valor estético, en Android (inset superior chico) el botón caía sobre la 3ª columna de stats.
- `app/admin/` — **panel admin (#7, vivo)**: `_layout.tsx` (guard, re-exporta `src/features/admin/admin-layout.tsx`), `index.tsx` (lista de inmobiliarias, SELECT directo RLS admin), `agencies/create.tsx` (form → invoca `admin-create-agency`), `agencies/[id].tsx` (detalle: token+invite-link de un solo uso vía params + lista de miembros) → [[inmobiliarias-y-agentes]]. **#168.5 (2026-08-16, sin desplegar):** el correo pasa a ser el canal PRIMARIO del alta — `create.tsx` cambia el copy a "le enviamos un correo de invitación a…" y mapea `AUTH_INVITE_FAILED` a un mensaje accionable (fail-closed: reintentar con el mismo correo funciona, GoTrue ya revirtió el usuario); `[id].tsx` degrada `invite_action_link` a respaldo visual (opacity + hint "solo si el correo no llega") condicionado a `email_sent==='true'`, **sin ocultarlo nunca** (contrato con builds instalados). No se construyó rama UI para `email_sent===false` (código muerto confirmado por guardian: `inviteUserByEmail` es atómico, GoTrue revierte el usuario si el envío falla — no existe "organización creada, correo no enviado").
- `src/features/admin/` — **`admin-layout.tsx` (#7, vivo)**: guard `role=admin` (isLoading→spinner, sin sesión→/login, no-admin→/(protected), admin→Slot) → [[inmobiliarias-y-agentes]]
- `src/hooks/useContactAgent.ts` — **el ÚNICO camino para "contactar al agente" (#75.4, vivo, 8 tests)**: `contact_agent(property_id)` invoca la EF `contact-agent` (que crea el lead) y **solo si responde 200** abre WhatsApp con el `phone` y el `message` del servidor; si falla, mensaje en español y **no abre**. El texto NO se arma en el cliente: §19.3 dice que el template es del sistema y no editable. Exporta `CONTACT_AGENT_ERROR_MESSAGES`/`map_contact_agent_error` (un solo mapa, no dos copias). **Por qué existe:** hasta #75.4 había DOS caminos y solo uno registraba el lead — `VideoFeedItem` y `AgentCard` llamaban `open_whatsapp()` directo, así que **se podía contactar al agente desde el feed y en Urbea no quedaba nada**: sin lead, sin el acceso a datos que §19.2 concede al agente sólo tras el contacto, sin los +10 de scoring y sin fila en el CRM (ambos tenían un `ponytail:` "llega en #11"). Hoy lo consumen los **tres** call sites: `ContactAgentButton`, `AgentCard`, `VideoFeedItem`. ⚠️ el cliente Supabase se resuelve **dentro de la acción**, no en el render: montar el hook (lo hace cada ítem del feed) no puede depender de `EXPO_PUBLIC_SUPABASE_URL`. → [[crm-leads]]
- `src/lib/{profileService,r2Resolver}.ts` + `src/hooks/useR2Urls.ts` — **assets vía R2 (#69, vivo)**: `saveProfile` migrado a R2 presigned con contrato de 3 estados en `imageUri` (**#69.6**: `undefined`=KEEP omite la columna en el upsert / `null`=REMOVE / string local=REPLACE sube y guarda el **r2_key** en `user_preferences.profile_photo_url`); `resolve_r2_urls(keys)` = resolver batch key→presigned GET fail-soft con **passthrough de URLs legacy `http(s)://`** (avatares pre-R2 siguen visibles); `useR2Urls` hook fino (consumen `ProfileHeader`, `AgentCard`, preview de `edit.tsx` — que separa `saved_avatar_key` de `picked_image_uri` para no re-subir el key guardado). 35 tests, guardian PASS. ⚠️ pendiente: avatares del CRM leen `users.avatar_url` (columna distinta, sin resolver aún). → [[storage-hibrido]]
- `src/lib/supabase/` — cliente tipado · `src/components/` — **`PrimaryButton.tsx` (#6, vivo)**: CTA liquid-glass salvia (variantes primary/ghost) · **`PropertyGridCard.tsx` (#16, vivo)**: card de firma (thumbnail 4:5, precio héroe Space Grotesk + tick Salvia, badges operación/Pausada; lo reusa el feed #9) · `StepIndicator.tsx` · **`GridSkeleton.tsx` (flash 2026-07-06, vivo)**: grilla 2-col fantasma con pulso (Reanimated) para loading de Saved/PropertiesGrid — complemento claro de `FeedSkeleton` · **`IsotipoMark.tsx` (#23.3→#43.4, vivo)**: isotipo de firma reutilizable `{size?,color?}` — **#43.4: react-native-svg** con los paths EXACTOS del logo final (`urbea-logo-final.html`): U/pin con pata derecha que sube a flecha ↑ + 2 puntos (Ü), trazo 19u round (viewBox 240); reemplaza la v1 de primitivas RN y **revierte la premisa 'sin react-native-svg' de #32/#11**; cableado en `PropertyGridCard` (size 34), `PropertyMarker` (pin, size 14) y `ProfileHeader` — interfaz intacta · **`UrbeaLockup.tsx` (#43.2, vivo)**: lockup del logo final (mark + wordmark 'URBEA' en Outfit) `{size?,color?,direction:'row'|'column'}`; column=hero vertical, lo usa el login · **Iconografía Phosphor (#43.1)**: TODA la app migró de `@expo/vector-icons` (Ionicons) → `phosphor-react-native` bold (fill en activos), 15 archivos + tab bar; `react-native-svg`+`phosphor-react-native` instalados (rebuild nativo). Ver [[design-system]] §Logo final; futuro: loaders, empty states, overlay de play del feed · **`FilterTabs.tsx` (#15, vivo)**: tabs de filtro genéricos `<T extends string>` (`tabs:{value,label}[]`/`value`/`onChange`); extraído del de `profile/components` (que ahora es wrapper fino) y reusado por el CRM · **`ContactAgentButton.tsx` (#14, vivo)**: CTA CRM que invoca EF contact-agent · **`LikeButton.tsx` (#13.1, vivo)**: botón corazón animado — presentacional puro (`active/onPress/count?/size?/style?`); animación spring bounce (1→1.3→1.0) al activarse con Reanimated 4; Salvia cuando active, outline blanco cuando no; sin fondo propio (consumidor provee el estilo del contenedor); reusable en feed overlay y PropertyDetail · **`SaveButton.tsx` (#13.3, vivo)**: botón bookmark animado — análogo a LikeButton (bounce sutil 1→1.15→1.0); Arcilla accent_soft cuando active; a11y labels default 'Guardar propiedad'/'Quitar de guardados' · **`src/theme/theme.ts` (#16, vivo; +#26)**: design system sembrado (tokens del kit 003: colors/radii/shadows/fonts/type_scale/spacing s_4…s_40; **+grupo `layout` #26**: `screen_inset:20`/`grid_gutter:14`/`grid_cols:2`, extraído del prototipo de layout; gestión-claro; dual-mode oscuro pendiente #9; **+#43: grupo `brand`** (verde #1A5E44/carnita #EEE4D0 del logo final, ADITIVO, solo icono+login) + `fonts.logo`=Outfit_600SemiBold) · **Tab bar split por plataforma (#65, vivo)**: `(tabs)/_layout.tsx` bifurca por `Platform.OS` → **`IosNativeTabsLayout.tsx`** (`NativeTabs` expo-router: UITabBar liquid glass iOS 26 nativa; íconos Phosphor rasterizados PNG en `assets/tab-icons/` `{default: regular, selected: fill}`; [+] = trigger `disabled`+listener `tabPress`→wizard; **#111: los PNG se resuelven a `file://` con `useAssets` (expo-asset) ANTES de dárselos a la barra nativa** — en dev el ImageSource era la URL http de Metro y la `UITabBar` bajaba cada ícono ella misma, async y **sin reintento**, dejando el slot vacío para siempre al perder la carrera (de ahí el síntoma intermitente); ⚠️ el helper `to_local` conserva `width/height/scale` vía `Image.resolveAssetSource` — un `{uri}` pelón pinta los íconos **gigantes** encima de las etiquetas) y **`AndroidTabsLayout.tsx`** (el `Tabs tabBar={GlassTabBar}` con la lógica pre-split) · **`GlassTabBar.tsx` (#65, vivo)**: pill flotante liquid glass Android — BlurView `dimezisBlurView` (⚠️ sin esa prop no desenfoca) + lupa deslizante (Reanimated spring 32/420, overlay por variante) + rim gradiente; hooks en `GlassTabBarRow` interno (⚠️ React Navigation llama tabBar() como función plana); rama GlassView (⚠️ jamás tintColor sólido, usar colorScheme) · **tokens `glass` (#65)**: en theme.ts — intensidades/overlays/lens_* + `floating_content_clearance` `Platform.select` (⚠️ iOS: insets.bottom YA incluye la barra nativa; consumidores: PropertyOverlay/AreaSearchPill/PropertyMiniCard/Saved/CRM/PropertiesGrid) · `expo-glass-effect@56.0.4` PINEADO (latest=57 incompatible), módulo nativo → build 1.0.2, no OTA · **`Urbea Prototipo (standalone).html` (raíz, #26)**: referencia canónica de **layout** (acomodo/grid/espaciado), complementa a `urbea-identidad-visual.html` (lenguaje visual); solo layout, nunca color/fuente — ⚠️ la tabbar anclada de ambos HTML quedó **obsoleta** desde #65 (divergencia consciente, ver [[design-system]] §Navegación) → [[design-system]]

## Edge Functions — `supabase/functions/` (Deno; correr `deno test/lint/fmt` DESDE este dir)
**Patrón DI (tarea #5, vivo):** cada función = `handler.ts` (lógica PURA con deps inyectables; lo que importan los tests, offline, sin supabase-js) + `index.ts` (entry de prod: construye deps reales con `Deno.serve`) + `*.test.ts`. Deps externas en `deno.json` (import map: `@supabase/supabase-js`, `@std/assert`). Errores siempre `{error:{code,message}}`. → [[inmobiliarias-y-agentes]], [[rls-seguridad]]
- `validate-invitation/` — **vivo**: POST `{invitationCode}` → 200 `{agency_name}` (preview del código antes de registrarse) | 404/422/400.
- `redeem-invitation/` — **vivo**: POST `{invitationCode,email,password,firstName,lastName}` → orquesta: validar token → `auth.admin.createUser` (email_confirm:true) → RPC `redeem_invitation_atomic` (canje atómico) → 200 `{user_id,agency_id,agency_name,agency_member_id}`. **Compensación** `deleteUser` si la RPC falla (no hay tx distribuida auth↔public). `x-forwarded-for` → 1ª IP (param `inet`).
- `admin-create-agency/` — **vivo (#7)**: POST (requiere JWT admin) `{name,slug,contact_*,owner_email,owner_first_name,owner_last_name}` → verifica `role=admin` (`AdminVerifier`: JWT→`auth.getUser`→`users.role`, 403 si no) → owner por **invitación** (**#168.4, 2026-08-16, sin desplegar: `auth.admin.inviteUserByEmail`** — el correo se ENVÍA de verdad, verificado E2E contra Mailpit local; `generateLink(type:'magiclink')` sobre el usuario ya creado sigue minteando `invite_action_link` como respaldo, fail-open si falla) → RPC **`admin_create_agency_atomic`** (0016→168.3→168.7: agency+member owner+UPDATE users+token hasheado+admin_actions, atómico; **12 params** tras #168, `p_can_publish_properties`/`p_can_advertise`/`p_advertiser_category` opcionales al final) → 201 `{agency_id,owner_user_id,invite_action_link,plain_token,token_id,email_sent,advertiser_category}` (los últimos 2, aditivos de #168). Token plano **solo en la respuesta**; persiste el hash. **Compensación** `deleteUser` si la RPC falla — **y ahora también si `inviteUserByEmail` falla en el envío**: GoTrue hace rollback atómico del usuario (fail-closed, decisión de Abraham), 0 organizaciones/usuarios huérfanos.
- `update-property-status/` — **vivo (#17.5)**: POST (JWT) `{property_id,new_status,closed_reason?}` → `handler` + `make_property_status_updater(client)` (transiciones VALID_TRANSITIONS / ownership del JWT / 🔒 closed exige closed_reason) → 200 propiedad | 400/401/403/404/500. 54 Deno tests.
- `mint-video-url/` — **vivo (#21, dep de #9)**: minter PURO de signed URLs de video. POST (JWT, **auth = verify_jwt del gateway**, sin auth custom en el handler) `{property_ids: string[]}` → 200 `{videos:[{property_id,video_id,signed_url}]}` | 400/405/500. `handler.ts` (DI: valida input→delega→mapea; exclusión silenciosa: property_ids sin video válido se OMITEN, **nunca 404**) + `make_video_url_minter(client)` en `_shared/clients.ts` (**única barrera de seguridad**: query `property_videos⋈properties!inner` filtra `status='active' AND pv.status='ready' AND deleted_at IS NULL`, luego `storage.from('property-videos').createSignedUrl(path,3600)`; service_role bypassa la RLS SELECT rota por el path 2-seg, fail-closed). 34 Deno tests (handler 20 + adapter 14), guardian PASS en críticas. ⚠️ **deploy gotcha:** el bundler del API NO auto-lee `deno.json` → pasar `--import-map supabase/functions/deno.json` (si no, falla el import bare `@supabase/supabase-js` de `clients.ts`; aplica a toda EF que use `clients.ts`). **⚠️ Desactualizado desde #68 — hoy es DUAL-REF:** fila con `cloudflare_uid` → **HLS firmado de Cloudflare Stream** (token RS256 **en el path**, no `?token=`) + `posterUrl` firmado (#68.15); fila solo con `storage_path` → signed URL de Storage (legacy). Precedencia Stream > legacy, fail-closed por fila. El `createSignedUrl` de arriba ya solo aplica a la rama legacy.
- `mint-r2-url/` — **vivo (#69, épica C)**: presigned URLs de **Cloudflare R2** para assets (bucket privado `urbea-assets`). POST (JWT) `{kind: avatar|logo|archive, op: put|get, key?, keys?}` → put: `{url,key,expires}` TTL 900s (key derivado del uid, **autz fail-closed en el handler**: avatar=prefix propio, logo=owner de la agencia vía `make_agency_ownership_verifier`, archive-put=403); get: `{urls:[…]}` en LOTE TTL 3600s (autenticado basta). Minter = adapter puro `make_r2_url_minter` (`_shared/clients.ts`, firma **aws4fetch** vía esm.sh, secrets `R2_*` en runtime). **40 Deno tests, guardian PASS.** ⚠️ deploy sin Docker: `supabase functions deploy mint-r2-url --import-map supabase/functions/deno.json --use-api`. → [[storage-hibrido]]
- `contact-agent/` — **vivo (#14, desplegada)**: contacto WhatsApp + lead CRM desde el **detalle**. POST (JWT) `{propertyId}` → 200 `{success,phone,message,lead_id,property_id}` | 401/400/404/500. **`handler.ts`** lógica pura por DI (4 puertos: `CallerVerifier`/`PropertyResolver`/`LeadRepo`/`OriginRepo`), pipeline: OPTIONS→405→JWT(401)→input UUID(400 manual sin Zod)→resolve propiedad+agente (404 NOT_FOUND / 400 INVALID_PROPERTY_STATE si no-active / 400 AGENT_PHONE_MISSING)→**self-contact 400 CANNOT_CONTACT_SELF**→lead idempotente (find/insert, race 23505→re-find, no 500)→`lead_origin_properties` ON CONFLICT DO NOTHING→**`contact_count` +1 solo si origin nuevo**→mensaje prellenado (`Intl.NumberFormat es-MX` MXN, `/mes` si rent|both) + phone sanitizado (solo dígitos). **`index.ts`** cablea los 4 resolvers reales (service_role): `auth.getUser`, query `properties⋈users!owner_user_id_fkey` (`agent_name`=CONCAT, `agent_id`=u.id), leads find/insert, origin `upsert ignoreDuplicates`+`increment_contact_count` (**read-then-write `// ponytail:` — PostgREST no admite `col=col+1`; RPC para prod**). **79 Deno tests** (5 subtareas críticas TDD+guardian PASS). Deploy con `--import-map`. ⚠️ `agent_id ≡ owner_user_id` (alias del resolver). video_id omitido del tipo (`// ponytail:`, el mensaje no lo usa). **#75.1: el lead nace en `whatsapp_opened` (antes `new`) y el insert se extrajo a `lead_repo.ts`** — vivía **inline en el closure de `Deno.serve`** en `index.ts` y no era testeable; se movió, no se reescribió (`lead_repo.test.ts`). **#75.4 (template §19.3 + video de origen + unificación pineada, 98 Deno tests):** el mensaje pasa a `build_contact_message()` — `Hola, vi tu propiedad en Urbea: {Tipo} en {dirección}, {zona}. Me interesa conocer más detalles.` **Se fue el precio y con él una fuga**: el template viejo lo metía SIEMPRE ignorando `properties.price_visible` (precio oculto → publicado igual en el prellenado); el arreglo es de raíz — `PropertyWithAgent` perdió `price` y `operation_type`, así que la EF ni siquiera pide la columna. **`property_resolver.ts` NUEVO** (mismo movimiento que 75.1 con `lead_repo`): el adapter vivía inline en `index.ts:43-85` y por eso nadie lo testeaba — ahí se escondía que **`video_id` nunca se llenaba** (había un `ponytail:` admitiéndolo), o sea `lead_origin_properties.property_video_id` era **NULL en toda la producción**, contra §9.6; ahora resuelve el video principal (`position` menor entre los no borrados). **Degradación deliberada:** §19.3 pide `(#código)` y `[deep link]`, que crean 74 (`public_code`) y 78.5 (`/v/[id]`, necesita dominio) → la **dirección** ocupa el lugar del código porque cumple su función (que el agente sepa de cuál propiedad le escriben); registrado en **#118**. **Unificación §19.5** ya existía (índice único parcial + find/insert/recovery 23505) pero nada pineaba que la clave es **(agente, usuario) y NO la propiedad** — 4 tests nuevos lo cazan. → [[crm-leads]]
- `update-lead-status/` — **vivo (#15.6, desplegada · reescrita en #75.1)**: POST (JWT del agente) `{lead_id, new_status, note?}` → `handler` + `make_lead_status_updater(client)` → 200 lead | 400/401/403/404/500. ⭐ **`VALID_TRANSITIONS` ELIMINADA (#75.1): transiciones libres**, incluido reabrir un lead cerrado; `INVALID_TRANSITION` ya no se emite (el updater bajó de 3 a 2 queries y dejó de leer el `status` actual). Lo que SÍ se conserva es la frontera de confianza: allowlist de los **11** valores del enum (los 4 nuevos incluidos — **su ausencia era el bug del usuario reencarnado, lo cazó el guardian**) → fuera del enum = 400 `INVALID_INPUT`. Persiste `status`+`updated_at`, e `internal_notes` **solo si viene `note`** (spread condicional). Ownership por **doble query** (`LEAD_NOT_FOUND` vs `UNAUTHORIZED_AGENT`). Códigos: `INVALID_INPUT`/`UNAUTHENTICATED`/`UNAUTHORIZED_AGENT`/`LEAD_NOT_FOUND`/`DB_ERROR`. El timeline lo escribe el **trigger DB** (`20260807000003`), no esta EF. Patrón DI clonado de `update-property-status/`. Deploy con `--import-map`. → [[crm-leads]]
- `update-lead-note/` — **vivo (#29, desplegada · ampliada en #75.5)**: POST (JWT del agente) `{lead_id, note?, is_follow_up?}` → `handler` + `make_note_updater(client)` → 200 `{lead:{id,internal_notes,is_follow_up?}}` | 400/401/403/404/500. Edita `internal_notes` y/o `is_follow_up` (+`updated_at`), **nunca el `status`** (test NU-11). ⭐ **#75.5: `is_follow_up` aditivo aquí en vez de una 4ª EF.** Ambos campos opcionales pero **al menos uno** debe venir; UPDATE por spread condicional con **`!== undefined` en los 3 puntos del flujo** — `is_follow_up=false` es desactivación EXPLÍCITA y un `if (value)` la perdería por falsy. Retrocompat total con el contrato v1.0.3 (solo `note`). `note=""` sigue válido → limpia a `null`. Ownership por **doble query** solo `agent_id`/admin, consistente con `private.can_edit_lead` — **NO** agency-owner/admin (decisión de producto: en solo-lectura, #31). Códigos y patrón DI clonados de `update-lead-status/`. Guardian PASS (orden real supabase-js, sin bug #23). Consumido por `useUpdateLeadNote`. → [[crm-leads]]
- `create-invitation/` — **vivo (#34, 2026-07-03, pendiente deploy al remoto)**: el OWNER genera códigos de invitación adicionales para SU agencia (el token inicial solo salía de `admin-create-agency`). POST (JWT) `{max_uses?, expires_at?}` → 201 `{invitation:{token_id, plain_token, agency_id, max_uses, expires_at}}` | 400/401/403 `NOT_AGENCY_OWNER`/422 `AGENCY_INACTIVE`/500. ⭐ `agency_id` NUNCA viaja en el payload: se deriva de la membresía owner activa del caller (`agency_members` user+owner+active; constraint `one_active_per_user` la hace única) → cero IDOR. Genera plano con `generate_invitation_code()` + `sha256_hex` de `_shared/crypto.ts`; el plano sale UNA vez en la respuesta. Patrón DI clonado de `update-lead-note` (`handler.ts` puro + `make_invitation_creator(client)` + `index.ts` wiring). Body vacío válido (= `{}`, defaults ilimitado/sin expirar). **24 Deno tests** (15 handler + 9 creator), TDD RED→GREEN en 34.1. → [[inmobiliarias-y-agentes]], [[rls-seguridad]]
- `_shared/` — `cors.ts`, `response.ts` (`json_response`/`error_response`), `crypto.ts` (`sha256_hex`; el token se guarda hasheado; **`generate_invitation_code(8)`** alfanumérico, #7), `validation.ts` (`parse_redeem_invitation_input`, **`parse_create_agency_input`** #7), `invitation.ts` (`validate_invitation_token`: NOT_FOUND→REVOKED→EXPIRED→MAX_USES→AGENCY_INACTIVE), `auth_user.ts` (`create_agent_auth_user`, **`create_owner_invite`** + `generateInviteLink` en `AuthAdminClient`, #7), `redeem.ts` (contrato + mapeo P0001→HTTP), **`admin_auth.ts`** (`AdminVerifier`, #7), **`agency.ts`** (`AgencyCreator` + mapeo error→HTTP, #7), **`clients.ts`** (adaptadores reales supabase-js `service_role`; ÚNICO sitio que importa supabase-js; `make_admin_verifier`/`make_agency_creator`/`make_auth_admin`/`make_property_status_updater`/**`make_video_url_minter`** #21/**`make_lead_status_updater`** #15).
- `upgrade-to-agent/` — **vivo (#71.3)**: POST (JWT) `{invitationCode}` → RPC `upgrade_to_agent_atomic` con service key; buscador existente sube a agente (Camino A). Mapea los 8 P0001 a 400/404/409/422. 19 Deno tests.
- `request-agent-upgrade/` — **vivo (#71.3)**: POST (JWT) `{reason?}` → INSERT en `agent_applications` con el **client del usuario** (RLS activa, no service); rol resuelto server-side → 409 `ALREADY_AGENT` si ya es agente/admin; dup pending → 409. 13 Deno tests. `_shared/clients.ts` ganó `user_client(authHeader)`.
- `register-agency/` — **vivo (#71.4)**: POST (JWT, prospecto) `{name,slug?,contact_*,logo_url?}` → RPC `register_agency_atomic` → agencia `pending_approval` SIN membresía (la activación la hace la aprobación admin, #71.5). Anti-spoofing created_by del JWT. 24 Deno tests.
- `manage-agency-member/` — **vivo (#71.6)**: POST (JWT owner/admin de agencia) `{member_id, action: suspend|reactivate|remove}` → `member_manager.ts` (reimplementa `can_manage_agency_member` en TS: 404 anti-IDOR, `CANNOT_MANAGE_OWNER`, VALID_TRANSITIONS; **16 tests unitarios propios** + 19 de handler). Listar miembros NO usa EF (RLS `members_select`).
- ⚠️ **`service_role` necesita grants DML explícitos** en `public` (migración 0014); sin ellos PostgREST devuelve 403 a la capa de servicio. → [[rls-seguridad]]
- _Pendientes_: `properties/` (publicación) → [[propiedades-y-video]].

## E2E — Maestro (`mobile/.maestro/`, subtarea 20.5)

Suite E2E declarativa contra **Supabase local con seed** (determinista) + emulador Android con dev build. **Runner: `bash mobile/.maestro/run-e2e.sh [flujo.yaml]`** (resetea BD + seed + videos + mp4 a la galería + `maestro test`). Prerrequisitos por sesión: `supabase start`, `supabase functions serve --import-map supabase/functions/deno.json`, emulador + Metro (`pnpm emu`), `mobile/.env.local` en bloque LOCAL.

- `login.yaml` — password mala (error ES) + login feliz + sesión persistente al reabrir.
- `botonera.yaml` — smoke de navegación: tabs Feed·Mapa·[+]·Leads·Perfil (+Guardados vía Perfil→menú ⋯), Mis publicaciones, FilterSheet, wizard.
- `feed-interaccion.yaml` — doble-tap like, guardar, swipe, grid Guardados, long-press quitar.
- `registro.yaml` — canje `DEMO2026` → alta 2 fases → onboarding → feed (reruns: requiere `db reset`).
- `contacto-crm.yaml` — filtro Terreno → detalle → CTA WhatsApp (EF `contact-agent`) → login del agente dueño → lead en CRM → estado → nota.
- `publicar.yaml` — wizard 3 pasos con video de galería → "¡Publicada!" → feed + Mis publicaciones.
- `helpers/launch.yaml` / `helpers/login.yaml` — arranque endurecido (deep link `10.0.2.2:8081`, dev menu condicional, apagar burbuja "Tools button") + login parametrizado por `EMAIL`.
- `ios/feed-hls.yaml` (#68.10) — **único flujo de iOS**: login como owner → feed → captura del HLS firmado de Stream. Vive en `ios/` a propósito: `config.yaml` declara `flows: ["*.yaml"]`, que matchea **solo el nivel superior**, así que NO entra en la suite Android de `run-e2e.sh`. Se corre a mano con `maestro --device <UDID-simulador> test mobile/.maestro/ios/feed-hls.yaml`. Gotchas iOS propios: permiso de ubicación es **`inuse`** (en Android `allow`; con `allow` Maestro aborta), deep link a **`localhost:8081`** (el simulador comparte la red del Mac; `10.0.2.2` es solo del emulador Android), **sin `clearState`** (dispara el developer menu del dev client, que en iOS tapa el login y no se cierra con `back` — no existe en iOS; "Continue" solo avanza al sheet principal) → por eso el login es **condicional**, y termina en **`stopApp`** (💰 el feed reproduce en loop = minutos facturados de Stream).

Gotchas Maestro (durables, aprendidos el 2026-07-04): matcheo de texto = **regex de elemento completo** (substring → `.*x.*`; paréntesis escapan); `hideKeyboard` manda BACK si el teclado ya se cerró (**prohibido en la suite** — tap en texto estático); inputs controlados RN pierden el primer `inputText` tras cargar el bundle (patrón retry + assert del texto); el photo picker sincroniza perezoso (`force-stop com.google.android.providers.media.module` tras `adb push` a DCIM; anclar el video por su duración `00:52`); el seed escalona `created_at` (1º del feed = prop0A) y siembra token `DEMO2026` + phones de agentes. testIDs: `login-email/password`, `register-*`, `map-picker`. → [[propiedades-y-video]], [[crm-leads]]

## Entornos de desarrollo — scripts de arranque (`scripts/`)

Un comando levanta el entorno local completo (stack Supabase + Edge Functions contra Cloudflare Stream **real** + Metro). Los tres son equivalentes y **solo difieren en cómo el dispositivo alcanza a la Mac**. `--stop` en cualquiera baja stack y functions. Detalle y riesgos en [[entornos-desarrollo]].

- `scripts/dev-emu.sh` — **emulador Android**. `adb reverse` de `8081` (Metro) y `54321` (Supabase) → la app usa `localhost`, inmune a la Wi-Fi/IP.
- `scripts/dev-ios.sh [simulador]` — **simulador iOS** (default `iPhone 17 Pro`). El simulador comparte la red del Mac → `localhost` directo, sin túnel. Avisa y sale si falta el dev-client.
- `scripts/dev-local.sh` — **teléfono real**. Recalcula la IP de la LAN en cada corrida y la escribe en `mobile/.env.local`.
- `mobile/scripts/emu.sh` (`pnpm emu`) — solo emulador + Metro, **sin** backend local. Anterior a los de arriba; útil cuando `.env.local` ya apunta al remoto.

⚠️ Los scripts **reescriben `EXPO_PUBLIC_SUPABASE_URL` en `mobile/.env.local`** (localhost vs IP de la LAN) → corre uno u otro, no dos a la vez. El dev-client debe estar instalado: `pnpm expo run:android` / `pnpm expo run:ios` una vez.

Gotchas durables: `pnpm --dir mobile`/`-C mobile` **NO funciona** bajo pnpm 11 (toma `mobile` como el comando → `ERR_PNPM_RECURSIVE_EXEC_FIRST_FAIL`) → usar `cd mobile && pnpm …`. El deep link del dev-client debe dispararse **después** de que Metro responda, si no la app arranca con "unexpected end of stream". → [[entornos-desarrollo]]

### Fixture del smoke de memoria (`supabase/scripts/`, #68.10)

- `smoke-oom-fixture.sql` / `smoke-oom-restore.sql` — **par sembrar/revertir, SOLO local.** (Nombrados así a propósito: `.gitignore` ignora `seed-*.sql`/`teardown-*.sql`, que es la convención de los seeds de demo local-only; este fixture SÍ se versiona porque su valor es ser repetible.) La cuenta de Cloudflare Stream tiene **un** video, así que el feed real monta 1 player y no reproduce el escenario del OOM #57 (que necesitaba ~4 players vivos). El seed marca los 10 videos placeholder como `ready` apuntando al **mismo `cloudflare_uid`** → 11 items reproducibles. Requiere **bajar el índice parcial `property_videos_cf_uid_unique`** (el fixture repite el uid a propósito); el teardown lo recrea idéntico. Uso: `docker exec -i supabase_db_urbea-app psql -U postgres -d postgres < supabase/scripts/<archivo>`.
- 🔴 **Correr SIEMPRE el teardown al terminar.** Dejar la BD local con el índice único caído y con filas `ready` falsas es justo la clase de "adaptación local que miente" que produjo el bug #68.16. → [[entornos-desarrollo]], [[feed-vertical-video]]

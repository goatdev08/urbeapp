---
tipo: codebase
actualizado: 2026-06-29
---

# Mapa del codebase

**Pieza clave del vault:** dominio/concepto → archivos exactos. Consultar ANTES de escribir código (¿ya existe algo reutilizable?). Reemplaza `grep`.

## Backend — Supabase (existe)
Detalle de tablas/migraciones en [[db-schema-map]].

| Dominio | Archivos | Concepto |
|---------|----------|----------|
| Identidad | `supabase/migrations/..._identity_users.sql` | [[roles-y-permisos]] |
| Inmobiliarias/Agentes | `..._agencies_and_agents.sql` | [[inmobiliarias-y-agentes]] |
| Propiedades/Video | `..._properties_and_videos.sql` | [[propiedades-y-video]] |
| Storage (video) | `..._storage_property_videos.sql` (0011: bucket `property-videos` + col `storage_path` + RLS INSERT/SELECT en `storage.objects`) · `..._property_videos_ready_requires_storage.sql` (0012: CHECK `ready` exige `storage_path` o `cloudflare_uid`) · `20260710000001_increase_video_size_limit.sql` (#49: `file_size_limit` 100→**500 MB**, idempotente con WHERE guard) · `20260710000002_fix_property_videos_select_rls.sql` (#52: policy SELECT **solo dueño** — rama pública muerta `[2]::uuid` eliminada, cerraba hueco con paths fabricados de 3 seg; lectura pública = solo EF `mint-video-url`; pgTAP `11`. ⚠️ el **límite GLOBAL de subida del proyecto** — 50 MB en plan Free — capea al bucket: subir >50 MB al remoto da 413) | [[propiedades-y-video]] |
| Storage (foto perfil) | `..._profile_photos_storage.sql` (0015, #6: bucket **público** `profile-photos` + RLS por path `auth.uid()` en `storage.objects` + cols `full_name`/`profile_photo_url` en `user_preferences`) | [[onboarding-y-preferencias]] |
| Publicación (RPC) | `20260625000001_publish_property_rpc.sql` (#8: RPC `publish_property_atomic` SECURITY DEFINER search_path fijo — inserta `properties` status=active + `property_videos` status=ready en 1 tx atómica sin EXCEPTION; `location=ST_Point(lng,lat,4326)`) + rollback | [[propiedades-y-video]] |
| Cercanía (RPC PostGIS) | `20260706000001_properties_within_radius_rpc.sql` (#40, **desplegada a urbea-app**: RPC `properties_within_radius(p_lat,p_lng,p_radius_m float8)` SECURITY DEFINER `search_path=public,extensions` — enfoque **A1 "flaco"**: devuelve SOLO `{id, distance_m}` de props `active`+`deleted_at null` dentro del radio, ordenadas por distancia asc; `ST_DWithin`+`ST_Distance`, reusa GiST `properties_location_gix` (0005), PostGIS schema-qualificado `extensions.ST_*`; ⚠️ `ST_Point(p_lng,p_lat)` lng primero; **`revoke execute from public,anon` + grant a `authenticated`** [advisor 0028, defense-in-depth]) + rollback + pgTAP `09` (8 asserts, guardian PASS). Lo consumirá el feed/mapa en #42 (Fase C) | [[mapa-y-ubicacion]], [[propiedades-y-video]] |
| Edge Functions | `supabase/functions/publish-property/` (#8: handler DI + `index.ts` impls reales `CallerVerifier`[agent/admin]+`PropertyPublisher`; valida payload, 401/403, llama RPC) · `update-property-status/` (#17.5: handler DI + `make_property_status_updater(client)` testeable — transiciones/ownership/closed_reason; 54 Deno tests) · `mint-video-url/` (#21: minter PURO de signed URLs, `make_video_url_minter`, auth=verify_jwt gateway, 34 Deno tests) · `contact-agent/` (#14: crea lead+origin+contact_count idempotente desde el detalle, handler DI + 4 resolvers reales en index.ts, **79 Deno tests**, desplegada) · `update-lead-status/` (#15.6: cambia estado del lead por el embudo, valida transición+ownership doble-query, note opcional, **55 Deno tests**) · `update-lead-note/` (#29: **edita `internal_notes` SIN tocar el status** — mirror de update-lead-status sin transiciones; `make_note_updater` doble-query ownership solo `agent_id`/admin, `note=""`→limpia a null, UPDATE nunca incluye `status`; **24 Deno tests**, guardian PASS) · `admin-create-agency/` (#7) · `create-invitation/` (#34: owner genera códigos para su agencia, agencia derivada del JWT, 24 Deno tests, **pendiente deploy**) · `_shared/` (cors/response/auth/validation/clients) | [[crm-leads]], [[propiedades-y-video]], [[feed-vertical-video]], [[inmobiliarias-y-agentes]] |
| Engagement/CRM | `..._engagement_crm.sql` · `20260701000001_engagement_count_triggers.sql` (#13.2/13.4: triggers `AFTER INSERT/DELETE` en `likes`/`saves` → `properties.like_count`/`save_count`, funcs `update_like_count`/`update_save_count` **SECURITY DEFINER** `search_path=''`, `GREATEST(0,-1)` guard, **+ backfill**; desplegada a `urbea-app` 2026-07-01) + rollback | [[crm-leads]], [[feed-vertical-video]] |
| Moderación/Analítica | `..._analytics_moderation_audit.sql` | [[moderacion]], [[notificaciones]] |
| RLS/Seguridad | `..._rls_helpers_and_policies.sql`, `..._security_perf_hardening.sql` | [[rls-seguridad]] |
| Legal | `..._user_profile_legal.sql` | [[legal-consentimientos]] |
| Tests | `supabase/tests/01_constraints_test.sql`, `02_rls_test.sql`, `03_storage_test.sql`, `03_redeem_invitation_test.sql`, `04_profile_photos_test.sql` (#6: 17 asserts bucket+RLS+cols), `05_admin_create_agency_test.sql` (#7: RPC 0016, plan 20), `06_publish_property_rpc_test.sql` (#8: RPC publish_property_atomic, plan 8), `07_engagement_counts_test.sql` (#13.2/13.4: triggers like_count/save_count + backfill, plan 14, verde en tx revertida contra remoto), `09_properties_within_radius_test.sql` (#40: RPC PostGIS por radio, plan 8 — existe/security-definer/incluye-dentro/excluye-fuera/orden-asc/status-active/deleted-null/orden-lng-lat con `ST_Project` a distancia conocida ±100m) | [[rls-seguridad]], [[mapa-y-ubicacion]] |
| Tests EF/hooks (mobile+deno) | `supabase/functions/publish-property/handler.test.ts` (#8: 43 Deno tests, DI) · `supabase/functions/update-property-status/{handler,property_status_updater}.test.ts` (#17.5: 44+10 Deno tests) · `mobile/src/features/publish/__tests__/{useVideoUpload,usePublish,usePublish.edit,useLoadProperty}.test.tsx` (#8+#17.8: 14+10+9+8 Jest) · `mobile/src/features/profile/__tests__/usePropertyActions.test.tsx` (#17.7: 22 Jest) | [[propiedades-y-video]] |

## Documentación de producto
| Concepto | Fuente |
|----------|--------|
| [[feed-vertical-video]] | `docs/PRD.md` §9 |
| [[propiedades-y-video]] | `docs/PRD.md` §12-13 |
| [[roles-y-permisos]] | `docs/PRD.md` §4 |
| [[crm-leads]] | `docs/PRD.md` §19 |
| [[monetizacion-pago-por-video]] | `docs/PRD.md` §17 |
| Alcance demo | `docs/PRD-MVP-demo.md` |
| Lineamientos | `docs/lineamientos-desarrollo.md` |

## App móvil — `mobile/` (inicializada · tareas #1, #2)
Base **existe** (Expo SDK 56, dev build, Expo Router, TS strict, standalone). Lo ya construido:
- `mobile/app.config.ts` — config dinámica: `com.urbea.app` (iOS+Android), slug/scheme `urbea`, owner EAS `deabratech`, Google Maps vía `process.env.GOOGLE_MAPS_API_KEY`, plugins (expo-dev-client/router/video). projectId EAS `85c7157a-…`.
- `mobile/eas.json` — perfiles `development`/`preview`/`production`. Proyecto EAS: `@deabratech/urbea`.
- `mobile/.npmrc` — `node-linker=hoisted` (gotcha Metro+pnpm). `mobile/.env.local` (gitignored) con credenciales Supabase; `.env.example` con nombres.
- `mobile/app/_layout.tsx` — root: `SafeAreaProvider > AuthProvider > Stack` (headerShown:false). `mobile/app/login.tsx` — pantalla de login (fuera del grupo protegido); **enlace "¿Tienes un código de invitación? Regístrate" → `/register` (#20.13** — antes el registro era inalcanzable desde el login). `mobile/app/register.tsx` — **dos modos (#15/#20/#50):** modo `user` (registro libre → `supabase.auth.signUp` → trigger `handle_new_user` → `role='user'` **buscador**) y modo `agent` (código de invitación, 2 fases: `validate-invitation` → `redeem-invitation` → auto-login → `/onboarding` → `role='agent'`). Feature `src/features/registration/`. El default de rol lo pone el trigger, NO el cliente — revertido a `user` por #50 (migración `20260708000001_signup_default_buscador`; ver [[roles-y-permisos]]). **branding espejo del login (#20.13):** fondo `brand.carnita` + `UrbeaLockup` + botón `brand.green` + tokens theme (antes grises hardcodeados "en pausa hasta #19", ya cancelada).
- `mobile/app/(protected)/_layout.tsx` — guard de rutas: `FilterProvider > LocationProvider > ProtectedLayoutWithLocation` (auth gate → **location gate** #41: `useLocation().status` → `LocationWall` por variante o el contenido) · `mobile/app/(protected)/index.tsx` — home protegida (placeholder "Urbea"; `/` resuelve aquí, el grupo es transparente a la URL).
- `mobile/src/lib/supabase/client.ts` — **cliente Supabase tipado** `createClient<Database>` + AsyncStorage (persistSession, autoRefreshToken) + listener `AppState` start/stopAutoRefresh (hotspot global; smoke test 200 OK contra remoto) → [[rls-seguridad]].
- `mobile/src/types/database.ts` — re-export de `supabase/types/database.types.ts`.
- `mobile/tsconfig.json` — strict (+ noUncheckedIndexedAccess, exactOptionalPropertyTypes), alias `@/*`→`src/*`.
- **Tests** (tarea #2): `jest-expo` + `@testing-library/react-native`; `mobile/jest.config.js` (preset, `moduleNameMapper @/`, `transformIgnorePatterns` con fix `.pnpm`), `mobile/jest.setup.js` (mock AsyncStorage). Correr: `pnpm test`. Ver [[comandos]].
- **Lint** (tarea #24, vivo): `mobile/eslint.config.js` — flat config CJS, **preset Expo mínimo** (`eslint-config-expo/flat`, sin reglas custom; `eslint@9.39.4`+`eslint-config-expo@56.0.4` alineado a SDK 56), `ignores` de artefactos (`android/`,`.expo/`,`dist/`,`ios/`,`node_modules/`). Script `pnpm lint` (`eslint .`) = **gate válido, exit 0** (baseline 17 errores→0; ~9 warnings documentados no bloqueantes). Solo cubre `mobile/` (backend Deno = `deno check`). Gotcha: dependencia nativa transitiva `unrs-resolver` requiere `pnpm approve-builds` (→ `mobile/pnpm-workspace.yaml`). El preset trajo `react-hooks/set-state-in-effect` (regla nueva): los 13 hits del patrón fetch-on-mount+ignore-flag llevan `eslint-disable` puntual justificado por línea. Correr: `cd mobile && pnpm lint`.

Estructura prevista por feature (carpetas `src/{features,components,theme,hooks}` ya creadas, vacías):
- `src/features/auth/` — **Auth email/password (tarea #2, vivo)**: `context.tsx` (AuthProvider + `useAuth()` → {session,user(perfil public.users),isLoading,signIn,signOut}; carga perfil por `id=auth.uid()`, listener onAuthStateChange), `validation.ts` (validación pura de form), `auth-errors.ts` (`map_auth_error` → mensajes ES), `protected-layout.tsx` (guard: loading→Redirect→Slot), `components/form-field.tsx`. Login email/password; el **registro** (libre→buscador / código→agente) vive en `app/register.tsx` (ver arriba, #15/#20/#50). → [[roles-y-permisos]], [[inmobiliarias-y-agentes]]
- `src/features/onboarding/` — **Onboarding agente (tarea #6, vivo)**: ruta `mobile/app/onboarding.tsx` → `OnboardingScreen.tsx` (nombre + foto, validación, skip foto, progreso, nav a `/(protected)`, guard re-show), `validation.ts` (`is_valid_full_name`), `components/AvatarPicker.tsx`, `hooks/useImagePicker.ts` (cámara/galería, permisos ES). Servicios en `src/lib/`: `profileService.ts` (`saveProfile`: **#52: upload por streaming `createSignedUploadUrl({upsert:true})` + `createUploadTask`** + upsert `user_preferences`), `imageUtils.ts` (`processProfileImage`: resize 512 + compresión ≤1MB; **#52: `new File(uri).exists/.size` — el `getInfoAsync` del entry principal LANZA en runtime en expo-file-system v56**). El thumbnail de video (`features/publish/lib/videoThumbnail.ts`) usa el mismo patrón streaming (#52). → [[onboarding-y-preferencias]]
- `src/features/feed/` — **feed vertical de video (tarea #9, vivo)**: ruta `app/(protected)/(tabs)/index.tsx` delega a `FeedScreen.tsx` (FlashList v2 vertical paginada — `pagingEnabled`+`snapToInterval(height)`+`drawDistance(height)` para precarga; skeleton/empty/error/pull-to-refresh/scroll-infinito). **#54: un solo return con contenido condicional (`is_skeleton`/`is_error`/`is_empty`/feed) + botón de filtros y `<FilterSheet>` renderizados UNA vez bajo `show_filters = !is_skeleton && !is_error` (alcanzables desde el empty state; ocultos en skeleton/error). El empty state se ramifica por `active_filter_count`: >0 = filtered-empty (icon MagnifyingGlass, "No hay propiedades con estos filtros" + CTA "Limpiar filtros"→`clear_filters()`, re-query automático por identidad de `filters`); =0 = BD-vacía (copy/CTA original). Fix de la trampa de UX: filtrar a 0 resultados ya no atrapa al usuario. NO crítica (pantalla).** `types.ts` (`FeedProperty`/`FeedPropertyWithUrl`=+signed_url+video_id). `lib/feedProperties.ts` (**crítica TDD, 15 tests**: `fetchFeedProperties(cursor)` → query properties active+video ready, llama EF `mint-video-url` con batch property_ids → `{videos:[{property_id,video_id,signed_url}]}`, **merge fail-closed reconciliando el video embebido con el video_id minteado**, nextCursor). Hooks: `useFeedProperties.ts` (estado loading/error/paginación/refetch/loadMore), `useFeedActiveIndex.ts` (viewability 70% + AppState + `useFocusEffect` de `expo-router` → isActive), `useLikeProperty.ts`/`useSaveProperty.ts` (**crítica TDD, 16 tests**: persistencia optimista en `likes`[por video]/`saves`[por propiedad], rollback, conflicto único 23505, user de `useAuth`; `likeOnly` idempotente para doble-tap). Componentes: `VideoFeedItem.tsx` (expo-video `useVideoPlayer`+`VideoView` cover, play/pause por isActive, fallback de error, spinner, doble-tap `GestureDetector`→likeOnly+`Haptics`), `PropertyOverlay.tsx` (precio MXN+specs+rail like/guardar glass+avatar, mockup identidad pantalla feed; **#27: bloque de info tappable `onPropertyPress`→`router.push(/property/[id])`, separado del doble-tap=like y del avatar=onAgentPress**), `HeartAnimation.tsx` (Reanimated), `FeedSkeleton.tsx` (shimmer). **#42 (Fase C proximidad, crítica TDD+guardian PASS)**: `fetchFeedProperties` ahora llama la RPC `properties_within_radius` (coords por DI `deps.coords` — producción las inyecta `useFeedProperties` desde `useLocation()`, fallback GDL, refetch al llegar — y `p_radius_m=filters.radius_m`) → `{id,distance_m}[]`; expansión ×2 máx 3 reintentos si vacía (agotada → vacío SIN tocar PostgREST; error → throw); **paginación rediseñada a offset sobre los ids de la RPC** (cursor=offset string, `ids.slice(offset,offset+10)`→`.in('id',…)`, nextCursor sobre ids → páginas cortas post-filtro aceptadas `// ponytail:`); re-sort cliente por `distance_map`; `build_filter_query` y merge fail-closed intactos. 20+15 tests (EC-16..19, EC-F14/F15). **#58→#62**: `filters.radius_m === null` (default) = "Sin límite" — ⭐ #62 corrigió el path plano de #58.3: el feed **SIEMPRE va por la RPC** (carga contextual por cercanía con todo radio); null → `p_radius_m = UNLIMITED_RADIUS_M` (21,000 km, exportada del lib) **sin expansión ×2** (vacío con radio infinito = no hay props); el `.range()` plano quedó eliminado del feed (helper `mint_and_build_feed_data` sigue compartido); ⚠️ el chequeo `=== null` va ANTES del `?? DEFAULT_RADIUS_M` (`??` trataría null como ausente — candados EC-NULL-RPC-1..7); `undefined` sigue cayendo al default 5 km. **#61**: `VideoFeedItem` con **player estable por instancia** — `useVideoPlayer` nace UNA vez con la fuente inicial; cambios de fuente (reciclaje FlashList / URL re-firmada en refetch) entran por `player.replaceAsync()` + reset `has_error`/`is_paused` + resume si activo (fix del error Android "Cannot set prop 'player'… shared object already released": `useReleasingSharedObject` liberaba/recreaba el player por cambio de fuente y el view nativo alcanzaba a recibir el liberado). Stack nativo (9.10): `@shopify/flash-list`+`react-native-reanimated`(v4 +`react-native-worklets`)+`react-native-gesture-handler`+`expo-haptics`+`phosphor-react-native` (#43, antes @expo/vector-icons) (babel reanimated plugin + `GestureHandlerRootView` en `app/_layout.tsx`). ⚠️ **modo oscuro** con `ink_feed`/hardcodes `// ponytail:` (dual-mode formal en theme pendiente). ⚠️ **pendiente: nuevo dev build** (módulos nativos nuevos) antes de probar en device. **#56 (buscar en esta zona, vivo)**: `fetchFeedProperties` gana rama **aditiva** `filters.area != null` que overridea coords→`area.center`, base_radius→`area.radius_m`, attempts→MAX (sin expansión, reusa el mecanismo `is_unlimited`); zona gana sobre `radius_m` (EC-Z1..Z7, guardian PASS); con `area==null` bit-idéntico a #42/#58/#62. `FeedScreen` empty state de zona ("No hay publicaciones en esta zona" + "Limpiar zona") como **PRIMERA** rama del `is_empty` (area no cuenta en `active_filter_count`) + `ZoneActiveChip` dark. → [[feed-vertical-video]]
- `src/features/property-detail/` — **pantalla de detalle de propiedad (tarea #10, vivo)**: ruta `app/(protected)/property/[id].tsx` (bajo guard, necesita auth para like/save) → `PropertyDetailScreen.tsx` (ensamblaje: hero video oscuro full-bleed + `ScrollView` paper claro con `layout.screen_inset`; loading→`DetailSkeleton`, error→UI con back; back flotante + `ActionButtons` flotante sobre el hero; CTA sticky "Contactar por WhatsApp"). **Tema híbrido fiel al mockup #5: hero oscuro + contenido paper claro** (NO todo oscuro). `types.ts` (`PropertyDetail` con `agent`/`agency`/`videos[]` anidados). `hooks/usePropertyDetail.ts` (query `properties` active+deleted_at null con embeds `users!properties_owner_user_id_fkey`+`agencies!properties_agency_id_fkey`+`property_videos`; **2ª query a `user_preferences`** para nombre/foto del agente — gotcha #16; filtro/sort de videos en cliente; **llama EF `mint-video-url` fail-soft** por el signed_url, igual que el feed #9). Componentes: `PropertyVideoPlayer.tsx` (hero `useVideoPlayer`+`VideoView` patrón VideoFeedItem, video position 1, badge "N videos", sin carrusel, cleanup-safe), `PropertyInfoHeader.tsx` (badge operación + property_type outline, precio-héroe MXN `Intl.NumberFormat` + tick salvia patrón `PropertyGridCard`, specs bed/water/resize Ionicons null-safe con hairlines silver), `AmenityChips.tsx` (chips de flags `pet_friendly`/`allows_no_guarantor`/`student_friendly` + array `amenities` con diccionario 13 valores; oculta si vacío), `PropertyMap.tsx` (`react-native-maps` patrón `MapPicker`, oculta si null), `AgentCard.tsx` (sheet paper, avatar patrón ProfileHeader, icono WhatsApp directo `open_whatsapp` = contacto rápido SIN CRM), **`ActionButtons.tsx` (CRÍTICA TDD, 10 tests, guardian PASS): reusa `useLikeProperty`/`useSaveProperty` de feed SIN tocarlos** (like por property_video_id oculto si null, save por property_id; accessibilityLabels), `DetailSkeleton.tsx` (shimmer patrón FeedSkeleton). Utils: `utils/parseLocation.ts` (**decodifica EWKB hex `0101000020…` Y WKT `POINT(lng lat)`; lng primero, 18 tests** — invertir = bug silencioso; #27: PostgREST devuelve geography como EWKB hex, no WKT) + `utils/whatsapp.ts` (`open_whatsapp(phone,address)` directo SIN CRM [AgentCard] + **`open_whatsapp_ef(phone,message)` #14**: deep link `whatsapp://send` → fallback `wa.me` → `Alert` "✓ Contacto enviado"). **CTA sticky CRM (#14): `mobile/src/components/ContactAgentButton.tsx`** (reusa `PrimaryButton`, llama EF `contact-agent`, mapea codes a ES, callback `onContactReady`→`open_whatsapp_ef`; gate de phone oculta el CTA). Share OMITIDO (decisión usuario). **#27: el feed (`PropertyOverlay`) y el perfil (`ProfileScreen`) navegan aquí vía `router.push(/property/[id])`.** → [[crm-leads]], [[propiedades-y-video]], [[feed-vertical-video]], [[design-system]]
- `src/features/search/` — **filtros básicos del feed + mapa (tarea #12, vivo)**: `FilterProvider` montado en `app/(protected)/_layout.tsx` (un solo `FilterState` compartido por feed y mapa). `components/FilterSheet.tsx` (**RN Modal nativo, NO @gorhom/bottom-sheet** — decisión para evitar z-index del autocomplete de zona dentro de un bottom-sheet; tema gestión-claro slide-up; secciones Operación/Tipo/Precio/Zona/Recámaras/Extras; footer Limpiar→`clear_filters()` / Aplicar→`onClose`). Componentes reutilizables: `FilterChipGroup.tsx` (multi-select de pills `{options,selected,onChange}`, usado por Operación=rent/sale y Tipo=5 enums), `BedroomsSelector.tsx` (segmented any/1+/2+/3+ → `bedrooms>=value`), `ZoneAutocomplete.tsx` (TextInput + dropdown inline sin portal). **`lib/filterQuery.ts` (CRÍTICA TDD, 27 tests, guardian PASS)**: `build_filter_query(query, filters)` puro aplica solo filtros activos sobre el query builder de supabase-js **ADEMÁS de** status/deleted_at base (NO en su lugar) — ⚠️ **sutileza `operation_type='both'`**: selección no vacía S → `.in('operation_type', [...S,'both'])` (una propiedad 'both' matchea cualquier modalidad); booleanos `true`→`.eq(col,true)`, `false`→sin filtro; zone→`.eq` exacto; `EMPTY_FILTERS`→cero llamadas. `get_active_filter_count` cuenta por grupo (precio=1) para el badge. **`lib/zones.ts` (CRÍTICA TDD, 15 tests, guardian PASS)**: `fetch_distinct_zones` (query `select('zone')` active/deleted_at null, dedup+orden client-side, **SIN ILIKE a DB** → honra el gotcha 🔒 no-ILIKE-sin-índice) + `filter_zones` (pura, substring case-insensitive, normaliza acentos NFD). **`lib/filterStorage.ts` (CRÍTICA TDD, 7 tests, guardian PASS)**: `save_filters`/`load_filters` AsyncStorage (key `urbea_filters`, **fail-safe**→`EMPTY_FILTERS` en null/JSON corrupto/shape inválida). `filterStore.tsx` (`FilterProvider` useReducer + `useFilters()` {filters, set_filter, clear_filters, active_filter_count}; hidrata en mount con `load_filters`, persiste debounced 500ms). `hooks/useZoneAutocomplete.ts` (carga única + debounce 300ms). `types.ts` (`FilterState`). **Integración**: `fetchFeedProperties(cursor?,deps?,filters?)` y `fetchMapProperties(deps?,filters?)` ganaron `filters` último param opcional (backward-compat: 25 tests preexistentes intactos); badge `active_filter_count` en `FeedScreen` y `MapSearchBar` (prop nueva); refetch por identidad de `filters` en useEffect. **Alcance: filtros demo completos incl. recámaras + toggles (pet_friendly/allows_no_guarantor/student_friendly)** — decisión usuario de ir más allá del demo-lean del concepto. **#42→#58→#62**: `radius_m: number | null` en `FilterState` — **default `null` = "Sin límite"** (#62: quita el TOPE pero el feed conserva el orden por cercanía vía RPC con `UNLIMITED_RADIUS_M`; solo el MAPA salta la RPC con null; 🔒 SOLO parámetro de la RPC de proximidad, JAMÁS entra a `build_filter_query` — candados EC-26/EC-F14/EC-M13/EC-30) + `components/RadiusSelector.tsx` (**#58: slider continuo custom PanResponder** 0–50 km step 1 km + toggle "Sin límite"→`onChange(null)`/reactivar→5 km, a11y adjustable increment/decrement, OTA-safe sin deps nuevas — descartados @react-native-community/slider [módulo nativo] y gesture-handler/reanimated [worklets no mockeables en jest]; antes pills 5/10/20/50) como **PRIMERA sección** del sheet; badge NO cuenta `radius_m` (`// ponytail:` en `get_active_filter_count`); `load_filters` hidrata pre-#42/pre-#58 con merge `{...EMPTY_FILTERS,...parsed}` (probado sin cambio de código, EC-STORAGE-1..7 en `filterStorage.migration.test.ts`). **#56 (buscar en esta zona, vivo)**: `FilterState` gana `area:{center:{lat,lng},radius_m}|null` (2º eje geoespacial, override de `radius_m`, gana sobre él); 🔒 igual que `radius_m`: fuera de `build_filter_query` y NO cuenta en `get_active_filter_count` (EC-32/33/34). **Efímera**: `filterStorage` excluye `area` (`save_filters` destructura fuera; `load_filters` fuerza `area:null` tras merge, defensivo vs JSON legacy; EC-AREA-1..4, + EC-S2/S6 actualizados al contrato area-excluida). `clear_filters` **preserva** `area` (ejes independientes). Chip `components/ZoneActiveChip.tsx` (prop `dark`, "Zona activa · Quitar"→`set_filter('area',null)`) reusado en feed+mapa. → [[busqueda-y-filtros]], [[feed-vertical-video]], [[mapa-y-ubicacion]]
- `src/features/map/` — **mapa global de propiedades (tarea #11, vivo)**: ruta `app/(protected)/(tabs)/map.tsx` (fina → `MapScreen`; tab "Mapa" añadido en `(tabs)/_layout.tsx` entre Inicio y Perfil). `MapScreen.tsx` (`MapContent` con hooks + `MapErrorBoundary` clase; MapView centrado en `GDL_REGION`, `onRegionChangeComplete`→region state, `cluster_properties(filtered,region)` memo, render `PropertyMarker`/`ClusterMarker`, selección→`PropertyMiniCard` overlay, cluster tap→`animateToRegion` delta/2 zoom-in, mini-card tap→`router.push(/property/[id])`, overlays loading/error). `constants.ts` (`GDL_REGION` Guadalajara). `types.ts` (`MapProperty`). **`lib/mapProperties.ts` + `hooks/useMapProperties.ts` (CRÍTICA TDD, 10 tests, guardian PASS)**: query `properties` active+deleted_at null (select +`location`, sin paginar) → `parse_location` (reusado de property-detail) → **fail-closed** omite location null/no-parseable. **`lib/clusterMarkers.ts` `cluster_properties()` (CRÍTICA TDD, 14 tests, guardian PASS)**: clustering **custom puro sin dependencia** (decisión grilling) — grid absoluto `cellSize=delta/divisions(8)`, 1→point/>1→cluster (id `cluster_<x>_<y>`, centroide media), guard `delta<=0`. `lib/formatPrice.ts` (`format_compact_price` $15k/$2.4M, `format_full_price` es-MX). Componentes: `PropertyMarker.tsx` (teardrop borderRadius+rotate-45 + **isotipo real `IsotipoMark` contra-rotado +45deg** (#32, antes play Ionicons) + price tag compacto; **rent=primary salvia / sale|both=accent arcilla**; `tracksViewChanges={false}` perf), `ClusterMarker.tsx` (círculo `ink` + count, tono distinto), `PropertyMiniCard.tsx` (card inferior flotante, **liquid glass** BlurView, NO Callout nativo por bug Android), `MapSearchBar.tsx` (barra top **liquid glass** + filtro **cliente** address/property_type, `useSafeAreaInsets`). **Sin lib de clustering** (custom). **#42 (Fase C proximidad, crítica TDD+guardian PASS)**: `fetchMapProperties` llama la RPC `properties_within_radius` (coords por DI `deps.coords`, fallback GDL) + expansión ×2 máx 3 reintentos → `.in('id', TODOS los ids)` **sin paginar ni re-ordenar** + `build_filter_query` intacto (loop de expansión duplicado del feed a propósito, `// ponytail:` techo = 3er consumidor → extraer helper); `useMapProperties` inyecta coords de `useLocation()` con refetch al llegar. **#58**: con `filters.radius_m === null` (default desde #58) el mapa **salta la RPC** → todas las activas sin `.in('id',…)` + `build_filter_query` (helper `build_map_result`; candados EC-MAP-NULL-1..3; `// ponytail:` techo = sin radio puede traer muchas props, aceptable escala demo). (#62 NO tocó el mapa: los pins no tienen orden — solo el FEED dejó de saltar la RPC con null.) **42.4**: `MapScreen` se recentra con `animateToRegion` UNA vez cuando las coords llegan post-mount (`coords_used_ref`; el gate deja pasar `loading`) — `GDL_REGION` queda como fallback (el "sin expo-location" de #11 quedó superado); ⚠️ punto azul `showsUserLocation` SE MANTIENE, pendiente confirmar con cliente. 14+14 tests (EC-MAP-1..3, EC-M11..M14). ⚠️ requiere dev build (react-native-maps nativo); `GOOGLE_MAPS_API_KEY` ya en `app.config.js`. Deuda: filtros sliders reales + guard NaN-delta = futuro. (#32→#43.4: el pin usa `IsotipoMark`, ahora **react-native-svg** con la geometría del logo final `urbea-logo-final.html`.) **#56 (buscar en esta zona, vivo)**: `lib/viewportToArea.ts` (**crítica TDD, 7 tests, guardian PASS**: `viewport_to_area(region)→{center,radius_m}` Haversine a mano radio=diagonal/2, clamp 100–50000, pura); `fetchMapProperties` gana rama **aditiva** `filters.area != null` (ANTES del check `radius_m===null`) que llama la RPC con center/radio de la zona sin expansión (zona gana sobre `radius_m`; EC-ZM1..ZM5); `MapScreen` gana `AreaSearchPill.tsx` (pill flotante, debounce 500ms en `onRegionChangeComplete`→`viewport_to_area`→`set_filter('area')`→`router.push('/')`) + `ZoneActiveChip` (claro). Aditivo: con `area==null` #42/#58/#62 bit-idénticos (verificado guardian). → [[mapa-y-ubicacion]], [[propiedades-y-video]], [[busqueda-y-filtros]]
- `src/features/location/` — **permiso + ubicación cliente, muro bloqueante (tarea #41 / Fase B, vivo)**: **`lib/permissionDecision.ts` (CRÍTICA TDD, 4 EC, guardian PASS)** `decide_permission_action({granted,canAskAgain})`→`'granted'|'request'|'open_settings'` (pura, sin `expo-location`). **`LocationProvider.tsx` + `useLocation()` (crítica de facto, 12 tests, guardian PASS)**: `{status:'loading'|'permission_denied'|'gps_off'|'granted', coords, request, refresh}`; `Accuracy.Balanced`, coord cacheada por sesión, `is_mounted` ref (patrón `AuthContext`). 3 estados en mount (`getForegroundPermissionsAsync`→`hasServicesEnabledAsync`→`getCurrentPositionAsync`). ⚠️ **`getCurrentPositionAsync` LANZA** "Current location is unavailable" en emulador sin fix → try/catch → `gps_off` (EC-8, cazado en smoke). **Listener `AppState 'active'`→`refresh()`** (EC-9): re-evalúa al volver de Ajustes sin re-pedir permiso. **`LocationWall.tsx`** muro full-screen funcional (tokens `theme.ts` + `PrimaryButton`, NO gate branding #19), 2 variantes `permission_denied`/`gps_off`. Gate en `app/(protected)/_layout.tsx` (uniforme por rol). Config: `expo-location@56.0.19` + plugin en `app.config.js` (⚠️ instalar con `pnpm dlx expo install`, NO `pnpm add` que trae 57.x incompatible SDK56) → **nuevo dev build**. E2E: `helpers/launch.yaml` concede `permissions.location: allow` + `setLocation` GDL → 6/6 verde. Fase C (#42, vivo): `useFeedProperties`/`useMapProperties` consumen `useLocation().coords` para feed/mapa por cercanía. → [[mapa-y-ubicacion]]
- `src/features/publish/` — **wizard 3 pasos + subida video (tarea #8, vivo)**: rutas `app/(protected)/publish/_layout.tsx` (`PublishFormProvider` envuelve Stack + `WizardHeader` con `useSegments()`→StepIndicator), `step1.tsx` (operación rent/sale/both + tipo casa/departamento/local/oficina/terreno), `step2.tsx` (price/recámaras/baños/m²/descripción + `AddressAutocomplete` + `MapPicker` + toggles nicho + `validate_step2`), `step3.tsx` (expo-image-picker video → preview expo-video → upload → botón Publicar). `store/PublishFormContext.tsx` (`usePublishForm`→{state,update,reset}), `store/types.ts` (`PublishFormState`/`PublishFormPayload`), `validation.ts` (`validate_step1/2/3`, `get_property_payload`, **`validate_video_size`+`MAX_VIDEO_SIZE_BYTES`=524288000 #49**). Componentes: `SelectionCard`, `NumericStepper`, `AddressAutocomplete` (Places API New REST+fetch, key `EXPO_PUBLIC_GOOGLE_PLACES_API_KEY`, degrada sin key), `MapPicker` (react-native-maps, marker draggable, no escribe coords falsas). Hooks (críticos, TDD): `hooks/useVideoUpload.ts` (genera video_id UUID → sube a `{user_id}/{video_id}.mp4` bucket property-videos, escribe storage_path solo en éxito; **#52: subida por STREAMING — `createSignedUploadUrl` + `new File(uri).createUploadTask` (PUT, `UploadType.BINARY_CONTENT`, Content-Type video/mp4, `onProgress` real con cap 0.99) — cierra de raíz el OOM de Hermes de #49; guard 500 MB con `File.size` síncrono ANTES del signed URL; status 413 → mensaje específico de límite del servidor; 21 tests Jest, guardian PASS**), `hooks/usePublish.ts` (**#17.8**: ramifica create/edit — create invoca EF `publish-property`; **editMode+propertyId → `from('properties').update({...}).eq('id')` UPDATE directo con RLS**, sin EF), `hooks/useLoadProperty.ts` (**#17.8**: carga propiedad+videos por id → mapea a `PublishFormState` para pre-fill; `FormPrefiller` en `_layout.tsx`). **#53 (fix duplicación al editar, vivo):** el "modo edición" ya no viaja por URL param (se perdía entre `router.push` step1→step2→step3 → step3 caía en create mode → duplicaba property+video). Ahora `PublishFormState` lleva **`edit_mode:boolean`+`property_id:string|null`** (default false/null en `INITIAL`); `FormPrefiller` (en `_layout.tsx`) hace `update({edit_mode:true, property_id})` de inmediato **y prellena una sola vez** (guard `useRef` — no pisa ediciones al re-montar step1); `step3.tsx` lee `is_edit_mode`/`property_id` del **contexto** (`state.edit_mode`), NO de `useLocalSearchParams` → inmune a la pérdida del param; en edit mode **oculta el picker de video** (placeholder informativo "El video no se puede cambiar en modo edición" — evita uploads huérfanos, video no reemplazable en v1). `CleanupOnUnmount` resetea el form al salir del wizard (defensivo: el Provider vive en `_layout` y ya se desmonta al salir). Sin migraciones (policy `properties_update` ya desplegada). RED 5 EC-CTX-* + guardian PASS (67/67 publish). Componente reutilizado fuera del feature: `src/components/StepIndicator.tsx`. → [[propiedades-y-video]]
- `src/features/profile/` (gestión, **tarea #17, vivo**) — **Mis publicaciones**: ruta `app/(protected)/profile/my-listings.tsx` (Stack bajo profile, NO tab; entrada desde `ProfileScreen`). `hooks/useMyProperties.ts` (todos los status del agente + video_count + contadores, `refetch`), `hooks/usePropertyActions.ts` (changeStatus/pause/close[guard closed_reason]/delete[soft-delete], invoca EF `update-property-status`; 22 tests TDD), `components/{PropertyListItem,PropertyActionMenu,FilterTabs,ClosePropertyDialog,DeletePropertyDialog}.tsx`, tipo `MyProperty` en `types.ts`. Token nuevo `colors.danger`. **#55 (vivo): refresco cross-pantalla al eliminar** — `src/lib/propertyEvents.ts` (mini pub/sub sin deps: `emitPropertyDeleted(id)`/`onPropertyDeleted→unsubscribe`, `Set` interno; tests `src/lib/__tests__/propertyEvents.test.ts`); `usePropertyActions.deleteProperty` **emite** en la rama de éxito; los 3 hooks de datos `useFeedProperties`/`usePropertiesGrid`/`useSavedProperties` **se suscriben** (`useEffect([])`, cleanup=unsubscribe) y quitan solo ese id del estado local (**remoción optimista, sin refetch** → feed no reinicia video ni pierde scroll; no montadas cargan limpias por `deleted_at`/RLS → sin `useFocusEffect`). Ambas subtareas CRÍTICAS TDD+guardian PASS (`__tests__/{useFeedProperties,usePropertiesGrid,useSavedProperties}.test.tsx`). → [[propiedades-y-video]], [[perfil-agente]]
- `src/features/agency/` — **invitaciones del owner (tarea #34, vivo)**: ruta `app/(protected)/agency/invitations.tsx` (guard `useAgencyRole().isOwner` — spinner en loading, `<Redirect>` a perfil si no es owner; form máx-usos opcional entero≥1, vacío=ilimitado; éxito → bloque one-time con aviso + `CopyCard` + "Generar otro"; `// ponytail:` sin campo de expiración — la EF ya soporta `expires_at`). `api.ts` (`create_invitation` invoca EF `create-invitation`, patrón `registration/api.ts`), **`hooks/useCreateInvitation.ts` (CRÍTICA TDD, 6 tests)**: `{create, invitation, loading, error_code, reset}` — `error_code` del backend o `'UNKNOWN'`, `create` limpia estado previo (gotcha: `renderHook` de RNTL es async → `await renderHook(...)`). `components/CopyCard.tsx` (theme-based, `expo-clipboard`, estado "Copiado ✓" 2s; el CopyCard del admin sigue pre-theme — migrarlo aquí es deuda del audit). Entrada: botón "Invitar agentes" en `ProfileScreen` solo `is_own_profile && isOwner`. **`src/lib/supabase/edge-errors.ts`** (#34.2): `extract_error_code` extraído de `registration/api.ts` para reuso cross-feature (5 tests). Código vigente en remoto: `DEMO2026` (GDL Premium, ilimitado). → [[inmobiliarias-y-agentes]], [[design-system]]
- `src/features/leads/` — **CRM del agente (tarea #15, vivo)**: ruta `app/(protected)/(tabs)/crm.tsx` (tab oculto a no-agentes vía `user.role==='agent'` + `<Redirect>`; tab declarado en `(tabs)/_layout.tsx` con `href:null` condicional — RLS/EF = capa real de seguridad). `screens/CRMScreen.tsx` (ensamblaje: `useAgentLeads` → `FilterTabs` + búsqueda `TextInput` + `FlatList<LeadCard>` + Modal `LeadExpandedView`; filtros y búsqueda **client-side** con `useMemo` en cascada, sin re-query; loading/error/empty states). Hooks: `hooks/useAgentLeads.ts` (query `from('leads')` con embeds `users!leads_user_id_fkey(first_name,last_name,avatar_url,phone)` (#30: identidad del buscador desde `users`, `full_name`=`build_full_name(...)`, `profile_photo_url`=`avatar_url`; ya NO `user_preferences`)+`lead_origin_properties→properties→property_videos`; RLS filtra `agent_id`; `.is('deleted_at',null)`; `.order('updated_at',{ascending:false})`; transforma raw→`AgentLead` null-safe; **10 tests**), **`hooks/useUpdateLeadStatus.ts` (CRÍTICA TDD, 13 tests, guardian PASS)**: invoca EF `update-lead-status` vía `functions.invoke` (DI del cliente, patrón `usePropertyActions`: `is_working_ref`+force-update síncrono, spread condicional de `note`, `onSuccess` solo en éxito; retorna `{ok,error}`). Componentes: `components/LeadCard.tsx` (avatar+nombre, badge de estado, thumbnail, tiempo relativo), `components/LeadExpandedView.tsx` (Modal bottom-sheet RN nativo: selector de 7 estados + textarea de notas + "Ver propiedad" `router.push('/property/[id]')` + WhatsApp `open_whatsapp(lead.phone,...)`). `lead_status_meta.ts` (**mapa compartido** estado→etiqueta/color, fuente única para Card+ExpandedView), `types.ts` (`AgentLead`/`LeadStatus`/**`Agent`** #28). **Owner-view de la agencia (tarea #28, vivo):** el dueño de la inmobiliaria ve los leads de todos sus agentes vía un selector — SIN cambios de backend (la policy RLS `leads_select` + helper `private.is_agency_owner_of(agent_id)` de migración 0010 ya lo permiten). Hooks nuevos (ambos CRÍTICOS TDD+guardian): **`hooks/useAgencyRole.ts`** (6 tests; query `agency_members` por `user_id=auth.uid()` + `status='active'` → `{isOwner,agencyId,memberRole}`; el rol de agencia vive SOLO en `agency_members.member_role`, NUNCA en `users.role` que es 'agent' incluso para owners) y **`hooks/useAgencyAgents.ts`** (8 tests; solo ejecuta si `isOwner`; embed `agency_members→users(id,first_name,last_name,avatar_url)` **SIN cast** — ⚠️ el nombre/foto se leen de `users`, NO de `user_preferences`: el owner NO puede leer el `user_preferences` de sus agentes por RLS `user_prefs_select` (fila propia), pero SÍ sus filas `users` vía `is_agency_owner_of`; `full_name`=first+last, `profile_photo_url`=avatar_url; filtra `member_role='agent'`+`status='active'`, orden client-side nulls al final; el owner NO aparece como opción). **`hooks/useAgentLeads.ts` extendido (#28, 15 tests):** firma `(agentId?: string | null)` retrocompatible — string añade `.eq('agent_id',id)`, null/ausente = sin filtro (RLS decide: agente ve los suyos, owner ve TODOS los de la agencia = semántica "Todos" agregada). ⚠️ **fix embed:** el buscador se embebe con FK explícita `users!leads_user_id_fkey(...)` — `leads` tiene DOS FKs a `users` (agent_id/user_id) y el embed bare rompía en runtime ("Could not embed... more than one relationship"); bug latente de #15 que los mocks no atrapaban (+test de regresión). Componente **`components/AgentSelector.tsx`** (no crítico; rail `ScrollView` horizontal de chips: "Todos" + un chip por agente con avatar/iniciales, activo en `colors.primary`; presentacional puro). Wiring en `CRMScreen.tsx`: `useAgencyRole`+`useAgencyAgents` incondicionales, selector renderizado solo si `isOwner && agents.length>0`, subtítulo "Leads de tu equipo" para owner. Demo: `owner.oeste@urbea.demo`/`urbea2026` (2 agentes). **Guardar nota sin cambiar estado (tarea #29, vivo):** **`hooks/useUpdateLeadNote.ts` (CRÍTICA TDD, 13 tests, guardian PASS)** invoca EF `update-lead-note` vía `functions.invoke` (mirror de `useUpdateLeadStatus`: `is_working_ref`+force-update síncrono que **bloquea doble-submit**, DI del cliente, `onSuccess` solo en éxito, `note=""` se pasa tal cual); en `LeadExpandedView` un botón **"Guardar nota"** (pill `colors.primary`, visible solo si la nota cambió, deshabilitado durante guardado, error inline separado del de status) persiste la nota sin forzar transición — **respeta `readOnly`** (owner solo-lectura #28: no se muestra). El status-change SIGUE mandando la nota (retrocompat #15). ⚠️ el GREEN tocó `mobile/jest.setup.js` (fix legítimo de drenado `act()` FIFO→LIFO por contaminación cruzada entre tests, verificado por guardian; NO enmascara bug del hook). Deuda menor: `#FFFFFF` hardcodeado (sin token `on_primary` en theme.ts, sigue convención preexistente del archivo). **Identidad del buscador (tarea #30, vivo):** el CRM mostraba "Usuario sin nombre" porque el agente/owner no podía leer la fila `users` del buscador de su lead. Fix RLS: helper `private.can_view_user_as_lead_searcher(user_id)` + cláusula OR en `users_select` (migración `supabase/migrations/20260702000001_rls_lead_searcher_identity.sql` + rollback; pgTAP `supabase/tests/08_rls_lead_searcher_test.sql` plan 12). Cliente: `useAgentLeads` lee identidad desde `users` (embed `users!leads_user_id_fkey(first_name,last_name,avatar_url,phone)`); `build_full_name` **extraída a `utils/full_name.ts`** (util compartido) e importada por `useAgentLeads` **y** `useAgencyAgents` (antes local en este último). `user_prefs_select` NO se toca. Suite leads 26/26 en esos 2 hooks, tsc 0, guardian PASS. Desplegada al remoto vía `apply_migration`. → [[crm-leads]], [[inmobiliarias-y-agentes]], [[rls-seguridad]], [[design-system]]
- `src/features/saved/` — **propiedades guardadas (tarea #13, vivo)**: ruta `app/(protected)/(tabs)/saved.tsx` (**sin tab desde el flash 2026-07-06**: `href:null`, se abre desde Perfil→menú ⋯ "Guardados"; la tab bar canónica es Feed·Mapa·[+]·Leads·Perfil per mockup `.tabbar`) → `SavedScreen.tsx` (FlatList 2-col reusa `PropertyGridCard`, RefreshControl, empty state inline; `hidden_ids:Set` para quitado optimista + rollback). **`hooks/useSavedProperties.ts` (CRÍTICA TDD, 11 tests, guardian PASS)**: query `from('saves')` embed `properties(+property_videos)`, RLS filtra al usuario, orden `created_at` DESC, transform → `GridProperty` (thumbnail por menor `position`, null-safe; descarta `properties=null`), DI del cliente + lazy require; **`saves` es DELETE duro (sin `deleted_at`)**. `components/SavedGridItem.tsx` (por celda: `useSaveProperty({property_id, initialSaved:true})`; long-press → `Alert` confirmar → unsave + `on_removed`/`on_synced`). Botones globales de engagement: **`src/components/LikeButton.tsx`/`SaveButton.tsx` (#13.1/13.3)** (Reanimated bounce, presentacionales, en `ActionButtons`); `PropertyGridCard` ganó prop opcional `onLongPress`. Contadores reales vía triggers DB (#13.2/13.4, ver migraciones). → [[feed-vertical-video]], [[design-system]]
- `src/features/profile/` — **perfil de agente (tareas #16 vivo, #22 vivo, #23 vivo)**: `ProfileScreen.tsx` (ensamblaje compartido `{agent_id, is_own_profile}`; **#23: llama `useAgentStats(agent_id)` y pasa `stats`/`loading` a `ProfileHeader`**), `hooks/useAgentProfile.ts` (identidad del agente: nombre+foto de `user_preferences`, bio/created_at de `users`, agencia de `agencies` — 2 queries `Promise.all`; **#22.5: re-fetch on focus vía `useFocusEffect`** para anti-stale tras editar), `hooks/useEditProfile.ts` (**#22, vivo**: `save({fullName,imageUri,bio,removePhoto})` dual-write híbrido → `profileService` user_preferences + `users.bio`; error independiente; devuelve `{ok,error}`), `hooks/usePropertiesGrid.ts` (propiedades active/paused del agente + primer video), **`hooks/useAgentStats.ts` (#23.1, CRÍTICA TDD, 5 tests, guardian PASS)**: 3 counts en `Promise.all` (publicaciones=`properties` owner_user_id+status in active/paused+deleted_at null; leads=`leads` agent_id+deleted_at null; cerrados=`leads` agent_id+status in closed_won/closed_lost+deleted_at null), todas `.select('id',{count:'exact',head:true})` — **orden real supabase-js `.select().eq().in().is()` (el guardian atrapó un test que codificaba el orden ficticio `.eq()...select()` + cast que lo ocultaba → habría dado `TypeError` en runtime real)**; error handling degrada a ceros sin throw; patrón `usePropertiesGrid`, `useEffect`+flag `ignore`, `AgentStats` exportado, `firma useAgentStats(agent_id)→{loading,stats|null}`, `components/ProfileHeader.tsx` (avatar/nombre display/badge agencia/miembro-desde/bio; **#23: props `stats?`/`loading?` → renderiza `ProfessionalStats` tras la bio; badge de isotipo `absolute` en esquina del avatar solo con foto (no en iniciales), vía `avatar_wrapper` sin `overflow:hidden`**), **`components/ProfessionalStats.tsx` (#23.2, no crítico)**: profstats sheet (3 columnas iguales publicaciones/leads/cerrados con dividers `colors.paper_3`; número `type_scale.h1` Space Grotesk `colors.ink`, label `type_scale.caption` uppercase `colors.gray_2`; `shadows.sm`+`radii.r_12`; loading→'—'; **`return null` si `stats` null o los 3 counts en 0**), `components/PropertiesGrid.tsx` (`FlatList numColumns=2`, celdas `PropertyGridCard`), `components/EmptyState.tsx` (`ListEmptyComponent`, copy por is_own_profile; **flash 2026-07-06: íconos Phosphor + CTA opcional + variante `dark` — lo reusan feed vacío, Saved y CRM**), `types.ts` (`AgentProfile`/`GridProperty`), `__tests__/{useEditProfile,useAgentProfile,useAgentStats}.test.tsx` (#22/#23, TDD). Rutas: `app/(protected)/(tabs)/profile.tsx` (propio) + `app/(protected)/profile/[id].tsx` (ajeno, bajo guard) + **`app/(protected)/profile/edit.tsx` (#22: editar avatar/nombre/bio, validación + char counter)**. → [[perfil-agente]], [[design-system]]
- `app/admin/` — **panel admin (#7, vivo)**: `_layout.tsx` (guard, re-exporta `src/features/admin/admin-layout.tsx`), `index.tsx` (lista de inmobiliarias, SELECT directo RLS admin), `agencies/create.tsx` (form → invoca `admin-create-agency`), `agencies/[id].tsx` (detalle: token+invite-link de un solo uso vía params + lista de miembros) → [[inmobiliarias-y-agentes]]
- `src/features/admin/` — **`admin-layout.tsx` (#7, vivo)**: guard `role=admin` (isLoading→spinner, sin sesión→/login, no-admin→/(protected), admin→Slot) → [[inmobiliarias-y-agentes]]
- `src/lib/supabase/` — cliente tipado · `src/components/` — **`PrimaryButton.tsx` (#6, vivo)**: CTA liquid-glass salvia (variantes primary/ghost) · **`PropertyGridCard.tsx` (#16, vivo)**: card de firma (thumbnail 4:5, precio héroe Space Grotesk + tick Salvia, badges operación/Pausada; lo reusa el feed #9) · `StepIndicator.tsx` · **`GridSkeleton.tsx` (flash 2026-07-06, vivo)**: grilla 2-col fantasma con pulso (Reanimated) para loading de Saved/PropertiesGrid — complemento claro de `FeedSkeleton` · **`IsotipoMark.tsx` (#23.3→#43.4, vivo)**: isotipo de firma reutilizable `{size?,color?}` — **#43.4: react-native-svg** con los paths EXACTOS del logo final (`urbea-logo-final.html`): U/pin con pata derecha que sube a flecha ↑ + 2 puntos (Ü), trazo 19u round (viewBox 240); reemplaza la v1 de primitivas RN y **revierte la premisa 'sin react-native-svg' de #32/#11**; cableado en `PropertyGridCard` (size 34), `PropertyMarker` (pin, size 14) y `ProfileHeader` — interfaz intacta · **`UrbeaLockup.tsx` (#43.2, vivo)**: lockup del logo final (mark + wordmark 'URBEA' en Outfit) `{size?,color?,direction:'row'|'column'}`; column=hero vertical, lo usa el login · **Iconografía Phosphor (#43.1)**: TODA la app migró de `@expo/vector-icons` (Ionicons) → `phosphor-react-native` bold (fill en activos), 15 archivos + tab bar; `react-native-svg`+`phosphor-react-native` instalados (rebuild nativo). Ver [[design-system]] §Logo final; futuro: loaders, empty states, overlay de play del feed · **`FilterTabs.tsx` (#15, vivo)**: tabs de filtro genéricos `<T extends string>` (`tabs:{value,label}[]`/`value`/`onChange`); extraído del de `profile/components` (que ahora es wrapper fino) y reusado por el CRM · **`ContactAgentButton.tsx` (#14, vivo)**: CTA CRM que invoca EF contact-agent · **`LikeButton.tsx` (#13.1, vivo)**: botón corazón animado — presentacional puro (`active/onPress/count?/size?/style?`); animación spring bounce (1→1.3→1.0) al activarse con Reanimated 4; Salvia cuando active, outline blanco cuando no; sin fondo propio (consumidor provee el estilo del contenedor); reusable en feed overlay y PropertyDetail · **`SaveButton.tsx` (#13.3, vivo)**: botón bookmark animado — análogo a LikeButton (bounce sutil 1→1.15→1.0); Arcilla accent_soft cuando active; a11y labels default 'Guardar propiedad'/'Quitar de guardados' · **`src/theme/theme.ts` (#16, vivo; +#26)**: design system sembrado (tokens del kit 003: colors/radii/shadows/fonts/type_scale/spacing s_4…s_40; **+grupo `layout` #26**: `screen_inset:20`/`grid_gutter:14`/`grid_cols:2`, extraído del prototipo de layout; gestión-claro; dual-mode oscuro pendiente #9; **+#43: grupo `brand`** (verde #1A5E44/carnita #EEE4D0 del logo final, ADITIVO, solo icono+login) + `fonts.logo`=Outfit_600SemiBold) · **`Urbea Prototipo (standalone).html` (raíz, #26)**: referencia canónica de **layout** (acomodo/grid/espaciado), complementa a `urbea-identidad-visual.html` (lenguaje visual); solo layout, nunca color/fuente → [[design-system]]

## Edge Functions — `supabase/functions/` (Deno; correr `deno test/lint/fmt` DESDE este dir)
**Patrón DI (tarea #5, vivo):** cada función = `handler.ts` (lógica PURA con deps inyectables; lo que importan los tests, offline, sin supabase-js) + `index.ts` (entry de prod: construye deps reales con `Deno.serve`) + `*.test.ts`. Deps externas en `deno.json` (import map: `@supabase/supabase-js`, `@std/assert`). Errores siempre `{error:{code,message}}`. → [[inmobiliarias-y-agentes]], [[rls-seguridad]]
- `validate-invitation/` — **vivo**: POST `{invitationCode}` → 200 `{agency_name}` (preview del código antes de registrarse) | 404/422/400.
- `redeem-invitation/` — **vivo**: POST `{invitationCode,email,password,firstName,lastName}` → orquesta: validar token → `auth.admin.createUser` (email_confirm:true) → RPC `redeem_invitation_atomic` (canje atómico) → 200 `{user_id,agency_id,agency_name,agency_member_id}`. **Compensación** `deleteUser` si la RPC falla (no hay tx distribuida auth↔public). `x-forwarded-for` → 1ª IP (param `inet`).
- `admin-create-agency/` — **vivo (#7)**: POST (requiere JWT admin) `{name,slug,contact_*,owner_email,owner_first_name,owner_last_name}` → verifica `role=admin` (`AdminVerifier`: JWT→`auth.getUser`→`users.role`, 403 si no) → owner por **invitación** (`auth.admin.generateLink({type:'invite'})`, sin password) → RPC **`admin_create_agency_atomic`** (0016: agency+member owner+UPDATE users+token hasheado+admin_actions, atómico) → 201 `{agency_id,owner_user_id,invite_action_link,plain_token,token_id}`. Token plano **solo en la respuesta**; persiste el hash. **Compensación** `deleteUser` si la RPC falla.
- `update-property-status/` — **vivo (#17.5)**: POST (JWT) `{property_id,new_status,closed_reason?}` → `handler` + `make_property_status_updater(client)` (transiciones VALID_TRANSITIONS / ownership del JWT / 🔒 closed exige closed_reason) → 200 propiedad | 400/401/403/404/500. 54 Deno tests.
- `mint-video-url/` — **vivo (#21, dep de #9)**: minter PURO de signed URLs de video. POST (JWT, **auth = verify_jwt del gateway**, sin auth custom en el handler) `{property_ids: string[]}` → 200 `{videos:[{property_id,video_id,signed_url}]}` | 400/405/500. `handler.ts` (DI: valida input→delega→mapea; exclusión silenciosa: property_ids sin video válido se OMITEN, **nunca 404**) + `make_video_url_minter(client)` en `_shared/clients.ts` (**única barrera de seguridad**: query `property_videos⋈properties!inner` filtra `status='active' AND pv.status='ready' AND deleted_at IS NULL`, luego `storage.from('property-videos').createSignedUrl(path,3600)`; service_role bypassa la RLS SELECT rota por el path 2-seg, fail-closed). 34 Deno tests (handler 20 + adapter 14), guardian PASS en críticas. ⚠️ **deploy gotcha:** el bundler del API NO auto-lee `deno.json` → pasar `--import-map supabase/functions/deno.json` (si no, falla el import bare `@supabase/supabase-js` de `clients.ts`; aplica a toda EF que use `clients.ts`).
- `contact-agent/` — **vivo (#14, desplegada)**: contacto WhatsApp + lead CRM desde el **detalle**. POST (JWT) `{propertyId}` → 200 `{success,phone,message,lead_id,property_id}` | 401/400/404/500. **`handler.ts`** lógica pura por DI (4 puertos: `CallerVerifier`/`PropertyResolver`/`LeadRepo`/`OriginRepo`), pipeline: OPTIONS→405→JWT(401)→input UUID(400 manual sin Zod)→resolve propiedad+agente (404 NOT_FOUND / 400 INVALID_PROPERTY_STATE si no-active / 400 AGENT_PHONE_MISSING)→**self-contact 400 CANNOT_CONTACT_SELF**→lead idempotente (find/insert, race 23505→re-find, no 500)→`lead_origin_properties` ON CONFLICT DO NOTHING→**`contact_count` +1 solo si origin nuevo**→mensaje prellenado (`Intl.NumberFormat es-MX` MXN, `/mes` si rent|both) + phone sanitizado (solo dígitos). **`index.ts`** cablea los 4 resolvers reales (service_role): `auth.getUser`, query `properties⋈users!owner_user_id_fkey` (`agent_name`=CONCAT, `agent_id`=u.id), leads find/insert, origin `upsert ignoreDuplicates`+`increment_contact_count` (**read-then-write `// ponytail:` — PostgREST no admite `col=col+1`; RPC para prod**). **79 Deno tests** (5 subtareas críticas TDD+guardian PASS). Deploy con `--import-map`. ⚠️ `agent_id ≡ owner_user_id` (alias del resolver). video_id omitido del tipo (`// ponytail:`, el mensaje no lo usa). → [[crm-leads]]
- `update-lead-status/` — **vivo (#15.6, desplegada)**: POST (JWT del agente) `{lead_id, new_status, note?}` → `handler` + `make_lead_status_updater(client)` → 200 lead | 400/401/403/404/500. Valida `new_status` ∈ enum `lead_status` + **transición** del embudo (`new→{contacted,discarded}`; `contacted→{in_progress,closed_lost,discarded}`; `in_progress→{visit_scheduled,closed_won,closed_lost,discarded}`; `visit_scheduled→{in_progress,closed_won,closed_lost,discarded}`; terminales `closed_won`/`closed_lost`/`discarded`). Persiste `status`+`updated_at`, e `internal_notes` **solo si viene `note`** (spread condicional, no sobrescribe). Ownership por **doble query** (`LEAD_NOT_FOUND` vs `UNAUTHORIZED_AGENT`). Códigos: `INVALID_INPUT`/`INVALID_TRANSITION`/`UNAUTHENTICATED`/`UNAUTHORIZED_AGENT`/`LEAD_NOT_FOUND`/`DB_ERROR`. Patrón DI clonado de `update-property-status/`. **55 Deno tests** (40 handler + 15 updater), guardian PASS. Deploy con `--import-map`. ⚠️ la nota va acoplada a la transición → editar nota independiente **resuelto en #29** (`update-lead-note/`, abajo). → [[crm-leads]]
- `update-lead-note/` — **vivo (#29, desplegada)**: POST (JWT del agente) `{lead_id, note}` → `handler` + `make_note_updater(client)` → 200 `{lead:{id,internal_notes}}` | 400/401/403/404/500. Edita SOLO `internal_notes` (+`updated_at`), **nunca el `status`** (test NU-11) — desacopla la nota del cambio de estado que acoplaba #15. `note=""` es válido → limpia a `null` (distingue "key ausente"→400 de "string vacío"→200). Ownership por **doble query** solo `agent_id`/admin (LEAD_NOT_FOUND vs UNAUTHORIZED_AGENT), consistente con `private.can_edit_lead` — **NO** agency-owner (decisión de producto: owners en solo-lectura). Códigos y patrón DI clonados de `update-lead-status/`. **24 Deno tests** (13 handler + 11 note_updater), guardian PASS (orden real supabase-js, sin bug #23). Consumido por el hook móvil `useUpdateLeadNote`. → [[crm-leads]]
- `create-invitation/` — **vivo (#34, 2026-07-03, pendiente deploy al remoto)**: el OWNER genera códigos de invitación adicionales para SU agencia (el token inicial solo salía de `admin-create-agency`). POST (JWT) `{max_uses?, expires_at?}` → 201 `{invitation:{token_id, plain_token, agency_id, max_uses, expires_at}}` | 400/401/403 `NOT_AGENCY_OWNER`/422 `AGENCY_INACTIVE`/500. ⭐ `agency_id` NUNCA viaja en el payload: se deriva de la membresía owner activa del caller (`agency_members` user+owner+active; constraint `one_active_per_user` la hace única) → cero IDOR. Genera plano con `generate_invitation_code()` + `sha256_hex` de `_shared/crypto.ts`; el plano sale UNA vez en la respuesta. Patrón DI clonado de `update-lead-note` (`handler.ts` puro + `make_invitation_creator(client)` + `index.ts` wiring). Body vacío válido (= `{}`, defaults ilimitado/sin expirar). **24 Deno tests** (15 handler + 9 creator), TDD RED→GREEN en 34.1. → [[inmobiliarias-y-agentes]], [[rls-seguridad]]
- `_shared/` — `cors.ts`, `response.ts` (`json_response`/`error_response`), `crypto.ts` (`sha256_hex`; el token se guarda hasheado; **`generate_invitation_code(8)`** alfanumérico, #7), `validation.ts` (`parse_redeem_invitation_input`, **`parse_create_agency_input`** #7), `invitation.ts` (`validate_invitation_token`: NOT_FOUND→REVOKED→EXPIRED→MAX_USES→AGENCY_INACTIVE), `auth_user.ts` (`create_agent_auth_user`, **`create_owner_invite`** + `generateInviteLink` en `AuthAdminClient`, #7), `redeem.ts` (contrato + mapeo P0001→HTTP), **`admin_auth.ts`** (`AdminVerifier`, #7), **`agency.ts`** (`AgencyCreator` + mapeo error→HTTP, #7), **`clients.ts`** (adaptadores reales supabase-js `service_role`; ÚNICO sitio que importa supabase-js; `make_admin_verifier`/`make_agency_creator`/`make_auth_admin`/`make_property_status_updater`/**`make_video_url_minter`** #21/**`make_lead_status_updater`** #15).
- ⚠️ **`service_role` necesita grants DML explícitos** en `public` (migración 0014); sin ellos PostgREST devuelve 403 a la capa de servicio. → [[rls-seguridad]]
- _Pendientes_: `properties/` (publicación) → [[propiedades-y-video]].

## E2E — Maestro (`mobile/.maestro/`, subtarea 20.5)

Suite E2E declarativa contra **Supabase local con seed** (determinista) + emulador Android con dev build. **Runner: `bash mobile/.maestro/run-e2e.sh [flujo.yaml]`** (resetea BD + seed + videos + mp4 a la galería + `maestro test`). Prerrequisitos por sesión: `supabase start`, `supabase functions serve --import-map supabase/functions/deno.json`, emulador + Metro (`pnpm emu`), `mobile/.env.local` en bloque LOCAL.

- `login.yaml` — password mala (error ES) + login feliz + sesión persistente al reabrir.
- `botonera.yaml` — smoke de navegación: tabs Feed·Mapa·[+]·Leads·Perfil (+Guardados vía Perfil→menú ⋯), Mis publicaciones, FilterSheet, wizard.
- `feed-interaccion.yaml` — doble-tap like, guardar, swipe, grid Guardados, long-press quitar.
- `registro.yaml` — canje `DEMO2026` → alta 2 fases → onboarding → feed (reruns: requiere `db reset`).
- `contacto-crm.yaml` — filtro Terreno → detalle → CTA WhatsApp (EF `contact-agent`) → login del agente dueño → lead en CRM → estado → nota.
- `publicar.yaml` — wizard 3 pasos con video de galería → "¡Publicada!" → feed + Mis publicaciones.
- `helpers/launch.yaml` / `helpers/login.yaml` — arranque endurecido (deep link `10.0.2.2:8081`, dev menu condicional, apagar burbuja "Tools button") + login parametrizado por `EMAIL`.

Gotchas Maestro (durables, aprendidos el 2026-07-04): matcheo de texto = **regex de elemento completo** (substring → `.*x.*`; paréntesis escapan); `hideKeyboard` manda BACK si el teclado ya se cerró (**prohibido en la suite** — tap en texto estático); inputs controlados RN pierden el primer `inputText` tras cargar el bundle (patrón retry + assert del texto); el photo picker sincroniza perezoso (`force-stop com.google.android.providers.media.module` tras `adb push` a DCIM; anclar el video por su duración `00:52`); el seed escalona `created_at` (1º del feed = prop0A) y siembra token `DEMO2026` + phones de agentes. testIDs: `login-email/password`, `register-*`, `map-picker`. → [[propiedades-y-video]], [[crm-leads]]

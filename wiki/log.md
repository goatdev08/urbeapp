# Bitácora del proyecto

Append-only. Prefijo: `## [YYYY-MM-DD] tipo | título`

## [2026-08-16] polish | #180 Acciones visibles en el perfil (editar/guardados o WhatsApp) y leads fuera del header

- Pedido de Abraham tras ver #179 en el dispositivo, con la referencia de Instagram anotada: "Editar perfil" y "Guardados" estaban escondidas tras el menú ⋯ y suben a botones visibles entre la bio y la grilla; en un perfil ajeno ese mismo espacio lo ocupa un botón de **contacto por WhatsApp** del mismo tamaño y forma. Texto a la izquierda, ícono Phosphor a la derecha (`PencilSimple`, `BookmarkSimple`, `WhatsappLogo`). Las dos entradas movidas se quitaron del menú: dos caminos al mismo destino no aportan.
- **Leads sale del header por completo** (decisión suya): es un dato de gestión que se consulta en el CRM y solo el dueño de la cuenta. Con él se fue la opción `include_leads` que #179 había introducido para no filtrarlo. Stats: propio = Publicaciones · Guardados · Me gusta; **ajeno = Publicaciones · Me gusta** (hacia afuera la señal pública es cuánto gusta el catálogo, no cuánta gente lo archivó).
- **El WhatsApp del perfil no pasa por el CRM, a propósito.** Se extrajo `open_whatsapp_text(phone, texto)` de `open_whatsapp` (que ahora la llama) en vez de duplicar el sanitizado. No se reusó `open_whatsapp_ef`: su Alert "✓ Contacto enviado" mentiría — contactar a un agente **desde su perfil** no nace de una propiedad y el lead exige `property_id`. Si el agente no tiene teléfono, el botón no se pinta.
- ⚠️ **Gotcha de layout que solo apareció en Android:** el botón flotante "⋯" caía justo encima de la tercera columna de estadísticas. El inset superior de Android es mucho menor que el notch de iOS, así que el `paddingTop` estético del header alcanzaba allá pero no acá. Ahora reserva la banda EXACTA que ocupa el botón (`s_8 + 40 + s_8`).
- Verificación: TDD en el hook (RED 5 fallos → GREEN, 8 tests; test L-1 vigila que nunca se consulte la tabla `leads`), Jest 1084/1084, tsc 0, lint 0 errores, smoke por CLI en iOS y Android en perfil propio y ajeno.
- Solo JS/UI: viaja por OTA, sin migraciones.

## [2026-08-16] polish | #179 Perfil estilo Instagram: grid 3 columnas, bio completa y header con estadísticas

- Pedido de Abraham (con capturas de dos perfiles de Instagram como referencia): 3 columnas en la grilla, bio COMPLETA (se cortaba) y header reacomodado. Decisiones tomadas con él: celda con precio + badge de operación, grilla borde a borde (gap 2, sin radios), Guardados también a 3 col, tercer stat = **Guardados** en vez de "Cerrados".
- **La card se volvió tile.** `PropertyGridCard` pasó de media 4/5 + body (título, zona, precio héroe con tick) a portada 3/4 a sangre con badge de operación arriba-izq y precio abajo-izq en blanco con `textShadow`: en 3 columnas la celda mide ~115-135px y el body no cabe. No es invención — el `.gcell` del mockup 10 de `urbea-identidad-visual.html` ya definía así la celda del perfil.
- ⚠️ **Gotcha de layout que costó pensar:** el ancho de la celda se calcula (`grid_tile_width(useWindowDimensions().width)`, tokens `layout.grid_cols:3` + `grid_tile_gap:2`) y se pasa por prop en vez de usar `flex:1`. Con `columnWrapperStyle` + `gap`, un `flex:1` **estira las celdas de la última fila parcial** — una sola propiedad ocupaba el ancho completo de la pantalla. Verificado en Guardados, que tiene 5 items: la 2ª fila deja su hueco.
- **La bio se cortaba por `numberOfLines={3}`** aunque `profile/edit.tsx` permite 280 caracteres. Ahora crece con el contenido y respeta los saltos de línea. El header pasó al patrón de Instagram: fila avatar (80px, antes 96) + estadísticas a la derecha, identidad alineada a la izquierda.
- **Tercer stat.** Se evaluó "Vistas" y se descartó: `properties.view_count` nunca se puebla y las vistas viven en `events_raw` con RLS privada — exigiría un RPC security-definer + revisión de privacidad. `save_count`/`like_count` en cambio ya los mantiene el trigger de `20260701000001`, así que **Guardados** salió gratis (suma en cliente, sin backend nuevo).
- 🔒 **Hallazgo de privacidad:** "Leads" es un dato privado y en un perfil AJENO la RLS devolvía 0 — el header pintaba "0 Leads" como si el agente no tuviera ninguno. Ahora `useAgentStats(agent_id, {include_leads})` ni siquiera consulta la tabla en perfiles ajenos, y ese perfil muestra Publicaciones · Guardados · Me gusta. Además `closed` salió del hook junto con su query: nadie lo consumía y el CRM tiene su propio `get_lead_stats` (`20260808000002`).
- Otros dos arreglos que salieron del rediseño: `ProfessionalStats` ya no se oculta con todo en 0 (dejaba al avatar solo en una fila a medias) y `GridSkeleton` replica el layout real de la grilla (si diverge vuelve el salto de layout que existe para evitar).
- Hallazgo del smoke: en Guardados el primer tile quedaba bajo el status bar — esa pantalla no tiene header de navegación propio y el padding lateral de las cards viejas lo disimulaba. Fix: `ListHeaderComponent` con `insets.top`.
- Verificación: TDD en el hook (RED 6 fallos → GREEN, 8 tests), Jest 1084/1084 (95 suites), tsc 0, lint 0 errores. Smoke por CLI en iOS (iPhone 17 Pro) y Android (emulador urbea): perfil propio, perfil ajeno y Guardados. No se probó el long-press de quitar guardado — dispararía un DELETE real sobre datos de producción y su código no cambió.
- Solo JS/UI: viaja por OTA, sin migraciones ni contratos nuevos.

## [2026-08-13] feat | #157 Búsqueda en el mapa estilo Google Maps (colonias DCAH/INEGI + perímetro + filtro espacial)

- Pedido de Abraham: buscar una colonia y que delimite su perímetro, con sugerencias mientras escribes. Hallazgo clave: el "catálogo INEGI" (72.1) era solo NOMBRES — cero geometría. Dataset elegido: **DCAH 2024 del INEGI** (75,516 polígonos nacionales; URLs vía el API de la biblioteca digital, la página es SPA con soft-404s). Decisión con el usuario: base propia PostGIS (patrón Zillow/Redfin), cobertura nacional (beta cerrada, no solo GDL).
- Backend (TDD estricto, pgTAP 42-45): `mx_neighborhoods` (CVEGEO=source_key, geography(MultiPolygon), name_normalized generada con `private.normalize_search_text` — sin unaccent) + bbox precalculado en `mx_municipalities`; RPCs `search_places` / `get_neighborhood_geojson` / `properties_within_neighborhood` (A1 flaco, ST_Intersects usa el GiST existente).
- Import SIN contraseña de DB (Abraham prefirió no compartirla): RPC `import_neighborhoods_batch` (solo service_role) + EF desechable `import-neighborhoods` (secret fail-closed, Deno 12/12) + pipeline mapshaper (reproyección LCC→WGS84, simplify 15m) — precedente "EF desechable" de 72.1. Jalisco 7,928/7,928 verificado (0 inválidas, p95 53 vértices); nacional por estados en lotes de 500.
- Mobile (33 tests nuevos, suite 1064/1064): libs `placeSearch`/`neighborhoodPolygon`/`bboxRegion`, hook `usePlaceSearch` (debounce+anti-stale), rama `neighborhood_id` en `fetchMapProperties` (máxima prioridad, invariante A1), dropdown `MapSearchSuggestions`, `<Polygon>` + chip con nombre. Decisiones D5/D6/D7/D9: municipio reusa `filters.area`; colonia = estado local (NO FilterState); filtro cliente viejo sustituido; colonia y area excluyentes.
- Smoke E2E (emulador, cuenta real): Providencia GDL dibujada EXACTA sobre las calles; Jocotán (match sin acento) muestra el pin de una propiedad real DENTRO del polígono; municipio Zapopan encuadra + chip de zona. Derivada: #158 (colonias "Ninguno" del DCAH en el autocomplete).
- Gotchas nuevos: RNTL 14 (`renderHook` async + `await act`); `adb input text` corta en espacios (usar `%s`); deploy EF con `--use-api` (docker-credential-desktop roto).

## [2026-08-11] fix | #155 Guardados: desfase de foco + scoping de admin (causa raíz del reporte)

- Reporte de Abraham: "no se eliminan los guardados / no se reflejan las publicaciones" en cuentas no-agente (Android + iPhone).
- Causa 1 (todos los roles): la pantalla es tab (slot 4 no-agentes), queda montada y el hook solo consultaba al montar → toda mutación desde feed/detalle dejaba la lista vieja. Fix: `useFocusEffect`→`refetch()` en `SavedScreen.tsx` (salta el primer foco).
- Causa 2 (la RAÍZ, cuentas admin = las de prueba): `saves_select` tiene `OR is_admin()` y el hook confiaba en RLS → un admin veía los saves de TODOS; los ajenos no se pueden borrar (`saves_delete` solo filas propias → DELETE de 0 filas sin error → el item revive al refetch) y 2 usuarios guardando la misma propiedad duplicaban keys de fila (`FlatList numColumns` une los keys con `:` — el warning "two children with the same key" llevó a la causa). Evidencia: admin@ tenía 2 saves en DB y la pantalla mostraba 6+.
- Fix TDD (hook crítico): RED EC-1-155/EC-2-155 (0 calls a `.eq`) → GREEN `.eq('user_id', user.id)` en `useSavedProperties.ts`. RLS intacta (is_admin() en SELECT queda para tooling futuro).
- Verificación: Jest 1031/1031 · tsc 0 · lint 0 errores · E2E emulador (dev-client + Metro): lista del admin = sus 2 saves exactos, quitar persiste en DB, 0 warnings dup key, force-stop al final.
- Derivada #156: el bookmark del feed/detalle no refleja el estado real (initialSaved siempre false → quitar desde el feed requiere 2 taps: el 1º hace INSERT→23505). Pendiente.

## [YYYY-MM-DD] tipo | título`

## [2026-08-10] decision | 0009 Producción viva — todo cambio se piensa para producción

Pedido de Abraham: hay personas reales conectadas probando la app y la base se puebla poco a poco — se acabó la mentalidad de demo. La normativa quedó integrada en el flujo en tres puntos para minimizar el error humano: **CLAUDE.md §0.5** (las 6 reglas: DB aditiva+rollback sin seeds/resets al remoto, compat hacia atrás con builds instalados con orden cliente-OTA-primero, merge=candidato a release, privacidad/RLS, cuotas reales, ante duda gana producción), **analista-subtareas Paso 5.5** (línea "Impacto producción" por subtarea: `sin riesgo | ⚠️ destructiva | ⚠️ contrato`), **`/tm-plan`** (impacto-prod obligatorio en el PLAN + pregunta de diseño forzada si hay ⚠️) y **`/tm-tarea`** (gate de producción viva en el cierre, paso 6.0, antes de done y del PR; CLAUDE.md §5.7 lo repite antes del merge). ADR: [[0009-produccion-viva]].

## [2026-08-15] producto | Quick fixes del wizard de publicación (paso 3/4) — sesión directa, sin ciclo tm-tarea

Pedido directo de Abraham (screenshots anotados a mano del wizard), 7 puntos + 1 pieza aprobada en el momento (selector de moneda, no estaba en la lista original). Solo el punto 4 (chips) tenía tarea de Taskmaster (#167); el resto se implementó y se documenta AQUÍ por instrucción explícita del usuario — no se crearon tareas nuevas para 1/2/3/5/6/7. Rama `tarea/167-wizard-quickfixes` desde `origin/main` fresco (⚠️ #167 solo existe en la rama sin mergear `tarea/168-org-can-advertise` — su `tasks.json` no llegó a `main` todavía; el `set-status done` de #167 queda pendiente de reconciliar cuando esa rama mergee, documentado también en mapa-codebase.md).

**1) Solo 2 tipos de operación.** `step1.tsx` retira `'Ambos'` del picker — decisión deliberada de NO tocar el enum/DB (`operation_type` sigue aceptando `'both'`): filtros (`filterQuery.ts`), tarjetas (`PropertyGridCard`, `PropertyMarker`, `PropertyListItem`) y detalle siguen mostrando propiedades `'both'` ya publicadas. Cambio 100% de presentación, sin migración.

**2) Superficie terreno + construida.** Columna nueva `built_square_meters` (numeric, nullable) — `square_meters` pasa a significar TERRENO. `step3.tsx` las muestra lado a lado (fila de 2 columnas, mismo patrón que baños/medios-baños). Threading completo: migración → RPC `publish_property_atomic` (20 params) → EF `publish-property`/`edit-property` → mobile (`types.ts`, `validation.ts`, `useDraftAutosave`, `usePublish`, `useLoadProperty`).

**3) Pin preciso — modo mira central.** El picker YA tenía tap-to-place + marker arrastrable (funcionaba), pero Abraham pidió el patrón Uber/Airbnb: el pin queda FIJO al centro de la pantalla y el usuario arrastra/hace zoom al MAPA. Más preciso porque se puede acercar el zoom antes de soltar. Implementado con `onRegionChangeComplete(region, details)` de react-native-maps 1.27.2 — `details.isGesture` distingue un pan/pinch real del usuario de un recentrado programático (montaje, o el efecto que sigue a `lat`/`lng` cambiando desde el autocomplete de dirección); sin ese guard el mapa entraría en loop de auto-recentrado. El pin es ahora un `<MapPinIcon>` con `position:absolute` (ya no un `<Marker>` geo-anclado — dejó de representar una coordenada del mapa para representar el centro fijo del viewport).

**4) Chips de características (#167).** `step4.tsx`: los 3 `Switch` (pet_friendly/allows_no_guarantor/student_friendly) → `FilterChipGroup`, promovido de `features/search/components/` a `src/components/FilterChipGroup.tsx` (componente cross-feature, como pedía la exploración 038). Modelo de datos sin cambio — 3 booleanos, la EF y `FilterSheet.tsx` siguen operando igual.

**5) Precio con separador de miles.** El usuario solo teclea dígitos; `format_price_digits` los agrupa (`1,000` / `1,000,000`) con `toLocaleString('en-US')` para MOSTRAR — `state.price` guarda siempre el número limpio.

**6) Medios baños.** Columna `half_bathrooms` (int, nullable), stepper a lado de "Baños" en `step3.tsx` (mismo `NumericStepper`, max=5).

**7) UX de campos faltantes — decidido con Abraham vía pregunta directa:** inline por campo (borde rojo + mensaje debajo), pero SOLO se pinta después de que el usuario intenta avanzar con algo incompleto — no al escribir ni al perder foco. Se quitó la caja resumen "Falta completar:" fija sobre el botón. Implicación no obvia: el botón "Siguiente" YA NO usa `disabled={!valid}` — tiene que poder recibir el press aun inválido para que `handle_next` dispare `attempted_submit=true` y las validaciones se pinten.

**Extra aprobado en la sesión — selector de moneda MXN/USD.** No estaba en la lista original de 7 puntos (una anotación suelta en el screenshot); se confirmó con Abraham antes de tocar código. Columna `currency` (`text check in ('MXN','USD') default 'MXN'`) — **solo etiqueta, sin conversión de tipo de cambio ni tabla de tasas** (ponytail: YAGNI hasta que se pida conversión real). Selector de 2 chips junto al precio en `step3.tsx`.

**Gotcha de sesión — retrocompatibilidad de campos NUEVOS vs. la regla #142.** `edit-property` exige que el body venga COMPLETO (clave ausente = 400, `#142`) para los campos que YA existían cuando esa regla se fijó — así ningún caller viejo puede borrar datos en silencio. Pero `built_square_meters`/`half_bathrooms`/`currency` son campos NUEVOS sobre un contrato YA publicado: un build instalado (sin el OTA de este cambio) JAMÁS los va a mandar. Tratarlos con la misma regla de #142 (ausente=400) habría roto la edición de TODA propiedad existente para cualquier cliente viejo en cuanto se desplegara la EF — viola §0.5.2 de frente. Se diseñaron como OPCIONALES: ausente = "no tocar" (mismo patrón que `location`, que ya era opcional desde #142). Contraste útil para la próxima vez que se agregue un campo a un contrato ya publicado: la regla "ausente=400" es correcta para el contrato QUE YA EXISTÍA cuando se fijó, no un default a copiar ciegamente para cualquier campo nuevo futuro.

**Gotcha de sesión — sentinel de TDD-guard cruzado entre ramas concurrentes.** Al arrancar esta rama (mientras otro agente trabajaba en paralelo la tarea #168 en su propia rama), `.taskmaster/.current-red` seguía apuntando a `168.3` (archivo gitignored, no fue afectado por el cambio de rama — vive a nivel de directorio de trabajo, no de rama). El guard bloqueó la primera migración hasta borrar el sentinel stale (el propio mensaje de error lo sugiere: "si abandonaste la subtarea"). El trabajo real de 168.3 no se perdió — vive en su propia rama con su propio historial; solo el archivo de coordinación local quedó desalineado. **Lección:** el sentinel NO es branch-scoped, así que dos ramas activas en el mismo working directory (aun con worktrees separados si comparten `.taskmaster/` fuera de git) pueden pisarse la señal.

**Gotcha de sesión — DB local COMPARTIDA entre worktrees.** El stack de Supabase local (`supabase status`, puerto fijo 54322) es un solo contenedor Docker por máquina — dos ramas trabajando en paralelo (esta y #168) comparten la MISMA base de datos Postgres local, aunque cada una tenga su propio checkout/worktree. `supabase migration up` falla con "Remote migration versions not found in local migrations directory" en cuanto la rama activa no conoce las migraciones que la OTRA rama ya aplicó a ese Postgres compartido — y `supabase migration repair` habría corregido el ledger MINE a costa de desalinear el de la otra rama. Se evitó tocar el ledger de la CLI por completo: las 3 migraciones nuevas se aplicaron con `docker exec supabase_db_urbea-app psql -U postgres -f <archivo>` directo (DDL puro, sin pasar por `supabase_migrations.schema_migrations`) y los tests pgTAP se corrieron igual por `psql` en vez de `supabase test db` (que además reveló que la extensión `pgtap` no estaba instalada en esa sesión de Postgres — normalmente la crea la CLI antes de correr, hubo que instalarla a mano: `create extension if not exists pgtap with schema extensions`). Nada del ledger de ninguna rama quedó tocado.

**Verificación (1ª ronda):** pgTAP 41+17 asserts nuevos (`06_publish_property_rpc_test.sql`, `39_moderate_property_atomic_test.sql`) + suite completa sin fallos. Deno 68+72 tests nuevos (`publish-property`, `edit-property`). Jest 1072/1072 + 40 tests nuevos. `pnpm tsc --noEmit` y `pnpm lint` en 0.

### Segunda ronda (mismo día) — smoke manual de Abraham en emulador/simulador

Abraham probó el build en Android + iOS contra el stack LOCAL y salieron 4 hallazgos; los 4 se arreglaron en la misma rama.

**a) Steppers cortados en columna angosta.** `NumericStepper` se diseñó para fila de ancho completo (label izquierda + stepper de ancho fijo a la derecha); al reusarlo en una columna de ~160px (Baños | Medios baños) el botón "+" quedaba cortado contra el borde, en AMBAS plataformas. Componente nuevo `CompactStepper.tsx`: label arriba, controles en `justifyContent:'space-between'` (proporcional al ancho real de la columna, no un `gap` fijo). Segunda pasada del mismo hallazgo: label y hint van FUERA de la caja con borde — igual que el campo de precio — así la caja solo envuelve el control y la altura baja; Recámaras se migró al mismo componente (comparte el grid de 2 columnas con un hueco vacío al lado) para que las 3 cajas midan igual. `NumericStepper.tsx` quedó huérfano → **borrado** (§0: no acumular código).

**b) Chips y divisa en el FEED (pedido nuevo).** El feed pintaba el precio con `Intl.NumberFormat(currency:'MXN')` hardcodeado y no mostraba operación ni tipo. Ahora: 2 chips sobre la dirección — operación (`op-badge` canónico de la identidad: salvia Renta / arcilla Venta, mismo criterio de color que `PropertyGridCard`) + tipo en glass oscuro como el rail; precio con la divisa como **sufijo siempre** (`$15,000 MXN` / `$1,500 USD`, decisión de Abraham entre 3 opciones); y `price_visible=false` → "Precio a consultar" (el campo se capturaba desde #73 pero el feed lo IGNORABA — bug latente encontrado de paso). `FEED_SELECT` gana `operation_type, property_type, currency, price_visible` (mismo viaje, cero RTT extra) con fail-open al mapear (`currency ?? 'MXN'`, `price_visible ?? true`) para filas viejas/fixtures.

**c) Helper único de precio.** Había 4 formateadores distintos (feed, detalle, grid del perfil, mensaje de compartir), todos con MXN hardcodeado. Se centralizaron en `src/lib/formatPrice.ts` (`format_price(price, currency?)`). ponytail: NO se usa `Intl` con `style:'currency'` porque para USD en `es-MX` produce `US$1,500`, asimétrico con el `$15,000` de MXN — se formatea solo el número y se arma la cadena. El detalle además muestra ya `2 baños + 1 medio` y `120 m² terreno · 85 m² const.`.

**d) "Cambiar video" no cancelaba la subida en curso.** Síntoma reportado: durante una subida, tocar "Cambiar video" respondía "Ya tienes un video en proceso, espera a que termine"; salir y reentrar no lo destrababa. Causa: `mint-upload-url` responde 409 `UPLOAD_IN_PROGRESS` mientras exista una fila `uploading` de menos de `STALE_UPLOAD_MS` (15 min, el reaper de #103) — y el wizard no tenía forma de cancelar la subida en vuelo. Arreglo en dos capas: (1) `useVideoUpload` gana `cancel()` + **supersede** — cada `upload()` invalida el anterior con un token de generación (`upload_seq_ref`) y aborta su binario vía `AbortSignal`; un upload cancelado/superado NUNCA vuelve a escribir status/error/form (invariante nueva, 4 tests CX-1..4). (2) contrato ADITIVO de la EF: body `{ replace: true }` → antes del chequeo de concurrencia soft-borra los pendientes del propio caller **con `property_id IS NULL`** (los del wizard en curso; los ya asociados a una propiedad NO se tocan y siguen bajo la invariante §13.2). Sin el flag, contrato idéntico al anterior → los builds instalados antes del OTA no cambian de comportamiento. `step5.tsx` habilita el botón durante la subida y limpia `video_id`/`cloudflare_uid` del form al reemplazar.

**Gotcha de sesión — el webhook de Cloudflare Stream NO llega al stack local.** El síntoma es "el video tarda muchísimo en subir" en local, pero el binario llega a Stream en ~2s (verificable con `GET /accounts/{id}/stream/{uid}`): lo que falta es el webhook, configurado hacia el proyecto REMOTO, que es quien escribe `property_videos.status='ready'`. Sin él la fila se queda en `uploading` para siempre, `useVideoReady` pollea eternamente y `publish-property` daría 409 `VIDEO_NOT_READY`. Workaround usado para el smoke: script en loop que pollea la API de Stream y hace el UPDATE en el Postgres local (simula el webhook). Alternativa real: túnel público al `functions serve` y reapuntar el webhook — no vale la pena para un smoke.

**Gotcha de sesión — crash de Reanimated al reabrir el dev-client (iOS).** `-[ReanimatedModule installTurboModule]` → `__assert_rtn` → SIGABRT al reabrir la app tras cerrarla. Es del **dev-client** (re-instalación del runtime JS), NO del build de release/OTA; se reprodujo también contra el remoto antes de estos cambios. Mitigación: cerrar desde el switcher (o `xcrun simctl terminate`) antes de reabrir.

**Deploy a producción (mismo día, autorizado por Abraham).** Las 3 migraciones aplicadas al remoto `mvpvqmyhrrkwbnpctpuq` vía `apply_migration` (NO `db push`, ver [[supabase_remote_deploy_apply_migration]]) — aditivas puras, el `default 'MXN'` hizo el backfill de las 20 propiedades (11 activas) sin un solo UPDATE manual, que era justo el requisito de Abraham ("todas fueron publicadas en pesos, todo debe ser consistente"). Los chips no necesitaron backfill: `operation_type`/`property_type` ya existían poblados. EFs desplegadas: `publish-property`, `edit-property`, `mint-upload-url` (`--import-map` + `--use-api`). **Orden obligado y no obvio: la migración va ANTES del OTA** — el `FEED_SELECT` nuevo pide `currency`, y PostgREST devuelve error ante una columna inexistente, así que un OTA adelantado habría roto el feed ENTERO de los builds instalados. Verificado post-deploy con el select exacto del feed autenticado contra PostgREST remoto (4 filas, chips y divisa correctos).

**Verificación (2ª ronda):** Jest 1078/1078 (95 suites, +6 tests nuevos: `formatPrice`, `feedProperties.listing-meta`, CX-1..4 de `useVideoUpload`). Deno 185/185 en las 3 EFs tocadas (+5 tests RP-1..5 de `mint-upload-url`). `deno check` limpio en `index.ts`/`_shared/clients.ts`. `pnpm tsc --noEmit` y `pnpm lint` en 0 errores.

## [2026-08-10] producto | #145 Identidad real del agente en el feed (+fix RLS del perfil ajeno)

Pedido de Abraham: foto real + nombre del agente en el overlay del feed, con tap → perfil público (`/profile/[id]`, que ya existía con sus publicaciones desde #16.6 — el stub era de 9.6). **El hallazgo grande:** `user_prefs_select` es "solo tu fila o admin", así que TODA lectura ajena de `full_name`/`profile_photo_url` devolvía 0 filas EN SILENCIO — el perfil público de otros agentes y la AgentCard del detalle llevaban semanas mostrando fallback sin que nadie lo notara. Fix de fondo: **vista `agent_public_profiles`** (security_invoker=false; SOLO user_id/full_name/profile_photo_url de roles agent|admin; grant a authenticated) — el comportamiento del buscador (presupuesto/ubicación) y la identidad de role=user siguen privados ("registrar ≠ exponer"). La vista se embebe ANIDADA en el select del feed (PostgREST la resuelve por los FKs de la tabla base — verificado por REST remoto con JWT demo: 0 RTT extra). Tipos de DB regenerados (la vista entra tipada). Foto = key R2 → `useR2Urls` en el overlay (patrón ProfileHeader). Todo el lado app es JS → viaja por OTA; la vista ya está en el remoto.

Gotcha de entorno (para no repetir la pérdida de tiempo): tras un `adb reboot`, el AVD queda SIN last-location en todos los providers y `adb emu geo fix` no aterriza sin un listener GPS activo (variante dura de #109) — el muro de ubicación bloquea a CUALQUIER código en ese estado; verificar por REST/pgTAP en su lugar. Además el DNS del emulador se pudre con uptime largo (`res_stats_usable_server: too many resolution errors`) → reboot de red, no de código.

## [2026-08-10] polish | #144 Arranque en frío veloz — last-known primero + mint paralelo

Derivada de #143 (revisión de Abraham: "que el splash no llegue nunca al techo de 7 s"). Dos cortes al camino crítico del arranque, ambos TDD: (1) `fetch_current_coords` consulta PRIMERO `getLastKnownPositionAsync({ maxAge: 10 min })` — una posición reciente destraba el gate de coords de inmediato en vez de esperar el fix fresco de GPS (segundos en dispositivo, el timeout completo de 10 s en el AVD); sin posición reciente, el camino #143.1 corre intacto. Trade-off aceptado explícitamente: el primer orden del feed puede ser el de hace ≤10 min y se autocorrige al siguiente foreground. (2) `fetchFeedProperties` invoca `mint-video-url` con los `page_ids` de la RPC EN PARALELO con el select de PostgREST (`Promise.all`) — antes eran 3 viajes en serie y el frío pagaba además el cold start de la EF bajo el splash; el merge fail-closed no cambia (mint de ids que el filtro descarta = desperdicio aceptable). Diagnóstico previo que motivó el lote: el "splash malo" reportado en iOS/Android era artefacto del dev-client 1.0.4 (splash nativo viejo horneado; el dev-client de Android ni lo muestra) — nada que corregir en código, se ve bien recién en el build 1.0.5.

## [2026-08-10] polish | #143 Quick fixes de arranque, navegación y adaptabilidad

Lote pedido por Abraham mientras se procesaba el submit de TestFlight 1.0.4. Seis fixes: (1) el muro de ubicación de Android se destrabó con fallback a `getLastKnownPositionAsync` — la raíz del bloqueo #109 era que el Fused Location Provider del AVD jamás resuelve `getCurrentPositionAsync`, y la última posición conocida (la del `geo fix`) SÍ está disponible; de pilón cubre el arranque en frío de usuarios reales. (2) Slider de la tab bar moderado (spring 30/240 ≈ crítico + overshootClamping). (3+5) Ícono y splash regenerados EXACTOS del spec `urbea-logo-final.html` con qlmanage (misma cara iOS/Android; NATIVO → build 1.0.5, no OTA). (4) `splash-gate.ts`: el splash nativo aguanta hasta la primera pantalla útil (login/muros/feed-con-datos) con timeout de seguridad de 7 s — los servicios cargan debajo del logo, adiós al desfile skeleton/spinner. (6) `insets.bottom` en 10 puntos (sheets, wizard 1-5, admin, sticky CTA del detalle) — verificado en emulador con navegación por botones: los CTAs del lead ya no quedan tapados.

Gotcha ya conocido reconfirmado: los componentes que ganan `useSafeAreaInsets` rompen sus tests sin el mock de `react-native-safe-area-context` (idioma en register-user-mode.test).

## [2026-08-09] fix | Las 6 bloqueantes del review de #73 (+deploy completo: DB + 4 EFs + OTA)

Cierra #126–#130 y #142 (PRs #57–#62, una rama/PR por tarea, TDD estricto: RED verificado por aserción en todas). Con esto, publicar → moderar → editar → cerrar queda usable de punta a punta, y TODO desplegado al remoto con el visto bueno de Abraham: 7 migraciones (`20260809000002`–`000008`), 4 EFs (`publish-property`, `edit-property`, `moderate-property`, `update-property-status`) y OTA a ambos canales (runtimes idénticos a los builds 1.0.4 — sin build nuevo, cero nativo tocado).

**Los hallazgos que valen la pena recordar:**
1. **#126 — la premisa del ticket estaba incompleta.** "Que el checker acepte `processing` como la RPC" era un **no-op**: nadie escribe jamás `processing` en `property_videos` (la fila va `uploading` → webhook → `ready`). El fix real fue del lado del cliente: validar la duración al ELEGIR el video y esperar el `ready` real de la DB (`useVideoReady`) antes de habilitar Publicar. Moraleja: antes de implementar la opción (a) de un ticket, verificar quién escribe realmente el estado del que depende.
2. **#130 — la atomicidad como diseño, no como parche.** Los 3 seams de escritura de `moderate-property` se fusionaron en UNO respaldado por la RPC `moderate_property_atomic` (patrón `publish_property_atomic`): la state machine sigue en el handler testeado, y el SQL solo aporta la transacción. El snapshot aplica por PRESENCIA de clave jsonb (`?`) — clave ausente ≠ null explícito, la misma distinción que #142 fijó en el parser de `edit-property`. Los tests de "fallo parcial" se borraron a propósito: ese estado ya no puede existir.
3. **#127 — la RLS como segunda capa tiene que tener dientes.** El hook de autosave era la única ruta de escritura de `properties` fuera de las EFs y la RLS no restringía `status`: cualquier caller con anon key + JWT de agente podía crear propiedades `active` sin moderación. `properties_insert` ahora exige `status='draft'` (pgTAP 40 lo fija con impersonación).
4. **Gotcha de testing (nuevo, costó ~30 min de bisección):** `unmount()` manual FUERA de `act()` bajo fake timers corrompe el montaje de efectos de TODOS los tests siguientes del archivo — los hooks nuevos renderizan pero sus `useEffect` jamás corren, y solo los tests positivos lo notan. Fix: `await act(async () => { unmount(); ... })`. Registrado en memoria (`rntl_unmount_fuera_de_act`).
5. **El lint `react-hooks` moderno prohíbe** escribir refs durante render (`state_ref.current = state` suelto) y `setState` síncrono dentro de un efecto — los patrones nuevos: asignar refs en un efecto sin deps, y derivar el estado visible de un estado "resultado final + uid" en vez de resetearlo en el efecto.

Ingest: fila nueva en `mapa-codebase.md` (fixes #126–#142 + RPC de moderación + candado draft-only), [[moderacion]] sigue vivo (la máquina de estados de la EF cambió: approve-con-revisión activa desde pending_review/needs_changes).

## [2026-08-09] tarea | #73 Ola 1 — Publicación completa (5 pasos + 16 estados + moderación manual)

Cierra la ola de publicación: wizard de 5 pasos con autoguardado, `property_status` extendido a 17 valores (10 operativos nuevos), `property_revisions` (doble versión §15.6), `property_video_slots` (abstracción de vigencia), y dos EFs nuevas — `edit-property` (re-revisión por edición) y `moderate-property` (approve/needs_changes/reject/suspend unificada). En beta el admin modera por Supabase Studio, sin panel visual todavía (Ola 3).

**La lección más valiosa de toda la tarea: 3 defectos críticos, los tres invisibles para la suite mockeada, los tres solo aparecieron al verificar contra el stack local real.**
1. **73.4 — `publish-property` nunca habría llegado a `pending_review` en producción.** `handler.ts` ya mandaba el literal correcto (57 tests DI en verde lo confirmaban), pero `index.ts` (el adapter real) nunca reenviaba `property_status` como parámetro a la RPC, que seguía hardcodeando `'active'`. Toda la publicación real habría quedado auto-aprobada en silencio pese a la suite completa en verde. Fix: parámetro `p_property_status` en la RPC + 3 asserts pgTAP contra la fila REAL (no un mock).
2. **73.6 — `edit-property`/`location.ts` sin cobertura real.** El diff de campo crítico "coordenadas cambiaron" solo se había probado con EWKT mockeado en ambos lados; el parser del formato real que devuelve PostgREST (EWKB hex) nunca se ejercitó. Mutation testing del guardian expuso el hueco — sin bug genuino esta vez, pero sí sin red. Cerrado con 11 tests contra fixtures NDR/XDR reales.
3. **73.9 — `moderate-property`/`apply_revision_snapshot` con spread crudo.** El objeto `changed_fields` es el `EditPropertyInput` completo, que incluye `property_id`; `properties` usa `id`. Resultado: **`approve` con revisión activa SIEMPRE fallaba 500** ("column property_id does not exist") en la DB real, con 39 tests DI en verde. Fix: whitelist explícito de columnas editables (`project_property_snapshot_fields`).

**Heurística que deja el patrón** (las 3 veces se repite): la pregunta correcta para decidir si un adaptador "delgado" necesita test contra schema real no es *"¿es una sola sentencia SQL?"* sino *"¿transforma o proyecta datos cuyo shape define otro componente?"* — los wrappers que solo pasan parámetros tal cual estuvieron siempre correctos; los tres que proyectaban/transformaban datos fueron exactamente los que tenían el bug.

**73.10 (esta subtarea) — E2E completo contra el stack local real**, encadenado de punta a punta (JWTs reales vía admin API + password grant, video fixture sembrado directo en `property_videos` para no depender de la red real de Stream): publish→`pending_review`+slot creado → approve sin revisión→`active` → edit precio→revisión `pending` (propiedad intacta) → approve con revisión→snapshot aplicado (`price` nuevo) + revisión→`approved` → close `rented`→terminal sin reapertura (verificado con un segundo intento que devuelve 400). Bonus: `needs_changes`, `reject` y `suspend` (incluido el bloqueo de suspender un estado terminal). **Cero hallazgos nuevos** — las tres correcciones de arriba ya sostenían el flujo completo. Verificación final de toda la tarea #73: Deno **968/968**, pgTAP **831/831** (39 archivos), Jest **988/988** (85 suites), tsc/lint limpios en ambos paquetes.

Ingest: [[moderacion]] pasa de `diferido` a `vivo` (reescrito con la máquina de estados real, el pipeline y los 3 defectos), [[propiedades-y-video]] gana la sección "Ola 1", `mapa-codebase.md` gana la fila de la Ola 1 completa.

## [2026-08-09] tarea | 75.4 Contacto WhatsApp + unificación — el feed contactaba sin crear lead

Tercera subtarea del CRM. Lo que parecía "cambiar el texto del mensaje" destapó **tres huecos**, dos de ellos invisibles desde los tests.

**1. Se podía contactar al agente sin que existiera el lead.** `VideoFeedItem` (feed) y `AgentCard` (detalle) abrían WhatsApp con `open_whatsapp()` directo, saltándose la EF: el agente recibía el mensaje y en Urbea **no quedaba nada** — sin lead, sin el acceso a datos que §19.2 le concede *sólo* tras el contacto, sin los +10 de scoring, sin fila en el CRM. Y el feed es donde más se contacta, así que el CRM perdía justo los contactos más frecuentes. Ambos lo admitían con un `ponytail:` ("llega en #11"). Fix: hook **`useContactAgent`** = un solo camino para los tres call sites (invoca EF → si 200 abre WhatsApp con el template del servidor → si falla **no** abre). `ContactAgentButton` perdió su copia del mapa de errores y su `onContactReady`; `open_whatsapp()` sobrevive sólo para la dirección inversa (el agente escribiéndole a su lead).

**2. El precio se publicaba ignorando `price_visible`.** El template metía el precio **siempre**: una propiedad con el precio oculto lo soltaba igual en el prellenado de WhatsApp. El §19.3 no lleva precio, así que el arreglo fue de raíz — `PropertyWithAgent` perdió `price` y `operation_type` y la EF **ya ni pide la columna**. No se filtra lo que no se lee.

**3. `property_video_id` era NULL en toda la producción.** El resolver de la EF vivía **inline en el closure de `Deno.serve`**, o sea sin un solo test, y omitía `video_id` con otro `ponytail:` — contra §9.6. Extraerlo a `property_resolver.ts` (mismo movimiento que 75.1 hizo con `lead_repo.ts`) fue lo que lo destapó: **el patrón se repite — lo que vive dentro del entry point no lo mira nadie.**

**Template §19.3, parcial a propósito:** `Hola, vi tu propiedad en Urbea: {Tipo} en {dirección}, {zona}. Me interesa conocer más detalles.` Faltan `(#código)` y `[deep link]` porque `public_code` lo crea la **74** y la página `/v/[id]` la **78.5** (que espera el dominio, decisión de hoy). La **dirección** ocupa el lugar del código porque cumple su función — que el agente sepa de cuál propiedad le escriben; quitarla sin código sería regresión. Derivada **#118**.

**Unificación §19.5:** ya funcionaba (índice único parcial + find/insert/recovery 23505) pero nada pineaba que la clave es **(agente, usuario) y NO la propiedad**; 4 tests nuevos mueren si alguien le agrega `property_id` al find. Suites: Deno **812** (98 en contact-agent, +27), Jest **952** (+8), tsc/lint 0. Los tests nuevos se verificaron **por mutación** (quitar el sort por `position` mató uno; quitar la zona del mensaje, otro).

**Desplegada al remoto** (`--import-map --use-api`) y **smoke contra PostgREST real** — la parte que los tests con cliente falso no pueden cubrir: el embed `property_videos(id,position,deleted_at)` y las columnas `property_type`/`zone` resuelven contra datos vivos. El smoke destapó de paso que **`properties.zone` es NULL en todas las propiedades reales**: la columna existe desde 0005 y **nadie la escribe** (ni el wizard, ni la EF de publicación, ni la RPC). O sea el "[tipo + zona]" del §19.3 degrada a tipo + dirección por falta de **dato**, no de código — y la búsqueda por colonia de §11.2 (74.8) se quedaría sin nada que buscar. Derivada **#119**. Moraleja repetida: **el smoke contra el remoto encuentra lo que la suite no puede ver.**

## [2026-08-08] tarea | #113 El detalle del lead abría VACÍO — un `flex: 1` colapsado a altura 0

Segundo reporte del dueño (*"en la cuenta de vladimir aún no puedo configurar los estados"*) tras un primer diagnóstico **equivocado** en #112: se atribuyó a permisos y era **layout**. El sheet abría con la cabecera y los botones, y nada en medio: ni Actividad, ni Cambiar estado, ni notas, ni historial — en TODAS las cuentas.

FIX2 de 75.6 había cambiado el ScrollView acotado del picker (`maxHeight: 280`, altura definida, funcionaba) por uno solo con `flex: 1`. `styles.sheet` no tiene altura definida (solo `maxHeight`) y su padre `KeyboardAvoidingView` no tiene estilo: la altura la fija el contenido. `flex: 1` implica `flexBasis: 0` → el contenido del ScrollView deja de contar para esa altura y, sin espacio libre que repartir, **Yoga le asigna 0**. `flexShrink: 1` lo arregla con una propiedad, y encima se comporta mejor con contenido corto.

⚠️ **Los 99 tests de leads pasaban igual antes y después.** RNTL no calcula layout: un colapso de flexbox le es literalmente invisible. Este sheet nunca se había verificado en pantalla. **Técnica que lo destapó y que conviene reusar**: parche temporal en `CRMScreen` que auto-selecciona `leads[0]` → Fast Refresh → `xcrun simctl io booted screenshot` → revertir. Es la forma de "tocar" la UI por CLI en el simulador de iOS sin secuestrar el mouse del usuario (Maestro no corre aquí: falta Java). Derivada #115: `FilterSheet` tiene un primo del mismo patrón, sin verificar.

## [2026-08-08] tarea | #114 El splash abría con un cuadro blanco — el isotipo había perdido la transparencia

*"No quiero que al abrir salga el cuadro beige con la U."* `assets/android-icon-foreground.png` estaba guardado con **fondo blanco opaco** (canal alfa presente, todo en 255). La config siempre estuvo bien: sin `backgroundColor` ni `removeTransparency`, `@expo/image-utils` **preserva** el alfa — el defecto estaba en el asset. En Android era peor: ese foreground opaco tapaba el background verde del adaptive icon, dejando un ícono blanco distinto del de iOS.

Fix por **des-composite exacto**, no chroma key: el PNG es crema `#EEE4D0` sobre blanco, así que `a = (255-B)/47` despeja el alfa y se reconstruye con crema plana. Error máximo 1.20/255; compuesto sobre `#1A5E44` difiere del ícono de iOS en ≤6/255 → las dos plataformas quedan con el mismo logo.

Dos gotchas que costaron tiempo: (1) el asset **entra en la huella del fingerprint** como `expoConfigExternalFile`, así que tocarlo cambia el runtime — por eso el OTA de #113 se publicó ANTES, sobre la huella del build instalado; (2) el simulador seguía mostrando el splash viejo con el binario nuevo, por el **caché de launch image de iOS** — se verificó instalando en un simulador limpio.

## [2026-08-08] tarea | #75.3 Privacidad del lead (§19.2) — registrar no es exponer

Cerrada una fuga **viva** que abrió #112 hace un día. `events_raw_select` nació como `user_id = auth.uid() OR can_manage_property(property_id)`: **sin mencionar el lead**. Bastaba con ser dueño de la propiedad para leer el comportamiento de cualquiera sobre ella. Medido en producción con un JWT real —no deducido—: un agente leyó filas `video_view` de otra persona que jamás lo contactó. Contradecía §19.1 de frente.

La regla del PRD cabe en una expresión: `private.can_view_user_events(user_id, property_id)` exige lead **ACTIVO** de esa persona, propiedad **del agente de ese lead** (de ahí "todas las publicaciones del agente") y que quien pregunta sea ese agente o el owner/admin de su agencia. Al derivarse del lead, **borrar el lead revoca el acceso** — sin código extra.

Al cerrarla apareció gratis un hueco de #75.5: el **admin de inmobiliaria no veía NINGÚN evento** del equipo (`can_manage_property` compone dueño + owner + admin de plataforma, pero no a él). Era el único punto del CRM donde el admin seguía ciego.

pgTAP `35_` (plan 15) + 3 asserts de `33_` reencuadrados con leads de fixture — no borrados: fijaban justo el comportamiento con fuga. **750/750 en 36 archivos.** Verificado por mutación: quitar "la propiedad es del agente del lead" mata 4 asserts; quitar "lead activo" mata el de la revocación.

**Documentación (lo que pidió el dueño):** [[privacidad-datos]] es el inventario **medido** (dato → tabla → quién lo lee → qué lo desbloquea) y `docs/aviso-privacidad.md` el aviso completo redactado contra el esquema real. ⚠️ **No se activó**: lo vigente es un placeholder de 113 caracteres que gente real ya aceptó, y publicarlo fuerza re-consentimiento a todas las cuentas + necesita revisión legal. Además **el sistema comparte hoy más de lo que ese texto promete** → deuda #116: `users_select` expone correo y teléfono de todo agente verificado a cualquier autenticado (comprobado), y el agente con lead ve la fecha de nacimiento exacta cuando §19.4 pide edad calculada. Ambas exigen expand·migrate·contract porque el grant es a nivel de columna y romperían el `select('*')` del login en las apps ya instaladas.

## [2026-08-08] tarea | #112 Estadísticas tangibles del lead — fuera el puntaje y el tibio/caliente, entra una barra de acciones

Pedido del dueño: *"para el marcador del puntaje no lo clasifiques como tibio/caliente, hay que separarlo en estadísticas tangibles… una barra como de acciones que hizo el usuario y que se vaya rellenando según la actividad"*, con el **like como filtro de entrada** para que ver unos segundos de video no genere ficha. Decisiones tomadas: registrar **siempre**, mostrar **solo tras el like** (si solo se registrara después se perdería el dato del video que *provocó* el like); fuera puntaje **y** temperatura; **no** se amplían permisos (owner/admin siguen de solo lectura, solo se explica mejor). 4 subtareas, 2 migraciones desplegadas al remoto, 3 rondas de guardian y **2 FAIL** que valieron cada minuto. **Reuso antes que invención:** `events_raw` ya existía con el esquema exacto (0 filas, **0 escritores**) y `private.can_manage_property`/`can_view_lead` ya resolvían la autorización — no se escribió un solo helper nuevo, y eso trajo gratis la corrección cross-agencia de #100 en vez de repetirla. **112.1** RLS de `events_raw` (tenía RLS activo con **0 policies y 0 grants**: nadie podía escribir ni leer). **112.2** captura en el feed. **112.3** `get_lead_stats(uuid[])`, batch para no hacer N+1, `security definer` porque las RLS de `likes`/`saves` solo dejan leer al propio usuario. **112.4** la barra + el banner de solo lectura.

⭐ **Los tres gotchas del video, todos de la familia "verde pero muerto":** (1) **`playToEnd` no sirve** — `VideoFeedItem` fija `loop = true` y con loop ese evento no dispara; el listener que había estaba muerto. (2) 🔴 **La feature podía entregarse COMPLETAMENTE MUERTA**: borrar `p.timeUpdateEventInterval = 0.5` dejaba la suite en **928/928 verde**, pero la doc de expo-video dice que con valor `0` —el **default**— el evento `timeUpdate` **no se emite**; sin esa línea, `video_completed` no se registra jamás y la estadística central queda permanentemente vacía. Los tests no lo veían porque el player falso emite a mano. (3) 🔴 **Bug de producción real**: el listener tenía deps `[player]`, y `player` es **estable por instancia** (el cambio de fuente al reciclar se hace con `replaceAsync`), así que tras el reciclaje de FlashList ver completo el video de **Z** registraba `video_completed` de **A** y marcaba A como ya contada — **falso positivo y falso negativo a la vez, silenciosos**, en el dato con el que un agente decidiría a quién llamar. Nada tronaba: los números simplemente mentían. Ningún test podía cazarlo porque **no existía un solo test de `VideoFeedItem` en todo el repo**. Además, la captura entera podía borrarse sin poner un test en rojo, y `appSession.ts` (crítico por la regla de path) tenía cero tests. **Dedupe obligatorio** por (sesión, propiedad): el feed reproduce en bucle y sin él un video olvidado generaría cientos de filas, inflando las estadísticas con actividad falsa.

⭐ **Del lado SQL, asserts que pasaban por la razón equivocada** (guardian PASS, endurecidos después): el gate del like **por la propiedad de ORIGEN** no lo vigilaba nadie (fuga de producto: estadísticas de un lead que nunca dio like al video de origen), `guardo` tampoco estaba atado a la propiedad, y el `EXISTS` del gate no tenía red — cambiarlo por `JOIN` (refactor de apariencia inocua) duplicaba `veces_visto` de **2 a 4**, en silencio y con la suite verde, porque `likes_user_video_unique` es `(user_id, property_video_id)` y un usuario puede likear 2 videos de la misma propiedad. También se puso el **candado append-only** en `events_raw`: estaba cerrada solo por ausencia de grant, sin un solo test — un `grant` de más habría dejado al usuario **borrar la evidencia de su propio comportamiento**.

⚠️ **Error de proceso del orquestador, documentado para no repetirlo:** se commiteó un checkpoint etiquetado "RED" **mientras un agente escribía en esos archivos**, así que el commit contenía los módulos ya implementados y **nunca estuvo rojo** (solo 5 tests fallaban, por contaminación entre pruebas, no por falta de la feature). El primer guardian no pudo detectarlo porque su red de seguridad era justo ese commit adulterado; el segundo lo cazó y arbitró 100% por mutación. **Lección: no commitear mientras haya un agente trabajando en ese territorio.**

**Verificación:** pgTAP **735** (35 archivos) · Jest **939** · tsc/lint 0 · migraciones desplegadas y verificadas contra el remoto real (2 policies, 2 índices, `security definer` con `search_path` cerrado, `anon` sin ejecutar ni insertar, `authenticated` **sin poder borrar**). Smoke por CLI en el simulador con datos reales: el lead sin like muestra *"Sin señales todavía"* y el que dio like y guardó muestra la barra con esos 2 tramos llenos. ⚠️ El **sheet de detalle no se verificó visualmente** (no hay forma de tocar la tarjeta por CLI); queda cubierto por tests. Deuda: la privacidad (**75.3**, §19.2) sigue pendiente y esto ya registra comportamiento de video de todos los usuarios.

## [2026-08-08] fix | #111 Los íconos de la tab bar de iOS desaparecían "a veces" — la UITabBar los bajaba por http de Metro, sin reintento

Reporte del usuario: "los íconos siguen desapareciendo, ya lo habíamos arreglado y se vieron pero después desaparecieron de nuevo". Ese *"y luego volvieron a desaparecer"* era el dato: el síntoma es **intermitente**, no binario. La nota que había en memoria del 2026-08-07 decía lo contrario ("en dev nunca se ven, en Release siempre; no es regresión, no toques el código") y **estaba mal** — se había escrito a partir de una sola observación. Hoy, con la misma build, los íconos salieron perfectos en **3 arranques en frío + 3 Fast Refresh**, en pantalla clara y oscura: no se logró reproducir la falla. **Causa leída en el fuente** (expo-router 56.2.11), no adivinada: `Icon src={...}` acaba en `convertOptionsIconToScreensPropsIcon` (`utils/optionsIconConverter.ios.js`) como `{type:'templateSource', templateSource:<ImageSource>}`, y en un dev-client ese ImageSource es **la URL http del asset server de Metro** → la `UITabBar` **nativa** sale a bajar cada ícono, async y **sin reintento**; si esa descarga pierde la carrera contra el primer render (Metro reiniciando, la IP LAN vieja que el dev-client cachea, un hipo de red) el slot queda vacío **para siempre**. En Release no ocurre porque ahí el ImageSource ya es un archivo del bundle; **Android es estructuralmente inmune** (su `GlassTabBar` dibuja componentes React de Phosphor, sin descargar nada). Fix: `useAssets` (expo-asset) resuelve los 12 PNG a `localUri` (`file://…`) **en JS** y la barra recibe un source local — la `UITabBar` deja de depender de una petición propia. Se descartó migrar a **SF Symbols** (sería el arreglo más nativo y también válido) por **decisión del dueño: conservar Phosphor**; se descartó `xcassets` por exigir config plugin + recompilar (no viaja por OTA) y `PromiseIcon` porque solo admite **un** ícono por trigger, no el par `{default, selected}` (se perdería el relleno de la tab activa). ⚠️ **Trampa cazada en el smoke:** pasar un `{uri}` pelón **rompe el tamaño** — sin `scale`, iOS dibuja el @3x a tamaño de píxel y los íconos salen gigantes encima de las etiquetas; el helper `to_local` conserva `width`/`height`/`scale` con `Image.resolveAssetSource` y solo cambia la uri. Cambio **solo de JS → viaja por OTA, sin recompilar**. Verificación: `pnpm tsc --noEmit` 0 · `pnpm lint` 0 errores · **Jest 885/885** · smoke por CLI en el simulador (`xcrun simctl`, nunca computer-use) con captura de la barra en feed oscuro, perfil claro y mapa. **Honestidad del alcance:** al no reproducirse la falla, el arreglo se sostiene en el mecanismo leído en el código, no en un RED que pase de rojo a verde.

## [2026-08-07] tarea | #75 tramo urgente — el CRM que sí cambia de estado: embudo §19.8 con transiciones libres, historial, scoring por actividad y visibilidad del admin

Reporte del usuario: "los estados no se cambian ni se muestran correctamente", la clasificación por actividad no funcionaba y "los errores se muestran de una manera muy cruda". Diagnóstico de los 3: (1) la pantalla ofrecía los 7 estados como tappables mientras la EF tenía una **máquina ESTRICTA con terminales** — un lead cerrado no se podía reabrir y cada toque inválido tronaba; (2) **no existía fuente de datos para el scoring**: `events_raw` **no tiene un solo escritor** (0 filas en el remoto), así que el diseño original de la subtarea leía un pipeline inexistente; (3) los hooks del CRM pintaban `error.message`, que ante cualquier 4xx de una EF es literalmente *"Edge Function returned a non-2xx status code"* — pese a que `extract_error_code` **ya existía** y lo usaban auth/registration/publish. Alcance ejecutado 75.1/75.2/75.5/75.6 (fuera: privacidad §19.2, unificación, export CSV+retención). **4 migraciones + rollback**: `20260807000002` (enum +`whatsapp_opened`/`interested`/`closed_won_rent`/`closed_won_sale`, **sola** por el gotcha `ALTER TYPE ADD VALUE` — el valor nuevo no se puede USAR hasta el commit) · `000003` (data-fix `new→whatsapp_opened`; `is_follow_up` como bandera **ortogonal**; `lead_status_history` append-only poblada por **trigger, nunca por las EFs**, así ninguna ruta futura se olvida de loguear; ⚠️ el guard de no-op va dentro del cuerpo con `TG_OP` porque Postgres prohíbe `OLD` en el `WHEN` de un trigger con INSERT) · `000004` (`score`/`level`, fórmula **10 + 4×saves + 1×likes** con las señales que YA existen, triggers calcados de `20260701000001`, score **retroactivo** en BEFORE INSERT, aislamiento por agente vía JOIN a `properties.owner_user_id`, umbrales en `app_config` con fallback COALESCE 15/30 y recálculo de los leads existentes al recalibrar) · `000005` (`private.is_agency_admin_of` + OR en los **3 puntos de LECTURA** → el **admin** de inmobiliaria ve el pipeline del equipo; **la escritura no se tocó**, 2 asserts la custodian). EFs: `update-lead-status` sin `VALID_TRANSITIONS` (transiciones libres; `INVALID_TRANSITION` extinto, pero el allowlist del enum se queda), `update-lead-note` +`is_follow_up` (aditivo, `!== undefined` para no perder el `false`), `contact-agent` nace en `whatsapp_opened` y por fin extrajo `lead_repo.ts` del closure de `Deno.serve`. Cliente realineado **en la misma subtarea que la migración** (sin ventana rota): `lead_error_messages.ts`, `useAgencyRole` a 4 roles con `canViewTeam`, orden por puntaje, `useLeadStatusHistory`, puntaje+nivel en `LeadCard`, sección fija "En seguimiento" y timeline en el detalle; de paso se **regeneró `database.types.ts`**, saldando el drift arrastrado desde la #71. Guardian **FAIL en la 1ª ronda** (el allowlist de la EF y el guard de no-op del trigger sobrevivían a su mutante) → cerrado con tests verificados **aplicando el mutante**. Suites: **680 pgTAP (31 archivos) · 795 Deno · 881 Jest · tsc/lint 0**. E2E de UI **bloqueado por el muro de ubicación** (→ #109), así que se verificó contra el REMOTO con JWT real: reabrir un cerrado 200 (antes 400), salto a estado nuevo 200, `is_follow_up` 200, estado inventado `INVALID_INPUT`, timeline con old/new correctos, y el owner **ve pero no edita** el lead de su compañero. Deuda: **#108** (`score`/`level` escribibles por REST — un agente puede inflar su puntaje; `changed_by` siempre NULL porque las EFs usan `service_client`) y **#109** (el `LocationWall` cubre TODA ruta protegida: bloquea el E2E **y** deja sin CRM a quien tenga el GPS apagado).

## [2026-08-07] tarea | #103 CERRADA — el falso negativo del upload: verificar antes de fallar (RLS del video en vuelo + reaper + poll del cliente)

Derivada de 68.4, cazada en el E2E del deploy-day #90. El video **llegaba completo** a Cloudflare Stream (`ready` en <30 s por webhook) pero `uploadAsync()` lanzaba `"Failed to read response"` al leer la respuesta HTTP → la UI decía "Error al subir el video" y **cada Reintentar dejaba otro video huérfano de pago** en Stream; reproducido 2/2 con 44 MB y con 4.3 MB (no dependía del tamaño). Fix en 3 piezas, decisión de Abraham "verificar antes de fallar": (1) **RLS** — la policy `videos_select` gana una tercera rama acotada al dueño (`property_id is null and agent_id = auth.uid() and deleted_at is null`, migración `20260807000001` + rollback + pgTAP `27` plan(8)): con el upload-first (`property_id NULL`) las dos ramas previas devolvían false en silencio, así que **el agente no podía leer su propio video** y el cliente no tenía nada que consultar; sin helpers de agencia a propósito (precedente #100). (2) **Reaper** en `mint-upload-url` — `count_active_uploads` contaba toda fila `uploading`/`processing` sin ventana, así que un binario que nunca llegaba dejaba al agente con un 409 **eterno**; ahora `STALE_UPLOAD_MS`=15 min vía `stale_before` (`processing` bloquea siempre, `uploading` colgado libera) y el fail-closed `return 1` **se loguea** (un `PGRST100` del `.or()` sería un 409 global y mudo). (3) **Cliente** — `useVideoUpload` pasa toda excepción o no-2xx por `verify_before_failing` (`status='verifying'`, poll 10×3 s consultando ANTES de dormir): `ready`/`processing` = éxito idéntico al 2xx, `failed` corta ya, resto → error neutro; callback `on_status_change` porque `step3` solo leía el status tras el `await` (la etiqueta "Verificando…" era rama muerta). Guardian PASS **con mutación real ejecutada** (policy −rama = 2 rojos, −`deleted_at` = 1, −scoping = 2, handler −`stale_before` = 1). E2E emulador→remoto: el binario volvió a fallar con el error exacto, la verificación lo recuperó **al primer poll** → publish completado y censo del remoto con **0 huérfanos**. Suites: 620 pgTAP (28 archivos) · 779 Deno · 854 Jest · tsc/lint 0. Desplegado: migración `20260807234556` + EF `mint-upload-url`. Deuda registrada #107 `hardening(103.1)`: `make_active_upload_checker` sin ningún test (revertir el filtro deja todo verde) y Reintentar tras un fallo REAL choca hasta 15 min con el 409.

## [2026-08-07] release | Deploy-day #90 CERRADA — backend remoto sincronizado, E2E real con Stream, builds v1.0.3 (fingerprint nuevo)

Sesión 06→07-ago. Remoto `urbea-app` al día con `main`: 15 migraciones vía `apply_migration` (INEGI 2,478 municipios por EF desechable, §5.1, gate legal, registro atómico, ola #71 + fixes #99/#100 con sondas en vivo), 6 EFs, webhook Stream registrado, par R2 rotado. E2E emulador→remoto: gate legal → video real a Stream (`ready` en 27 s) → publish 201 con `agency_id` denormalizado (fix #100 vivo) → detalle reproduce HLS firmado. Derivadas de bugs reales: #103 (falso negativo del upload, ALTO), #104 (archive-video sin token firmado en la descarga), #105 (rotar STREAM_API_TOKEN, hardening). Lección de release: el OTA del 06-ago fue NO-OP silencioso — `expo-web-browser` (#72) movió el fingerprint y ningún build instalado (v1.0.2) tenía el runtime destino; regla nueva en [[estrategia-releases]] (comparar runtime del update vs `eas build:list`). Resolución: version 1.0.3 + builds EAS ambos FINISHED (Android APK `preview` con link directo; iOS `production`, submit de Abraham). Verificación iOS en simulador: dev-client recompilado (ExpoWebBrowser missing), falsa regresión de íconos NativeTabs descartada con build Release + Maestro (solo-dev: la UITabBar no carga PNGs de Metro). envs EAS: preview == production (testing replica producción).

## [2026-08-06] tarea | #99 y #100 CERRADAS — fixes ALTO derivados de #71/PR #41 (deadlock de aprobación, degradación de admin, suspensión no efectiva, agency_id nunca denormalizado)

PR #42 (#99) y PR #43 (#100), TDD estricto, guardian PASS en ambas rondas (con hallazgos reales resueltos antes de cerrar, no dejados como deuda). #99: migración `20260805000010` — trigger de aprobación de agencia ya no queda sin salida cuando el creador tiene membresía activa en otra agencia (`MEMBER_OF_OTHER_AGENCY`, decisión: sin auto-switch); ambos triggers de aprobación ya no degradan a un admin a `agent` en silencio. #100: migración `20260805000011` — `publish_property_atomic` bloquea a un suspendido (antes bypaseaba la RLS del INSERT directo, el flujo real nunca la evaluaba) y denormaliza `properties.agency_id` (antes letra muerta, con backfill); `properties_select`/`properties_update` unifican owner+admin de agencia en `agency_role_of(agency_id)` (cerraba un leak y un falso-bloqueo cross-agencia tras un switch). Quedan pendientes #96/#97/#98/#101/#102 (media-baja prioridad, fuera de alcance de esta sesión por decisión de Abraham); #98 se amplió con la pregunta de baja de inmobiliaria completa, candidata a `/tm-explore`. Suites: 612 pgTAP · 778 Deno · tsc/lint 0.

## [2026-08-05] tarea | #71 Ola 1 Roles y multi-tenant CERRADA — jerarquía 4 niveles, upgrade a agente, self-service de inmobiliaria, aprobaciones por trigger

7 subtareas, TDD estricto en 5 (guardian PASS ×5, ~2 mutaciones supervivientes convertidas en tests ancla). Backend: 9 migraciones (`20260805000001`–`000009`) — `is_premium` derivado, enum `agency_member_role` a 4 niveles + matriz RLS, RPC `upgrade_to_agent_atomic` (espejo, NO refactor de redeem), `agent_applications` REUSADA (estaba latente desde 0003 — el plan pedía tabla nueva y el RED la encontró: reusar>reescribir), RPC `register_agency_atomic`, triggers de aprobación con auditoría `admin_actions` inmutable (Studio-only para agencies por column-grants; actor JWT>GUC), enum +suspended, `switch_agency_atomic` y suspensión-bloquea-publicar (gap real destapado por el RED). 4 EFs nuevas (upgrade-to-agent, request-agent-upgrade, register-agency, manage-agency-member) con patrón DI. Mobile: wizard `/upgrade` (2 caminos), `/agency/register` (logo ponytail-diferido), `/agency/members`. Deuda registrada: #96 EXECUTE PUBLIC, #97 INSERT directo agencies, #98 owner huérfano; followers→#78. Suites finales: **592 pgTAP (27 archivos) · 772 Deno · tsc/lint 0**. Todo local, sin deploy al remoto aún.

## [2026-07-31] tarea | #72 Ola 1 Auth CERRADA — verificación de email + reset + Google OAuth, E2E local vía Mailpit

Las 3 subtareas diferidas por cuentas externas se destrabaron al notar que **Mailpit** (`:54324`) captura los correos del GoTrue local sin SMTP: 72.3 y 72.5 corrieron TDD completo (RED→GREEN→guardian; 1 FAIL de cobertura corregido con +11 tests) **y E2E por API** contra el stack local (registro → correo → link verify → `302 urbea://verify-email#access_token…` → confirmado; recover → `type=recovery` → nueva contraseña → login nueva 200/vieja 400). Cambio de contrato: la EF `register` crea usuarios **sin confirmar** y el cliente dispara el correo; adiós auto-login post-registro; guard `email_confirmed_at` en `protected-layout` antes del gate legal. Patrón reutilizable nuevo: `deep-link-session.ts` (`parse_session_from_url`) + `useSessionFromDeepLink` (on_success parametrizado) — lo consumen verify-email, reset-password y `useGoogleOAuth` (72.4: flow implícito + `expo-web-browser` [⚠️ nativo nuevo → rebuild, no OTA], flag apagado hasta el client de Google Cloud; candado EC-B5 protege el false del gate App Store 4.8). `redeem-invitation` intacto a propósito. Config local: `[auth.email] enable_confirmations=true` + `additional_redirect_urls=["urbea://*"]`. Pendiente SOLO config remota (Resend/Google/flip) → `docs/TODO-pendientes.md`. Jest 842 (75 suites) · Deno 681 · pgTAP 336 · tsc/lint 0.

## [2026-07-04] ingest | Documentación de cuentas demo (LOCAL/REMOTO) y arranque manual del emulador Android

Nueva página [[entornos-y-cuentas]]: catálogo de las 11 cuentas del seed LOCAL (`supabase/seed.sql`, sin admin) vs las cuentas del REMOTO `urbea-app` (mismo set + `admin@urbea.demo` + las 2 cuentas históricas `@urbea.app`), cómo swapear `mobile/.env.local` entre entornos, y el código de invitación `DEMO2026` en ambos. `comandos.md` ganó la sección "Emulador Android" con el desglose manual paso a paso (env vars, `emulator -avd urbea`, `adb reverse`, deep link) detrás de `pnpm emu`. Sin cambios de código de app.

## [2026-07-06] tarea | Flash ronda 5: mapa fiel a la dirección + edición de ubicación reparada + entrega 1.0.1 y wipe del seed

Diagnóstico del reporte "los pines no son consistentes": (1) la propiedad publicada en prod decía GDL pero su pin era el centro del fallback CDMX — el wizard permitía escribir la dirección a mano (sin sugerencia) y fijar el pin tocando el mapa donde fuera; (2) el seed compartía 4 coordenadas entre 14 propiedades (pines encimados); (3) el modo edición estaba roto de raíz: `useLoadProperty`/`usePublish` leían/escribían columnas `lat`/`lng` **inexistentes** (properties solo tiene `location geography`). Fixes (commit `e16ede0`): `AddressAutocomplete` geocodifica el texto libre al blur vía Places **Text Search** (misma key; `resolved_text_ref` evita re-geocodificar/pisar ajustes manuales), `useLoadProperty` parsea `location` con `parse_location`, y el UPDATE de edición escribe EWKT `SRID=4326;POINT(lng lat)`. La **miniatura de frame medio** resultó bien implementada pero jamás ejercitada: `expo-video-thumbnails` es nativo y el APK 1.0.0 no lo traía. Verificación E2E real en emulador con el APK 1.0.1: publicación completa → pin exacto en Av. la Paz/Americana y `thumbnail_url` escrito con el frame medio (validado visualmente). Entrega: OTA de los fixes al canal preview (runtime 1.0.1, grupo `53d78618`), **wipe del seed remoto aprobado y ejecutado** (0 propiedades/videos/engagement, storage vacío vía Storage API — `storage.objects` no admite DELETE por SQL —, quedan solo `admin@urbea.demo` y `swacg08@gmail.com` ascendida a `agent`). Tests actualizados al contrato real del esquema; 495/495, tsc 0.

## [2026-07-06] tarea | Flash ronda 4: tab bar canónica con FAB central + rebuild 1.0.1 (expo-image, splash) + skeletons/empty states

Cuarta ronda flash (plan aprobado con grilling). **Tab bar según mockup** (`urbea-identidad-visual.html` `.tabbar`/`.tab-fab`): Feed·Mapa·**[+]**·Leads·Perfil — el FAB flotante del feed (tapaba el rail de acciones: `bottom:32` fijo vs rail `insets+100`) se eliminó y ahora es el botón central de la barra (48×48, radio 16, verde primario, −16px, borde del fondo, variante oscura sobre el feed); Guardados salió de la barra → Perfil→menú ⋯ (`href:null`, ruta viva). **Nativos nuevos** (→ `version: 1.0.1`, runtime OTA nuevo, requiere APK nuevo): `expo-image` (cache disco + fade-in en todas las miniaturas/avatares, 8 archivos) y `expo-splash-screen` (isotipo sobre verde de marca; adiós flash blanco del arranque). **Fluidez JS**: `React.memo` en PropertyGridCard/LeadCard/PropertyListItem; `freezeOnBlur` en tabs (congela mapa/CRM); `slide_from_right` en el Stack; `GridSkeleton` (nuevo, `src/components/`) reemplaza ActivityIndicator en Saved/PropertiesGrid; `EmptyState` (profile) ganó íconos Phosphor (adiós emojis)+CTA+variante dark y lo usan feed vacío ("Sé el primero en publicar"→wizard), Saved y CRM; poster de video persistente en feed (antes solo en `loading`→flash oscuro) y nuevo en el hero del detalle (`thumbnail_url` añadido a `usePropertyDetail`); `LayoutAnimation` al quitar guardados/filtrar CRM; `drawDistance` 1.5×. Flujos Maestro actualizados a la nueva navegación. Verif: tsc 0, lint 0 err, **495/495**. Ingest: [[design-system]], [[mapa-codebase]].

## [2026-07-06] tarea | Flash ronda 3: acento verde de marca + microinteracciones + todo registro publica + OTA operativo

- **Acento global = verde Urbea del logo `#1A5E44`:** `colors.primary` (familia completa + sombra) en `theme.ts` + 12 archivos con salvia `#5A8A5E` hardcodeada migrados. Like/guardar activos ahora en verde de marca (era arcilla `accent_soft`). Pins del mapa todos idénticos en verde (el "pin café" era el color-por-operación de venta; eliminado).
- **Microinteracciones (guiadas por skill ui-ux-pro-max):** like del rail dispara el corazón grande + haptic light (antes solo doble-tap); haptic selection al guardar; pressed scale/opacity en rail + WhatsApp; filas del ProfileMenu con pressed + `android_ripple`.
- **Todo registro puede publicar:** migración `20260707000001_signup_default_agent` (+ rollback) — `handle_new_user` crea perfiles `role='agent'`; aplicada al remoto (aprobado). El FAB "+", la RLS `properties_insert` y la EF `publish-property` ya exigían agent → cero cambios en esas capas. Cuentas de HOY con role user quedaron sin migrar (UPDATE retroactivo bloqueado por permisos; registrarse de nuevo).
- **EAS Update operativo:** primer OTA publicado al canal `preview` (runtime 1.0.0, update group `52f19103`). ⚠️ Gotcha: `eas update` truena el bundler bajo pnpm ("transformFile undefined"); workaround = `npx expo export --output-dir dist --experimental-bundle --dump-sourcemap --dump-assetmap --platform=android --clear` y luego `eas update --skip-bundler`.
- Infra de la entrega: env vars de `.env.local` en el env store de EAS (preview/production, aprobado); APK preview compilado en la nube (build `a67a41c0`); `mailer_autoconfirm=true` en el remoto. Suite 495/495, tsc 0.

## [2026-07-06] tarea | Flash ronda 2: registro libre sin código + pins de mapa unificados (Phosphor MapPin)

Segunda ronda de la sesión flash (entrega Android hoy; probadores: 1 iOS pendiente de Apple Developer, 1 Android).
- **Registro libre (rol `user`):** el backend YA lo soportaba (trigger `handle_new_user` → `public.users` con `role='user'`); faltaba la UI. `signUp(email, password, first_name?)` nuevo en `AuthContext` (`supabase.auth.signUp` + metadata). `app/register.tsx` ahora tiene 2 modos: `'user'` default (nombre opcional + correo + contraseña + confirmar, mín 6) y `'agent'` (flujo original por código, intacto) con links para alternar. CTA del login: "¿No tienes cuenta? Regístrate" (ya no menciona código). E2E `registro.yaml` gana un tap "Regístrate como agente".
- ⚠️ **Config remota cambiada (aprobado por Abraham):** `mailer_autoconfirm=true` en `urbea-app` vía Management API — sin esto el signup NO devolvía sesión (pedía confirmar correo). Verificado con curl (signup → access_token directo); usuarios de prueba borrados. Revertir en dashboard → Auth cuando la demo cierre si se quiere confirmación real.
- **Pins unificados:** nuevo `mobile/src/components/MapPinIcon.tsx` (Phosphor `MapPin` weight=fill) = pin canónico de los 3 mapas. `PropertyMarker` (mapa global) pierde el teardrop+isotipo+price pill (el precio vive en la mini-card al tocar el pin); conserva color por operación (renta salvia / venta arcilla) y `tracksViewChanges=false`. `MapPicker` (publicar) y `PropertyMap` (detalle) dejan el pin nativo por el mismo icono.
- **Mapas centrados en el usuario:** `MapScreen` y `MapPicker` arrancan en `useLocation().coords` (permiso garantizado por LocationWall #41) con fallback GDL/CDMX; el mapa global además muestra `showsUserLocation` + botón de mi ubicación.
- **Fix preexistente:** `login-submit.test.tsx` fallaba desde `c81746c` (mock de expo-router sin `Link`/`Redirect`) — mock completado. Suite completa 44/44, 495 tests, tsc 0, lint 0 err.

## [2026-07-06] tarea | Sesión flash pre-entrega: 9 mejoras de demo (feed, publicación, perfil, ubicación) — vivo

Bundle rápido de UX contra el REMOTO (`.env.local` ya apunta a `urbea-app`; requiere **dev build fresco** — la iteración anterior en device no traía el FAB "+" ni estos cambios). Sin TDD (flash, aprobado). tsc 0, lint 0 err. **Rama:** flash sobre `tarea/20-…` (sin commitear aún).
- **P1 botón "+":** ya existía y cableado (`FeedScreen.tsx` FAB Phosphor → `/publish/step1`, agentes/admin). No era código faltante sino build viejo. Sin cambios.
- **P2 icono de Guardados vacío:** emoji `🔖` → Phosphor `BookmarkSimple` outline (distinto al `Bookmarks` del tab). `SavedScreen.tsx`.
- **P3 WhatsApp en el feed:** botón `WhatsappLogo` en el rail del `PropertyOverlay` → `open_whatsapp` directo (sin lead CRM). Requirió teléfono del agente en la query: `feedProperties.ts` embebe `users!properties_owner_user_id_fkey(phone)` → `FeedProperty.agent_phone`.
- **P4 ubicación:** `LocationProvider`/`LocationWall` ya correctos (permiso/GPS on-off + `AppState 'active'→refresh()` detecta cambios de Ajustes). Verificado, sin cambios.
- **P5 compartir:** `src/lib/shareProperty.ts` (`share_property`, Share nativo del signed_url del mp4 → abre video sin cuenta). Botón `ShareNetwork` en el rail. ⚠️ TTL = 3600s de mint-video-url (link durable de 7d = subir el expires_in de la EF, diferido).
- **P6 dirección→pin preciso:** `AddressAutocomplete` gana `onPlaceSelected` (Place Details New, FieldMask `location` → lat/lng); `MapPicker` recentra con `animateToRegion` al cambiar coords; step2 hace `update({address,lat,lng})`.
- **P7 portadas de video:** `expo-video-thumbnails` (nuevo, ~56.0.3) → `src/features/publish/lib/videoThumbnail.ts` (`generate_and_store_thumbnail`: frame medio → sube a bucket **público** `profile-photos` `{uid}/thumb_{video_id}.jpg` → UPDATE `property_videos.thumbnail_url` vía RLS `videos_update`; fail-soft). Cableado post-publish en step3 (captura video_id/uri/duración antes del `reset()`). Consumo: feed (`VideoFeedItem` póster mientras `player_status==='loading'`; `feedProperties` selecciona `thumbnail_url`) + grids (perfil/guardados ya lo leían). Sin migración/RPC/EF.
- **P8 perfil 3 botones→menú:** `ProfileMenu.tsx` (bottom-sheet nativo) + botón "⋯" flotante en `ProfileScreen`; opciones Mis publicaciones/Invitar agentes(owner)/Editar perfil/Cerrar sesión.
- **P9 gestos iOS:** root `Stack` con `gestureEnabled`+`fullScreenGestureEnabled` (swipe-back a pantalla completa desde el detalle).

**Pendiente de entrega:** dev build fresco (módulos nativos nuevos: expo-video-thumbnails) + decisión de git (commit en esta rama vs. rama limpia). Ingest a `mapa-codebase` diferido.

## [2026-07-03] feat | Pre-#20: invitaciones owner (#34), fixes y remoto sembrado (#35, #37)

- **#35** (PR #9): tap de my-listings → `/property/[id]` (era el stub más visible).
- **#34** (PR #10): EF `create-invitation` (TDD 24 tests; agencia derivada del JWT, plano una-sola-vez) + `useCreateInvitation` + pantalla `(protected)/agency/invitations.tsx` gateada por `isOwner` + botón "Invitar agentes" en perfil. Extraído `extract_error_code` a `src/lib/supabase/edge-errors.ts`. Suites: Deno 427, jest 478.
- **#37**: remoto `urbea-app` sembrado (12 usuarios incl. `admin@urbea.demo`, 3 agencias, 10 propiedades con video `ready` en Storage, código `DEMO2026`) y verificado E2E por API (password grant, mint-video-url → HTTP 200, validate-invitation). `.env.local` → remoto.
- **#36** (review): las 2 env keys de Google son LA MISMA key → dividir antes de restringir; receta con SHA-1 en `auditoria-pre-tarea-20.md` §9. **#38** (review): pase manual listo (`pnpm emu`).
- ⏳ Pendiente aprobación del usuario: deploy de `create-invitation`, `admin-create-agency`, `update-property-status` al remoto.

## [2026-07-03] query | Auditoría pre-tarea #20 (calidad + seguridad + plan de cierre)
Auditoría completa antes de arrancar la #20, con 2 subagentes paralelos (calidad de código mobile + seguridad Supabase/cliente). **Gates: tsc 0, lint 0 err/9 warn, Jest 467/467, Deno 403/403; backlog 31/31 done.** Calidad: 1 huérfano real (`feed-item-placeholder.tsx`), `expo-status-bar` sin uso, tap de my-listings no-op, 2 islas pre-theme con paleta gris Tailwind fuera de `theme.ts` (auth/onboarding/publish + PrimaryButton) y ~25 `#FFFFFF` sin migrar a `on_primary`/`surface`; ~130 `ponytail:` sanos. Seguridad: **APTA para demo cerrada**; antes de usuarios reales → restringir keys Google por bundle-id/SHA-1 (M1), rotar password demo + seed nunca a prod (M2), rate-limit invitaciones (B1), column-grants en `properties` (B2), validar ownership de `storage_path` en publish-property (B3). Gap PRD: owner sin UI de invitaciones/equipo (mitigado: token inicial `max_uses=NULL`). Plan E2E propuesto: **Maestro** (6 flujos YAML contra Supabase local). Distribución: Android = EAS preview APK interno; iOS = simulador gratis primero (xcode-select apunta a CommandLineTools, corregir) → TestFlight interno si hay tester iOS. **Doc de trabajo: `.taskmaster/docs/auditoria-pre-tarea-20.md`** (checklist de 15 features manuales incluida); hallazgos también en bitácora de 20.1.

## [YYYY-MM-DD] tipo | título`.
Tipos: `fundacion`, `decision`, `ingest`, `query`, `lint`, `tarea`, `explore`.
Tip: `grep "^## \[" log.md | tail -5` → últimas 5 entradas.

## [2026-07-03] tarea | #32 Isotipo real en placeholders (grid + pin de mapa) — vivo
El "bloqueante" (rebuild nativo por `react-native-svg`) **se evaporó** al abrir el terreno: la premisa de #32 era incorrecta. #23.3 NO dejó un PNG temporal — creó `IsotipoMark.tsx` con **primitivas RN puras** (Views+bordes, cero deps nativas), ya vivo en `ProfileHeader`. Los 2 consumidores objetivo (placeholder de grid size 34, pin de mapa size 14) **no requieren fidelidad vectorial fina** → instalar react-native-svg + rebuild EAS era sobre-ingeniería (el propio techo del componente dice diferir hasta ≥2 consumers con curvas exactas). **Resolución ponytail (aprobada por usuario):** reusar el `IsotipoMark` existente en `PropertyGridCard` (reemplaza `▷`) y `PropertyMarker` (reemplaza Ionicons play, contra-rotación +45deg intacta); **revertido el install especulativo** (package.json + pnpm-lock idénticos a origin/main); limpiados comentarios `ponytail:` obsoletos + import/estilo Ionicons huérfanos. Forward-compatible: el código consumidor (`<IsotipoMark size color/>`) es idéntico si algún día se migra el interno a SVG. **Sin rebuild, sin EAS, sin baile de .apk.** No crítica (styling); tsc+lint limpios. Rama `tarea/32-svg-isotipo`. Ingest: [[mapa-codebase]], [[design-system]].

## [2026-07-03] tarea | #33 Tokens on_primary + surface, migra #FFFFFF en LeadExpandedView — vivo
Salda la deuda de theming que el guardian marcó en #29. **XS, no crítica** (styling puro, sin lógica/RLS/EF → solo tsc+lint, sin TDD). 2 subtareas deterministas (no `expand`, para evitar drift del AI). **Decisión del usuario:** (1) **2 tokens semánticos separados** en `theme.ts` — `on_primary` `#FFFFFF` (texto/iconos sobre primary) + `surface` `#FFFFFF` (superficie blanca de modal/card) — en vez de reusar `on_primary` como fondo (evita la deuda semántica de "contenido sobre primary" usado como background); (2) **alcance acotado** solo a los 3 `#FFFFFF` de `LeadExpandedView` (L372 sheet→`surface`, L614/649 texto de botón→`on_primary`); `LeadCard.tsx:162` + `AgentSelector.tsx:150,159` **fuera** (su `ponytail:` sigue abierto; `surface` queda listo para adopción en una auditoría de theming futura). Cero cambio visual (mismo `#FFFFFF`). tsc+lint limpios (warning L98 exhaustive-deps preexistente ajeno). Rama `tarea/33-on-primary-token`. Ingest: [[design-system]].

## [2026-07-03] tarea | #29 Guardar nota del lead sin cambiar estado — vivo
Desacopla la edición de `internal_notes` de la transición de estado que acoplaba #15. **5 subtareas, todas críticas TDD.** Decisión de flujo (usuario): el decompose partió RED (29.2/29.3) y GREEN (29.4) en subtareas hermanas → chocaba con `tdd-guard.sh` (matchea el sentinel contra la subtarea exacta, bug documentado en `tdd_guard_split_red_green_siblings.md`); se **fusionó en un solo ciclo RED→GREEN continuo** manteniendo `.current-red=29.2` fijo durante todo el GREEN (patrón #15, sin tocar el hook de seguridad). Orden: 29.1 types → 29.2+29.3+29.4 (EF fusionada) → 29.5 (hook+wiring). **Backend:** EF `update-lead-note` (`{lead_id,note}` → edita solo `internal_notes`+`updated_at`, **nunca `status`**; doble-query ownership solo `agent_id`/admin consistente con `private.can_edit_lead` — **NO** agency-owner, decisión de producto: owners en solo-lectura; `note=""`→limpia a null, distingue key-ausente(400) de string-vacío(200); **24 Deno tests**, guardian PASS orden postgrest real sin bug #23). **Móvil:** `useUpdateLeadNote` (mirror `useUpdateLeadStatus`, bloqueo síncrono de doble-submit, 13 tests, guardian PASS) + botón "Guardar nota" en `LeadExpandedView` (visible solo si la nota cambió, deshabilitado durante guardado, **respeta `readOnly`** del owner #28, error inline separado; status-change sigue mandando la nota = retrocompat #15). ⚠️ **Incidencia:** el agente GREEN móvil tocó `mobile/jest.setup.js` (fuera del footprint) para arreglar contaminación cruzada del drenado `act()` (FIFO→LIFO) entre tests — el guardian lo **escrutó a fondo y verificó empíricamente** que es un fix legítimo de infra (EC-10 pasa igual con el setup viejo → el bloqueo lo hace el hook, no la infra), no una trampa. Suite completa **467/467**, tsc+lint limpios. **Follow-up capturado (add-task):** sembrar token `colors.on_primary` en theme.ts y migrar 3 `#FFFFFF` hardcodeados. ✅ EF **desplegada al remoto** (urbea-app, 2026-07-03, `--import-map`; smoke OPTIONS→200 / POST sin auth→401). Rama `tarea/29-update-lead-note`. Ingest: [[crm-leads]], [[mapa-codebase]].

## [2026-07-02] tarea | #12 Filtros básicos del feed + mapa — vivo
`FilterSheet` (**RN Modal nativo**, NO @gorhom/bottom-sheet — evita z-index del autocomplete dentro de un bottom-sheet) con estado en `FilterProvider` (Context + AsyncStorage `urbea_filters`, hidrata en mount, persiste debounced 500ms), compartido por feed y mapa desde `app/(protected)/_layout.tsx`. Filtros: operación (rent/sale) · tipo (5 enums) · precio (min/max) · zona (colonia) · recámaras (mínimo) · toggles pet_friendly/allows_no_guarantor/student_friendly. 7 subtareas, **4 críticas TDD+guardian PASS**. Orden analista: 12.1→12.5→12.2→12.3→12.4→12.6→12.7. **Críticas:** **12.3** `validation.ts` rango de precio (38 tests: null/0/negativos/NaN/decimales/min==max/min>max, `parse_price` nunca NaN sin coma-miles/notación-científica); **12.4** `zones.ts` (**sin ILIKE a DB** — `fetch_distinct_zones` distinct client-side + `filter_zones` puro con normalización de acentos NFD → honra gotcha 🔒; 15 tests); **12.6** `filterQuery.ts` (`build_filter_query` puro: ⚠️ **sutileza `operation_type='both'`** → `.in(op,[...S,'both'])`, booleanos false=sin filtro, zone=.eq exacto, EMPTY_FILTERS=0 llamadas; `get_active_filter_count` por grupo; 27 tests) + `filterStore.tsx` (Context useReducer); **12.7** integración `fetchFeedProperties(cursor?,deps?,filters?)`/`fetchMapProperties(deps?,filters?)` **backward-compat** (filters último opcional, EMPTY_FILTERS = comportamiento previo → **25 tests preexistentes intactos**) + `filterStorage.ts` fail-safe (JSON corrupto→EMPTY_FILTERS) + badge/clear/refetch-por-identidad; 32 tests nuevos, suite completa **427 verdes**. No-críticas (tsc): 12.1 FilterSheet+entry-points (ícono dormido de MapSearchBar activado), 12.2 `FilterChipGroup` multi-select (reusa patrón FilterTabs), 12.5 `BedroomsSelector`+toggles. **Decisiones del usuario:** (1) implementar **completo** incl. recámaras+toggles pese a que el concepto los marcaba diferidos (columnas ya en `0005` con índices parciales); (2) **RN Modal** vs bottom-sheet; (3) zona por **autocomplete DISTINCT + match exacto** vs ILIKE+índice. Estado con **Context** (no Zustand: no instalado + ponytail). **Incidencias de ejecución:** 2 agentes GREEN (12.3) cortados por API mid-response — trabajo persistido igual; el guardian de 12.3 notó que el corte dejó la sección UI de precio sin JSX → completada por el orquestador; guardian de 12.6 cortado → verificación anti-cheat+lógica hecha directo por el orquestador. Rama `tarea/12-feed-filters`. Ingest: [[busqueda-y-filtros]], [[mapa-codebase]], [[feed-vertical-video]], [[mapa-y-ubicacion]].

## [2026-06-30] tarea | #15 CRM del agente (lista de leads + embudo de estados) — vivo
Tab **CRM** para agentes: lista de sus leads, filtros, búsqueda, y cambio de estado por el embudo de 7 estados vía nueva EF. 9 subtareas, **3 críticas con TDD+guardian PASS**. Orden: 15.6 (EF, fija el contrato) → lo móvil apilado → vista expandida al final. Críticas: **15.6** EF `update-lead-status` (valida enum+transición del embudo, ownership doble-query `LEAD_NOT_FOUND` vs `UNAUTHORIZED_AGENT`, `internal_notes` solo si viene `note`; patrón DI clonado de `update-property-status`; **55 Deno tests**); **15.2** `useAgentLeads` (query con embeds `users(phone,user_preferences(...))`+`lead_origin_properties→properties→property_videos`, RLS filtra `agent_id`, transform null-safe; **10 tests**; guardian validó un fix de tipos en fixtures como no-weakening); **15.4** `useUpdateLeadStatus` (invoca la EF, patrón `usePropertyActions` con `is_working_ref`+force-update síncrono, spread condicional de `note`; **13 tests**). No-críticas (verificación ligera, tsc): 15.1 pantalla+role guard (`user.role==='agent'`+`<Redirect>`, tab `href:null`); 15.3 `LeadCard`; 15.7 `FilterTabs<T>` genérico (extraído de profile, profile→wrapper fino) + lista + filtro **client-side** `useMemo`; 15.8 búsqueda por nombre client-side; 15.9 empty states (generalizó `EmptyState` de profile); 15.5 textarea notas + "Ver propiedad" + WhatsApp (reusa `open_whatsapp`). **Correcciones de alcance:** enum real `{new,contacted,in_progress,visit_scheduled,closed_won,closed_lost,discarded}` (NO el `qualified/visit_done/negotiation/closed` del PRD), nota=`internal_notes`, ownership=`agent_id`. Decisiones del usuario: orden del analista · role guard simple · FilterTabs genérico · recrear LeadCard sin preview HTML. **Trabajo nuevo capturado:** #28 (owner ve leads de sus agentes), **#29** (editar nota sin cambiar estado — la EF hoy acopla nota+transición). **EF `update-lead-status` desplegada a producción** (urbea-app, 2026-06-30, `--import-map`; smoke remoto 401 sin auth / 200 OPTIONS) → el CRM funciona E2E. Hook/tooling: repo **sin ESLint** → verificación = `pnpm tsc --noEmit` (gate real). Rama `tarea/15-crm`. Ingest: [[crm-leads]], [[mapa-codebase]].

## [2026-06-30] decision | Criticidad TDD = regla determinista por path (inspirado en AJN)
La decisión "¿esto lleva tests?" deja de ser un **juicio** del analista y pasa a **derivarse del footprint** por una regla canónica única (`CLAUDE.md` §5). Crítica (TDD: RED→GREEN→guardian) ⟺ toca `supabase/functions/**`, `supabase/migrations/**`, o lógica móvil pura `mobile/**/{lib,hooks,utils}/**` y `**/validation*`; no crítica (tsc+lint+smoke) ⟺ presentación/config/docs; desempate → crítica. Contraste con AJN: AJN usa regla léxica (6 palabras clave) + sentinel `.tests-optional` y vigila todo `src/**`; Urbea ya separaba crítico/no-crítico **por path en el hook**, más limpio — solo faltaba quitar el residuo subjetivo del analista y extender el hook a la lógica móvil (antes solo vigilaba Supabase). Cambios: `CLAUDE.md` §5 (regla canónica), `.claude/agents/analista-subtareas.md` paso 5 (deriva, no juzga), `.claude/hooks/tdd-guard.sh` (añade globs `lib/hooks/utils/validation`). Verificado: matching + smoke E2E del hook (lógica bloquea sin tests, presentación pasa, test files pasan, sin sentinel no aplica). No se copió `.tests-optional` (maquinaria innecesaria dado el gate por path). Se mantienen `.current-red` y `guardian`.

## [2026-06-30] tarea | #14 Contacto WhatsApp + lead CRM automático — vivo
Botón de contacto en el **detalle** que crea un lead vía la Edge Function **`contact-agent`** (desplegada a `urbea-app`). 9 subtareas, **5 críticas con TDD+guardian** (handler del EF construido por DI, una subtarea por capa): 14.2 skeleton (auth 401 + validación UUID manual sin Zod), 14.3 retrieval propiedad+agente (404 NOT_FOUND / 400 INVALID_PROPERTY_STATE / 400 AGENT_PHONE_MISSING), 14.4 lead idempotente (índice único por par + self-contact 400 CANNOT_CONTACT_SELF + race 23505→re-find), 14.5 `lead_origin_properties` ON CONFLICT DO NOTHING + **`contact_count` +1 solo si el origin fue nuevo** (cuenta contactos únicos, no taps), 14.6 mensaje prellenado (`Intl.NumberFormat es-MX`, `/mes` rent|both) + sanitización tel + respuesta `{success,phone,message,lead_id,property_id}`. **79 Deno tests**. No-críticas: 14.8 seed phone de agentes demo (migración `20260630000001`, idempotente); 14.9 cablear los 4 resolvers reales en `index.ts` + **deploy** (`--import-map`) + **smoke E2E** (401/400/404 + self-contact 400 OK — happy-path de creación de lead NO testeable E2E: solo existe el agente demo y sus propiedades son propias → cubierto por unit tests); 14.1 `ContactAgentButton` (reusa `PrimaryButton`, mapea errores ES, SOLO detalle no feed); 14.7 deep link `whatsapp://send` + fallback `wa.me` + `Alert` nativo. Decisiones del usuario: solo detalle · seed migración · Alert nativo (no toast lib) · validación manual. Guardian pescó 1 violación en 14.3 (index.ts no actualizado → TS2345) corregida en ciclo 1. Gotchas durables: `agent_id≡owner_user_id`, `increment_contact_count` read-then-write `// ponytail:` (RPC para prod), 2º camino WhatsApp sin CRM en AgentCard. Rama `tarea/14-whatsapp-lead`. Ingest: [[crm-leads]], [[mapa-codebase]].

## [2026-06-29] tarea | #27 Cablea feed/perfil → detalle + fix mapa EWKB — vivo
Descubierto al abrir el primer **dev build nuevo** (recompilado con los nativos de #9 vía `expo run:android`) para probar #10 en emulador: la pantalla de detalle estaba construida pero **inalcanzable** — `VideoFeedItem` no tenía `onPress` y `ProfileScreen` mostraba un `Alert "(próximamente)"` stub (sobró de #16). Además el **mapa nunca aparecía**. Fixes: (1) **navegación** — `PropertyOverlay` recibe `onPropertyPress` y hace tappable el bloque de info → `router.push(/property/[id])` (separado del doble-tap=like y del avatar); `ProfileScreen.handle_press_property` reemplaza el Alert por `router.push`. (2) **mapa** — PostgREST devuelve `location` (geography) como **EWKB hex** (`0101000020…`), no WKT texto, así que `parse_location` daba `null` y el mapa se ocultaba; extendí `parse_location` para **decodificar EWKB hex little-endian** (DataView+getFloat64) además de WKT (lng antes que lat sigue siendo crítico). **+5 tests (252 total), tsc 0.** **Verificado en vivo en emulador:** feed→detalle navega, mapa pinta el pin en Zapopan y Tijuana, video reproduce, renta muestra /mes. Gotcha durable en [[propiedades-y-video]]. Rama `tarea/10-property-detail` (commit `feat(27)`). Ingest: [[propiedades-y-video]], [[mapa-codebase]], [[feed-vertical-video]].

## [2026-06-29] tarea | #10 Pantalla de detalle de propiedad — vivo
Feature `mobile/src/features/property-detail/` + ruta `app/(protected)/property/[id].tsx` (7 subtareas, rama `tarea/10-property-detail`). **Tema híbrido (decisión usuario vía /tm-plan, ANULA suposición "todo oscuro" del analista): hero video oscuro full-bleed + contenido sobre paper claro**, fiel al mockup #5 de `urbea-identidad-visual.html`. **10.1** `usePropertyDetail` — query `properties` active+deleted_at null con embeds (FK hints `users!properties_owner_user_id_fkey`/`agencies!properties_agency_id_fkey`+`property_videos`) + **2ª query a `user_preferences`** (nombre/foto del agente — gotcha #16, no `users.avatar_url`) + **EF `mint-video-url` fail-soft** por el signed_url (reusa patrón feed #9; URL pública rota por path 2-seg #21). **10.2** `PropertyVideoPlayer` (hero `useVideoPlayer`+`VideoView` patrón VideoFeedItem, video position 1 + badge "N videos" sin carrusel, cleanup-safe). **10.3** ensamblaje + `PropertyInfoHeader` (precio-héroe MXN+tick salvia patrón `PropertyGridCard`, specs null-safe con hairlines). **10.4** dirección+descripción+`AmenityChips` (flags+amenities, oculta si vacío). **10.5** `PropertyMap` (`react-native-maps` patrón `MapPicker`) + **`parseLocation` WKT `POINT(lng lat)` lng primero, 13 tests** (invertir=bug silencioso). **10.6** `AgentCard`+WhatsApp (`open_whatsapp` → `Linking.openURL(wa.me)`, **sin lead CRM — // ponytail: llega en #11**) + CTA sticky. **10.7 (única CRÍTICA, TDD→guardian PASS):** `ActionButtons` like/save flotantes que **reusan `useLikeProperty`/`useSaveProperty` de #9 SIN tocarlos** (10 tests, EC-9 verifica save sin property_video_id = invariante schema 0006) + `DetailSkeleton`. **Decisiones usuario:** like+save sí, **Share OMITIDO** (sin `react-native-share`); multi-video = solo position 1. **Ponytail:** revertí cruft que un agente metió — `eslint`+`eslint-config-expo` sin configurar + placeholder roto en `pnpm-workspace.yaml` (el setup formal de lint es la tarea **#24**; el gate del repo es `tsc`). Verif: **suite 247/247, tsc 0**. ⚠️ pendiente: nuevo dev build (módulos nativos de #9) + probar en device/emulador. Ingest: [[propiedades-y-video]], [[feed-vertical-video]], [[design-system]], [[mapa-codebase]].

## [2026-06-29] tarea | #9 Feed vertical de video — TikTok-style, vivo
Feed inmersivo en `mobile/src/features/feed/` (10 subtareas, rama `tarea/9-feed-video`). **9.10 prereq**: stack nativo `@shopify/flash-list`+`react-native-reanimated` v4 (+`react-native-worklets`, gotcha pnpm: peerDep no hoisteado, instalado explícito)+`react-native-gesture-handler`+`expo-haptics`+`@expo/vector-icons`; babel reanimated plugin + `GestureHandlerRootView` raíz. **Arquitectura:** `FeedScreen` (FlashList v2 paginada, `drawDistance` para precarga — expo-video SDK56 **no** expone prefetch; cleanup auto al desmontar), `VideoFeedItem` (expo-video play/pause por viewability), `useFeedActiveIndex` (70% + AppState + `useFocusEffect` de **expo-router**), `PropertyOverlay` (mockup identidad: precio MXN, rail like/guardar glass, avatar). **2 críticas TDD→guardian PASS:** (1) `lib/feedProperties.ts` (15 tests) — query active+ready + EF **`mint-video-url`** (batch property_ids→`{videos:[…]}`) + **merge fail-closed**; guardian cazó hueco mock-vs-prod (lección #8): tomaba `property_videos[0]` sin reconciliar → ahora `.find(v.id===video_id)` + omite si no matchea (EC-14/15). (2) `useLikeProperty`/`useSaveProperty` (16 tests) — persistencia optimista real en `likes`(por video)/`saves`(por propiedad), rollback, conflicto único 23505, user de `useAuth`; `likeOnly` idempotente=doble-tap. **Decisiones cliente (vía /tm-plan):** instalar stack+rebuild (vs RN puro), persistir like/guardar de verdad, feed **simple** (sin radio progresivo/anti-clustering — diferibles por PRD). **Ponytail:** descartado un mini-renderer de hooks de 150 líneas que el GREEN agent metió en `jest.setup.js` (parcheaba React internals) — innecesario tras añadir `await renderHook` (RNTL v14 lo tipa async). Verif: **suite completa 224/224, tsc 0**. ⚠️ **pendiente: nuevo dev build** (módulos nativos) antes de probar en device. Ingest: [[feed-vertical-video]], [[mapa-codebase]].

## [2026-06-28] tarea | #21 EF mint-video-url — signed URLs de video con service_role
Resuelve el gotcha del path 2-seg que rompía la RLS SELECT pública (#8): el feed (#9) no puede leer videos de terceros. EF **`mint-video-url`** (minter PURO, dep de #9): POST `{property_ids:string[]}` (JWT, **auth=verify_jwt del gateway**, sin auth custom en el handler) → 200 `{videos:[{property_id,video_id,signed_url}]}`. `handler.ts` (DI: valida input→delega→mapea; **exclusión silenciosa**: property_ids sin video válido se omiten, **nunca 404**) + **`make_video_url_minter(client)`** en `_shared/clients.ts` = **única barrera de seguridad** (service_role bypassa RLS): query `property_videos⋈properties!inner` filtra `status='active' AND pv.status='ready' AND deleted_at IS NULL` → `storage.from('property-videos').createSignedUrl(path,3600)`. **Fail-closed** (query/storage error o storage_path null → excluye fila, nunca minta de más). 5 subtareas; críticas 21.2/21.3 con TDD RED→GREEN→**guardian PASS** (blindó los 3 filtros con asserts de args exactos + expiresIn===3600). 21.4 index.ts = glue (desviación TDD documentada: wiring no-testeable, lógica ya cubierta). Tests: 34 Deno (handler 20 + adapter 14), deno check/lint limpios. **Desplegado** al remoto urbea con verify_jwt ON. ⚠️ **deploy gotcha durable:** el bundler del API NO auto-lee `supabase/functions/deno.json` → `--import-map supabase/functions/deno.json` obligatorio (si no, falla el import bare `@supabase/supabase-js` de `clients.ts`; aplica a toda EF que use `clients.ts`). Pendiente del usuario: curl smoke test (necesita JWT real + property active con video ready sembrada). Rama `tarea/21-mint-video-url`. Ingest: [[propiedades-y-video]], [[feed-vertical-video]], [[mapa-codebase]].

## [2026-06-28] tarea | #26 Sistema de layout desde el prototipo Claude Design
- **#19 cancelada** (superada): nació de la exploración 001 (21-jun, identidad provisional URBEARG `#1F3B2D`+Fraunces), NO del HTML canónico. Reemplazada por el kit 003 (Salvia/Arcilla + Space Grotesk/Hanken); su contenido vivo ya está en el HTML + theme.ts + método orgánico desde #16.
- **Modelo dual-referencia** (CLAUDE.md §8): `urbea-identidad-visual.html` = techo del **lenguaje visual**; `Urbea Prototipo (standalone).html` (raíz, export Claude Design) = techo del **layout**. Del prototipo SOLO layout, nunca color/fuente.
- **Extracción medida en navegador** (Chrome MCP + `getComputedStyle`, servido por http local porque `file://` se rechaza): frame 390×844; inset de página 18→**20**; grid 2-col `170+14+170` (gutter **14**); el prototipo es off-grid (11/13/18/30) → se mapea la intención a base-4, no se copian px. Doc `.taskmaster/docs/exploraciones/026-layout-system-extraction.md`.
- **`theme.ts` (aditivo)**: `spacing.s_20`/`s_40` + `export const layout {screen_inset:20, grid_gutter:14, grid_cols:2}`. Gate `pnpm tsc --noEmit` limpio (lint N/A hasta #24). Anatomía por pantalla en [[layout-anatomy-screens]].

## [2026-06-28] tarea | #17 Mis publicaciones (gestión de propiedades del agente)
- Pantalla `app/(protected)/profile/my-listings.tsx` (Stack bajo profile, NO tab — mockup #9 tab activo=Perfil; entrada desde `ProfileScreen`). 8 subtareas en serie.
- **EF `update-property-status`** (crítica, TDD): patrón DI + `make_property_status_updater` extraído a módulo testeable (transiciones/ownership/closed_reason); 54 Deno tests. Guardian dio FAIL en ciclo 1 (lógica de dominio solo en `index.ts`, sin tests = trampa mock-vs-prod del #8) → se extrajo y se añadieron 10 tests sobre la lógica real; PASS verificado **por mutación**.
- **UI**: `useMyProperties` (todos los status + video_count + contadores) · `PropertyListItem` (badge/stats) · `FilterTabs` · `PropertyActionMenu` (Modal nativo, sin dep nueva).
- **Acciones** (crítica, TDD): `usePropertyActions` (22 tests) — guard cliente close-sin-reason NO invoca EF; soft-delete por RLS; diálogos Close (motivo obligatorio)/Delete. Guardian PASS.
- **Editar** (crítica, TDD): `useLoadProperty` pre-llena el wizard; `usePublish` ramifica create(EF)/edit(**UPDATE directo con RLS**, decisión cliente); video opcional en edit; 17 tests + guardias de regresión del create. Guardian PASS (create intacto).
- Verificación final: mobile **193/193**, EF deno **54/54**, `tsc` 0. Follow-up creado: cablear `isWorking` en my-listings (UX, low). Commits locales en `tarea/17-mis-publicaciones`.

## [2026-06-27] decision | referencia visual canónica = urbea-identidad-visual.html
- Cliente: usar `urbea-identidad-visual.html` (raíz) como guía de diseño + plan de la demo. Enshrined en CLAUDE.md §8, [[design-system]] (mapeo pantalla→tarea + divergencias #16 vs mockup #10 + firmas pendientes: isotipo U+play, iconos a medida) y memoria branding-working-method.
- Regla durable: cada pantalla del mockup = techo de alcance de su tarea (UI ausente = trabajo nuevo, no scope-creep).
- Gotcha grabado: `expo-linear-gradient`/`react-native-svg` no están en el dev build → crashean; PropertyGridCard pasó a placeholder sólido.

## [2026-06-17] fundacion | vault inicializado
- Estructura del vault creada (MOCs, conceptos, codebase, decisiones, estado).
- Sembrado desde `docs/` y `supabase/` (DB migrada 0001–0010).

## [2026-06-17] decision | alcance del primer hito = demo cerrada de 3 semanas
- Sesión de preguntas (7 rondas) con el cliente. Ver [[0005-demo-cerrada-3-semanas]].
- PRD destilado en `docs/PRD-MVP-demo.md`.
- Demo: inmobiliaria+agentes, código de invitación, email+contraseña, wizard 3 pasos, video real a Supabase Storage (sin transcoding), feed vertical, filtros básicos, mapa con clustering, CRM lista+estados, contacto WhatsApp+lead. Dev build (expo-dev-client). Backend = Supabase remoto. Sin pagos.

## [2026-06-17] decision | ADRs 0001–0004 aceptadas
- [[0001-alcance-mvp-recomendado]], [[0002-monetizacion-pago-por-video]], [[0003-vault-obsidian-como-memoria]], [[0004-taskmaster-motor-de-ejecucion]].

## [2026-06-17] ingest | vault densificado a síntesis densa
- Los 13 conceptos reescritos con modelo de datos, invariantes, flujos y reglas + punteros precisos a las fuentes.
- Añadido [[MOC-fuentes]] (catálogo de docs/ y supabase/) — la capa que dice "a dónde ir por el detalle".
- Decisión del nivel de profundidad: síntesis densa (Karpathy), no espejo de los docs.

## [2026-06-17] fundacion | Taskmaster inicializado + backlog generado
- `task-master init` en la raíz; provider **claude-code/sonnet** (sin API key, $0). Canal: **CLI** (no MCP).
- `parse-prd` sobre `docs/PRD-MVP-demo.md` → **20 tareas** (tag `master`).
- `analyze-complexity` → 6 alta / 11 media / 3 baja. Reporte en `.taskmaster/reports/task-complexity-report.json`.
- Next: **#1** init Expo + dev build. Branding (#19) en pausa por decisión del cliente.

## [2026-06-17] decision | CLAUDE.md + workflow de ejecución (ADR 0006)
- Creado `CLAUDE.md` (schema operativo): principio rector, inicio de sesión, stack, Taskmaster CLI, workflow de ejecución, cierre, mantenimiento del vault.
- **PNPM siempre** como gestor de paquetes y dev server (nunca npm/yarn).
- Workflow: Taskmaster como bitácora viva vía `update-subtask`; verificación con `pnpm`; ingest al vault al cerrar. Ver [[0006-workflow-ejecucion-tareas]].

## [2026-06-17] fundacion | tareas expandidas a subtareas (Opus)
- `task-master expand --all` con provider claude-code/**opus** → 20 tareas, **144 subtareas** (0 fallos, $0).
- Modelo devuelto a `sonnet` para la operación diaria.
- Backlog listo para ejecución fina con el workflow ([[0006-workflow-ejecucion-tareas]]). Next: **#1**.

## [2026-06-17] fundacion | workflow multi-agente construido (ADR 0007)
- `.claude/agents/`: analista-subtareas, mobile, supabase, design, test-author, guardian.
- `.claude/skills/`: urbea-context, urbea-expo, urbea-supabase, urbea-design, urbea-testing.
- `.claude/commands/`: tm-plan, tm-tarea (modo auto), tm-status. Hooks: tdd-guard.sh (pragmático), close-reminder.sh. settings.json con permisos + hooks.
- TDD pragmático por criticidad, serie con checkpoints, manejo de bloqueantes, convención de nombres snake_case. graphify contemplado a futuro. Ver [[0007-workflow-multiagente]].

## [2026-06-21] explore | aprobado tarea 19 — Branding + Design System de Urbea

## [2026-06-23] tarea | #1 init Expo + dev build — app móvil inicializada
- `mobile/` scaffold Expo SDK 56 (blank-typescript, standalone, `.npmrc` hoisted), Expo Router, TS strict.
- EAS configurado: `app.config.ts` (`com.urbea.app`, owner `deabratech`), `eas.json` (development/preview/production); proyecto `@deabratech/urbea` registrado (projectId 85c7157a-…).
- Deps nativas: expo-dev-client, expo-router, react-native-maps@1.27.2 (sin fricción en SDK 56), expo-video, screens, safe-area-context.
- Cliente Supabase tipado `src/lib/supabase/client.ts` (createClient<Database> + AsyncStorage); smoke test **200 OK** contra remoto `mvpvqmyhrrkwbnpctpuq`. Credenciales en `.env.local` (gitignored).
- Mapa-codebase actualizado (sección móvil → archivos reales). Rama `tarea/1-init-expo-mobile` (commits locales). Pendiente humano: primer `eas build` para ver la app en device.

## [2026-06-23] hito | #1 dev build instalado en device
- Primer `eas build` (development, Android) exitoso tras resolver cascada: `npx eas-cli` (no pnpm-global), `app.config.js` (no `.ts`), y `mobile/` self-contained (sin `pnpm-workspace.yaml` raíz que hoisteaba deps nativas fuera de EAS).
- `.apk` instalado; Urbea corriendo nativo en device vía `pnpm expo start --dev-client` (Supabase activo).
- Nuevo: `wiki/codebase/comandos.md` (referencia de comandos dev/EAS/verificación/Taskmaster).

## [2026-06-24] tarea | #2 Auth Supabase email/password en mobile
- BD verificada para la demo: tabla public.users + trigger handle_new_user + RLS + column-grants YA cubren el flujo (sin migración nueva). Hueco menor: last_login_at no se auto-actualiza (cosmético).
- AuthContext (src/features/auth/context.tsx): useAuth {session,user(perfil public.users),isLoading,signIn,signOut} + onAuthStateChange. Login solo (cuentas sembradas; signup/invitación → #3).
- Pantalla login (app/login.tsx) + validación pura (validation.ts) + mapeo de errores ES (auth-errors.ts) + guard de rutas (protected-layout.tsx → app/(protected)/) + AuthProvider en root.
- Persistencia AsyncStorage ya estaba en client.ts (tarea #1); añadido listener AppState para start/stopAutoRefresh.
- Harness de tests jest-expo + testing-library (nuevo). TDD en críticas (2.1, 2.4, 2.5) con guardian (3 mutantes verificados por subtarea). Total: 60 tests verdes, tsc strict limpio.
- Estructura feature-based confirmada (src/features/auth/). Rama tarea/2-auth-supabase (commits locales).

## [2026-06-24] tarea | #3 Supabase Storage — bucket property-videos + RLS
- Migración 0011 (`supabase/migrations/20260604000011_storage_property_videos.sql`, +rollback): bucket `property-videos` (privado, 100 MB, mp4/quicktime/webm), columna `property_videos.storage_path` (único parcial), y 2 políticas RLS en `storage.objects`.
- RLS INSERT (`property_videos_storage_insert`): agente/admin sube solo a su propio path (`foldername[1]=auth.uid()` + rol). Gate dueño+rol como `properties_insert`; no hay admin-anywhere.
- RLS SELECT (`property_videos_storage_select`): lectura pública si `private.property_is_public(foldername[2])` (helper SECURITY DEFINER 0010, sin JOIN inline → evita colisión RLS anon) o si eres el dueño.
- Convención de path: `{user_id}/{property_id}/{video_id}.mp4`. Ver [[propiedades-y-video]] §Storage.
- CORS: hallazgo — Supabase no tiene CORS por bucket (gateway permisivo; subidas nativas Expo/RN no aplican CORS). Nada que configurar para la demo.
- TDD contra remoto vía MCP (sin stack local): RED→GREEN en tx con rollback, `apply_migration` al final. Tests `supabase/tests/03_storage_test.sql` (pgTAP, 16 asserts). Guardian PASS en 3.1/3.2/3.3. Aplicada a `urbea-app` (migración 20260604000011 en historial). Rama `tarea/3-storage-videos`.
- ⚠️ Gotcha: `task-master update-task --id=3` (provider AI) **regeneró las subtareas** (pasaron de split por-feature a split por-fase). Trabajo idéntico y completo, pero los títulos quedaron reescritos. Usar `update-subtask` para notas, no `update-task`.

## [2026-06-24] tarea | #4 property_videos — CHECK ready_requires_storage (0012) + regen tipos
- **Reencuadre de alcance**: la tarea #4 se redactó antes de la #3; su meta principal (añadir col `storage_path`) **ya la hizo 0011** (tarea #3). Subtarea 4.1 cerrada como cubierta por 0011 (no se recreó la columna). Nombre de archivo del enunciado (`20260617000011_demo_video_storage.sql`) obsoleto.
- Migración **0012** (`supabase/migrations/20260604000012_property_videos_ready_requires_storage.sql`, +rollback): CHECK `property_videos_ready_requires_storage` = `status <> 'ready' OR storage_path IS NOT NULL OR cloudflare_uid IS NOT NULL`. Idempotente (drop if exists + add).
- **Decisión de diseño**: constraint **condicional al status** (no plana), espejando `property_closed_requires_reason`. Motivo: `status` default `'uploading'` y el `storage_path` (`{user_id}/{property_id}/{video_id}.mp4`) depende del `id` de la fila → la fila existe antes de tener referencia; una CHECK plana rompería el INSERT de subida y los fixtures pgTAP existentes (que tenían videos `ready` sin referencia → ajustados con `cloudflare_uid`/`storage_path`).
- TDD contra remoto vía MCP (sin stack local): RED (asserts 14-18 en `01_constraints_test.sql`, plan(13)→plan(18)) → GREEN (`apply_migration`) → probe focalizado 5/5 ok. Guardian PASS.
- ⚠️ Gotcha MCP: el archivo `01_constraints_test.sql` **completo no corre por `execute_sql`** — su assert #11 (pre-existente) usa un CTE data-modifying anidado que Postgres rechaza fuera de top-level. Para verificar por MCP se usa un probe focalizado (subset de asserts) recogiendo el TAP en una temp table (execute_sql solo devuelve el último result set).
- 4.3: regenerados tipos TS vía MCP (`property_videos.storage_path` ya aparece en Row/Insert/Update; antes faltaba), `pnpm tsc --noEmit` rc=0; `supabase/README.md` documenta 0011 y 0012 (demo, no billing). Rama `tarea/3-storage-videos`.
## [2026-06-24] tarea | #5 Canje de invitación + registro de agente
- Edge Functions (Deno, patrón DI handler.ts puro / index.ts entry): `validate-invitation` (POST {invitationCode}→200 {agency_name}) y `redeem-invitation` (orquesta validar token→auth.admin.createUser→RPC canje→200; compensa deleteUser si falla). `_shared/` con cors/response/crypto(sha256)/validation/invitation/auth_user/redeem/clients (clients.ts = único import de supabase-js). Import map en deno.json; correr deno DESDE supabase/functions/.
- Atomicidad: migración 0013 RPC `redeem_invitation_atomic` (SECURITY DEFINER): consumo token + agency_members + denorm users + 4 user_consents; errores P0001→HTTP. user_preferences NO (es onboarding de buyer).
- Mobile: `app/register.tsx` en 2 fases (valida código→muestra agencia→datos+canje→auto-login signInWithPassword) + `src/features/registration/` (validation, registration-errors, api). 17 tests Jest.
- **Bug sistémico hallado en despliegue real:** `service_role` sin grants DML en `public` (0008 solo otorgó a authenticated/anon) → supabase-js service_role recibía 403 de PostgREST. Las RPC SECURITY DEFINER no se veían afectadas (corren como postgres). Fix: **migración 0014_service_role_grants**. Documentado en [[rls-seguridad]].
- Otros 2 fixes de integración: filtro embebido PostgREST `.is("agencies.deleted_at",null)` descartaba la fila padre (→ validar en JS); `x-forwarded-for` lista CSV rompía el param `inet` (→ 1ª IP).
- Verificación: smoke E2E contra remoto (validate 200/404/400; redeem 200 con efectos atómicos + compensación verificados; datos limpiados). deno fmt/lint/test: 65 verdes. Advisors security sin WARN/ERROR nuevos. Migraciones 0011–0014 aplicadas en `urbea-app`.
- Pendiente (cliente): consolidar ramas #1→#2→#3→#5 a main.

## [2026-06-24] tarea | #6 onboarding del agente (nombre + foto) — vivo
- **Flujo completo (mobile):** `app/onboarding.tsx` → `OnboardingScreen` con nombre (validación `is_valid_full_name` ≥2 trim), `AvatarPicker` (`useImagePicker`: cámara/galería, permisos ES con manejo de denegación), compresión (`imageUtils.processProfileImage`: resize 512 + JPEG iterativo 0.8→0.6→0.4 hasta ≤1MB), guardado (`profileService.saveProfile`: upload a Storage + upsert `user_preferences`), "Saltar foto", progreso (`loading`), nav `router.replace('/(protected)')`, guard de re-show (full_name proxy).
- **Backend (migración 0015, aplicada a `urbea-app`):** bucket **público** `profile-photos` + RLS por path `(storage.foldername(name))[1] = auth.uid()::text` (INSERT/UPDATE/DELETE dueño, SELECT público) + cols `full_name`/`profile_photo_url` en `user_preferences`. TDD: pgTAP `04_profile_photos_test.sql` (17 asserts, verde en tx contra remoto). Guardian PASS en 6.3 y 6.5.
- **Componente de firma:** `PrimaryButton` liquid-glass salvia (BlurView + overlay salvia con **opacidad por superficie** light 0.82 / dark 0.48 — decisión del cliente; adapta el kit `003-kit` sin `backdrop-filter`). Variantes primary/ghost, reutilizable en CTAs de la app.
- **Decisiones:** fuentes del sistema (Fraunces/Hanken diferidas a #19 branding); liquid-glass real con `expo-blur` variando opacidad por superficie.
- **Deuda:** regenerar `database.types.ts` (post-0015) para quitar casts `as never`/`as {...}` en profileService y guard.
- Tests: 101/101 (mobile) + 17 pgTAP. tsc limpio. Migración 0015 en historial remoto. Rama `tarea/6-onboarding`.

## [2026-06-25] tarea | #7 Panel admin: alta de inmobiliarias + owner
Edge Function `admin-create-agency` (patrón DI handler/index) + RPC **`admin_create_agency_atomic`** (migración **0016**, una sola firma 9 params SECURITY DEFINER): verifica `role=admin` del caller (JWT→users.role, 403 si no), crea owner por **invitación** (`generateLink type=invite`, sin password → action_link), y atómicamente INSERT agency(active)+agency_member(owner)+UPDATE users(role=agent)+token **hasheado**+admin_actions. Compensación `deleteUser` si la RPC falla. Token inicial 8-char alfanumérico, plano retornado 1 vez. Panel mobile `app/admin/` (guard `role=admin` en `src/features/admin/admin-layout.tsx`, lista, form que invoca la función, detalle `[id].tsx` con token/invite-link copiables un-solo-uso + miembros). TDD en backend (7.4–7.6 críticas, guardian PASS; capturó y revirtió un `ON CONFLICT DO NOTHING` que silenciaba ALREADY_ACTIVE_MEMBER). +`expo-clipboard ~56.0.4`. Tests: deno **112/112**, pgTAP 05 plan(20) vs remoto `urbea-app`, mobile tsc 0 + admin 8/8. Decisiones (atomicidad RPC / owner por email-invite / token 8-char) confirmadas con el cliente. Ingest: [[inmobiliarias-y-agentes]], [[mapa-codebase]], [[db-schema-map]].

## [2026-06-26] tarea | #8 Wizard de publicación 3 pasos + EF publish-property
Wizard `mobile/app/(protected)/publish/` (estado compartido `PublishFormContext` — React Context, no Zustand): step1 (operación rent/sale/both + tipo casa/departamento/local/oficina/terreno), step2 (precio/recámaras/baños via `NumericStepper`/m²/descripción + `AddressAutocomplete` [Google **Places API New** REST+fetch, key `EXPO_PUBLIC_GOOGLE_PLACES_API_KEY`, degrada sin key] + `MapPicker` [react-native-maps, marker draggable, no escribe coords falsas] + toggles nicho + `validate_step2`), step3 (video via expo-image-picker → preview expo-video → upload → Publicar). Backend: Edge Function **`publish-property`** (handler DI + impls `CallerVerifier`[agent/admin, 401/403]+`PropertyPublisher`) + RPC **`publish_property_atomic`** (migración **20260625000001**, SECURITY DEFINER search_path fijo, INSERT properties(active)+property_videos(ready) en 1 tx atómica sin EXCEPTION, `location=ST_Point(lng,lat,4326)`). **Upload "Opción C"**: cliente genera `video_id` UUID y sube a `{user_id}/{video_id}.mp4` **antes** de la EF → la propiedad solo se crea si el upload tuvo éxito (sin huérfanos). TDD en críticas (8.9 EF, 8.8 useVideoUpload, 8.10 usePublish): guardian PASS en las 3 con mutation testing; en 8.8 validó que el patch de infra de RNTL `act.js` no enmascara fallos. Tests: deno **43/43** (EF) + pgTAP 06 plan(8) + mobile **24/24** (publish) + tsc 0. Decisiones (Opción C upload / Places REST / expo-image-picker / navegar al feed) confirmadas con el cliente. ⚠️ **Hallazgo en ingest**: el path de 2 segmentos rompe la **RLS SELECT pública** (`property_is_public(foldername[2]::uuid)` espera property_id) → terceros no leen el video; **diferido a tarea #21** (feed minta signed URLs vía EF con `service_role`, dep de #9). Rama `tarea/8-publish-wizard`. Ingest: [[propiedades-y-video]], [[mapa-codebase]].

## [2026-06-27] tarea | #16 Perfil de agente + design system sembrado
Primera pantalla con identidad Urbea tras **levantarse el gate de branding** (cliente 2026-06-26). 6 subtareas, ninguna crítica (pantalla de lectura). **#16.1** Tabs navigator `app/(protected)/(tabs)/` (Inicio+Perfil; admin/publish siguen Stack). **#16.2** `useAgentProfile` — ⚠️ corrección de fuente: la identidad del agente (nombre+foto) vive en `user_preferences` (la escribe el onboarding #6), NO en `users`; bio de `users.bio` (sin writer → null). **#16.3** **siembra `mobile/src/theme/theme.ts`** (tokens del kit 003: colors/radii/shadows/fonts/type_scale/spacing, gestión-claro, dual-mode pendiente #9) + carga fuentes (`@expo-google-fonts` space-grotesk+hanken via `useFonts` en root `_layout`) + `ProfileHeader`. ⚠️ **Tipografía canónica = Space Grotesk (kit), NO Fraunces (doc 003)** — confirmado cliente. **#16.4** `PropertiesGrid` (`FlatList numColumns=2`). **#16.5** `PropertyGridCard` (firma, global, lo reusa feed #9) — **preview HTML aprobado** (`003-kit/property-grid-card-preview.html`) → portado: thumbnail 4:5, precio héroe + tick Salvia, badges operación/Pausada, placeholder café. **#16.6** ensamblaje `ProfileScreen` compartido + botones (Editar=stub→**#22**, Logout=signOut) + `EmptyState`; `[id].tsx` movido a `(protected)`. Deps: `@expo-google-fonts/{space-grotesk,hanken-grotesk}`, `expo-font`, `expo-linear-gradient`. tsc 0 errores. Tareas nuevas: **#22** (editar perfil). Pendientes: detalle `/property/[id]` (stub), regenerar database.types post-0015. Rama `tarea/16-perfil-agente`. Ingest: [[perfil-agente]], [[design-system]], [[onboarding-y-preferencias]], [[mapa-codebase]].

## [2026-06-27] tarea | #22 Editar perfil de agente (avatar/nombre/bio)
Pantalla `app/(protected)/profile/edit.tsx` + hook `useEditProfile` (mobile, 5 subtareas, 22.3/22.5 críticas TDD). **Modelo bio = Opción A híbrido (decisión cliente, SIN migración):** `save({fullName,imageUri,bio,removePhoto})` hace **dual-write con manejo de error INDEPENDIENTE** — (A) `profileService.saveProfile` → foto Storage + UPSERT `user_preferences`; (B) `supabase.from('users').update({bio}).eq('id',userId)`. B se intenta aunque A falle; cualquier fallo expuesto; `save()` devuelve `{ok,error}` → Alert en fallo / `router.back()` en éxito. **Quitar foto:** `removePhoto=true` → `imageUri=null` → UPSERT `profile_photo_url=null` (profileService ya lo soportaba, 0 cambios). Pre-fill: 1 query a user_preferences (bio ya en memoria vía `useAuth().user` — ponytail). Validación nombre (reusa `is_valid_full_name`) + char counter bio `{n}/280`. **#22.5 anti-stale:** `useAgentProfile` migrado de `useEffect` a `useFocusEffect(useCallback(…,[agent_id]))` → re-fetch al recuperar foco. ⚠️ **Guardian capturó 3 bugs reales test-verde/prod-roto:** (1) `isSaving` no llegaba al botón (faltaba re-render al inicio del save), (2) `error` leído de snapshot obsoleto de closure en edit.tsx, (3) ref-guard `is_fetching` calibrado a un mock defectuoso que en prod tragaba el 1er re-fetch-on-focus (el escenario editar→volver) → mock corregido a semántica commit-once + guard eliminado. Los 3 corregidos, guardian PASS. Tests: `useEditProfile.test.tsx` 15 + `useAgentProfile.test.tsx` 6, suite **154/154**, tsc 0. Tareas nuevas: **#24** (configurar ESLint — el repo no tiene `pnpm lint` que el workflow asume). Rama `tarea/22-editar-perfil`. Ingest: [[perfil-agente]], [[mapa-codebase]].

## [2026-06-30] tarea | #11 Mapa global de propiedades (pines + clustering)
Pantalla canónica "6·MAPA" → feature `mobile/src/features/map/` + tab "Mapa" (`app/(protected)/(tabs)/map.tsx`). 7 subtareas, **2 críticas TDD** (11.2 datos, 11.3 clustering, ambas guardian PASS). **Grilling de diseño (4 decisiones del usuario, antes de codear):** (1) clustering **custom puro sin dependencia** — `lib/clusterMarkers.ts` `cluster_properties()` grid absoluto `cellSize=delta/divisions(8)`, 1→point/>1→cluster (id `cluster_<x>_<y>`, centroide media), guard `delta<=0`, 14 tests; (2) **centrado default Guadalajara SIN `expo-location`** — `GDL_REGION` en `constants.ts`, "centrar en mí" diferido (esto **eliminó el único bloqueante de rebuild nativo** que el analista marcó); (3) búsqueda = **filtro cliente** address/property_type (`MapSearchBar` liquid glass, sin geocoding); (4) marcadores custom alta fidelidad **+ liquid glass + neomorphism**. **11.2** `lib/mapProperties.ts`+`hooks/useMapProperties.ts`: query properties active+deleted_at null (+`location`, sin paginar) → **reusa `parse_location`** de property-detail (EWKB hex/WKT) → **fail-closed** omite location inválida (10 tests). **Marcadores** (`components/`): `PropertyMarker` (teardrop+isotipo play Ionicons+price tag compacto; **rent=salvia/sale|both=arcilla**; `tracksViewChanges={false}`), `ClusterMarker` (círculo `ink`+count, tono distinto), `PropertyMiniCard` (card inferior, **NO Callout nativo** por bug Android), `MapSearchBar` — ambos overlays con **BlurView** (liquid glass). `lib/formatPrice.ts` (compacto/full). **11.6** integra todo en `MapScreen` (region state, cluster memo, mini-card overlay, cluster tap→`animateToRegion` zoom-in, tap→`/property/[id]`). Sin nueva dependencia: `react-native-maps`/`expo-blur`/`expo-linear-gradient`/`@expo/vector-icons` ya instalados; `GOOGLE_MAPS_API_KEY` ya en `app.config.js`. Verif: jest map **24/24**, tsc 0. Deuda: filtros sliders reales + isotipo SVG (falta `react-native-svg`) + guard NaN-delta + "centrar en mi ubicación" = trabajo futuro. ⚠️ requiere dev build (módulo nativo react-native-maps) para smoke en device. Worktree `tarea/11-map` (base tarea/10). Ingest: [[mapa-y-ubicacion]], [[mapa-codebase]].

## [2026-07-01] componentes | #13.1 + #13.3 LikeButton + SaveButton (engagement)
Componentes compartidos de engagement: `mobile/src/components/LikeButton.tsx` (bounce 1→1.3→1.0, Salvia activo) y `SaveButton.tsx` (bounce 1→1.15→1.0, Arcilla activo). Presentacionales puros — estado en el padre via useLikeProperty/useSaveProperty. Integrados en `ActionButtons.tsx` (sin cambios a los 10 tests). `jest.setup.js` actualizado: mock manual de react-native-reanimated (v4.3.1 — reanimated/mock.js circular, requiere factory inline). Tests: 281/281 passed, tsc 0.

## [2026-07-02] tarea | #13 Like/Save + contadores reales + pantalla Guardados — vivo
7 subtareas, **3 críticas con TDD+guardian**. **Redimensionada por el análisis:** la persistencia de like/save (hooks `useLikeProperty`/`useSaveProperty`) y los botones ya existían de #9/#10 → 13.5 se cerró sin trabajo. **Decisión de arquitectura (usuario):** en vez de Edge Functions toggle-like/toggle-save (duplicarían el toggle direct-client sin ganar seguridad), 13.2/13.4 se convirtieron en **migración `20260701000001` con triggers atómicos + backfill** para `like_count`/`save_count` reales — desplegada a `urbea-app` (2026-07-01). Regla no negociable: funcs **SECURITY DEFINER** `search_path=''` (la RLS `properties_update` bloquearía el UPDATE del no-dueño; el pgTAP NO lo detecta porque corre como superusuario). TDD pgTAP `07_engagement_counts_test.sql` (14 asserts, RED 9/14→GREEN 14/14 en tx revertida). 13.1/13.3 botones animados (Reanimated) en `ActionButtons`. **13.6 pantalla Guardados** (tab 🔖, `Inicio·Guardados·Mapa·Perfil` per mockup p12): `useSavedProperties` (crítica, 11 tests) query `saves`+embed→`GridProperty`, reusa `PropertyGridCard` 2-col; **fix de spec: `saves` es DELETE duro, sin `deleted_at`**. 13.7 **quitar = long-press → Alert confirmar** (no swipe, no encaja en grilla; decisión usuario) reusando `useSaveProperty`, optimista `hidden_ids`+rollback. Suite final mobile 292/292, tsc 0. Gotcha de tooling detectado: `tdd-guard.sh` no permitía `*.test.tsx` (fix pendiente de decisión del usuario — bloqueado por el clasificador de auto-modo). Rama `tarea/13-like-save` (desde `origin/main`). Ingest: [[feed-vertical-video]], [[mapa-codebase]].

## [2026-07-02] tarea | #28 CRM: el owner de la inmobiliaria ve los leads de todos sus agentes — vivo
5 subtareas, **3 críticas con TDD+guardian PASS** (28.1 useAgencyRole, 28.2 useAgencyAgents, 28.3 useAgentLeads(agentId)) + 2 UI no críticas (28.4 AgentSelector, 28.5 wiring en CRMScreen). **Sin backend nuevo:** la RLS `leads_select` + `private.is_agency_owner_of` (migración 0010) ya permitían al owner ver leads ajenos; #28 es 100% cliente. **Correcciones al enunciado (verificadas en repo):** (a) nombre/foto del agente viven en `user_preferences.full_name/profile_photo_url` (cast `as never`), NO en `users`; (b) el rol de agencia se detecta con `agency_members.member_role`, NO con `users.role` (que es 'agent' incluso para owners); (c) semántica de "Todos los agentes" = **agregado RLS-driven** (`useAgentLeads(null)` sin filtro → owner ve toda la agencia), descartada la lectura "cae a user.id" del texto original. Firma `useAgentLeads(agentId?)` retrocompatible → los 10 tests previos intactos. Demo: `owner.oeste@urbea.demo`/`urbea2026` (2 agentes: Valentina, Miguel). Suite leads 40/40, tsc 0. Rama `tarea/28-agency-owner-leads` (desde `origin/main`). Ingest: [[crm-leads]], [[mapa-codebase]].

## [2026-07-02] fix | #28 CRM owner-view — 2 bugs encontrados al verificar en vivo (emulador)
Verificación en el emulador (owner.oeste@urbea.demo) reveló 2 bugs. (1) **Crash "Could not embed because more than one relationship was found for 'leads' and 'users'"** en `useAgentLeads`: `leads` tiene DOS FKs a `users` (agent_id/user_id) y el embed bare `users(...)` es ambiguo → se calificó con `users!leads_user_id_fkey(...)` (el buscador). Bug latente desde #15 que los mocks no atrapaban; +test de regresión (EC-embed). (2) **Selector mostraba "? Agente"** en vez de nombres: el owner NO puede leer el `user_preferences` de sus agentes (RLS `user_prefs_select`=fila propia) → `useAgencyAgents` reescrito para leer `users.first_name/last_name/avatar_url` (legible vía `is_agency_owner_of`, sin cast `as never`); RED→GREEN→guardian PASS, 8 tests. Ambos verificados recargando el emulador vía `adb screencap` (selector con nombres reales MH/VL + filtrado por agente OK). Suite leads 42/42, tsc 0. Bug pendiente: el nombre del **buscador** del lead ("Usuario sin nombre") requiere política RLS → **tarea #30** (add-task, high). Ingest: [[crm-leads]], [[mapa-codebase]].

## [2026-07-02] fix | #28 owner-view solo-lectura — la vista owner mostraba el selector de estado pero la EF rechazaba al owner (UNAUTHORIZED_AGENT → non-2xx); `LeadExpandedView` gana prop `readOnly` (lead ajeno = solo lectura). Tarea #31 abierta para que el owner gestione (EF+RLS).

## [2026-07-02] tarea | #30 RLS identidad del buscador — CRM ya muestra nombre/foto/tel del buscador (fin del "Usuario sin nombre")
Fix del bug heredado de #15/#14: el agente/owner no podía leer la fila `public.users` del **buscador** de su lead. **Vía RLS, no denormalización.** Helper `private.can_view_user_as_lead_searcher(user_id)` (migración `20260702000001`, SECURITY DEFINER `set search_path=public`, patrón `can_view_lead`): true si hay lead ACTIVO (`deleted_at is null`) con ese `user_id` como buscador y quien pregunta es el agente dueño (`agent_id=auth.uid()`) o el owner de su agencia (`is_agency_owner_of`). Se añade como cláusula OR a `users_select`; **`user_prefs_select` NO se toca**. **Decisión de diseño (usuario):** identidad del buscador SIEMPRE desde `users` (first_name/last_name/avatar_url/phone), consistente con #28 — se descartó extender `user_prefs_select` (Parte 3 del plan original quedó obsoleta). Cliente: `useAgentLeads` cambió el embed a `users!leads_user_id_fkey(...)` con `build_full_name` **extraída a `utils/full_name.ts`** (reuso; antes local en `useAgencyAgents`). TDD: pgTAP `08_rls_lead_searcher_test.sql` (plan 12, RED→GREEN→guardian PASS) + jest 26/26 + tsc 0. **Deploy remoto vía `apply_migration`** (MCP) — `supabase db push` NO aplica por drift benigno de versiones (todas las migraciones locales ya están en remoto bajo timestamps regenerados por apply_migration; ver [[mapa-codebase]]). Verificado bajo RLS por impersonación JWT (helper true para agente dueño, false para no-dueño) + advisors 0 alertas nuevas (helper en schema `private`). Limitación demo: el único lead activo remoto tiene buscador = cuenta agente verificado (visible de por sí), así que la demo visual "Usuario sin nombre"→nombre real necesita sembrar un lead con buscador `role='searcher'` (seed local, no-commit). Rama `tarea/30-rls-lead-searcher` (desde `origin/main`). Ingest: [[crm-leads]], [[rls-seguridad]], [[mapa-codebase]].

## [2026-07-02] tarea | #23 profstats row + IsotipoMark — completada la pantalla de perfil vs mockup #10
Los dos componentes de firma que #16 dejó fuera del mockup #10. **#23.1 `useAgentStats` (CRÍTICA TDD):** 3 counts en `Promise.all` (publicaciones/leads/cerrados) con degradación graceful a ceros. ⚠️ **El guardian atrapó un green-pero-roto:** el test RED codificaba el orden de cadena supabase-js ficticio `.eq().in().is().select()` y el SUT lo implementó con un cast que lo ocultaba — habría dado `TypeError` contra Supabase real y devuelto siempre ceros. Fix: test **y** SUT al orden real `.select().eq().in().is()` (patrón `usePropertiesGrid`), sin cast; guardian PASS al 2º pase. **#23.2 `ProfessionalStats`:** profstats sheet 3-col (tokens de identidad), `return null` si todo en 0. **#23.3 `IsotipoMark`:** **cambio de enfoque ponytail** — se descartó el PNG@2x/3x+`react-native-svg` planeados (sin convertidor SVG→PNG en la máquina; svg exigía rebuild nativo del dev build) por **primitivas RN puras** (View bucket-shape + triángulo por bordes), cero assets/deps nativas, teñible/escalable; techo: migrar a svg si ≥2 consumers piden fidelidad vectorial. Badge de isotipo en avatar (solo con foto). Rama `tarea/23-profstats-isotipo` (desde `origin/main`). Verificación: tsc 0, suite profile 53/53. Ingest: [[perfil-agente]], [[mapa-codebase]], [[design-system]].

## [2026-07-02] tarea | #24 ESLint configurado en mobile/ — pnpm lint ya es gate válido
El repo nunca se había lint-eado (solo `tsc`). #24 instala ESLint con **preset Expo mínimo** (decisión usuario: sin reglas custom, ponytail — gate ligero que complementa a tsc): `mobile/eslint.config.js` (flat CJS, `eslint-config-expo/flat`, `eslint@9.39.4`+`eslint-config-expo@56.0.4` alineado a SDK 56, ignores de artefactos), script `"lint":"eslint ."`. **24.2 dejó el árbol en exit 0**: baseline 17 errores/128 warnings → 0 errores/9 warnings (auto-fix aplicó 111 en ~55 archivos; arreglos reales de raíz para unused vars y un refactor `useRef().current`→`useMemo` en `useFeedActiveIndex.ts` para no leer ref en render; los 13 `set-state-in-effect` del patrón fetch-on-mount llevan `eslint-disable` puntual justificado por línea, no global — reescribir 13 hooks excede un lint cleanup). Warnings residuales aceptados: 8× `no-require-imports` deliberados + 1× `exhaustive-deps` a propósito. Verificación: `pnpm lint` exit 0, `pnpm tsc --noEmit` limpio, `pnpm test` 454/454 sin regresiones (incluye hooks/lib tocados por --fix). Gotcha instalación: `unrs-resolver` (nativo transitivo) requiere `pnpm approve-builds`→`pnpm-workspace.yaml`. Solo cubre `mobile/` (Deno usa `deno check`). Memoria `no-eslint-use-tsc` actualizada (antes decía "no hay lint"). Rama `tarea/24-eslint` (desde `origin/main`). Ingest: [[mapa-codebase]].

## [2026-07-04] explore | aprobado tareas 40,41,42 — Ubicación obligatoria (muro bloqueante) + feed/mapa por cercanía

## [2026-07-04] tarea | 20.5 E2E Maestro — suite completa 6/6 verde + 3 bugs reales corregidos
Suite Maestro en `mobile/.maestro/` (6 flujos §6 de la auditoría pre-#20) contra **Supabase local sembrado** (determinista): login, botonera, feed-interacción, registro, contacto→CRM, publicar. **Corrida integral 6/6 en 7m54s** vía `run-e2e.sh`. La E2E encontró **3 bugs reales**: (1) **`signIn` tragaba el error** de `signInWithPassword` (supabase-js v2 devuelve `{error}`, no lanza) → con password mala navegaba a `/` y el guard remontaba el form vacío sin mostrar error; fix TDD (test EC-6b) + throw. (2) **FAB de publicar visible para buscadores y encimado al botón Guardar** del rail → gate `role==='agent'` (overlap para agentes → tarea #39). (3) **Onboarding inalcanzable** — nadie enrutaba a `/onboarding`; register.tsx ahora hace `replace('/onboarding')` tras el auto-login. Infra: maestro CLI (formula `mobile-dev-inc/tap/maestro`, NO el cask `maestro`), seed extendido (token `DEMO2026` sha256, phones de agentes — la migración de phones corre ANTES del seed —, `created_at` escalonado para feed determinista), testIDs (`login-*`, `register-*`, `map-picker`). Gates: tsc 0 · lint 0 err · jest **479/479** · suites Deno intactas. Gotchas Maestro durables en [[mapa-codebase]] §E2E.

## [2026-07-04] explore | aprobado tarea 43 — Iconografía Phosphor (bold) + logo login (re-skin) + icono de app (revierte "sin react-native-svg" de #32)

## [2026-07-05] feat | #43 iconografía Phosphor + logo final (icono de app + login)
Migró TODA la iconografía de `@expo/vector-icons` (Ionicons) a **`phosphor-react-native`** bold (fill en estados activos), 15 archivos + tab bar. Instalados `react-native-svg` + `phosphor-react-native` (rebuild nativo del dev-client) — **revierte la premisa "sin react-native-svg" de #32/#11** (cliente aprobó). Logo final canónico = **`urbea-logo-final.html`** (símbolo nuevo U/pin+flecha↑+Ü; verde #1A5E44 + carnita #EEE4D0; wordmark "URBEA" en Outfit): `IsotipoMark` migrado a SVG con esa geometría; nuevo `UrbeaLockup` (row/column); login re-skineado (hero vertical carnita + botón verde, subtítulo Outfit); icono de app regenerado (verde + símbolo carnita, iOS+adaptive Android) — assets SVG→PNG vía **`qlmanage`** de macOS (resuelve el bloqueo de #32). `theme.ts`: +`brand` tokens + `fonts.logo`=Outfit (ADITIVO, verde/carnita solo en icono+login). Extra: FAB de publicar (+) habilitado también para admin + sombra. Verificado: tsc/lint/jest 479 + smoke on-device (feed Phosphor + login), 2 gates de marca aprobados por cliente. → [[design-system]]

## [2026-07-06] tarea | #40 RPC PostGIS `properties_within_radius` — cercanía backend (Fase A) + desplegada a urbea-app
Primera tarea del épico de ubicación por cercanía (exploración 027, 40→41→42). RPC **`properties_within_radius(p_lat,p_lng,p_radius_m float8)`** (`supabase/migrations/20260706000001_...` + rollback + pgTAP `09`, 8 asserts): enfoque **A1 "flaco"** — devuelve SOLO `{id, distance_m}` de props `active`+`deleted_at null` dentro del radio, ordenadas por distancia asc (`ST_DWithin`+`ST_Distance`); el feed/mapa (#42) resolverán el resto con `build_filter_query` intacto (cero drift). `SECURITY DEFINER` + `search_path=public,extensions` (PostGIS en `extensions`, patrón `publish_property_atomic`); filtros base en SQL (2ª capa); reusa GiST `properties_location_gix` (0005). **TDD estricto RED→GREEN→guardian PASS** (RED y GREEN en la MISMA subtarea para no romper el `tdd-guard` que matchea subtarea exacta). **Deploy a urbea-app** vía `apply_migration` (MCP, no db push): verificado — `properties_within_radius(20.6736,-103.344,5000)` → 8 filas dentro de 5km ordenadas asc, 13 active/8 en 5km/12 en 50km. **Seguridad:** el advisor 0028 destapó anon-executable por el EXECUTE default de PUBLIC → `revoke execute from public,anon` + grant a `authenticated` (defense-in-depth; todo el feed/mapa detrás del auth wall). Cabos capturados: **#45** (drift del entorno pgTAP local: 01/02/04/06 fallan por razones ajenas) y hallazgo (fuera de #40) de que los SECURITY DEFINER previos del repo comparten el advisor anon-executable. Rama `tarea/40-properties-within-radius-rpc` (commits locales: test(red), feat(green), chore revoke). Ingest: [[mapa-y-ubicacion]], [[mapa-codebase]].

## [2026-07-06] tarea | #41 Permiso de ubicación obligatorio + LocationProvider (Fase B) — muro bloqueante, validado en device + E2E 6/6
Fase B del épico de ubicación (exploración 027). **Gate de ubicación bloqueante** tras el auth gate en `mobile/app/(protected)/_layout.tsx`, uniforme para todos los roles. Feature nueva `mobile/src/features/location/`: **`lib/permissionDecision.ts`** (pura, CRÍTICA TDD 4 EC, guardian PASS), **`LocationProvider.tsx`/`useLocation()`** (3 estados `permission_denied`/`gps_off`/`granted` + `loading`, `Accuracy.Balanced`, coord cacheada por sesión; crítica de facto, 12 tests, guardian PASS), **`LocationWall.tsx`** (muro funcional 2 variantes, tokens `theme.ts`+`PrimaryButton`, NO dispara gate branding #19). Config: `expo-location@56.0.19` (⚠️ `pnpm dlx expo install`, NO `pnpm add`→57.x incompatible SDK56) + plugin en `app.config.js` → **nuevo dev build**. **2 bugs cazados en smoke on-device (screenshots de los 3 muros + feed):** (1) `getCurrentPositionAsync` LANZA "Current location is unavailable" en emulador sin fix → rechazo sin capturar → fix try/catch→`gps_off` (EC-8); (2) el estado no se re-evaluaba al volver de Ajustes → **listener `AppState 'active'`→`refresh()`** (EC-9, reportado por el usuario). **E2E Maestro:** el gate rompería las 6 flows → `helpers/launch.yaml` con `permissions.location: allow` + `setLocation` GDL → **6/6 PASSED** contra stack local. Gotchas de sesión: `run-e2e.sh` NO arranca Edge Functions (prereq manual, doc [[entornos-y-cuentas]]: `supabase functions serve --import-map ...`) — sin ellas registro/publicar/contacto dan "error inesperado"; el emulador lanzado con `nohup&` en background muere al limpiar el harness. Cuentas: `buscador1@urbea.demo`/`urbea2026` válida en ambos entornos (la doc lo confirma). Rama `tarea/41-ubicacion-permiso-provider`. Fase C (#42): feed/mapa por cercanía. Ingest: [[mapa-y-ubicacion]], [[mapa-codebase]].

## [2026-07-06] tarea | #20.13+20.14 Registro accesible + branding espejo del login + prueba integral multi-usuario en REMOTO
Trabajo de demo-readiness bajo #20, decidido con el usuario (espejo del login · probar en remoto · buscadores sembrados). **#20.13 (UI):** (1) `mobile/app/login.tsx` — enlace "¿Tienes un código de invitación? Regístrate" → `/register` (antes la pantalla de código de invitación era **inalcanzable** desde el login; el propio `registro.yaml` lo documentaba entrando por deep link). (2) `mobile/app/register.tsx` revestido **espejo del login**: fondo `brand.carnita`, header `UrbeaLockup size=95` + subtítulo `fonts.logo/green_deep`, botones `brand.green`+texto `brand.carnita`, `agency_banner` `colors.primary_tint/brand.green`, error/toggle/login_link a tokens (antes grises hardcodeados con comentario "en pausa hasta #19" — ya cancelada). Sin tocar `features/registration` (lógica ya testeada). `registro.yaml` E2E mejorado: entra por el enlace real "Regístrate" + `scrollUntilVisible "Crear cuenta"` (el header alto empuja el CTA bajo el fold en fase 2). tsc+lint verdes; smoke on-device confirma el espejo. **#20.14 (prueba integral en remoto `urbea-app`):** Maestro contra remoto (Metro→remoto; `helpers/launch.yaml` con `setLocation` mock = forma FIABLE de pasar el location gate, a diferencia del `adb emu geo fix` que es flaky). **6/7 flows verdes:** registro (crea agente real `e2e.agente@urbea.demo` end-to-end), login, botonera, feed, publicar. **contacto-crm destapó un hallazgo real (divergencia local↔remoto):** el botón "Contactar por WhatsApp" solo se renderiza si `has_phone` (`AgentCard.tsx`), y **7/9 agentes demo tenían `phone=NULL` en remoto** (la migración `20260630000001_seed_demo_agent_phone` corrió ANTES de sembrar esos agentes el 03-jul; es un UPDATE de una vez) → contacto roto demo-wide. **Fix de datos en remoto** (vía `execute_sql` MCP, aprobado): backfill `phone='+523312345678'` a agentes sin teléfono (8 tocados) → contacto verificado (lead **Fernando Castillo Núñez → Diego Ramírez Castro** creado en remoto). ⚠️ backfill de `full_name` NO surte efecto: `user_prefs_select` RLS bloquea lectura cross-user → el detalle muestra "Agente" genérico para TODOS (pre-existente, igual en local; exponer el nombre = tarea nueva). **Gap sistémico:** el alta por invitación no captura teléfono → cada agente nuevo queda sin botón de contacto (tarea nueva: capturar teléfono en onboarding). Ingest: [[mapa-codebase]], memoria [[demo_account_credentials]].

## [2026-07-07] fix | Demo-readiness fuera de protocolo (rush): tab bar simétrica + muro de ubicación guiado + causa raíz "los cambios no se ven" + re-seed mínimo del remoto
Sesión de arreglos rápidos para sacar la demo (PR #16, rama `tarea/fix-tabbar-muro-eas`, pendiente merge del usuario). **(1) Tab bar** (`(tabs)/_layout.tsx`): el bug era que los no-owner (buscadores) tenían **4 slots** (`Feed·Mapa·[+]·Perfil`) → el FAB `[+]` quedaba descentrado. Fix: el 4º slot **comparte** Leads (agentes) / **Guardados** (no-agentes) vía `is_agent = user?.role==='agent'` (Guardados sale de la barra para agentes con `href:null`, Leads para no-agentes) → **5 slots simétricos, `[+]` centrado en todos los roles**. Verificado en vivo. **(2) LocationWall** (`features/location/LocationWall.tsx`): reescrito para GUIAR (antes solo abría Ajustes sin decir qué hacer) — 3 modos: `ask` (dispara diálogo SO + hint), `settings` (pasos numerados Permisos›Ubicación›Permitir›volver), `gps_off` (Android enciende inline con `enableNetworkProviderAsync`, iOS pasos); botón secundario "Ya la activé — Reintentar" (`refresh()`); ícono + feedback. Provider crítico intacto (16 tests verdes). **(3) `eas.json`**: cada perfil mapea a su `environment` (preview→preview) para tomar env vars EXPO_PUBLIC_*/Google desde EAS. Gates: tsc 0 · lint 0 · jest **495/495**. **(4) 🔑 Causa raíz "los cambios no se ven"** (memoria [[dev_client_vs_release_apk]]): el emulador tenía un **APK release** (JS embebido, jamás conecta a Metro) → Fast Refresh inútil; además firma incompatible con el dev-client (`INSTALL_FAILED_UPDATE_INCOMPATIBLE`). Fix: `adb uninstall` + `expo run:android` (dev-client debuggable) → Fast Refresh real. **(5) Remoto vaciado (autorizado) y re-sembrado mínimo** (memoria [[demo_account_credentials]]): 3 cuentas `owner@`/`agente@`/`buscador@urbea.demo` (`urbea2026`), SIN propiedades/videos, + teardown reversible `supabase/scripts/teardown-demo-minimal.sql`. **Pendiente deploy:** merge PR #16 → crear 4 EAS env vars del entorno `preview` → `eas build --profile preview` (Android) + iOS TestFlight → teardown antes de entregar vacío.

## [2026-07-07] lanzamiento | Build de feedback (Android+iOS) con owners reales + gap analysis PRD + estrategia de releases
Decisión del usuario: lanzar la versión actual **tal cual** (con atajos de demo) a Android (1 usuario) + iOS/TestFlight (1 usuario) para recoger feedback, con solo cuentas reales y 0 videos. **(1) Cuentas plantadas (remoto `urbea-app`, reversible):** `supabase/scripts/seed-clientes.sql` + `teardown-clientes.sql` (local-only) → **4 cuentas, 1 inmobiliaria "Tu Casa con Vlad", 0 propiedades/videos**: `s.ramos2308@gmail.com` + `vladimiryeh@gmail.com` (ambos **owner** de la misma agencia), `swacg08@gmail.com` ascendido a **admin**, `admin@urbea.demo` conservado. Borró el seed demo previo (`owner@/agente@/buscador@urbea.demo` + "Inmobiliaria Demo"). Password `urbea2026`; **login por password grant verificado HTTP 200** para ambos owners (patrón [[auth_user_sql_seed_gotcha]], 8 tokens en `''`). Memoria [[demo_account_credentials]] reescrita. **(2) Deploy:** EAS ya autenticado (`deabratech`) y las 4 env vars EXPO_PUBLIC_*/Google **ya existían** en `preview` y `production` → **Android `preview` APK disparado** (keystore reusado, mismo signing que el preview anterior). iOS TestFlight = pasos interactivos del usuario (login Apple). **(3) Documentos:** Artifact "Urbea — De la demo al PRD" (gap analysis 13 áreas + estrategia updates) + páginas durables [[brechas-demo-vs-prd]] y [[estrategia-releases]]. Hallazgo de proceso: **falta el ADR** que formaliza "construir la versión del PRD" (último es `0007`) → crear `0008-arquitectura-real-prd.md`. Regla clave: OTA (`eas update`) para todo lo JS/UI, rebuild solo para cambios nativos; migración demo→beta→final = **expand-contract** (aditiva, no rewrite). Ingest: [[brechas-demo-vs-prd]], [[estrategia-releases]], [[mapa-codebase]].

## [2026-07-07] explore | aprobado tareas 48,49 — Fixes de demo: teléfono de owner sembrado + crash por límite de video (500 MB)
Dos pendientes de feedback de demo (ver [[demo_account_credentials]] del build 2026-07-07), corridos por `/tm-explore` → doc `.taskmaster/docs/exploraciones/029-fixes-demo-telefono-owner-y-limite-video.md` (aprobado). **P1 (owner "Tu Casa con Vlad" sin teléfono real):** `seed-clientes.sql`/migración `20260630000001` ya setean un placeholder `+523312345678` a Ramos y Vlad; el cliente confirmó números REALES distintos por owner (Ramos `+523315637152`, Vlad `+523335785799`). Fix = **solo dato** (`public.users.phone` nullable, sin constraint/trigger, NO toca `auth.users.phone` — no hay phone-auth), migración idempotente (`IS DISTINCT FROM`) + rollback, sin build. **P2 (crash OOM al subir video >100 MB):** causa raíz `useVideoUpload.ts:125` — `fetch(uri).arrayBuffer()` carga el video COMPLETO en memoria **sin try/catch** → excepción no capturada → red screen en `step3.tsx`. Decisión del cliente (intake): **a+b+c** — (a) subir `storage.buckets.file_size_limit` de 100→**500 MB** (`524288000`, alinea con `docs/PRD.md:627`), (b) validación pre-upload con `expo-file-system` (`validate_video_size`, misma constante que el bucket), (c) `try/catch` robusto con mensaje legible ES. Paths críticos (`hooks/**`, `**/validation*`) → TDD estricto; requiere dev build/EAS update. **Promovidos como 2 tareas separadas** (footprint/criticidad/build distintos, no comparten archivos): **#48** (P1, XS, sin build, TDD ligero) y **#49** (P2, S, con build, TDD crítico). **Análisis de complejidad corrido** (`.taskmaster/reports/exploracion-029-complexity.json`, vía codebase real — leídos `useVideoUpload.ts`, `validation.ts`, migración `20260604000011_storage_property_videos.sql`, `03_storage_test.sql`, mapa-codebase): **#48 = complejidad 2/10** (3 subtareas: migración → rollback → deploy+smoke); **#49 = complejidad 6/10** (6 subtareas: migración bucket+pgTAP → `validate_video_size` RED/GREEN → pre-upload check RED/GREEN → try/catch RED/GREEN → deploy → smoke device). **Ninguna de las dos tareas se implementó en esta sesión** — quedan `pending` en Taskmaster, listas para `/tm-plan` → `/tm-tarea`. Ver [[propiedades-y-video]], [[crm-leads]], [[mapa-codebase]].

## [2026-07-08] lanzamiento | iOS a TestFlight completado + 2 fixes de infra de build (export compliance, pnpm v11) + fixes UI de notch/wizard
Cierre del build de feedback iOS + Android con la **última versión** (todos los fixes en `main`). Tres PRs squasheados a `main` esta sesión.

- **(1) Fixes UI del build (PR #16, commit final del squash `a8d320e` — no narrados en la entrada previa de #16):** (a) `StepIndicator` del wizard de Publicar **centrado** con un `wizard_spacer` (View de `width:40`, gemelo del `BackButton`) en `WizardHeader` de `mobile/app/(protected)/publish/_layout.tsx` — el offset venía del layout del header, no del componente. (b) **Notch/Dynamic Island ya no tapa la foto** en Editar perfil / Mis publicaciones / Invitar agentes: `headerShown:true` + `headerBackButtonDisplayMode:'minimal'` en el `Stack.Screen` (el Stack de `(protected)` trae `headerShown:false` por defecto → sin header el safe-area no se respetaba). (c) `(protected)/_layout` pasó de `<Slot/>` a `<Stack>` con `gestureEnabled`+`fullScreenGestureEnabled` → restaura swipe-back iOS y botón físico atrás Android app-wide; `BackButton` compartido nuevo (Phosphor `CaretLeft`). (d) `.gitignore`: `supabase/scripts/seed-*.sql` y `teardown-*.sql` local-only (memoria [[seed_demo_not_committed]]). Verificado en vivo en simulador iOS.
- **(2) Export compliance iOS (PR #17, `13f6d1d`):** `ios.infoPlist.ITSAppUsesNonExemptEncryption: false` en `app.config.js` — Urbea solo usa HTTPS estándar (Supabase, Google APIs) → cifrado **exento**. Declararlo **salta el cuestionario de "export compliance" en cada build de TestFlight** (si no, Apple lo pide manualmente por build).
- **(3) 🔑 Fix de autolinking bajo pnpm v11 (PR #18, `308a250`) — el bloqueante grande del build iOS:** el build fallaba con *"Cannot find 'expo-modules-autolinking' package in your project"*. Causa raíz: **pnpm v10+/11 dejó de leer sus ajustes desde `.npmrc`** (breaking change) → el `node-linker=hoisted` del `.npmrc` quedó **muerto** (`pnpm config get node-linker` = `undefined`), node_modules se instalaba `isolated` (symlinks .pnpm, ~42 entradas top-level) y ni Metro ni `eas-cli` resolvían deps transitivas desde la raíz. Fix: mover el ajuste a **`mobile/pnpm-workspace.yaml` → `nodeLinker: hoisted`** (nueva ubicación oficial) + reinstalar en árbol nuevo (699 entradas flat). ⚠️ Gotcha: cambiar node-linker **requiere borrar/mover node_modules** (`pnpm install --force` NO relinkea un árbol existente); `rm -rf` está bloqueado por el harness → se usó `mv`. Verificado con `npx expo config --type introspect` (exit 0, autolinking resuelve). `.npmrc` quedó comentado.
- **(4) iOS TestFlight COMPLETADO (ruta interactiva simple):** tras varios intentos con la ruta de **App Store Connect API Key** (`.p8`, registrada en EAS pero abandonada), el login interactivo de Apple **empezó a funcionar al cambiar el usuario su contraseña de Apple ID** (la anterior daba estado de sesión inválido pese a ser correcta). Flujo final: `cd mobile && npx -y eas-cli@latest build --profile production --platform ios` (login Apple + 2FA → generar Distribution Certificate + Provisioning Profile) → `eas submit --profile production --platform ios` → **binario subido a App Store Connect, App ID `6788608353`** (https://appstoreconnect.apple.com/apps/6788608353/testflight/ios). Pendiente del usuario: esperar el procesado de Apple (~5-10 min) + agregar tester interno en TestFlight. **Android**: APK `preview` fresco desde `main` (todos los fixes).
- ⚠️ **Reglas durables aprendidas:** (a) **correr SIEMPRE los comandos EAS desde `mobile/`** — desde la raíz se crea un proyecto EAS falso (pasó: `e062a691`, bundle `com.goatdev08.urbea`; limpiado). (b) Iconos verificados idénticos en ambas plataformas (isotipo crema sobre verde `#1A5E44`). Ingest: [[estrategia-releases]], [[mapa-codebase]].

## [2026-07-08] tarea | #50 Revertir default de rol en registro libre a 'user' (buscador) — TDD estricto + deploy remoto + limpieza de 1 cuenta
Reversión de la regresión del flash de demo: la migración `20260707000001_signup_default_agent` (PR #15) había forzado `handle_new_user` a insertar `role='agent'` para **TODO** registro, rompiendo el modelo de negocio (registro libre debe crear **buscadores**; el alta de agente es SOLO por invitación). **TDD estricto (RED→GREEN→guardian PASS), 50.1+50.2 en la MISMA sesión de agente** (el `tdd-guard` matchea la subtarea exacta del sentinel `.current-red` y habilita el GREEN solo si el último commit es `test(red): <sentinel>` → se mantuvo el sentinel en `50.1` durante ambos; memoria [[tdd_guard_split_red_green_siblings]]). **RED** (`supabase/tests/10_signup_default_role_test.sql`, pgTAP plan 4): (a) `signup_default_role_is_user` — insert directo en `auth.users` dispara el trigger → `public.users.role` debe ser `'user'` (FALLABA con `have: agent`); (b) `invitation_flow_still_creates_agent` — no-regresión: `redeem_invitation_atomic` sigue subiendo a `role='agent'` + `agency_id NOT NULL` (3 asserts). **GREEN** (`supabase/migrations/20260708000001_signup_default_buscador.sql` + rollback): `CREATE OR REPLACE handle_new_user` **sin listar la columna `role`** en el INSERT → delega al default `'user'` de la columna (`0002`). Idempotente; timestamp ANTES de #47.6 (captura de teléfono, pendiente). Verif local: `10_*` 4/4 verde, idempotencia (`db reset`) y rollback OK; `pg_get_functiondef` ya no contiene `'agent'`. **50.3 OPS remoto `urbea-app`** (autorizado por el usuario tras detección read-only, patrón [[supabase_remote_deploy_apply_migration]]): `apply_migration` (NO db push) del trigger revertido + **limpieza de datos**: la detección (`role='agent' AND agency_id IS NULL AND created_at>='2026-07-06'`) encontró **1 sola cuenta** (`an.landeros.ro@gmail.com`, registro libre real de hoy vuelto agente) → `UPDATE ... SET role='user'` con guarda `id + agency_id IS NULL` (service_role vía MCP); los 2 agentes legítimos (Ramos/Vlad, con `agency_id`) intactos por el filtro. Verif post: trigger no inserta agent, 0 afectadas restantes, 2 legítimos, cuenta corregida=user. **50.4** (no crítica): copy de `mobile/app/register.tsx` YA correcto (modo `user` no promete publicar; L267/362-365/511-514), `tsc` 0 + `lint` 0; smoke E2E SKIP (sin emulador) → verificación manual post-merge documentada. **Infra de sesión:** Docker Desktop corría pero el symlink `/usr/local/bin/docker` apuntaba al DMG desmontado (`/Volumes/Docker/...`) → `docker` no resolvía; fix = symlink en `/opt/homebrew/bin/docker` al binario real de `/Applications/Docker.app/...`. Ingest: [[roles-y-permisos]], [[mapa-codebase]]. Depende de esto #47 (captura de teléfono solo en flujo de agente).

## [2026-07-09] tarea | #48 Teléfonos reales de owners demo (P1 de exploración 029) — fix de dato, sin build + #47 deferida
Fix de feedback de demo: los owners de "Tu Casa con Vlad" (Ramos `s.ramos2308@gmail.com` id `…a1`, Vlad `vladimiryeh@gmail.com` id `…a2`) fueron sembrados por `seed-clientes.sql` y quedaron con el placeholder `+523312345678` (de `20260630000001`); el CTA "Contactar por WhatsApp" del detalle abría un número que no recibe mensajes. **Migración de dato** (LIGHT, no crítica: `supabase/migrations/**` pero UPDATE puro sin lógica/RLS/constraints → criticidad determinista por path resuelta a ligera, alineado con el test-strategy de la tarea y la exploración 029). `supabase/migrations/20260709000001_set_demo_owner_phones.sql` = dos `UPDATE public.users SET phone=<real> WHERE id=<owner> AND phone IS DISTINCT FROM <real>` (Ramos `+523315637152`, Vlad `+523335785799`); **solo `public.users.phone`, NO `auth.users.phone`** (login es email/password, no hay phone-auth → el UPDATE no dispara flujos GoTrue). `IS DISTINCT FROM` = idempotente y cubre transición desde NULL/placeholder. Rollback en `rollbacks/` revierte a placeholder con match exacto id+phone (no pisa cambios de usuario). Timestamp `0709` para quedar DESPUÉS de la de #50 (`0708`). **Verif local** (txn con rollback; fixtures vía `insert auth.users`→trigger `handle_new_user`): forward `UPDATE 1+1`, 2ª forward `UPDATE 0+0` (idempotente), rollback `UPDATE 1+1`. **Deploy remoto `urbea-app`** (`mvpvqmyhrrkwbnpctpuq`) vía `apply_migration` (NO db push, [[supabase_remote_deploy_apply_migration]]), autorizado por Abraham tras **detección read-only** (blast radius = 2 filas placeholder, 0 con números reales, `auth.users.phone` sin setear). **Post-deploy**: ramos=`+523315637152`, vlad=`+523335785799`, filas con números reales=2, colaterales=0, `auth.users.phone`=0. Pendiente: **smoke manual** en device (login Ramos/Vlad → CTA abre número real) — sin build, es dato. **#47** (captura de teléfono en registro/onboarding) marcada **`deferred`**: es la "Tarea C" que la exploración 029 dejó fuera de alcance por decisión de Abraham (2026-07-07); su parte secundaria (nombre "Agente" genérico) ya vive en **#51**. Ingest: [[crm-leads]], [[mapa-codebase]].

## [2026-07-09] tarea | #49 Fix crash del wizard al subir video grande + límite de bucket a 500 MB — TDD estricto 5 subtareas, deploy remoto PENDIENTE
Bug de demo: el wizard de publicación (step3) crasheaba (red screen, unhandled rejection) al subir videos grandes — `useVideoUpload.ts` cargaba todo el binario en RAM vía `fetch().arrayBuffer()` sin guardas, y Hermes daba OOM en archivos >100 MB. Además el bucket tenía `file_size_limit=100 MB`, por debajo del spec del PRD (500 MB). **5 subtareas, TODAS críticas (path rule: `migrations/**`, `**/validation*`, `hooks/**`), TDD estricto RED→GREEN→guardian PASS en cada una.** Orden ejecutado: 49.1→49.5→49.2→49.3→49.4. **49.1+49.5** (migración+pgTAP, un solo ciclo): `supabase/migrations/20260710000001_increase_video_size_limit.sql` = `UPDATE storage.buckets SET file_size_limit=524288000 WHERE id='property-videos' AND file_size_limit=104857600` (idempotente por WHERE guard) + rollback (mismo nombre en `migrations/rollbacks/`, guard inverso); RED = assert #3 de `supabase/tests/03_storage_test.sql` a `524288000::bigint` (falla have 104857600). Verif local (stack docker): `03_storage_test.sql` 16/16, idempotencia `UPDATE 0` en 2ª corrida, rollback `UPDATE 1`→100MB y forward de vuelta. **49.2** `mobile/src/features/publish/validation.ts`: `MAX_VIDEO_SIZE_BYTES=524288000` + `validate_video_size(size_bytes)` puro (rechaza >500MB con mensaje ES que incluye size_mb y 500 MB, redondeo `Math.round(bytes/1024²)`), 5 tests nuevos. **49.3** `hooks/useVideoUpload.ts`: `getInfoAsync` (expo-file-system, patrón `imageUtils.ts`) mide el archivo tras marcar 'uploading' y ANTES de getSession → `return` temprano si `!exists` o `>500MB` (previene el OOM antes de asignar el ArrayBuffer); tests mockean `expo-file-system` (EC-15/16/17). **49.4** mismo hook: `try/catch` alrededor de `fetch().arrayBuffer()`+`storage.upload`+`update`; catch fija `status='error'` + mensaje amigable fijo `'Error al subir el video. Intenta de nuevo con un archivo más pequeño.'` + `console.warn(err)` (EC-18 OOM, EC-19 red). Verif final: **mobile 505/505 tests, tsc 0, lint 0 errores**. Deuda `ponytail:` en el hook: migrar a `createSignedUploadUrl`+`FileSystem.uploadAsync` (streaming) para no cargar en RAM. ⚠️ **PENDIENTE deploy remoto**: el `apply_migration` a `urbea-app` (`mvpvqmyhrrkwbnpctpuq`) lo **bloqueó el clasificador de auto-mode** (deploy a producción no autorizado explícitamente) — remoto sigue en 104857600; requiere autorización de Abraham (patrón [[supabase_remote_deploy_apply_migration]]). Sin deploy, el smoke (video 100-500 MB) fallará porque el bucket aún rechaza >100 MB. Ingest: [[propiedades-y-video]], [[mapa-codebase]].

## [2026-07-09] tarea | #52 Subida de video por STREAMING (fix de raíz del OOM) + fixes de imágenes + RLS SELECT solo dueño + OTA a clientes
Cierra de raíz el OOM de #49 y su deuda `ponytail:`. **6 subtareas, 4 críticas con TDD estricto (guardian PASS en todas).** **52.1** `useVideoUpload.ts` reescrito a streaming: `new File(uri).exists/.size` (guard 500 MB antes de la red) → `createSignedUploadUrl` → `file.createUploadTask(signedUrl, {PUT, BINARY_CONTENT, Content-Type video/mp4, onProgress}).uploadAsync()` — progreso real (cap 0.99), sin binario en el heap JS; 21 tests. **52.4** mismo patrón en imágenes: `imageUtils.ts` (⚠️ `getInfoAsync` del entry principal de expo-file-system v56 LANZA en runtime — foto de perfil estaba ROTA en prod), `profileService.ts` (upsert:true) y `videoThumbnail.ts`; 31 tests nuevos, suite 520/520. **52.5** migración `20260710000002_fix_property_videos_select_rls.sql`: policy SELECT de property-videos SOLO dueño — la rama pública `[2]::uuid` era dead code (path real = 2 segmentos) Y un hueco (path fabricado de 3 seg daba lectura a cualquiera); lectura pública = solo `mint-video-url`; pgTAP `11` (7 asserts) + `03` ajustado; **deploy remoto PENDIENTE de autorización** (clasificador auto-mode). **52.6** micro-fix UX: status 413 → mensaje específico de límite del servidor. **52.2 smoke**: Android E2E Maestro (`publicar.yaml`, video 43 MB/52 s generado con ffmpeg) → subida+publicación+thumbnail OK, **0 OOM en logcat**; diagnóstico decisivo con curl: **el límite GLOBAL del proyecto (50 MB, plan Free) capea al bucket de 500 MB** → 413 con >50 MB; contrato del signed URL verificado (PUT+Content-Type basta). **OTA publicado a clientes** (Android `preview` 019f499c-08ce…, iOS `production` 019f499c-d1d7…, runtime 1.0.1, receta export manual + `--skip-bundler`). **Decisión de rumbo (Abraham): migrar video a Cloudflare Stream** ($5/mes, sin límite práctico de tamaño) — próxima sesión `/tm-explore` XL "camino demo → PRD" (PRD-beta.md + ADR 0008). Flow `publicar.yaml` actualizado (perfil rediseñado: publicaciones inline, sin "Mis publicaciones"). Regla nueva en CLAUDE.md §3: testing en emulador SIEMPRE por CLI (adb/simctl/Maestro), NUNCA computer-use. Ingest: [[propiedades-y-video]], [[mapa-codebase]], [[onboarding-y-preferencias]].

## [2026-07-09] explore | aprobado tarea 53 — Fix: editar publicación duplica la propiedad (edit_mode por contexto)

## [2026-07-09] explore | aprobado tarea 54 — Fix: filtros accesibles en el empty state del feed

## [2026-07-09] explore | aprobado tarea 55 — Fix: eliminar publicación no refresca grid/feed/guardados

## [2026-07-09] explore | aprobado tarea 56 — Feature: "Buscar en esta zona" (viewport → feed + marcadores, depende de #42)

## [2026-07-09] tarea | #53 Fix: editar publicación duplica la property — edit_mode/property_id ahora viajan por PublishFormContext (no URL param); step3 lee del contexto; picker de video oculto en edición; reset al salir. Sin migraciones. TDD 5 EC-CTX-* + guardian PASS (67/67 publish).

## [2026-07-09] tarea | #54 Fix: filtros accesibles en el feed vacío — FeedScreen refactorizado a un solo return; botón filtros + FilterSheet hoisted (visibles en empty state, ocultos en skeleton/error); empty state ramificado por active_filter_count (filtered-empty "Limpiar filtros"→clear_filters vs BD-vacía). 100% UI, sin backend. No crítica (tsc+lint verdes).

## [2026-07-09] tooling+deploy | OTA automatizado (`pnpm ota`) + release #53/#54 a testers — `mobile/scripts/ota.sh` publica a ambos canales (production/iOS + preview/Android) sorteando el gotcha de eas-update+pnpm (export manual + --skip-bundler). OTA publicado con #53 (edición no duplica) + #54 (filtros en feed vacío): update groups android 27ffd90a / ios a3e13aa3, RV 1.0.1. Registrado en CLAUDE.md §3, comandos.md y estrategia-releases.md.

## [2026-07-10] tarea | #55 Fix: eliminar publicación no refresca la UI (grid perfil, feed, guardados) hasta reiniciar — approach B (decisión usuario "hacer bien desde el inicio", supera el approach A useFocusEffect del intake): mini pub/sub sin deps `mobile/src/lib/propertyEvents.ts` (`emitPropertyDeleted`/`onPropertyDeleted→unsubscribe`); `usePropertyActions.deleteProperty` emite en la rama de éxito; los 3 hooks (`useFeedProperties`/`usePropertiesGrid`/`useSavedProperties`) se suscriben y hacen **remoción optimista** del id (`prev.filter`) sin refetch → feed no reinicia video ni pierde scroll; no montadas cargan limpias por `deleted_at`/RLS (sin useFocusEffect). 2 subtareas CRÍTICAS TDD→guardian PASS (55.1 pub/sub+emisión EC-1..EC-6/EC-23/EC-24; 55.2 suscripción 3 hooks EC-1..EC-4 c/u). jest verde, tsc/lint limpios. Ingest: [[propiedades-y-video]] + mapa-codebase. Base extensible (emitPropertyUpdated futuro).

## [2026-07-10] tarea | #42 Feed/mapa por cercanía (Fase C, cierra el épico 40→41→42) — approach A1 cableado E2E: RPC `properties_within_radius` (coords por DI desde `useLocation()`, `p_radius_m=filters.radius_m`) + expansión ×2 máx 3 reintentos + `build_filter_query` INTACTO sobre `.in('id', ids)`; feed re-paginado a **offset sobre los ids de la RPC** (decisión usuario) + re-sort por `distance_map`; mapa sin paginar/re-ordenar; `radius_m` en `FilterState` (default 5 km) + `RadiusSelector` (5/10/20/50 km) en FilterSheet; MapScreen se recentra al llegar coords tardías. 3 subtareas críticas TDD+guardian PASS (candados A1: radius_m jamás al builder), suite 563/563, tsc/lint verdes. Punto azul se mantiene (pendiente confirmar con cliente). Habilita #56. Ver [[mapa-y-ubicacion]], [[busqueda-y-filtros]].

## [2026-07-10] tarea | #57 Fix Android OOM en feed (crash de testers) — causa: heap Java 192MB + ~4 ExoPlayers con buffer sin tope bufereando video crudo (okhttp/okio = víctima, no causa). Fix ponytail: `bufferOptions {preferredForwardBufferDuration: 10, maxBufferBytes: 25MB}` en los 3 `useVideoPlayer` (VideoFeedItem, PropertyVideoPlayer, step3) + `drawDistance` del feed a 1 pantalla. Gotcha: default Android real = 20s (no ~50s). Stress test emulador: 25 swipes, Java Heap plano 107–117MB (<150 objetivo), cero OOM post-fix (sí había pre-fix en logcat). PR #26 squash a main + OTA android (canal preview, RV 1.0.1, grupo 9361e340). 57.5 (largeHeap) CANCELADA: el fix OTA basta; reabrir solo si reaparece en devices con poca RAM. Fondo: Cloudflare Stream/HLS en beta. Ver [[feed-vertical-video]].

## [2026-07-10] tarea | #58 UX filtro de radio: slider continuo 0–50 km + "Sin límite" (radius_m nullable) — `FilterState.radius_m` evoluciona a `number | null` con **default `null` = sin filtro de proximidad** (decisión de producto: el radio pasa a refinamiento opt-in); con null el feed/mapa **saltan la RPC** `properties_within_radius` y van por query plana PostgREST (base + `build_filter_query`, feed con paginación OFFSET `.range()`, sin distance_m ni re-sort) — el gotcha `?? DEFAULT_RADIUS_M` (que trataba null como ausente) quedó guardado con candados EC-FEED-NULL-1..5/EC-MAP-NULL-1..3; numérico = path #42 intacto. `RadiusSelector` reescrito: pills → **slider continuo custom con PanResponder** (core RN, cero deps nuevas, OTA-safe; se descartó @react-native-community/slider por módulo nativo y gesture-handler/reanimated por el motor de worklets no mockeable en jest) + toggle "Sin límite", step 1 km, clamp 0–50 km, a11y adjustable/increment/decrement (contrato testeable; el drag comparte el mismo helper clamp/step). Storage migra solo (merge `{...EMPTY_FILTERS,...parsed}` probado con EC-STORAGE-1..7, sin cambio de código). FilterSheet: "Radio de búsqueda" ahora PRIMER filtro; badge sigue sin contar radius_m (`// ponytail:`). 4 subtareas críticas TDD+guardian PASS, 596/596 tests, tsc/lint verdes, smoke emulador adb 6/6. #56 sigue pending y rebasea sobre esto (usa campo `area` separado, nunca produce null). Ver [[busqueda-y-filtros]].

## [2026-07-10] deploy | OTA #58 a AMBOS canales — iOS se empareja (traía 2 features de rezago) — auditoría de canales: Android `preview` estaba al día (el OTA de #57 hace 2 h arrastró #42), pero iOS `production` llevaba ~13 h sin OTA (último = #55) y le faltaban #42 (cercanía + radio v1) y #57 (anti-OOM). Fix: PR #27 squash de #58 a main (`a5e58b7`) + `pnpm ota "…" all` → iOS group `f8698bd8` / Android group `2db0c1c4`, RV 1.0.1, commit a5e58b7. iOS recibe #42+#57+#58 de golpe; Android recibe #58. Lección: los OTA de una sola plataforma (como el android-only de #57) desincronizan los canales — default a `all` salvo razón explícita.

## [2026-07-10] tarea | #61 Fix Android "shared object already released" al togglear Sin límite (#58) — causa raíz: `useVideoPlayer` recrea el player cuando cambia la fuente (`useReleasingSharedObject`) y con FlashList reciclando ítems + URLs re-firmadas en cada refetch, el `VideoView` nativo alcanzaba a recibir el player viejo liberado (carrera intermitente, LogBox en dev). Fix ponytail en `VideoFeedItem`: player **estable por instancia** (nace una vez con la fuente inicial) + `player.replaceAsync()` al cambiar fuente + reset `has_error`/`is_paused` + resume si activo — solo JS, OTA-safe (expo-video 56.1.4 ya es la última de SDK 56). Verificación: tsc/lint 0 errores, jest 596/596, smoke emulador 3 ciclos de toggle + 5 swipes de scroll reciclado, 0 errores en logcat. Gotcha operativo: `grep` sobre dumps de logcat necesita `LC_ALL=C` + `-a` (bytes inválidos UTF-8 lo rompen en silencio). Ver [[feed-vertical-video]].

## [2026-07-10] tarea | #62 Carga contextual SIEMPRE: "Sin límite" conserva el orden por cercanía — feedback de producto sobre la decisión #58.3 (null saltaba la RPC → el feed ordenaba por created_at y aparecía Cabo San Lucas antes que Zapopan). Contrato nuevo: null = quitar el TOPE de distancia, cercanía intacta — el feed llama `properties_within_radius` con `UNLIMITED_RADIUS_M` (21,000 km > media circunferencia, cubre el planeta) sin expansión ×2 (vacío con radio infinito = no hay props); numérico = path #42 intacto; el path plano `.range()` quedó eliminado del feed (el MAPA lo conserva: pins sin orden). 100% JS/OTA-safe (la RPC no capea el radio: ST_DWithin puro). TDD: EC-NULL-RPC-1..7 reescriben feedProperties.radius-null.test.ts + EC-F12 actualizado; 598/598 tests, tsc/lint verdes. Smoke emulador: con Sin límite el 1er video ahora es Zapopan (La Toscana) y Cabo queda al final; con 5 km mismo orden inicial; 0 errores logcat. Misma rama/PR que #61 (ambos parchan #58). Ver [[busqueda-y-filtros]].

## [2026-07-10] deploy | OTA #61+#62 a ambos canales (hotfix sobre #58) — PR #28 squash a main (`5e187c0`) + `pnpm ota "fix: video estable al cambiar filtros y orden por cercanía con radio sin límite" all` → iOS `production` group `d86664c7` / Android `preview` group `22be7630`, RV 1.0.1, commit 63e5f79. Plan de reversa acordado con el usuario: si algo falla en device, se re-publica OTA desde el commit anterior (`a5e58b7`, estado #58) — `git checkout a5e58b7 && pnpm ota "revert" all` o `eas update:republish` del grupo anterior.

## [2026-07-10] tarea | #56 "Buscar en esta zona" (viewport del mapa → feed + marcadores acotados) — patrón mapa→feed estilo Airbnb (exploración 030, G1: reusar RPC `properties_within_radius`, SIN migración nueva). 5 subtareas serie, TDD estricto en las 3 críticas + verificación ligera en las 2 UI. **Restricción dura del usuario: modo zona PURAMENTE ADITIVO — no alterar #42/#58/#62.** Cumplida y verificada por guardian (con `area==null`, `is_unlimited` y las ramas del mapa son bit-idénticas al original; las 11 suites existentes de feed/map pasaron SIN tocarse). **56.1** `FilterState.area:{center,radius_m}|null` + `map/lib/viewportToArea.ts` (Haversine diagonal/2, clamp 100–50000, pura; area fuera de build_filter_query y sin contar en el badge, EC-32/33/34). **56.2** `filterStorage` excluye `area` (efímera; `save` destructura fuera, `load` fuerza null tras merge; EC-AREA-1..4 + EC-S2/S6 actualizados al contrato area-excluida — `radius_m`/#58 intacto). **56.3** rama zona aditiva en `feedProperties.ts` + `mapProperties.ts` (override center/radio, sin expansión, zona gana sobre radius_m; EC-Z1..Z7/EC-ZM1..ZM5). **56.4** `AreaSearchPill.tsx` (pill, debounce 500ms → `viewport_to_area` → `set_filter('area')` → tab feed; mini-spec §8, tokens theme.ts). **56.5** `ZoneActiveChip.tsx` (feed+mapa, "Zona activa · Quitar") + empty state de zona como primera rama del `is_empty` de `FeedScreen`. Suite mobile **624/624** verde, tsc/lint limpios. Commits locales por fase RED/GREEN; PR a main pendiente (decisión humana). ⚠️ Smoke en device (7 pasos del test-strategy) pendiente: requiere dev build (react-native-maps). Nota de higiene: un doc `.taskmaster/docs/exploraciones/034-…mattpocock.md` (stray de otra sesión) se coló por `git add -A` y se desbundleó del commit de 56.3; queda untracked + en `.git/info/exclude`. Ver [[mapa-y-ubicacion]], [[busqueda-y-filtros]].

## [2026-07-10] deploy | OTA #56 a ambos canales — "buscar en esta zona" (viewport del mapa acota feed y marcadores)  → PR #29 squash a main (`1152283`) + `pnpm ota "..." all` → Android `preview` group `318b2de3` / iOS `production` group `b065d223`, RV 1.0.1, commit `1152283`. ⚠️ Smoke manual en device (7 pasos del test-strategy) sigue pendiente — la suite automatizada (624/624) y el guardian ya verificaron la lógica.

## [2026-07-10] explore | aprobado tarea 63 — Enriquecer criterio de agentes TDD (guardian/test-author/mobile/supabase) con vocabulario traducido de mattpocock/skills

## [2026-07-11] tarea | #59 Fix UX feed: esperar coords reales antes de la 1ª carga (elimina el flash de orden GDL) — reporte de testers post-#57: al entrar al feed se veía un instante el orden centrado en el fallback GDL (RPC con `deps=undefined` mientras `coords` era null) y luego saltaba al orden por cercanía al llegar la coord real. **Fix (crítico, TDD+guardian PASS):** guard `_can_load = coords !== null` + early-return en `load_initial` de `useFeedProperties.ts` (dep `_can_load`) → `isLoading` se queda en true (skeleton) hasta que llega la coord; entonces `load_initial` cambia de identidad y el `useEffect` de FeedScreen re-dispara la carga UNA vez con coords reales (una sola RPC, sin fallback GDL). El fallback GDL de `feedProperties.ts` se conserva como red de seguridad (AC-3). **Timeout GPS (#59.6, decisión usuario, crítica-por-desempate):** `LocationProvider.fetch_current_coords` ahora usa `Promise.race([getCurrentPositionAsync, timeout(GPS_TIMEOUT_MS=10s)])` con `clearTimeout` en finally → si el GPS cuelga sin fix (emulador), cae a `gps_off` (LocationWall con retry) en vez de dejar el skeleton colgado indefinido. Tests: 5 `gate-EC-1..5` en `useFeedProperties.test.tsx` (default de `useLocation` a granted en beforeEach para no romper los tests de borrado #55.2) + `EC-10` en `LocationProvider.test.tsx` (fake timers + `advanceTimersByTimeAsync`). Suite mobile **630/630**, tsc/lint verdes. **Mapa (#59.5): sin cambio** — `MapScreen` ya recentra suave (`animateToRegion` una vez vía `coords_used_ref`) y los markers son espaciales (sin orden), no hay salto equivalente; aplicar el gate lo empeoraría (mapa en blanco). ⚠️ Smoke manual del feed (cold start → skeleton sin flash) **diferido** (#59.4): el dev-client no estaba instalado; riesgo de integración bajo (el wiring de FeedScreen no cambió, ya refiraba en #42). Ver [[feed-vertical-video]], [[mapa-y-ubicacion]].

## [2026-07-11] tarea | #63 Enriquecer agentes TDD con vocabulario mattpocock — criterio, no enforcement

Injerto quirúrgico traducido al stack en los 4 agentes de ejecución (+31/−12 líneas): `guardian.md` gana sección de calidad del test (3 anti-patrones como observación NO bloqueante; PASS/FAIL intacto); `test-author.md` acuerda y anota `SEAMS` (contrato Edge Function / RLS vía impersonación JWT) en la subtarea antes de enumerar + anti-tautología + mocks solo en fronteras; `mobile.md`/`supabase.md` verifican DURANTE el GREEN (un test a la vez en los seams), auto-check de conformidad Spec/PRD antes de reportar (campo `Conformidad spec` — cierra el hueco de las no críticas) y checklist condicional de 4 smells curados. Trade-off resuelto: el "vertical slicing" literal chocaba con el RED-batch que el guardian exige → RED sigue completo, "un test a la vez" vive en el GREEN. Enforcement (`tdd-guard.sh`, sentinel) sin tocar. Detalle: [[0007-workflow-multiagente]] + exploración 034.

## [2026-07-11] tarea | #64 Fix marcador de propiedad duplicado con el pin rojo default de Android — reporte del usuario. Causa raíz: `tracksViewChanges={false}` fijo desde el 1er render de `<Marker>` en `PropertyMarker.tsx`/`ClusterMarker.tsx`; el contenido custom es SVG (`MapPinIcon` → phosphor-react-native → react-native-svg), que no pinta síncrono en el primer frame en Android — el snapshot nativo del marker se congela ANTES de que el SVG termine de pintar y Google Maps deja su pin rojo de respaldo visible, duplicado con el pin temático. Descartadas causas de datos (revisadas por código, sin cambios): la RPC `properties_within_radius` es un SELECT puro sin joins (no duplica ids) y `cluster_properties()` asigna cada propiedad a exactamente una celda del grid. **Fix** (no crítico, `components/**`): `tracksViewChanges` arranca en `true` (dejar que RN tome el snapshot ya pintado) y se congela a `false` a los 300ms vía `useState`+`useEffect`+`setTimeout`, mismo patrón en ambos componentes, sin deps nuevas, 100% JS/OTA-safe. Verificación: tsc/lint 0 errores, suite map 58/58 sin regresión, smoke emulador real en 3 escenarios (mount inicial, zoom-in con 3 pins nuevos simultáneos, unmount/remount del tab) — pines limpios, 0 errores logcat en los tres. Gotcha operativo: el Metro que llevaba corriendo desde una sesión anterior (~40h) quedó atascado (spinner infinito, sin logs `ReactNativeJS`) — se reinició limpio (bundling 4.5s) y se reconectó vía deep link `urbea://expo-development-client` + `adb reverse`. Ver [[mapa-y-ubicacion]].

## [2026-07-11] explore | aprobado tarea 65 — Tab bar glass flotante (pill liquid glass)

## [2026-07-11] tarea | #65 Tab bar glass — NativeTabs iOS (liquid glass real) + GlassTabBar pill Android — la tabbar anclada se reemplazó tras 5 rondas de iteración visual con el dueño. iOS: NativeTabs de expo-router 56 (UITabBar del sistema, liquid glass/lupa nativos; íconos Phosphor rasterizados a PNG default/selected — gotcha qlmanage aplana alpha, se reconstruye con PIL; [+]=trigger disabled+tabPress→wizard). Android: GlassTabBar pill flotante custom (BlurView dimezisBlurView — sin esa prop NO desenfoca, causa de la franja opaca; lupa deslizante Reanimated spring denso; GlassView jamás con tintColor sólido → colorScheme). Tokens glass en theme.ts + floating_content_clearance Platform.select (iOS: insets.bottom YA incluye barra nativa — sumarle el token Android duplicaba el despeje, bug real cazado en vivo). expo-glass-effect@56.0.4 PINEADO (latest=57 incompatible SDK56) = módulo nativo → version 1.0.2 + eas build, no OTA. Divergencia consciente del mockup registrada. PR #33 squash-merged. 630/630 tests.

## [2026-07-12] decisión+plan | ADR 0008 + PRD-beta.md + árbol de épicas del camino a producción (71-84)
Sesión de dirección estratégica del **camino completo demo→beta→PRD**. Creado **`wiki/decisiones/0008-arquitectura-real-prd.md`** (reorienta el norte del proyecto de "demo" a "versión real"; fija: base reutilizable/migración aditiva, storage híbrido Stream+R2, corte de beta, pagos como capstone, secuencia por olas, admin web repo aparte + push Expo). Creado **`docs/PRD-beta.md`** (backbone maestro: acota `PRD.md` a la beta, decisiones transversales, mapa módulo→ola→tarea; el detalle por módulo se rellena SECUENCIAL por olas). **Decisiones de Abraham (2026-07-12):** (1) beta = **casi-final sin cobrar** (Olas 0–3, todo primordial visible/usable); (2) **pagos = ÚLTIMO módulo**, con **Stripe test-mode** (flip-ready a cobro real MX cambiando llaves test→live), pero el **modelo de datos de pagos se siembra temprano (Ola 1)** con slot gratis por flag → toda la lógica de vigencia/expiración cableada desde el inicio; (3) método = **secuencial por olas** con aprobación entre cada una. **14 épicas creadas (71-84)** por escritura directa en tasks.json (add-task roto): Ola 1 núcleo (71 roles, 72 auth, 73 publicación, 74 feed/mapa/búsqueda, 75 CRM, 76 modelo pagos), Ola 2 engagement (77 notificaciones, 78 comentarios/follow/share, 79 reportes/moderación), Ola 3 datos&admin (80 métricas, 81 admin web, 82 landing), transversal (83 observabilidad+deep links), Ola 4 capstone (84 Stripe test-mode, dep 76). Dependencias validadas. Siguiente: bajar a spec detallado **ola por ola** (empezando por consolidar Ola 0 / arrancar Ola 1). Ver [[0008-arquitectura-real-prd]], [[brechas-demo-vs-prd]].
Sesión de planeación estratégica (sin código de app). **036 — Camino a producción** (`.taskmaster/docs/exploraciones/036-camino-a-produccion.md`): decisión de **storage híbrido Cloudflare — Stream (video del feed, HLS adaptativo → resuelve cap 50 MB + OOM #57 + thumbnails) + R2 (assets/originales, egress $0)**, precisando el "solo Stream" del 2026-07-09; el schema ya está listo (`cloudflare_uid`+`storage_path`, CHECK 0012 acepta cualquiera) → migración aditiva reusando el patrón EF `mint-video-url`. Decisiones de Abraham: pagos **Stripe** (inactivos por flag en beta), admin web **repo aparte**, push **Expo Notifications**, beta **pública limitada**, video **greenfield**, archivar original en R2. **037 — graphify** (`037-integracion-graphify-workflow.md`): graphify YA instalado (`~/.local/bin/graphify`), umbral de tamaño cumplido (321 archivos, ~38.7k LOC) → integrar YA; grafo **versionado con merge-driver**, install **completo** (CLAUDE.md + hooks anti-grep, replica AJN), complementa el mapa manual, fase 2 (worktrees paralelos) diferida a tarea L futura. **Promoción "olas listas + graphify":** creadas tareas **66** (épica A: PRD-beta + ADR 0008), **67** (G: runtimeVersion→fingerprint), **68** (B: Cloudflare Stream, dep 66), **69** (C: Cloudflare R2, dep 66), **70** (integrar graphify). Épicas D (push), E (admin web), F (pagos-Stripe) quedan documentadas en 036 como pendientes, sin tarea aún. ⚠️ Gotcha: `task-master add-task` TRUENA en este entorno (diagnóstico definitivo del 2026-07-12) — **NO es hooks** (descartado por aislamiento). El provider `claude-code` (`ai-sdk-provider-claude-code@2.3.0`) rompe **solo en `generateObject`** (JSON estructurado: add-task/expand/parse-prd) → duplica `tool_use` ids → API 400 → exit 1. Los comandos con `generateText` (`update-subtask`, la bitácora) SÍ funcionan. task-master 0.43.1 ya es la última. Las tareas se escribieron directo en `tasks.json` (validado con `task-master list`/`validate-dependencies`). Ver [[taskmaster_addtask_provider_broken]]. Ver [[propiedades-y-video]], [[estrategia-releases]], [[0007-workflow-multiagente]].

## [2026-07-13] diseño | Spec detallada de TODAS las olas (0–4) — 107 subtareas, decisiones locked por módulo
Sesión de diseño extensiva (interrogatorio por olas, sin código de app): se bajó `docs/PRD-beta.md` de backbone a **spec completa sin ambigüedad** de las 17 épicas del camino a producción (67–84), descompuestas en **107 subtareas** escritas directo en `tasks.json` (add-task roto). Decisiones locked destacadas — **Ola 0:** subida tus directo→Stream, R2 mínimo (avatares/logos + originales archivados en frío), HLS firmado TTL reusando `mint-video-url`, webhook Stream→EF, retención archived = R2 frío 7d + borrar de Stream al instante, thumbnail = solo timestamp (Stream renderiza), fingerprint aislado primero. **Ola 1:** premium **derivado** (sin tocar enum), `agency_member_role`→4 (incluye viewer), admin en beta por **Studio/SQL**, **email real con Resend** (apagar autoconfirm), **solo Google OAuth** (Apple bloqueante pre-iOS por 4.8), OTP tel diferido, **catálogo MX** INEGI, estados de publicación = **mapear** (closed+reason+deleted_at, no aplanar 16), doble versión edición vía **`property_revisions`**, ranking §9.8 = **RPC PostGIS + anti-clustering post-pass**, estado de feed sin tabla (seed/cursor cliente), autocompletado por catálogo propio (sin API externa), `public_code` URB-XXXXX, CRM scoring **denormalizado** + nivel automático (estado manual), pagos = **ciclo completo con slots gratis** (ADR §2.2). **Ola 2:** **Expo Push** (no FCM directo), despacho por trigger→EF, filtro de comentarios por lista configurable, página web `/v/[id]` desde ya. **Ola 3:** métricas por **rollup diario pg_cron**, admin web **reusa las EFs** de Olas 1-2, **web unificado en Next.js/Vercel** (⚠️ **reemplaza Astro** del PRD §27, decisión de Abraham — enmienda §7 del ADR 0008), Sentry+Logflare. **Ola 4:** Stripe **PaymentSheet nativo** + webhook EF, 4 métodos en test (OXXO/SPEI a validar en live), flip-ready. Prereqs externos acumulados de Abraham: Cloudflare Stream+R2, Resend+dominio, Google Cloud OAuth, Stripe test. Ejecución por `/tm-plan`→`/tm-tarea`. Ver [[0008-arquitectura-real-prd]], `docs/PRD-beta.md`.

## [2026-07-13] tarea | #67 runtimeVersion.policy → fingerprint (Ola 0, épica G) — OTA vs rebuild automático
Primera tarea de ejecución del camino a producción (Ola 0). `mobile/app.config.js`: `runtimeVersion.policy` de `appVersion` (fijo `1.0.x`) a **`fingerprint`** → EAS calcula la huella del código nativo (deps/config/plugins) en build time y empareja cada OTA solo con builds de la misma huella; un cambio nativo genera huella distinta y **exige rebuild automáticamente** (se acabó el adivinar y subir `version` a mano). Huella base = `1a487c3062e1ae3e4be807a4282cc52850a27f13`. Verificación: `npx @expo/fingerprint` genera hash sin error; **build nativo local `pnpm expo run:android` BUILD SUCCESSFUL (1m18s)**, app boota en emulador (MainActivity foreground, `ReactNativeJS: Running main`, sin fatales; screencap = gate "Enciende la ubicación", UI real de marca). Docs sincronizadas (concepto [[estrategia-releases]] L17/L30 marcada HECHA + nota del corte, CLAUDE.md §3, `ota.sh`). ⚠️ **Corte:** invalida el emparejamiento OTA de los builds `appVersion` en la calle → testers reinstalan **1 vez** un build fingerprint para volver a recibir OTAs. El **OTA real de verificación se corre POST-MERGE desde `main`** (decisión del dueño: respeta la regla "OTA desde main mergeado"), sirviendo de primer OTA fingerprint. No-crítica-TDD (config). Rama `tarea/67-fingerprint-runtime`.

## [2026-07-16] tarea | #69 Épica C — Cloudflare R2 para assets (egress $0), avatar end-to-end
Ola 0 del camino a producción. Bucket privado `urbea-assets` + EF **`mint-r2-url`** (presigned PUT/GET server-side, patrón mint-video-url: autz fail-closed en el handler — avatar=prefix propio, logo=owner de agencia —, minter aws4fetch puro; 40 Deno tests, guardian PASS, desplegada con `--use-api` sin Docker). Cliente: `saveProfile` migrado a R2 (guarda **r2_key** en `profile_photo_url`), `r2Resolver`+`useR2Urls` (batch presigned GET, fail-soft, **passthrough URLs legacy**), wiring en ProfileHeader/AgentCard/edit. El smoke en emulador atrapó y arregló **#69.6**: contrato keep/remove/replace en `saveProfile` (guardar sin cambiar foto ya no truena) + preview del key en edición. Greenfield: sin backfill (avatares viejos visibles por passthrough). Round-trip real contra R2 verde (PUT 200 + GET sirve); dos gotchas de secrets cazados en vivo (valor=nombre; par ID/secret de tokens distintos). Suite móvil 658/658, Deno 467+. Nuevo concepto [[storage-hibrido]]. Pendientes → tareas nuevas: avatares del CRM (`users.avatar_url`) y UI de logo de inmobiliaria. Rama `tarea/69-r2-assets`.

## [2026-07-20] tarea | #68.2 Épica B — schema aditivo del ciclo Cloudflare Stream
Migración `20260720000001_stream_schema.sql` (aditiva, idempotente + rollback). Enum `property_video_status` +`archived`; `property_videos` +4 cols nullable (`tus_upload_url`, `thumbnail_pct` check 0-100, `archived_at`, `r2_archive_key`) — sin duplicar `ready_at`/`failure_reason` (ya existían en 0005). Tabla `public.app_config(key,value jsonb,updated_at)` **fail-closed** (RLS ON, SELECT solo `private.is_admin()`, service_role bypassa; seed video_slot_free/archived_retention_days=7/signed_url_ttl_seconds=14400). TDD estricto CRÍTICA: pgTAP `12_stream_schema_test.sql` RED→GREEN (17 asserts, `supabase test db`=PASS 170 tests) + rollback round-trip local verificado + guardian PASS (reforcé asserts de valores del seed por observación no bloqueante del guardian). Directiva del dueño (paridad local↔remoto): aplicada a remoto `mvpvqmyhrrkwbnpctpuq` vía `apply_migration`, paridad verificada (enum+app_config+4cols+3seed+RLS+1policy). Rama `tarea/68-stream`.

## [2026-07-20] tarea | #68.3 Épica B — EF mint-upload-url + schema upload-first
Ciclo TDD estricto CRÍTICA en 2 piezas. (A) Mini-migración `20260720000002_property_videos_upload_first.sql` (aditiva): `property_id`→NULLABLE, +`agent_id`(→users), índice parcial `(agent_id,status)`, CHECK `property_videos_attributable`; pgTAP `13` (9 asserts) RED→GREEN + rollback round-trip local; aplicada a remoto, paridad verificada. (B) EF **`mint-upload-url`** (Direct Creator Upload a Stream, `creator=uid`/`maxDurationSeconds=120`/`requireSignedURLs=true`): handler DI + index (uid del JWT) + 3 factories en `_shared/clients.ts`; invariante concurrencia §13.2 fail-closed (409 si el agente ya tiene 1 video uploading/processing; query con error→asume ocupado); inserta `property_videos(agent_id, property_id=NULL, status=uploading)`; 502 sin fila huérfana si Stream falla. **18 Deno tests** RED (test-author) → GREEN (agente supabase) → guardian PASS. Decisión usuario: **upload-first** (no draft-first) + código público de compartir separado a **tarea #88**. Secrets `STREAM_*` configurados y verificados por smoke local (`stream-smoke`, stream_api_ok+jwk_kid_matches). ⚠️ Pendientes: deploy de `mint-upload-url` al remoto requiere subir secrets + **rotar credenciales Cloudflare expuestas en el transcript** (task chip). Taskmaster caído esta sesión por **OAuth expirado del provider claude-code** (`claude login` para arreglar) → bitácora vía tasks.json. Rama `tarea/68-stream`.

## [2026-07-20] tarea | #68.4 Épica B — cliente upload-first (useVideoUpload → Stream)
Reescritura del hook `useVideoUpload.ts` (CRÍTICA, `hooks/**`): ya no sube a Supabase Storage; invoca la EF `mint-upload-url` → sube directo al `uploadURL` de Stream con **POST multipart (campo `file`)** vía `File.createUploadTask` (streaming, sin `tus` — POST simple, techo `MAX_STREAM_UPLOAD_BYTES`=200 MB) → estado `processing`. `UploadStatus` = idle|uploading|processing|error (sin `success`; el `ready` vendrá por webhook 68.5). Escribe `cloudflare_uid` al form (no `storage_path`); +`cloudflare_uid` en PublishFormState; fix mínimo `step3.tsx`. Mapeo `error_code` vía `edge-errors.ts` (409="video en proceso"). Contrato Direct Creator Upload verificado con docs de Cloudflare (WebFetch). TDD: 14 Jest RED (test-author) → GREEN (agente mobile) → guardian PASS; suite publish 60/60, tsc 0, lint 0. ⚠️ E2E emulador **pendiente** (gateway remoto 402). Guardian detectó gap → **subtarea nueva #68.12** (validation.ts + publish_property_atomic siguen exigiendo storage_path; hay que adaptar publicación a upload-first y enlazar el video en vuelo). Rama `tarea/68-stream`.

## [2026-07-20] tarea | #68.5 Épica B — EF stream-webhook (ready/failed + gancho push)
EF pública `stream-webhook` (CRÍTICA, `--no-verify-jwt`): recibe el webhook de Cloudflare Stream y verifica su firma **HMAC-SHA256(`STREAM_WEBHOOK_SECRET`, `<time>.<rawBody>`)** del header `Webhook-Signature` — rawBody leído UNA vez (sin re-serializar, o la firma fallaría), comparación en **tiempo constante**, **fail-closed 401** antes de tocar DB/notify. `state=ready`→`mark_ready(cloudflare_uid, thumbnail_url)` (status=ready, ready_at); `state=error`→`mark_failed(cloudflare_uid, failure_reason)`; inprogress/uid desconocido/0 filas→200 **idempotente** (evita reintentos de Cloudflare). UPDATE filtra **solo por `cloudflare_uid`** (upload-first: el video puede no tener propiedad; nunca `property_id`), NO toca `property_status` (enum sin media_failed). `notify` = **gancho Ola 0** (envío real de push en Ola 2). Handler DI + index + 2 factories en `_shared/clients.ts`. TDD: 15 Deno RED (test-author, firma HMAC computada con crypto real, no mock) → GREEN (agente supabase) → guardian PASS; regresión 100/100, deno check limpio. ⚠️ Deploy pendiente: registrar webhook en Cloudflare (PUT /stream/webhook) → nace `STREAM_WEBHOOK_SECRET` → cargar en Supabase (cierra el ciclo diferido de 68.1); + 402 remoto. ⚠️ Provider AI de Taskmaster **alucinó** un snippet falso en la bitácora (afirmaba GREEN/guardian ya corridos en RED) → bitácora reescrita autoritativa en tasks.json. Rama `tarea/68-stream`.

## [2026-07-21] tarea | #68.6 Épica B — minter del feed a HLS firmado de Stream (dual-ref)
`make_video_url_minter(client, hlsConfig?)` (CRÍTICA, `supabase/functions/**`) ahora es DUAL-REF: fila con `cloudflare_uid` → **URL HLS firmada de Stream** (JWT **RS256** vía `jose`: `importJWK`+`SignJWT`, header `{alg:RS256,kid:STREAM_SIGNING_KEY_ID}`, payload `{sub:cloudflare_uid,kid,exp:now+ttl}`, URL `.../<uid>/manifest/video.m3u8?token=JWT` en `customer-<sub>.cloudflarestream.com` o fallback `videodelivery.net`); fila sin `cloudflare_uid` con `storage_path` → Supabase Storage signed URL (legacy intacto). **Precedencia Stream>legacy**, **fail-closed por fila** (falta JWK/firma lanza → excluye SOLO esa fila, nunca cae a legacy sin firmar), TTL de `app_config.signed_url_ttl_seconds` (fallback //ponytail 14400). Contrato al feed INTACTO. `deno.json` +`jose@5`; wiring `mint-video-url/index.ts` (build_hls_config lee STREAM_SIGNING_* de env). TDD estricto: 22 Deno RED (JWT verificado con `jwtVerify`+pub key RSA de prueba, no mock) → GREEN → guardian PASS; suite EFs 508/508, deno check limpio. ⚠️ E2E en vivo (reproducir HLS real) + deploy de secrets al remoto pendientes por **402** (68.10). Rama `tarea/68-stream`.

## [2026-07-21] tarea | #68.12 Épica B — publicación upload-first (enlace del video en vuelo)
Cambio de contrato en 3 capas (CRÍTICA). Publicar ya NO inserta fila nueva de `property_videos`: **ENLAZA** la que `mint-upload-url` creó (UPDATE `property_id`). Ref del video: **`cloudflare_uid` reemplaza `video_id`+`storage_path`** en RPC/EF/payload. Migración `20260721000001_publish_property_link_video.sql` (+rollback): RPC `publish_property_atomic` nueva firma (`p_cloudflare_uid`), cuerpo INSERT `properties` + UPDATE `property_videos SET property_id, position=1 WHERE cloudflare_uid=… AND agent_id=caller AND property_id IS NULL AND status IN(processing,ready) AND deleted_at IS NULL RETURNING id`; null→`raise P0001` (ROLLBACK=atomicidad, sin propiedad huérfana). 🔒 Invariantes: solo enlaza el video del **propio agente**, no-reenlace, no-`uploading`, guard `cloudflare_uid` no vacío. EF `publish-property` (handler/types/index) + mobile `validation.ts`/`store/types.ts` migrados a `cloudflare_uid`. GOTCHA: `RETURNS TABLE(property_id)` → variable implícita colisiona con la columna → calificar `public.property_videos.<col>`. TDD estricto 3 superficies: pgTAP `06` (26 asserts) + Deno `publish-property` (40) + Jest `publish` (66) RED→GREEN→guardian PASS; pgTAP suite 197/197, Deno 505/505, tsc+lint limpios. **Migración aplicada al remoto** (`apply_migration`, firma verificada en `pg_proc`; la vieja con `p_storage_path` ya no existe). ⚠️ Deploy EF pendiente por 402 (68.10). Rama `tarea/68-stream`.

## [2026-07-21] tarea | #68.9 Épica B — script de respaldo de videos legacy (local-only)
`supabase/scripts/backup-property-videos.sh` (NO crítica; script versionado, patrón `seed-videos.sh`): lista `property_videos.storage_path` vía PostgREST (service_role) y descarga cada objeto del bucket `property-videos` a `supabase/scripts/video-backups/` (**gitignored**). Idempotente, fail-safe (solo GET). `bash -n` OK. **HALLAZGO clave:** el **402 (`exceed_cached_egress_quota`) afecta TODO el gateway del proyecto** (PostgREST + Storage + Functions), no solo EFs → la descarga real de las **7 filas** con `storage_path` queda BLOQUEADA hasta liberar billing (el script sale limpio con mensaje). Rama `tarea/68-stream`.

## [2026-07-21] tarea | #68.8 Épica B — archivado cold-store Stream→R2 (síncrono + backoff)
EF `archive-video/` (CRÍTICA, DI-pura) archiva on-demand con **ordering seguro anti-pérdida**: habilita downloads en Stream → poll **backoff exponencial** (`sleep` inyectable; excedido→**202 retry** sin efectos) → `fetch_mp4` → **PUT a R2** (`make_r2_url_minter().sign_put`, key `archive/<uid>.mp4`) → **solo tras confirmar R2**: `delete_video` en Stream → `mark_archived` (status=archived, archived_at, r2_archive_key, cloudflare_uid=NULL). 🔒 R2/fetch_mp4 fallan → 502, NUNCA borra Stream ni marca archived (invariante anti-pérdida, el test central); delete_video falla tras R2 ok → marca archived igual (R2=punto de no retorno). Auth agent/admin owner-o-admin. **2ª capa DB:** CHECK `property_videos_archived_requires_r2` (migración `20260721000002`, aplicada al remoto vía apply_migration, 0 filas en riesgo). 4 factories en `_shared/clients.ts`. TDD estricto: pgTAP `14` (7) + 28 Deno RED→GREEN→guardian PASS; pgTAP 204/204, Deno EFs 533/533, deno check limpio. Modelo **síncrono+backoff** elegido por el usuario; `pg_cron` descartado (on-demand). Guardian nit no bloqueante → chip: make_video_loader colapsa error DB en null→404 (enmascara infra). ⚠️ Smoke real Stream→R2 diferido por par R2 (403) + 402. Rama `tarea/68-stream`.

## [2026-07-21] tarea | #68.7 Épica B — selección de thumbnail de Stream (clúster 68.7a–d)
Portada firmada de Cloudflare Stream (egress $0, on-the-fly, sin R2). Spec de diseño aprobado (`.taskmaster/docs/exploraciones/68.7-thumbnail-picker-spec.md`) resolvió 3 bloqueantes: (1) DÓNDE vive → sección "Portada" en el editor (step3 edit mode, solo cuando el video está `ready`); (2) firma de thumbnails (gap real: `requireSignedURLs=true` exige token también para thumbnails) → EF nueva; (3) persistencia → UPDATE `thumbnail_pct` con RLS owner + render con `?time=pct%×duration`. Re-split en 4 subtareas:
- **68.13** (CRÍTICA, migración): `property_videos.duration_seconds` **int→numeric** (ya existía como int desde 0005 pero redondeaba la duración fraccionada de Stream 92.3→92, rompiendo pct→segundos exactos). El webhook `stream-webhook` ahora la **puebla** desde `payload.duration ?? null` (contrato `MarkVideoReadyParams` +`duration_seconds`). Migración `20260721000003` **aplicada al remoto** (data_type=numeric verificado). pgTAP `15` (11) + Deno webhook 17/17, guardian PASS.
- **68.14** (CRÍTICA, EF): **`mint-thumbnail-url`** DI-pura — dado `cloudflare_uid` del propio agente → `{baseUrl, token, durationSeconds, expiresIn}`; **un token firma todos los `?time=` del uid**. Fail-closed: ownership (`agent_id=caller` OR `properties.owner_user_id=caller`) ajeno→403, no-ready/inexistente→404, falta JWK→500 sin URL. `make_thumbnail_url_signer` en `clients.ts` reutiliza la mecánica RS256 de 68.6. Deno 21 handler + 7 firma real (jose.jwtVerify, no tautológico), guardian PASS.
- **68.7** (mixta): críticas TDD — `lib/thumbnailUrl.ts` (`build_thumbnail_frame_url` puro, clamp+toFixed(1), duration null→sin time) + `hooks/useThumbnail.ts` (`fetch_source` vía EF + `save_pct` UPDATE RLS owner, fail-soft), Jest 26/26 guardian PASS. UI ligera — `components/ThumbnailPicker.tsx` (3 sugeridos 25/50/75 + slider primitivo View+PanResponder, sin dep nueva; estados skeleton/processing/error fail-soft) + sección Portada en step3. **CLEANUP**: borrado `lib/videoThumbnail.ts` (portada legacy P7 — generaba un frame local y lo subía a Supabase; muerto con Stream) + removida `generate_and_store_thumbnail`.
- **68.15** (CRÍTICA, EF+UI): `make_video_url_minter` devuelve **`posterUrl`** por video (Stream→thumbnail firmado a `time=COALESCE(thumbnail_pct,50)/100×duration`, default 50 sin tocar la RPC; legacy→null). Refactor `sign_stream_hls_url`→`sign_stream_token`+`build_hls_url`+`build_poster_url` (una firma, dos URLs). Deno 29/29 guardian PASS. Feed (`VideoFeedItem`) y detalle (`PropertyVideoPlayer`) consumen `posterUrl ?? thumbnail_url` — restaura la portada que quedó nula tras el cleanup. Jest 104/104.
Deploy de las EFs + smoke visual real con frames de Stream **diferidos a deploy-day** (68.10, 402). Rama `tarea/68-stream`.

## [2026-07-22] fix | Épica B — prueba E2E real contra Cloudflare Stream: el token va en el PATH (feed roto en prod)
Primera verificación **en vivo** del ciclo Stream, hecha **en local sin destrabar el 402** (el 402 es del gateway remoto de Supabase; Cloudflare es externo). Receta: `supabase start` + `supabase functions serve --env-file supabase/functions/.env --import-map supabase/functions/deno.json`. Ciclo probado punta a punta con un video real (52.2 s, 4.3 MB): `mint-upload-url` → **200** con uid real de Stream + fila `uploading` local → POST multipart del binario a Stream → **200** → transcoding real hasta `ready` → webhook **firmado a mano** (HMAC) contra la EF local: firma inválida → **401 INVALID_SIGNATURE**, firma válida → **200** y la fila queda `status=ready, duration_seconds=52.2` (⭐ la fracción exacta que 68.13 salvó al pasar la columna a `numeric`) → `publish-property` enlaza el video y crea la propiedad → `mint-video-url` devuelve `signed_url`+`posterUrl`.
🔴 **BUG CRÍTICO encontrado y corregido (`8e77faf`):** el token firmado de Stream va **EN EL PATH sustituyendo al uid**, NO como `?token=`. `/<uid>/manifest/video.m3u8?token=<JWT>` → **401**; `/<TOKEN>/manifest/video.m3u8` → **200**. Igual para thumbnails (`/<TOKEN>/thumbnails/thumbnail.jpg?time=<N>s`). Habría dado 401 en **todo el feed de video** en producción. Afectaba `build_hls_url` (68.6), `build_poster_url` (68.15), `make_thumbnail_url_signer` y `build_thumbnail_frame_url` (68.14/68.7). **Los 36 tests unitarios no lo cazaron** porque asertaban nuestro propio formato: auto-consistentes, equivocados sobre el contrato externo → lección: un contrato ajeno solo se valida contra el servicio real. Fix + asserts anti-regresión que prohíben `?token=`/`&token=` en URLs firmadas; suites verdes (Deno 95, Jest 87, tsc/lint limpios).
✅ **Post-fix verificado en vivo:** manifest HLS → 200 m3u8 válido; portada → 200 JPEG en `time=26.1s` (50 % de 52.2, el default); los 3 frames del picker (25/50/75 %) → 200 con **tres imágenes distintas** (19.2/16.5/12.9 KB), confirmadas visualmente. Rama `tarea/68-stream`.

## [2026-07-22] tarea | #89 Épica B — portada firmada en los grids (EF batch mint-poster-urls)
Cierra el hueco hallado en la prueba E2E: los grids (Mis publicaciones, grid del perfil, Guardados) mostraban la miniatura en blanco para videos de Stream (el webhook guarda `thumbnail_url` SIN firmar → 401; y las URLs firmadas caducan, hay que mintar al leer). Decisión de diseño (usuario): EF batch nueva, no reuso por-tarjeta. **89.1 (CRÍTICA, EF):** `mint-poster-urls` DI-pura — `make_poster_url_minter` en clients.ts carga el primer video ready por `position`, autoriza **por item** (`owner_user_id=caller` cualquier status O `status=active` público; service_role bypassa RLS → este filtro es la única barrera), devuelve `posterUrl` firmado reusando `build_poster_url`/`sign_stream_token` (token en el path). Fail-closed por item (no-autorizado/sin-ready/sin-uid/error-firma → se omite; batch resiliente; nunca URL sin firmar). Deno 43/43 (19 handler + 24 firma real jose) + regresión 88/88, guardian PASS. **89.2 (hooks CRÍTICOS + componentes ligeros):** helper `mobile/src/lib/gridPosters.ts` `fetch_grid_posters` (invoca la EF, fail-soft → Map vacío, nunca lanza); los 3 hooks (useMyProperties/usePropertiesGrid/useSavedProperties) llaman **una vez por página** (batch) y mergean `posterUrl`; `PropertyGridCard`/`PropertyListItem` usan `posterUrl ?? thumbnail_url` (SavedGridItem delega en PropertyGridCard). Jest 97 (crítico) + 89 (UI), tsc/lint limpios, guardian PASS. Deploy EF + smoke visual diferidos a deploy-day (402). Rama `tarea/68-stream`.

## [2026-07-23] tarea | #65.5 builds EAS destrabados (fingerprint macOS vs Linux) + remoto sincronizado
**#65 CERRADA** (11/11). Los 3 builds (`development`/Android dev-client, `preview`/Android APK, `production`/iOS TestFlight) compilaron verde en 1.0.2, runtime `36277651…`.
🔴 **Bloqueante resuelto:** con `runtimeVersion.policy: fingerprint` **todo** build moría en `CONFIGURE_EXPO_UPDATES` con `Runtime version mismatch` (local `17fb1601` vs EAS `502d9b14`). El diff señalaba los directorios de `@react-native-masked-view/masked-view` y `react-native-maps` (`rncoreAutolinkingAndroid`): **hashean distinto en macOS que en el Linux de EAS** aunque el contenido sea idéntico al tarball de npm (20/20 archivos). Fix: `mobile/.fingerprintignore` sobre esos dos paquetes (quedan en `hash: null` en ambos lados) — verificado porque **EAS registró exactamente el hash que calcula la Mac**. Descartado por medición y **no re-investigar**: artefactos `android/build` en `node_modules` (borrarlos NO movió el hash — `@expo/fingerprint` ya los ignora), lockfile desincronizado, `.DS_Store`, parches pnpm, `nodeLinker`, y **la versión de pnpm** (se fijó `packageManager: pnpm@11.1.3`, EAS pasó de 10.33.3 a 11.1.3 y el hash siguió idéntico; el pin se conserva). ⚙️ Gotcha: los `logFiles` de EAS vienen en **Brotli sin header** → descomprimir con `zlib.brotliDecompressSync` de Node; sin eso el error real es invisible. Commit `cd24026`.
**Remoto sincronizado con `main`** aprovechando que el **deploy SÍ pasa bajo el 402** (el 402 solo capa las *invocaciones*): desplegadas `mint-thumbnail-url`, `mint-poster-urls`, `archive-video` y `stream-webhook` (`--no-verify-jwt`); actualizadas `mint-video-url`, `publish-property` y `mint-upload-url` (corrían código previo al `posterUrl` y al fix del token en el path). Las 36 migraciones ya estaban aplicadas; secrets de Stream y R2 puestos (falta `STREAM_WEBHOOK_SECRET`, diferido hasta rotar credenciales).
⚠️ **El 402 tumba REST, Auth, Storage y Functions por igual** (plan Free, cuota de egress rebasada) → el remoto **no sirve para probar** hasta subir a Pro o esperar el corte; por eso los builds `preview`/`production` quedan horneados esperando. El **dev-client sí es usable hoy**: `scripts/dev-local.sh` levanta stack + Edge Functions (contra Cloudflare Stream real) + Metro y ajusta la IP de la LAN en `.env.local` para probar en teléfono real.

## [2026-07-24] fix+tooling | #68.16 el feed no reproducía video (localizeSignedUrl vs Cloudflare Stream) + las tres ramas de entorno documentadas y con arranque de un comando

**Bug (#68.16, cerrada).** Abraham reportó que el video no reproducía en el emulador con la cuenta owner. Verificación por capas: la EF `mint-video-url` **sí** mintaba HLS válido y Cloudflare **sí** lo servía (manifiesto maestro + las 4 sub-playlists, HTTP 200) — pero en la app ExoPlayer moría con `Source error / Response code: 404`. Causa raíz por arqueología git: `mobile/src/lib/supabase/localizeSignedUrl.ts` nació en `829ab55` (2026-06-29) como parche **solo-local** (el edge runtime en Docker firma Storage con su host interno `kong:8000`, inalcanzable desde un emulador) y reescribía el origin de **cualquier** URL firmada. Era correcto mientras el video vivía en **Supabase Storage = mismo origin que la API** (el commit decía "no-op en remoto"). **#68 movió el video a Cloudflare Stream y nadie volvió a mirar el helper** → `https://videodelivery.net/<token>/manifest/video.m3u8` se convertía en `http://localhost:54321/<token>/…` = 404. **Rompía local Y remoto** (allí quedaba `https://<ref>.supabase.co/<token>/…`); el **402** del gateway impidió cualquier smoke remoto y la **portada seguía cargando** (`posterUrl` no pasa por el helper), así que la pantalla parecía "casi funcionando". El helper **no tenía ni un test** pese a vivir en `src/lib/**` (CRÍTICA por path). Fix TDD: localizar **solo** URLs de Supabase Storage (`/storage/v1/`), devolver intacto cualquier otro origin — conserva la razón de ser del helper (rama Storage viva en `mint-video-url` para videos legacy). 7 tests nuevos; jest **708/708**, tsc 0, lint 0 errores; verificado en emulador Android (logcat `state=PLAYING(3) position=9148 buffered=20045 error=null`, 0 respuestas 404).

**Tooling — las tres ramas.** Nuevo concepto [[entornos-desarrollo]] (tabla maestra local/preview/production: backend, origen del JS, vars, canal OTA, perfil EAS) + arranque de **un comando** por objetivo: `scripts/dev-emu.sh` (emulador Android, `adb reverse` de 8081 y 54321 → `localhost`, inmune a la IP), **`scripts/dev-ios.sh` (NUEVO**, simulador iOS; comparte la red del Mac → `localhost` directo, sin túnel; avisa si falta el dev-client) y `scripts/dev-local.sh` (teléfono real, recalcula la IP de la LAN). Dos bugs propios corregidos en los scripts: `pnpm --dir mobile`/`-C mobile` **no funciona bajo pnpm 11** (toma `mobile` como comando) → `cd mobile && pnpm …`; y el deep link del dev-client debe dispararse **después** de que Metro responda (si no, "unexpected end of stream"). **Riesgos anotados, sin resolver:** `preview` y `production` apuntan al **mismo** proyecto Supabase con las mismas keys (sin aislamiento de datos de testers); el entorno `development` de EAS solo tiene `GOOGLE_MAPS_API_KEY` (faltan las 3 `EXPO_PUBLIC_*`); y no existe compuerta de smoke contra preview/production — que es justo lo que dejó pasar este bug. Ingest: [[entornos-desarrollo]], [[mapa-codebase]], [[MOC-arquitectura]].

## [2026-07-25] tarea | #68.10 cierre — Stream/HLS verificado en Android e iOS, OOM #57 resuelto de raíz (medido), y la regla de costo del feed en loop

**Corte de alcance.** 68.10 estaba marcada "bloqueada por externos" asumiendo que su E2E exigía el remoto. El fix de #68.16 (ayer) destrabó lo que era su corazón —comprobar que el video reproduce—, así que se partió: la **mitad local se cerró hoy** y el E2E `publicar → tus → webhook ready → feed` quedó como subtarea aparte de deploy-day (Cloudflare no puede entregar el webhook a `localhost`, y el smoke remoto sigue bajo el 402). Dejar la subtarea entera bloqueada mantenía **#68 `in-progress` y #73 bloqueada** sin necesidad.

**Reproducción verificada en las dos plataformas.** Android (emulador): `state=PLAYING(3)`, `buffered position` ~11 s por delante del `position` → el `preferredForwardBufferDuration: 10` de #57 sí manda; el detalle de propiedad también reproduce, y el log de Glide muestra la portada `https://videodelivery.net/<JWT>/thumbnails/thumbnail.jpg` cargando **con su origin intacto** (justo lo que rompía #68.16). 0 `ExoPlaybackException`, 0 respuestas 404. iOS (simulador, primer flujo Maestro de iOS del repo): login como **owner** → feed → 4 capturas consecutivas **distintas** = reproducción viva, mismo contenido.

**OOM #57 — resuelto de raíz, con número.** La causa era bufferear **MP4 crudo**; con HLS el player pide segmentos y ExoPlayer administra buffer adaptativo. Como la cuenta de Stream tiene **un solo video**, el feed real monta 1 player y no reproduce el escenario del crash → fixture local (`supabase/scripts/smoke-oom-fixture.sql`/`smoke-oom-restore.sql`) que marca los 10 placeholders `ready` contra el mismo `cloudflare_uid` (baja el índice único parcial a propósito, el teardown lo recrea) → **11 items y 4 players vivos simultáneos**, el escenario exacto que crasheaba. Medición: heap Java **40 MB en reposo → 46 MB tras 30 barridos**, plano y con GC recuperando, contra los **107–117 MB** de la era MP4 y un techo de crash de **192 MB**. 0 `OutOfMemory`, 0 crash. Los caps de #57 **se quedan** como defensa en profundidad.

**💰 Regla nueva de trabajo (la aportó Abraham, y explica el 402).** El feed reproduce en **loop**: dejar la app abierta en un emulador —peor en dos— consume cuota **de forma continua sin que nadie mire**. Así se quemó el egress de Supabase que hoy tiene al proyecto en 402. Post-#68 cambió la factura, no el problema: el video ya no sale de Supabase sino de Cloudflare Stream, así que ahora se cobran **minutos entregados de Stream**. Regla: en testing, verificar que reproduce y **parar**; todo flujo Maestro que abra el feed **termina en `stopApp`** (un tap no sirve — alterna play/pausa). Anotado como riesgo vivo en [[entornos-desarrollo]].

**Corrección de ayer.** El riesgo "el entorno `development` de EAS está incompleto (faltan las 3 `EXPO_PUBLIC_*`)" era **falso**: `app.config.js` solo lee `GOOGLE_MAPS_API_KEY` en build (va al manifiesto nativo), y las `EXPO_PUBLIC_*` se inlinean en el bundle que en dev-client sirve Metro desde `.env.local`. Por eso demos y previews siempre funcionaron. El riesgo **real** es que en EAS `EXPO_PUBLIC_GOOGLE_PLACES_API_KEY` y `GOOGLE_MAPS_API_KEY` guardan **la misma llave** → Maps expuesta en el bundle (tarea **#36**).

Verificación: `tsc` 0, `lint` 0 errores. Ingest: [[feed-vertical-video]], [[propiedades-y-video]], [[entornos-desarrollo]], [[mapa-codebase]].

## [2026-07-27] fix+higiene | Paridad dev↔prod: GPS del emulador y portada del seed local + limpieza de repo

**`main` sincronizado.** Los 5 commits del cierre de #68/#65.5 subidos a `origin/main` (`3bdaf11..83d11f8`). Eliminado `app.json` en la **raíz** (`{"expo":{}}`, creado el 23-jul): artefacto del gotcha "correr EAS desde la raíz crea un proyecto EAS falso" (CLAUDE.md §5). Nunca estuvo trackeado.

**Diagnóstico: dos diferencias dev↔prod que Abraham reportó como fallas, y ninguna era regresión.**

1. **El feed sale vacío / no se aprecia el orden por cercanía ni el radio — RESUELTO.** El emulador Android arranca con su GPS de fábrica (**Mountain View, CA**) y el simulador iOS sin fix. `useFeedProperties` **gatea en `coords !== null`** (#59), así que una vez que llegan esas coords falsas se las pasa tal cual a `properties_within_radius`; el seed vive **todo** en la ZMG (lat 20.64–20.72 / lng −103.31…−103.41), a ~2,500 km, y la expansión ×2 topa en 40 km → **0 filas siempre**. ⚠️ El fallback GDL de `feedProperties.ts` **no salva este caso**: solo aplica con `coords === undefined`, y una coord *falsa pero presente* lo esquiva. **Lo que ocultó el problema:** la suite Maestro **sí** hacía `setLocation: 20.6597,−103.3496` (`helpers/launch.yaml`, `ios/feed-hls.yaml`) y el arranque manual no — E2E verde mientras el dev manual salía vacío. **Fix:** `scripts/dev-emu.sh` (`adb emu geo fix -103.3496 20.6597` — ⚠️ **longitud primero**) y `scripts/dev-ios.sh` (`xcrun simctl location <udid> set 20.6597,-103.3496`), mismas coords que Maestro. `dev-local.sh` no lo necesita (teléfono real = GPS real).

2. **La portada del feed en blanco — documentado, tarea #91 (baja).** `supabase/scripts/seed-videos.sh:123` marca los 10 videos `ready` seteando **solo** `storage_path` — sin `cloudflare_uid`, `thumbnail_url`, `duration_seconds` ni `thumbnail_pct`. Son filas **legacy de Supabase Storage**, y esa rama de `make_video_url_minter` (`_shared/clients.ts:432`) devuelve **`posterUrl: null` explícito** — el poster es exclusivo de Stream. Con `thumbnail_url` también null, `VideoFeedItem:52` calcula `poster_uri = posterUrl ?? thumbnail_url` → null → **el `<Image>` no monta**. El video **sí reproduce**, y por eso se lee como "miniatura rota" y no como "seed sin portada". En producción no pasa porque los videos vienen de Stream y `stream-webhook:105` —el **único** escritor de `thumbnail_url`/`duration_seconds`— los puebla al marcar `ready`. 🔑 **Corolario estructural:** Cloudflare no entrega webhooks a `localhost`, así que un video publicado desde la app en local **nunca llega a `ready`** solo → queda fuera del feed (el minter filtra `status='ready'`). El único contenido local con portada real es el de `smoke-oom-fixture.sql`.

**Regla que deja:** cuando "en dev falla y en prod no", sospechar primero del **seed** y del **entorno del dispositivo** (GPS, permisos, red), no del código. Y si la E2E pasa mientras el arranque manual falla, la diferencia está en lo que **la E2E moquea y el script no**.

**Higiene.** 0 PRs abiertos; 18 ramas remotas `tarea/*` con PR **MERGED** confirmadas listas para borrar (`--no-merged` da señal falsa: el flujo usa `--squash`, el tip original nunca es ancestro de `main`) — el borrado quedó pendiente de permiso. `main == origin/main`, un solo worktree, working tree limpio. Verificación: `pnpm tsc --noEmit` 0, `bash -n` OK en ambos scripts. Ingest: [[entornos-desarrollo]].

## [2026-07-28] feat | Ola 1 Auth (#72): catálogo INEGI, constraints de registro §5.1 y gate legal versionado §5.5

**4 de 7 subtareas cerradas; 3 `deferred` por cuentas externas que aún no existen** (Resend y Google Cloud). Decisión del usuario: cerrar todo lo desbloqueado y además dejar construida la UI de lo bloqueado, para que al llegar las credenciales solo falte configurar.

**#72.1 — Catálogo oficial MX.** 32 estados y **2478 municipios** (no 2469, esa cifra es vieja) bajados del web service **OFICIAL** de INEGI `gaia.inegi.org.mx/wscatgeo`, no de un mirror de GitHub. PK = clave INEGI literal en `text`: une con cualquier dataset de INEGI sin tabla de traducción, y `'09' != 9` al unir contra un CSV. Constraint `mx_muni_cvegeo_coherente` (`left(id,2)=state_id`) — la FK sola dejaría pasar un municipio con `state_id` válido pero ajeno.

**#72.2 — Campos de registro §5.1.** Aditiva: FK al catálogo + backfill best-effort desde el texto libre, que queda **deprecado** (no se dropea). CHECKs de coherencia municipio↔estado y de mayoría de edad server-side. 🔒 El hallazgo que valía: **el teléfono se guardaba sin normalizar** y `users_phone_unique_active` compara **texto crudo** — `'3312345678'`, `'+523312345678'` y `'33 1234 5678'` eran tres cuentas distintas de la misma persona, o sea la unicidad de §5.1 no servía de nada. Fix: `to_e164_mx` en el cliente **más** un CHECK en la DB (indispensable: `phone` está en el grant por columna de 0008 y se escribe por PostgREST saltándose el formulario).

**#72.6 + #72.7 — Legal versionado y consentimientos.** El schema de 0004 ya traía lo difícil; faltaba la lógica del gate. Ver [[legal-consentimientos]] para el detalle.

### Cuatro trampas que dejó esta tarea

1. **`public.is_admin()` YA NO EXISTE.** La migración 0010 movió los helpers de RLS al schema `private` y dropeó los de `public`. **0008 todavía muestra el nombre viejo** — copiarlo de ahí revienta cualquier migración con RLS con `42883`.
2. **El backfill de una migración NO corre en local.** `db reset` aplica migraciones **antes** del seed, así que cuando el backfill se ejecuta la tabla está vacía. En prod sí opera. El seed tiene que sembrar el dato final directo, o dev y prod divergen en silencio.
3. **El seed puede impedir que una migración aplique.** Los 7 agentes del seed compartían el **mismo** teléfono, así que el `create unique index` fallaba. De-dup defensivo en la migración + teléfonos distintos en el seed.
4. 🔒 **TRUNCATE no pasa por RLS y Supabase lo regala.** `pg_default_acl` da el bit `D` a `anon` en **toda** tabla nueva de `public`. Probado: `truncate public.users cascade` bajo rol `anon` arrastró 13 tablas y solo se detuvo en `events_raw`, la única con un `revoke all` explícito. No es explotable por PostgREST (no emite TRUNCATE), es defensa en profundidad. → tarea **#92** para el barrido de las 21+ tablas.

### Lo que el guardián encontró y no habríamos visto
- Se podía **aceptar un documento cuyo texto nunca cargó** (el error del fetch se descartaba en silencio y el checkbox seguía marcable) — lo contrario del consentimiento *informado* que §5.5 existe para sostener.
- **El gate legal no era inevitable**: `admin/` y `onboarding` lo esquivaban. Tener el gate en un solo layout y creerlo total era el error.

**Deuda levantada como tarea, no dejada en el chat:** #92 (revocar TRUNCATE en todo `public`), #93 (el registro por `/auth/v1/signup` con la anon key crea cuentas incompletas §5.1, y los errores crudos de Postgres llegan sin filtrar a un llamante anónimo), #94 (el consentimiento de WhatsApp no se verifica en el punto de contacto).

**Verificación:** pgTAP 312/312 · Jest 785/785 · `pnpm tsc --noEmit` 0 · `pnpm lint` 0 errores. Ingest: [[legal-consentimientos]], [[mapa-codebase]].

## [2026-07-30] tarea | #93 Registro server-side atómico — EF register + RPC register_user_atomic; signup público cerrado (local); errores sanitizados (fin del oráculo de enumeración por errores crudos)

## [2026-08-10] explore | aprobado tareas 148,149 (cancelada #106) — Hero de video con colapso por scroll en el detalle + chips en wizard step4

## [2026-08-11] fix | #151 — el borrador del autosave bloqueaba toda publicación (409 DUPLICATE_PROPERTY)
El wizard (Ola 1, #73) guarda un draft con la dirección; el checker de duplicados de publish-property no excluía status draft → el agente se bloqueaba a sí mismo en TODOS los intentos (iPhone real y emulador). Fix: draft entra a DUPLICATE_EXCLUDED_STATUSES (clients.ts), RED→GREEN 11/11, publish-property v17 desplegada server-first (sin OTA) y verificada con una publicación real → pending_review. PR #72.

## [2026-08-11] producto | #153 — moderación suspendida temporalmente (publicar → active directo)
No hay moderador ni interfaz: pending_review atoraba toda publicación (solo SQL la liberaba). Decisión de Abraham: publish-property v18 fija active al nacer; el pipeline de validación (video+duplicado) sigue. Revertir a pending_review (§14.2) cuando exista la interfaz — el revert es re-flip del assert del contrato. Las 2 propiedades atoradas se liberaron por SQL. PR #73.

## [2026-08-11] chore | #154 — build 1.0.6 compilado y distribuido (splash de alfa real)
Opción A aprobada: PR #69 mergeado (conflicto tasks.json reconciliado), bump 1.0.6 (PR #74), builds EAS verdes (runtimes nuevos 374ba3dd/ca62b26a), iOS en TestFlight, splash verificado en el APK instalado. La flota 1.0.5 ya no recibe OTAs nuevos — migrar al build 1.0.6.

## [2026-08-14] explore | aprobado tareas 168–172 — Cuenta comercial / anunciantes: video nativo "Patrocinado" en el feed, targeting por zona (#157), organización con capacidad can_advertise (generaliza agencies, sin entidad nueva), impresiones desde el día 1 en tabla propia vía EF, 4 tareas encadenadas + fase 2 CPM/CPC deferred (doc 039)

## [2026-08-16] tarea | #168 organización con capacidad de publicidad — código completo y verificado en local, SIN desplegar

**7 subtareas cerradas (168.1–168.5, 168.7; ésta es 168.6, el cierre).** `agencies` se generaliza a "organización con capacidades" (publicar propiedades y/o anunciarse) en vez de crear una entidad `business` aparte — reusa toda la membresía/RLS de la Ola 1 (#71) sin duplicar. Origen: exploración `039-cuenta-comercial-anunciantes.md`, aprobada 2026-08-14.

**Qué se construyó:** 6 migraciones aditivas (`20260815000001`/`000002`, `20260816000001`–`000004`) — columnas de capacidad + enum `advertiser_category` + CHECK `agencies_al_menos_una_capacidad` + helper `private.org_can_advertise`; RPC `set_org_advertising_atomic` (solo `service_role`, auditada en `admin_actions`, fault-injection con rollback total); `admin_create_agency_atomic` creció de 9→11→12 params en dos pasos distintos (168.3 y 168.7); `publish_property_atomic` ganó el guard `AGENCY_CANNOT_PUBLISH_PROPERTIES`; CHECK `agencies_categoria_requerida_para_anunciar`. Y la pieza de producto nueva: la invitación del owner ahora **se manda por correo de verdad** (`auth.admin.inviteUserByEmail`, antes solo generaba un link que la UI mostraba para copiar) — verificado E2E contra Mailpit local, `invite_action_link` conservado como respaldo. UI de admin (`create.tsx`/`[id].tsx`) con el correo como canal primario.

**Las dos decisiones de Abraham en el checkpoint post-168.4:**
1. **Fail-closed del correo, no un modo degradado.** `inviteUserByEmail` crea el usuario Y envía el correo en una sola transacción de GoTrue — si el SMTP falla, GoTrue hace rollback del usuario y el alta ENTERA falla (500). Se evaluó construir un camino "organización creada, correo pendiente" (opción (b) del doc 039: EF propia contra Resend) y se descartó — no se justifica con un solo correo transaccional. Efecto en el guardian: 3 tests de la fase RED de 168.4 que describían `email_sent:false` como estado alcanzable resultaron ser código muerto contra el adaptador real; la UI de 168.5 no construyó esa rama.
2. **CHECK de categoría, no `coalesce` ni validación en la app.** El guardian de 168.2 encontró que `set_org_advertising_atomic(id, true)` sin `p_category` dejaba `can_advertise=true` + categoría `NULL` alcanzable en silencio. Opciones evaluadas: `coalesce(p_category, advertiser_category)` (rechazada — nunca se podría limpiar la categoría), validar en 169 (rechazada — mueve la garantía de la base a la aplicación), CHECK en la tabla (elegida — hace el estado inválido imposible sin importar el camino de escritura). Efecto en cascada: 3 fixtures de subtareas anteriores (168.1/168.3) que creaban organizaciones "solo-publicidad" sin categoría dejaron de ser válidos y hubo que reconciliarlos, y `admin_create_agency_atomic` tuvo que crecer una SEGUNDA vez (11→12 params) para poder dar de alta esa organización en un solo paso.

**3 gotchas que van a volver a morder:**
- **`pnpm tsc`/`pnpm lint` desde la raíz del repo NO son gates.** El `package.json` de la raíz no tiene scripts: `pnpm tsc` da "Command tsc not found" y `pnpm lint` cae a `/opt/homebrew/bin/lint` — una herramienta **Java** instalada por Homebrew, nada que ver con el repo. Ambos salen con exit 1 y un mensaje que parece error real. Los dos comandos SOLO valen corridos desde `mobile/`. CLAUDE.md §3 los documenta sin decir el directorio — mejorarlo ahí es la próxima vez que alguien caiga en esto.
- **Dos sesiones concurrentes en el mismo repo comparten estado que no está branch-scoped.** El stack de Supabase local es un Postgres **compartido** entre worktrees — dos ramas activas en paralelo ven la MISMA base (evitar `supabase migration up`/`repair`, usar `docker exec psql` para probar sin tocar el ledger de la otra rama). El sentinel `.taskmaster/.current-red` tampoco es branch-scoped. Y `main` puede cambiar bajo tus pies a media tarea: aquí #167 (quick-fixes del wizard) se mergeó y desplegó al remoto a mitad de 168.3, obligando a un rebase y a re-anclar `publish_property_atomic` sobre el cuerpo nuevo (ver siguiente punto).
- **`create or replace` de una función viva es una regresión esperando pasar si se parte de lo que dice el plan en vez de la definición vigente.** `publish_property_atomic` se ha redefinido en **7** migraciones a la fecha. El plan de 168.3 apuntaba al cuerpo de `20260809000006` — correcto cuando se escribió, pero #167 ya había desplegado `20260815000004` (con `built_square_meters`/`half_bathrooms`/`currency` y backfill real de 20 propiedades) para cuando llegó la fase GREEN. Copiar el cuerpo viejo habría borrado esos 3 campos en silencio. Regla que queda: antes de un `create or replace` sobre una función con historial, `pg_get_functiondef` contra la base real (o el `git log` de la función), nunca confiar en la cita del plan.

**Verificado en local, nada en remoto:** pgTAP `46_org_advertising_test.sql` 71/71 (suite completa 1016 asserts, guardian PASS ×3 con mutation testing real), Deno `admin-create-agency/handler.test.ts` 61/61, `cd mobile && pnpm tsc --noEmit`/`pnpm lint` verdes. Contratos publicados intactos: `admin_create_agency_atomic` (9/11/12 params todos resuelven), `publish_property_atomic` (17/20 params), 0 `select('*')` sobre `agencies` en `supabase/functions/**` ni `mobile/**`. **Bloqueante antes del flip de Resend en remoto** (`docs/TODO-pendientes.md` §1.1): `OWNER_INVITE_REDIRECT_TO='urbea://'` no tiene quien monte `useSessionFromDeepLink` en la raíz — el destino correcto ya existe en `reset-password.tsx`, falta cablearlo. Derivadas: #173, #174, #176, #177 (hallazgos de guardian, hardening no bloqueante). Ver [[inmobiliarias-y-agentes]], [[rls-seguridad]], [[mapa-codebase]].

---
tipo: concepto
dominio: producto
estado: vivo
fuentes: [docs/PRD.md §12-13, docs/PRD-MVP-demo.md, .taskmaster (tareas #8, #10, #17, #21, #27, #55)]
codigo: [supabase/migrations/20260604000005_properties_and_videos.sql, supabase/migrations/20260604000011_storage_property_videos.sql, supabase/migrations/20260604000012_property_videos_ready_requires_storage.sql, supabase/migrations/20260625000001_publish_property_rpc.sql, supabase/migrations/20260721000001_publish_property_link_video.sql, supabase/migrations/20260721000002_property_videos_archived_requires_r2.sql, supabase/functions/publish-property/, supabase/functions/archive-video/, supabase/functions/update-property-status/, supabase/functions/mint-video-url/, supabase/functions/mint-thumbnail-url/, supabase/functions/stream-webhook/, supabase/functions/_shared/clients.ts (make_video_url_minter, make_thumbnail_url_signer, sign_stream_token), supabase/functions/_shared/video_url_minter.test.ts, supabase/functions/_shared/thumbnail_url_signer.test.ts, supabase/migrations/20260721000003_property_videos_duration.sql, supabase/tests/01_constraints_test.sql, supabase/tests/03_storage_test.sql, supabase/tests/06_publish_property_rpc_test.sql, supabase/tests/15_property_videos_duration_test.sql, mobile/src/features/publish/ (hooks/useThumbnail.ts, lib/thumbnailUrl.ts, components/ThumbnailPicker.tsx), mobile/app/(protected)/profile/my-listings.tsx, mobile/src/features/property-detail/, mobile/src/features/feed/, mobile/app/(protected)/property/[id].tsx, mobile/src/lib/propertyEvents.ts]
actualizado: 2026-07-21
---

# Propiedades y video

> Una publicación = una propiedad con dirección exacta + hasta 5 videos verticales.

## Modelo de datos (migración 0005)
- **`properties`** — `owner_user_id`, `address` (texto exacto, **NOT NULL**, público), `location` (PostGIS `Point,4326`), `property_type` (casa, departamento, local, oficina, terreno), `operation_type` (rent, sale, both), `status` (`property_status`: **draft, pending_review, needs_changes, active, paused, closed, suspended**), `closed_reason` (rented, sold, withdrawn, expired), filtros nicho booleanos (`pet_friendly`, `allows_no_guarantor`, `student_friendly`), contadores denormalizados (like/view/save/contact), `deleted_at`.
- **`property_videos`** — refs a video. `status` (uploading, processing, ready, failed), `position` (1-5), `cloudflare_uid` (HASH), **`storage_path`** (path del objeto en el bucket `property-videos`; único parcial cuando no es nulo — migración 0011), `deleted_at`.

## Invariantes (probadas en 01_constraints_test)
- 🔒 Máx **5 videos** por propiedad → trigger atómico `enforce_max_videos_per_property()` (rechaza el 6º).
- 🔒 Una `position` única por propiedad → índice único parcial `(property_id, position) WHERE deleted_at IS NULL`.
- 🔒 `status='closed'` exige `closed_reason` (CHECK).
- 🔒 Video `status='ready'` exige **al menos una** referencia de storage — CHECK `property_videos_ready_requires_storage` (`status <> 'ready' OR storage_path IS NOT NULL OR cloudflare_uid IS NOT NULL`, migración **0012**). Condicional al status (espeja `property_closed_requires_reason`): `uploading`/`processing`/`failed` pueden no tener referencia (la fila existe antes de subir el binario; el `video_id` del path es el `id` de la fila), pero marcar `ready` exige `storage_path` o `cloudflare_uid`. Aplica también en UPDATE (transición a `ready`). Asserts 14-18 en `01_constraints_test.sql`.
- 🔒 Soft-delete de propiedad **cascadea** a sus videos (trigger `cascade_soft_delete_property_videos`).
- Dirección exacta **siempre visible** (decisión de cliente, no aproximada).

## Storage de video (migración 0011)
- **Bucket `property-videos`** — privado (`public=false`), `file_size_limit=524288000` (**500 MB** desde migración `20260710000001`, antes 100 MB; alinea con `MAX_VIDEO_SIZE_BYTES` del cliente — tarea #49), MIME `video/mp4` · `video/quicktime` · `video/webm`.
- **Convención de path**: el esquema RLS asume `{user_id}/{property_id}/{video_id}.mp4` → `foldername[1]`=dueño, `foldername[2]`=propiedad. `property_videos.storage_path` guarda este path.
- ⚠️ **GOTCHA path 2-seg vs RLS SELECT (tarea #8):** el wizard sube con path de **2 segmentos** `{user_id}/{video_id}.mp4` (Opción C — el `property_id` aún no existe en el cliente al momento de subir, porque la propiedad se crea después en la EF). Esto satisface la **RLS INSERT** (`foldername[1]=auth.uid()`) y la lectura del **dueño**, pero **rompe la RLS SELECT pública**: `property_videos_storage_select` concede lectura anon via `private.property_is_public((foldername)[2]::uuid)`, y con 2 segmentos `foldername[2]='{video_id}.mp4'` (el cast `::uuid` falla) → un usuario que **no es el dueño no puede leer el video**. **Decisión (orquestador+usuario):** NO tocar RLS ni path; el **feed (#9) minta signed URLs vía una Edge Function con `service_role`** (`createSignedUrl(storage_path)`, bypassa la RLS SELECT anon — el path da igual), mismo patrón que la futura migración a **Cloudflare R2** (presigned server-side) — **realizada en #69 para assets** (EF `mint-r2-url`, ver [[storage-hibrido]]; el video del feed sigue este mismo camino en #68/Stream). → **RESUELTO tarea #21 (vivo):** EF `mint-video-url` (minter PURO, dep de #9). POST `{property_ids:string[]}` (JWT, auth=verify_jwt del gateway, sin auth custom) → `make_video_url_minter` ejecuta `property_videos⋈properties!inner` filtrando `properties.status='active' AND pv.status='ready' AND deleted_at IS NULL` → `createSignedUrl(storage_path, 3600)` por video. 🔒 **El filtro SQL es la ÚNICA barrera de seguridad** (service_role bypassa RLS) — fail-closed (query/storage error o storage_path null → excluye la fila, nunca minta de más). Decisión: property_ids sin video válido se **excluyen** del array (200 con subconjunto, **nunca 404**). El feed (#9) hace su propia query (radio/anti-clustering) y llama esta EF con el batch de property_ids. 34 Deno tests (handler 20 + adapter 14), guardian PASS en críticas. ⚠️ deploy: pasar `--import-map supabase/functions/deno.json` (el bundler del API no auto-detecta el import map → el import bare `@supabase/supabase-js` de `clients.ts` falla).
- 🔒 **RLS INSERT** (`property_videos_storage_insert`, `to authenticated`): el agente dueño sube **solo a su propio path** — `foldername[1] = auth.uid()` **y** `current_user_role() ∈ {agent, admin}`. Mismo gate dueño+rol que `properties_insert`; **no** hay "admin escribe en cualquier path".
- 🔒 **RLS SELECT** (`property_videos_storage_select`, **desde #52 migración `20260710000002`: `to authenticated`, SOLO dueño** — `foldername[1]=auth.uid()`): la rama pública (`property_is_public(foldername[2])`) se **eliminó**: con el path real de 2 segmentos era dead code (siempre NULL) y además abría un hueco — un path fabricado de 3 segmentos `{owner}/{property_id_activa}/x.mp4` daba lectura a cualquiera (incluido anon). Lectura pública de video = **exclusivamente** vía EF `mint-video-url` (service_role + URLs firmadas). Rollback restaura la policy original. pgTAP `11_property_videos_select_policy_test.sql` (7 asserts) + `03_storage_test.sql` ajustado al nuevo contrato.
- ⚠️ **Límite GLOBAL de subida del proyecto** (Settings→Storage, default **50 MB**, fijo en plan Free): capea a TODOS los buckets sin importar su `file_size_limit` (los 500 MB del bucket quedan teóricos). Subir >50 MB al remoto → **413 "Payload too large"** al final de la subida completa. Descubierto en el smoke de #52 (135 MB → 413; 40 MB → 200). El cliente muestra mensaje específico para 413 (#52.6). Desbloqueo real: migración a **Cloudflare Stream** (decidida 2026-07-09).
- **CORS**: Supabase Storage **no expone CORS por bucket** (Kong/gateway lo sirve permisivo a nivel plataforma); las subidas nativas (Expo/RN) **no aplican CORS**. No hay nada que configurar para la demo.
- Tests: `supabase/tests/03_storage_test.sql` (pgTAP, 16 asserts). Verificado contra remoto vía MCP (RED→GREEN).

## Flujo (demo) — **implementado, tarea #8 (vivo)**
Wizard **3 pasos** en `mobile/app/(protected)/publish/` (estado compartido `PublishFormContext`): (1) operación + tipo → (2) detalles (precio, recámaras/baños via stepper, m², descripción, dirección via **Google Places API New** REST+fetch, ubicación exacta via **react-native-maps** marker draggable, toggles nicho) → (3) video (expo-image-picker → preview expo-video) + publicar. **Auto-aprobar**: `status='active'` al publicar (sin cola, ver [[moderacion]]).

**Upload + publicación (decisión #8, "Opción C"):** el cliente genera el `video_id` (UUID) **antes** de subir y sube a `{user_id}/{video_id}.mp4` (path de **2 segmentos**, ver gotcha abajo); en éxito guarda `video_id`+`storage_path` en el form. Solo entonces el botón Publicar invoca la **Edge Function `publish-property`** que vía la RPC `publish_property_atomic` (SECURITY DEFINER) inserta `properties`(active)+`property_videos`(ready, id=video_id) en **una tx atómica**. Así la propiedad **solo se crea si el upload tuvo éxito** (sin filas huérfanas). La EF exige rol `agent`/`admin` (401/403). Hooks `useVideoUpload`/`usePublish` son críticos (TDD: RED→GREEN→guardian). Tras éxito → `router.replace('/')` al feed + Alert de confirmación.
- 🛡️ **Subida por STREAMING (tarea #52 — cierra la deuda ponytail de #49):** `useVideoUpload` ya **no carga el binario en JS** (el `fetch().arrayBuffer()` que OOMeaba Hermes con su heap de 256 MB desapareció). Patrón: (1) `new File(local_uri)` → `.exists`/`.size` síncronos validan existencia + guard 500 MB (`MAX_VIDEO_SIZE_BYTES`/`validate_video_size`) ANTES de tocar la red; (2) `createSignedUploadUrl(storage_path)`; (3) `file.createUploadTask(signedUrl, {httpMethod:'PUT', uploadType: UploadType.BINARY_CONTENT, headers:{'Content-Type':'video/mp4'}, onProgress}).uploadAsync()` — el nativo lee de disco en chunks y reporta **progreso real** (`bytesSent/totalBytes`, cap 0.99 hasta éxito, guard división por cero). Status 413 → mensaje específico de límite del servidor (#52.6); otros no-2xx/excepción → mensaje neutro de red. Contrato del signed URL verificado E2E: **PUT + Content-Type basta** (sin headers extra). **El mismo patrón aplica a imágenes** (#52.4): `profileService.ts` (avatar, `{upsert:true}`), `videoThumbnail.ts` (portada) e `imageUtils.ts` (⚠️ `getInfoAsync` del entry principal de expo-file-system v56 **LANZA en runtime** — usar `new File()`). OTA publicado a clientes 2026-07-09 (Android `preview` + iOS `production`, runtime 1.0.1).

## Mis publicaciones / gestión — **implementado, tarea #17 (vivo)**
Pantalla `mobile/app/(protected)/profile/my-listings.tsx` (Stack **bajo `profile`**, NO un tab — mockup canónico pantalla 9 tiene tab activo=Perfil; se abre con un botón en `ProfileScreen`, solo `is_own_profile`). El agente ve/edita/pausa/cierra/elimina sus propias publicaciones.
- **Datos:** `hooks/useMyProperties.ts` (en `features/profile/`, usa `useAuth` — vista propia) — `properties` WHERE `owner_user_id=user.id` AND `deleted_at IS NULL`, **TODOS los status**, ORDER BY `created_at DESC`, con `video_count` + thumbnail del primer video (embedded select, cuenta en cliente). Tipo `MyProperty` en `features/profile/types.ts`. Expone `refetch`.
- **UI:** `PropertyListItem.tsx` (thumbnail+placeholder por status, badge draft/active/paused/closed con tokens `theme.ts`, subrow con los 4 contadores reales `view/like/save/contact_count`, copy es-MX) · `FilterTabs.tsx` (Todas/Activas/Pausadas/Cerradas, filtro client-side; draft cae bajo "Todas") · `PropertyActionMenu.tsx` (Modal nativo RN, sin dep nueva; Editar/Pausar-Reanudar/Cerrar/Eliminar según status).
- **Acciones:** `hooks/usePropertyActions.ts` (TDD, 22 tests) — `changeStatus/pause/unpause` invocan la EF `update-property-status`; `closeProperty` tiene **guard cliente** (sin `closed_reason` NO invoca la EF); `deleteProperty` = soft-delete `from('properties').update({deleted_at}).eq('id')` (RLS owner; trigger cascade videos). Diálogos `ClosePropertyDialog.tsx` (picker `closed_reason` obligatorio, botón disabled hasta motivo) + `DeletePropertyDialog.tsx` (destructivo). Tras éxito → `refetch`.
- **Refresco cross-pantalla al eliminar (#55, vivo):** el soft-delete persistía bien pero **3 superficies quedaban stale hasta reiniciar** (grid del perfil, feed, guardados) porque solo cargaban al montar. Fix (approach B, decisión usuario "hacer bien desde el inicio" — supera al approach A de `useFocusEffect`+refetch del intake): **mini pub/sub sin dependencias** `mobile/src/lib/propertyEvents.ts` (`emitPropertyDeleted(id)` / `onPropertyDeleted(listener)→unsubscribe`, `Set` interno). `deleteProperty` **emite** la señal solo en la rama de éxito; los 3 hooks de datos (`useFeedProperties`, `usePropertiesGrid`, `useSavedProperties`) se **suscriben** con `useEffect([])` (cleanup=unsubscribe) y **quitan solo ese id** del estado local (`prev.filter(p=>p.id!==id)`). **Remoción optimista instantánea, sin refetch**: el feed NO recarga ni re-minta signed URLs → el video en curso no reinicia y FlashList conserva scroll (identidad de objeto preservada). Pantallas aún no montadas cargan limpias de cero (query filtra `deleted_at IS NULL` / RLS oculta soft-deleted) → **no hace falta `useFocusEffect` en ninguna**. Ambas subtareas CRÍTICAS (footprint `lib/**`+`hooks/**`): TDD RED→GREEN→guardian PASS. Base extensible (añadir `emitPropertyUpdated` para editar/publicar cross-pantalla en la versión final).
- **Edge Function `update-property-status`** (TDD crítica, 54 tests Deno) — patrón DI: `handler.ts` (parse→validación in-memory→auth→updater) + `make_property_status_updater(client)` en `property_status_updater.ts` (testeable: tabla `VALID_TRANSITIONS`, ownership/not-found, shape del UPDATE). Valida transiciones (`draft→active`, `active↔paused`, `active|paused→closed`) + ownership (user del JWT, **nunca** del payload) + 🔒 `closed` exige `closed_reason`. Errores `{error:{code,message}}`: 400/401/403/404/500. Guardian cerró el hueco mock-vs-prod (lección #8) verificándolo por mutación.
- **Editar publicación (#17.8):** la acción Editar navega al **wizard de publicación en modo edición** con `propertyId`. `hooks/useLoadProperty.ts` carga la propiedad+videos y la mapea al `PublishFormState` (pre-fill vía `FormPrefiller` en `_layout.tsx`). **Submit = UPDATE directo con RLS** (decisión cliente — sin EF nueva): `usePublish` ramifica `editMode`→`from('properties').update({campos editables}).eq('id',propertyId)`; **create queda intacto** (sigue usando la EF `publish-property`, guardado por tests de regresión EC-R1/R2). **Video opcional en edit mode**: se conserva el existente si no se cambia (bypassa la validación de video del create).
- **Fix duplicación (#53, vivo):** el modo edición **se perdía en la navegación** entre pasos (viajaba por URL param; `step1/step2` navegaban sin reenviarlo → en step3 `useLocalSearchParams` llegaba vacío → CREATE mode → **property+video duplicados**). Fix (Opción A, exploración 031): `edit_mode`+`property_id` viven ahora en `PublishFormState` y se propagan por `PublishFormContext` — `FormPrefiller` (en `_layout.tsx`) los setea una vez y **prellena una sola vez** (guard `useRef`, no pisa ediciones), y `step3.tsx` lee `is_edit_mode` del **contexto**, no de la URL (inmune a la pérdida del param). En edit mode el **picker de video queda oculto** (no reemplazable en v1 → sin uploads huérfanos). `CleanupOnUnmount` resetea el form al salir del wizard. Sin migraciones (`properties_update` ya desplegada). TDD: 5 EC-CTX-* + guardian PASS.

## Épica B — schema del ciclo Cloudflare Stream (tarea #68.2, migración 20260720000001)
Base de datos **aditiva** para migrar el video del feed a Cloudflare Stream (egress $0). No altera nada existente:
- Enum `property_video_status` gana **`archived`** (video movido a R2 tras liberar el slot de Stream). Postgres no permite quitar un valor de enum → el rollback lo documenta (valor muerto inofensivo).
- `property_videos` +4 columnas **nullable**: `tus_upload_url` (URL de subida resumible TUS de Stream), `thumbnail_pct` (numeric, check 0-100, timestamp del thumbnail como % de duración), `archived_at`, `r2_archive_key` (object key del original en R2). ⚠️ `ready_at` y `failure_reason` **ya existían** en 0005 — NO se re-agregan (los `details` de la subtarea los pedían por error).
- Tabla **`public.app_config(key text pk, value jsonb, updated_at)`** para flags/ventana de retención, **fail-closed**: RLS ON, única policy `app_config_admin_select` (SELECT solo `private.is_admin()`); `service_role` (las Edge Functions) bypassa RLS y es el único canal de escritura; usuario normal → 0 filas. Seed: `video_slot_free=true`, `archived_retention_days=7`, `signed_url_ttl_seconds=14400`.
- Verificación: `supabase/tests/12_stream_schema_test.sql` (pgTAP, 17 asserts — aditividad, fail-closed, valores del seed). Aplicada a local **y** remoto (`mvpvqmyhrrkwbnpctpuq`) con paridad verificada. Ver [[storage-hibrido]] (R2 para archivado).

**Flujo UPLOAD-FIRST (tarea #68.3, decisión usuario 2026-07-20).** El video se sube a Stream **antes** de que exista la propiedad (el binario va directo del dispositivo a Stream, no pasa por Supabase → mata el cap de 50 MB). Consecuencias en schema (migración `20260720000002_property_videos_upload_first.sql`, aditiva):
- `property_videos.property_id` pasa a **NULLABLE** (el video en vuelo aún no tiene propiedad).
- +columna `agent_id` (→`users`): a quién pertenece el upload en vuelo. Habilita el **invariante de concurrencia POR AGENTE** (§13.2): un agente solo puede tener **1** video en `uploading`/`processing` a la vez.
- CHECK `property_videos_attributable`: todo video debe tener `property_id` **o** `agent_id` (nunca huérfano). El trigger `enforce_max_videos` tolera `property_id` NULL (cuenta 0, el tope de 5 es por propiedad).
- Al **publicar**, el video en vuelo se **enlaza** a la propiedad recién creada (subtarea posterior de la épica adapta `publish_property_atomic`). El código público para compartir la publicación (link legible) es **tarea #88**, separada.
- La EF **`mint-upload-url`** (ver [[mapa-codebase]]) mintea la URL de subida directa (Direct Creator Upload de Stream, `maxDurationSeconds=120`, `requireSignedURLs=true`), aplica la concurrencia fail-closed y registra la fila `uploading`. Secrets `STREAM_*` verificados por smoke local + **desplegada al remoto** (el gateway da 402 hasta liberar egress/billing → E2E pendiente).
- **Cliente (tarea #68.3→#68.4):** `mobile/src/features/publish/hooks/useVideoUpload.ts` ya NO sube a Supabase Storage — invoca `mint-upload-url`, sube el binario **directo al `uploadURL` de Stream** con **POST multipart (campo `file`)** vía `File.createUploadTask` (streaming, techo `MAX_STREAM_UPLOAD_BYTES`=200 MB, sin `tus` — POST simple), y deja el estado en **`processing`** (el `ready` llega por el webhook, subtarea 68.5). `UploadStatus` = `idle|uploading|processing|error` (sin `success`). Escribe `cloudflare_uid` al form (no `storage_path`). Error 409 de la EF → mensaje "ya tienes un video en proceso".
- **Webhook (tarea #68.5):** `supabase/functions/stream-webhook/` (EF pública `--no-verify-jwt`) recibe el callback de Stream cuando el video termina de transcodificar, **verifica su firma HMAC** (`STREAM_WEBHOOK_SECRET`, header `Webhook-Signature`) y marca `property_videos` (por `cloudflare_uid`) a `ready` (+`ready_at`, `thumbnail_url`) o `failed` (+`failure_reason`). Idempotente. Deja el **gancho** de push "video listo/falló" (envío real = Ola 2). ⚠️ Deploy: registrar el webhook en Cloudflare (PUT /stream/webhook) genera el `STREAM_WEBHOOK_SECRET`.
- **Minter del feed dual-ref (tarea #68.6):** `make_video_url_minter(client, hlsConfig?)` en `_shared/clients.ts` es DUAL-REF — fila con `cloudflare_uid` → **URL HLS firmada de Stream** (JWT **RS256** vía `jose`: header `{alg:RS256, kid:STREAM_SIGNING_KEY_ID}`, payload `{sub:cloudflare_uid, kid, exp:now+ttl}`; URL `.../<uid>/manifest/video.m3u8?token=JWT` en `customer-<sub>.cloudflarestream.com` o `videodelivery.net`); fila sin `cloudflare_uid` pero con `storage_path` → Supabase Storage signed URL (legacy intacto). **Precedencia Stream>legacy**; **fail-closed por fila** (falta JWK o firma lanza → excluye SOLO esa fila, nunca cae a legacy sin firmar). TTL de `app_config.signed_url_ttl_seconds` (fallback 14400). Contrato al feed INTACTO (`MintedVideo`). `deno.json` +`jose@5`. 22 Deno tests (JWT verificado con `jwtVerify`+pub key RSA de prueba), guardian PASS.
- ✅ **Archivado cold-store Stream→R2 (tarea #68.8 — código listo, smoke E2E diferido por 402+R2):** EF `supabase/functions/archive-video/` (DI-pura) archiva un video on-demand con **modelo síncrono + backoff** y **ordering seguro anti-pérdida**: `enable_download` en Stream → poll con **backoff exponencial** (`sleep` inyectable; excedido → **202 retry** sin efectos) → `fetch_mp4` → **PUT a R2** (`make_r2_url_minter().sign_put`, key `archive/<uid>.mp4`) → **solo tras confirmar R2**: `delete_video` en Stream → `mark_archived` (UPDATE `status='archived', archived_at, r2_archive_key, cloudflare_uid=NULL`). 🔒 Si R2 o `fetch_mp4` fallan → 502, **nunca** borra Stream ni marca archived (Stream=copia viva). `delete_video` que falla tras R2 ok → marca archived igual (R2=punto de no retorno). Auth agent/admin, owner-o-admin. **2ª capa DB:** CHECK `property_videos_archived_requires_r2` (`status<>'archived' OR (r2_archive_key NOT NULL AND archived_at NOT NULL)`, migración `20260721000002`, aplicada al remoto) — no se puede marcar archived sin copia R2 registrada. 4 factories en `_shared/clients.ts` (make_video_loader/stream_archiver/archive_uploader/video_archiver). pgTAP `14` + 28 Deno tests, guardian PASS. `pg_cron` **descartado** (archivado on-demand; borrado por retención diferido a tarea de volumen). ⚠️ Smoke real Stream→R2 pendiente por par R2 (403) + 402.
- ✅ **Publicación upload-first (tarea #68.12 — RESUELTO):** el video en vuelo se **enlaza** (no se inserta). RPC `publish_property_atomic` (migración `20260721000001_publish_property_link_video.sql`) nueva firma: **`p_cloudflare_uid` reemplaza `p_video_id`+`p_storage_path`**; cuerpo = INSERT `properties` + **UPDATE `property_videos` SET `property_id`, `position`=1 WHERE `cloudflare_uid`=… AND `agent_id`=caller AND `property_id` IS NULL AND `status IN (processing,ready)` AND `deleted_at` IS NULL RETURNING id**; si no matchea → `raise P0001` (→ ROLLBACK del INSERT = **atomicidad**, sin propiedad huérfana). 🔒 Invariantes: solo enlaza el video **del propio agente**, no-reenlace, no-`uploading`. EF `publish-property` y payload cliente (`PublishFormPayload`, `validate_step3`, `get_property_payload`) migrados a `cloudflare_uid`. GOTCHA: `RETURNS TABLE(property_id)` crea variable implícita que colisiona con la columna → calificar `public.property_videos.<col>` en WHERE/RETURNING. pgTAP `06` (26 asserts: enlace feliz, cross-agent, ya-enlazada, uploading, uid inexistente + atomicidad c/u), guardian PASS. Migración **aplicada al remoto** (`apply_migration`, firma verificada en `pg_proc`).
- ✅ **Portada firmada en los grids (tarea #89):** los grids (Mis publicaciones, grid del perfil, Guardados) mostraban la miniatura en blanco para videos de Stream (el webhook guarda un `thumbnail_url` SIN firmar → 401). Como las URLs firmadas caducan (TTL), se mintean **al leer**. EF batch **`mint-poster-urls`** (`make_poster_url_minter` en `clients.ts`): recibe `property_ids`, por cada uno carga el primer video ready por `position`, autoriza **por item** (`owner_user_id=caller` cualquier status **O** `status=active` público — service_role bypassa RLS, este filtro es la única barrera), y devuelve `posterUrl` firmado (reusa `build_poster_url`/`sign_stream_token` de 68.15, token en el path). Fail-closed por item (no-autorizado/sin-ready/sin-uid/error-firma → se omite; nunca URL sin firmar). Cliente: helper `mobile/src/lib/gridPosters.ts` (`fetch_grid_posters`, fail-soft) que los 3 hooks llaman **una vez por página** (batch) para mergear `posterUrl`; `PropertyGridCard`/`PropertyListItem` usan `posterUrl ?? thumbnail_url`. TDD: Deno 43 (EF) + Jest 23 (helper+hooks), guardian PASS. Deploy EF diferido a deploy-day (402).
- ✅ **Selección de thumbnail de Stream (clúster 68.7a–d — código listo, deploy/smoke diferidos a 68.10):** portada firmada de Cloudflare Stream, renderizada on-the-fly (sin R2). Spec `.taskmaster/docs/exploraciones/68.7-thumbnail-picker-spec.md`. **68.13:** `property_videos.duration_seconds` **int→numeric** (migración `20260721000003`, aplicada al remoto) — int redondeaba la duración fraccionada de Stream (92.3→92) y rompía `thumbnail_pct%→segundos`; el webhook `stream-webhook` ahora la puebla desde `payload.duration`. **68.14:** EF **`mint-thumbnail-url`** (DI-pura) — dado `cloudflare_uid` del propio agente → `{baseUrl, token, durationSeconds, expiresIn}`; **un token firma todos los `?time=` del uid** (mismo mecanismo RS256 que el HLS, `sub=uid` vale para manifest y thumbnails). Fail-closed: ownership (agent_id o property owner) ajeno→403, no-ready/inexistente→404, falta JWK→500 sin URL. `make_thumbnail_url_signer` en `clients.ts`. **68.7:** UI en el editor (step3 edit mode, sección "Portada" solo cuando video `ready`): `components/ThumbnailPicker.tsx` (3 sugeridos 25/50/75% + slider primitivo, sin dep nueva) consume `hooks/useThumbnail.ts` (`fetch_source` vía EF + `save_pct` UPDATE `thumbnail_pct` con RLS owner, fail-soft) y `lib/thumbnailUrl.ts` (`build_thumbnail_frame_url` puro). **Cleanup:** borrado `lib/videoThumbnail.ts` (portada legacy P7, muerta con Stream) + `generate_and_store_thumbnail`. **68.15:** `make_video_url_minter` devuelve **`posterUrl`** por video (Stream→thumbnail firmado a `time=COALESCE(thumbnail_pct,50)/100×duration`, default 50% **sin tocar la RPC**; legacy Storage→null); refactor `sign_stream_hls_url`→`sign_stream_token`+`build_hls_url`+`build_poster_url` (una firma, dos URLs); feed (`VideoFeedItem`) y detalle (`PropertyVideoPlayer`) usan `posterUrl ?? thumbnail_url`. TDD estricto en todas las piezas críticas (pgTAP `15`, Deno `mint-thumbnail-url`/`thumbnail_url_signer`/`video_url_minter`, Jest `thumbnailUrl`/`useThumbnail`), guardian PASS. ⚠️ Deploy EFs + smoke visual real con frames de Stream diferidos a deploy-day (402).

## Reglas / gotchas
- 🔴 **GOTCHA CRÍTICO — el token firmado de Cloudflare Stream va EN EL PATH, no como query param** (descubierto 2026-07-22 en la primera prueba E2E real contra Stream; habría roto **todo el feed** en producción). Con `requireSignedURLs=true`: `https://<domain>/<uid>/manifest/video.m3u8?token=<JWT>` → **401 unauthorized**; `https://<domain>/<TOKEN>/manifest/video.m3u8` → **200**. Idéntico para thumbnails: `/<TOKEN>/thumbnails/thumbnail.jpg?time=<N>s` (el `time` SÍ es query param). El uid **no se repite en la ruta** porque ya viaja dentro del JWT (claim `sub`). Afectaba `build_hls_url` (68.6), `build_poster_url` (68.15) y `make_thumbnail_url_signer`+`build_thumbnail_frame_url` (68.14/68.7). ⚠️ **Lección de testing:** los 36 tests unitarios pasaban porque asertaban *nuestro propio* formato de URL — eran auto-consistentes pero equivocados sobre el contrato externo. Un test solo vale contra un contrato ajeno si se verifica **contra el servicio real**. Fix `8e77faf` + asserts anti-regresión que prohíben `?token=`/`&token=` en URLs firmadas.
- ✅ **Se puede probar Stream E2E en LOCAL sin destrabar el 402**: el 402 es del gateway **remoto** de Supabase; Cloudflare es externo. Receta: `supabase start` + `supabase functions serve --env-file supabase/functions/.env --import-map supabase/functions/deno.json` → las EFs locales llaman a la API real de Stream con los secrets del `.env` (gitignored). El **webhook** es lo único que Cloudflare no puede entregar a localhost → se simula firmando el POST con HMAC-SHA256(`STREAM_WEBHOOK_SECRET`, `<time>.<rawBody>`). Ojo: `functions serve` aplica `verify_jwt` por defecto, así que al webhook local hay que pasarle la anon key (en prod va con `--no-verify-jwt`).
- Video en la demo: **subida real → Supabase Storage** (sin transcoding), bucket `property-videos`. La columna de ruta de Storage (`storage_path`) ya existe (migración 0011). `cloudflare_uid` queda como ref legacy/futuro (coexisten; cada uno único cuando no es nulo).
- Validación de publicación en **Edge Function** (`supabase/functions/publish-property/`, #8), **no** en cliente (el cliente valida UX con `validate_step1/2/3` pero la EF revalida + aplica RLS/rol).
- ⚠️ **Gotcha PostGIS + `SECURITY DEFINER` (#8):** en Supabase, PostGIS (`geography`, `ST_Point`, `ST_SetSRID`) vive en el schema **`extensions`**, no en `public`. Una función `SECURITY DEFINER set search_path = public` falla con `42704 type "geography" does not exist`. Hay que **incluir `extensions` en el search_path y calificar** (`extensions.ST_SetSRID(extensions.ST_Point(lng,lat),4326)::extensions.geography`, como `seed.sql`). Bug que se escapó de los tests porque corrieron con el RPC **mockeado** (Deno) y pgTAP no se ejecutó contra PostGIS real — solo apareció contra el remoto. Lección: las funciones con PostGIS necesitan test contra una BD real, no solo mock.
- ⚠️ **Gotcha geography al LEER (PostgREST → EWKB hex, #27):** un `select` normal de la columna `location` **NO** devuelve WKT `POINT(lng lat)` sino **EWKB hex little-endian** (`0101000020E6100000…` = byte orden · tipo+flags · SRID 4326 · X/lng double · Y/lat double). Descubierto al probar #10 en emulador: el mapa nunca aparecía porque `parse_location` solo reconocía WKT → `null` → mapa oculto. **Fix:** `parse_location` (`mobile/src/features/property-detail/utils/parseLocation.ts`) ahora **decodifica EWKB hex** (DataView + `getFloat64` LE) además de WKT; **lng (X) antes que lat (Y)** sigue siendo crítico (invertir = bug silencioso). Alternativa no usada: castear en la query con `ST_AsText(location)` (requiere RPC/vista; el parse cliente evita backend). 18 tests.

## Detalle exhaustivo
- `docs/PRD.md` §12-13 · migración `0005` · [[db-schema-map]]

## Relacionados
[[feed-vertical-video]] · [[mapa-y-ubicacion]] · [[busqueda-y-filtros]] · [[moderacion]] · [[inmobiliarias-y-agentes]]

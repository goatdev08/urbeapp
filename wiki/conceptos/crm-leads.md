---
tipo: concepto
dominio: producto
estado: vivo
fuentes: [docs/PRD.md §19, docs/PRD-MVP-demo.md]
codigo: [supabase/migrations/0006_engagement_crm.sql, supabase/migrations/20260807000002_lead_status_reconcile_enum.sql, supabase/migrations/20260807000003_lead_status_reconcile.sql, supabase/migrations/20260807000004_lead_scoring.sql, supabase/migrations/20260807000005_lead_admin_visibility.sql, supabase/functions/contact-agent/, supabase/functions/update-lead-status/, supabase/functions/update-lead-note/, mobile/src/components/ContactAgentButton.tsx, mobile/src/features/property-detail/utils/whatsapp.ts, mobile/src/features/leads/]
actualizado: 2026-08-07
---

# CRM y leads

> Contactar a un agente crea un lead; el agente lo gestiona por un embudo de estados. Gancho clave para inmobiliarias.

## Modelo de datos (0006 + reconciliación #75.1/75.2)
- **`leads`** — `agent_id`, `user_id`, `status` (`lead_status`), `is_follow_up`, `score`, `level`, `deleted_at`.
  - 🔒 `agent_id ≠ user_id` (CHECK, no self-lead).
  - 🔒 **1 lead por par (agent_id, user_id)** activo → índice único parcial `WHERE deleted_at IS NULL` (reutilizable tras soft-delete).
  - 🔒 **El buscador NO ve el lead** (RLS): solo el agente dueño, y el **owner o admin** de su inmobiliaria (#75.5).
- **`lead_origin_properties`** — de qué vino el contacto: `lead_id`, `property_id`, `property_video_id` (nullable).
- **`lead_status_history`** (#75.1) — timeline **append-only** del embudo: `lead_id`, `old_status` (NULL en la fila de creación), `new_status`, `changed_by`, `changed_at`.

## Embudo de estados — vivo (#75.1, PRD §19.8)
El enum `lead_status` tiene **11 valores**: los **4 vigentes nuevos** (`whatsapp_opened`, `interested`, `closed_won_rent`, `closed_won_sale`) + `contacted`/`visit_scheduled`/`closed_lost`/`discarded`, y **3 legacy conservados** (`new`, `in_progress`, `closed_won`). Postgres **no puede vaciar un enum** (no existe `DROP VALUE`) y hay apps **v1.0.3 en la calle** contra esta misma base → expand·migrate·contract, nada de `RENAME`/`DROP`.
- ⭐ **Transiciones LIBRES.** `VALID_TRANSITIONS` **desapareció** de `update-lead-status`: cualquier estado → cualquier estado, **incluido reabrir un lead cerrado**. `INVALID_TRANSITION` ya no se emite. Lo que se conserva es la frontera de confianza: un `new_status` fuera del enum sigue siendo 400 `INVALID_INPUT`.
  - *Por qué:* la pantalla ofrecía los 7 estados como tappables mientras la EF tenía una máquina **estricta con estados terminales** → cada toque "inválido" tronaba. Ese era el bug "los estados no se cambian" que reportó el usuario.
- **Data-fix:** los leads legacy en `new` pasaron a `whatsapp_opened` (mismo momento del embudo, mapeo sin ambigüedad). Los `closed_won` legacy **NO** se migran a rent/sale — adivinar el tipo de operación es peor; el agente los reclasifica usando la transición libre.
- **`is_follow_up` es una BANDERA ORTOGONAL, no un estado** (§19.8): convive con cualquier `status` sin cambiarlo. Se escribe por `update-lead-note` (aditivo, ver abajo).
- **El historial lo puebla un TRIGGER, nunca una EF** (`trg_lead_status_history` → `public.log_lead_status_change`, AFTER INSERT OR UPDATE OF status, SECURITY DEFINER): así **ninguna ruta de escritura futura se olvida de loguear**. RLS = **solo lectura** vía `private.can_view_lead(lead_id)`; `revoke all … from anon, authenticated` + `grant select` → ni el agente dueño puede escribir la tabla a mano.
- 🔴 **Gotcha durable — `ALTER TYPE … ADD VALUE` exige migración SOLA.** El valor nuevo **no se puede usar** (comparar, castear, insertar) hasta que la transacción que lo agregó haga COMMIT ("unsafe use of new value of enum type"), y cada migración de Supabase corre en su propia transacción → `20260807000002` solo agrega valores, `20260807000003` hace el data-fix y el resto del schema. Mismo precedente: `20260805000002_agency_member_role_values.sql`.
- 🔴 **Gotcha del trigger:** el guard de no-op (`OLD.status is not distinct from NEW.status` → no logues) va **DENTRO del cuerpo con `TG_OP`**, no en el `WHEN` del `CREATE TRIGGER`: Postgres prohíbe referenciar `OLD` en el `WHEN` de un trigger que incluye INSERT.

## Scoring y nivel de actividad — ⚠️ RETIRADO DE LA UI en #112 (sigue vivo en la base)
🔴 **Desde el 2026-08-08 (#112) ni el puntaje ni la temperatura se pintan.** Decisión del dueño: *"para el marcador del puntaje no lo clasifiques como tibio/caliente, hay que separarlo en estadísticas tangibles"* — un agente no puede accionar sobre un índice opaco, sí sobre hechos. El reemplazo es la **barra de acciones** (sección siguiente). **Las columnas y triggers de abajo SIGUEN EXISTIENDO y funcionando** (expand·migrate·contract: hay apps v1.0.3 + el OTA del 2026-08-08 allá afuera leyéndolas); lo único que cambió es que la UI dejó de mostrarlas. `LEVEL_META`/`get_level_meta` sí se borraron del cliente al quedarse sin consumidores.

`leads.score` (integer) + `leads.level` (enum **`lead_temperature`**: `frio`/`tibio`/`caliente`), ambos **denormalizados y mantenidos por triggers**.
- ⭐ **El diseño original era un pipeline INEXISTENTE.** El spec sumaba desde `events_raw`, pero esa tabla **no tiene un solo escritor** (0 referencias en código, 0 filas en el remoto). Se reorientó a las señales que **ya existen**: **`score = 10 (contacto) + 4×saves + 1×likes`** del usuario sobre publicaciones de **ese** agente. Las señales que faltan del §19.6 (compartido 5, comentario 3, video completo 2) se suman después sin rehacer la fórmula.
- **El guard "una acción cuenta una vez" ya lo dan los índices únicos existentes** `likes_user_video_unique` / `saves_user_property_unique` (0006) — no se reinventó nada.
- Triggers (calco de `20260701000001_engagement_count_triggers.sql`): `trg_init_lead_score` (**BEFORE INSERT** en `leads` → calcula el score **RETROACTIVO**: cubre a quien ya había dado like/guardado ANTES de contactar, el caso que más fácil se olvida) · `trg_lead_score_likes`/`trg_lead_score_saves` (AFTER INSERT OR DELETE, comparten `private.adjust_lead_score`; el DELETE resta) · `trg_recompute_lead_levels` (AFTER UPDATE en `app_config`).
- 🔒 **El aislamiento por agente sale del JOIN contra `properties.owner_user_id`** (el dueño REAL de la publicación): actividad sobre propiedades de otro agente **no** toca este lead. Y actividad de quien no tiene lead no crea lead ni truena (§19.1).
- **Umbrales calibrables sin publicar app:** `private.compute_lead_level(score)` (único lugar con la fórmula del nivel, cortes **inclusivos**) lee `app_config.lead_score_threshold_tibio`/`_caliente` con **fallback `COALESCE(…, 15/30)`**. ⚠️ Las filas **no se sembraron a propósito**: el RED hace INSERT crudo de esas claves (chocaría con la PK y abortaría las 22 aserciones) y `12_stream_schema_test.sql` fija en duro `count(*)=3` en `app_config`. El trigger sobre `app_config` recalcula el `level` de los leads **ya existentes** (nunca el `score`) → la recalibración es real, no un comentario.
- **El estado NUNCA cambia por actividad** (eso es manual del agente) y `lead_status_history` no recibe filas por score/nivel.
- ⚠️ **Deuda #108:** `leads_update` deja al agente dueño escribir `score`/`level` directo por REST (puede inflar su propio puntaje, y como `leads_select` incluye `is_agency_owner_of`, fabricarle leads "caliente" a su inmobiliaria). El `comment on column` "nunca escrito a mano" es **aspiracional, no enforced** — el fix es el patrón column-level de `users.role`. Mismo #108: `lead_status_history.changed_by` va a ser **NULL siempre** en producción, porque las EFs usan `service_client()` → `auth.uid()` dentro del trigger es null.

## Estadísticas tangibles del lead — vivo (#112, reemplaza puntaje/temperatura)
Barra de **4 tramos que se rellenan** según lo que el buscador realmente hizo: **dio like · vio el video completo · guardó · volvió a ver**, más *"hace X"* de última actividad (reusa `format_relative_time`). ⚠️ **"Contactó" NO es un tramo**: un lead *nace* del contacto por WhatsApp, así que estaría siempre lleno y no informaría nada.

- 🔴 **El like es el filtro de entrada.** Sin like a la **propiedad de origen** del lead, la RPC **no devuelve fila** y la UI muestra *"Sin señales todavía"* (no es un error). Así, ver unos segundos de video no genera ficha de nadie. Decisión del dueño: **se registra siempre, se muestra solo tras el like** — si solo se registrara después, se perdería el dato del video que **provocó** el like, que suele ser el más revelador.
- **Captura** (`events_raw`, que por fin tiene escritores): `video_view` y `video_completed`, escritos **directo desde el cliente bajo RLS** (mismo patrón que `likes`/`saves`; es telemetría sin lógica de negocio, no amerita EF).
  - 🔴 **`playToEnd` NO SIRVE**: `VideoFeedItem` fija `p.loop = true` y con loop ese evento **no dispara**. La compleción se detecta por **`timeUpdate`** (umbral 0.95 de `duration`).
  - 🔴 **`p.timeUpdateEventInterval` debe ser > 0 o la feature está MUERTA**: con el valor por defecto (`0`) expo-video **no emite** `timeUpdate` y `video_completed` no se registra jamás. Hay un assert que lo fija — lo cazó el guardian borrando esa línea con la suite en verde.
  - 🔴 **Dedupe por (sesión, propiedad)**, obligatorio: el feed reproduce **en bucle**, así que sin dedupe un video olvidado en pantalla generaría cientos de filas e **inflaría las estadísticas con actividad falsa** (y quemaría cuota). `session_id` = un UUID por apertura de app (`lib/appSession.ts`); el store de dedupe vive a **nivel de módulo**, no en el componente, porque **FlashList recicla** las tarjetas.
  - ⚠️ **Bug que costó una ronda de guardian:** el listener de `timeUpdate` tenía deps `[player]`, y `player` es **estable por instancia** (el cambio de fuente al reciclar se hace con `replaceAsync`). Resultado: ver completo el video de Z registraba `video_completed` de **A**, y marcaba A como ya contada — falso positivo **y** falso negativo, silenciosos. Fix: `property.id` en las deps.
- **Lectura**: `public.get_lead_stats(p_lead_ids uuid[])` (`20260808000002`), **batch** (una llamada por pantalla, nunca N+1). `security definer` **porque las RLS de `likes`/`saves` solo dejan leer al propio usuario** — un agente jamás vería esas filas con `security_invoker`; la autorización real vive en el cuerpo vía `private.can_view_lead` (reusado).
  - 🔴 **`EXISTS`, nunca `JOIN`, en el gate del like**: `likes_user_video_unique` es `(user_id, property_video_id)`, así que un usuario **puede** likear 2 videos de la misma propiedad y el JOIN duplicaría `veces_visto` (medido: **4** donde lo correcto es **2**). Hay un assert que lo fija.
- **RLS de `events_raw`** (`20260808000001`): el usuario escribe **solo sus propios** eventos; lee quien puede gestionar la propiedad, vía **`private.can_manage_property` reusado** — eso trae gratis la corrección cross-agencia de #100 en vez de repetir el error con un helper ad-hoc. Es **append-only**: `authenticated` no tiene grant de `update`/`delete`, y **hay 2 pins que lo custodian** (sin ellos, un `grant` de más dejaría al usuario **borrar la evidencia de su propio comportamiento**).
- ⚠️ **Privacidad:** esto registra comportamiento de video de todos los usuarios. La subtarea **75.3 (privacidad §19.2) sigue pendiente** — conviene que el consentimiento salga antes de que haya volumen real.

## Flujo (demo) — vivo (#14)
Contacto desde el **detalle** de una propiedad → CTA sticky **"Contactar por WhatsApp"** (`ContactAgentButton`) llama la **Edge Function `contact-agent`**, que: valida (JWT, propertyId UUID) → resuelve propiedad+agente → corta self-contact → crea/recupera el lead (idempotente por par) → inserta `lead_origin_properties` → incrementa `contact_count` **solo si el origin fue nuevo** → devuelve `{ success, phone, message, lead_id, property_id }`. El cliente abre el **deep link** `whatsapp://send?phone=&text=` (fallback `wa.me`) y confirma con `Alert` nativo.

## Pantalla CRM del agente — vivo (#15, ampliada en #75.6)
El agente abre el **tab CRM** (oculto a no-agentes vía `user.role === 'agent'` + `<Redirect>` en la ruta — RLS/EF son la capa real de seguridad) y ve la **lista de sus leads** (`useAgentLeads`: query `from('leads')` con embeds `users!leads_user_id_fkey(first_name, last_name, avatar_url, phone)` (identidad del buscador desde `users`, #30) + `lead_origin_properties→properties→property_videos`; RLS filtra `agent_id`; `.is('deleted_at', null)`). **Orden por PUNTAJE** (#75.6): dos `.order()` encadenados — primario `score` DESC (o `last_contact_at` DESC con `nullsFirst:false` en modo `last_contact`), desempate **siempre** `updated_at` DESC. Cada **`LeadCard`** muestra avatar+nombre, **badge de estado**, la **barra de acciones** (#112 — reemplazó a la pill de nivel + puntaje), thumbnail de la propiedad de origen y tiempo relativo. **Filtros** (tabs: Todos / Nuevos / En progreso / Cerrados — cada grupo incluye su equivalente **legacy** para que un lead viejo no caiga en el grupo equivocado) y **búsqueda por nombre** siguen **client-side** (`useMemo`, sin re-query). Arriba, **sección fija "En seguimiento"** (`is_follow_up`): independiente del tab de estado, sí respeta el buscador, y la lista de abajo **la deduplica** (cada lead aparece en un solo lugar). Tap en un card → **`LeadExpandedView`** (Modal bottom-sheet RN nativo) con: **selector de los 8 estados vigentes** (todos elegibles — el backend ya no impone transiciones), **toggle de bookmark "en seguimiento"**, **textarea de notas internas**, **sección Historial** (timeline de `useLeadStatusHistory`, más reciente primero), botón **"Ver propiedad"** y botón **WhatsApp**. Al elegir un estado, `useUpdateLeadStatus` llama la **EF `update-lead-status`**; en éxito → `refetch` + cierra el modal.

⭐ **Errores en español, no `error.message` crudo (#75.6).** Los hooks del CRM mostraban `error.message`, que ante **cualquier** 4xx de una Edge Function es literalmente *"Edge Function returned a non-2xx status code"* — el tercer defecto reportado por el usuario. Fix: `mobile/src/features/leads/lead_error_messages.ts` (mapa código→español, patrón de `ContactAgentButton`) cableado con **`extract_error_code`** — el helper de `lib/supabase/edge-errors.ts` que auth/registration/upgrade/agency/publish **ya usaban** y el CRM se había saltado. `useAgentLeads`/`useLeadStatusHistory` degradan igual a un mensaje neutro en vez del texto crudo de PostgREST.

## Reglas / gotchas
- **EF es `contact-agent/`** (no `leads/` como decía el PRD). Desplegada al remoto con `--import-map` (gotcha de [[mapa-codebase]]). Auth obligatoria (401 sin JWT).
- **Idempotencia en 2 capas:** índice único parcial en `leads` (par activo) + `ON CONFLICT (lead_id,property_id) DO NOTHING` en `lead_origin_properties`. El `contact_count` cuenta **contactos únicos lead↔propiedad, no taps** (increment solo si el origin fue fila nueva). Race de insert lead → 23505 → recuperar el existente, no 500.
- **Self-contact** (`caller == owner`) → 400 `CANNOT_CONTACT_SELF` (respalda el CHECK `agent_id ≠ user_id`). `agent_id ≡ owner_user_id` (alias sintético del resolver: `u.id AS agent_id ON owner_user_id = u.id`).
- **Teléfono obligatorio:** agente sin `users.phone` → 400 `AGENT_PHONE_MISSING`; el CTA se oculta si ya se sabe que no hay phone. Los agentes demo se sembraron con un **placeholder** (`+523312345678`) en migración `20260630000001` (#14.8); los **owners demo Ramos/Vlad** recibieron sus números **reales** en `20260709000001_set_demo_owner_phones` (#48) para que el WhatsApp de la demo llegue de verdad. ⚠️ **Causa raíz sin cerrar:** el flujo de registro/onboarding **nunca captura teléfono** → toda cuenta creada por `/register` nace con `phone NULL` y sin CTA (la captura en registro es #47, **deferida** por decisión de producto).
- **`increment_contact_count` es read-then-write** (2 queries, no atómico) — `// ponytail:` con RPC como alternativa para prod (PostgREST no admite `col = col + 1` en PATCH).
- ~~Sin scoring automático~~ → **scoring vivo desde #75.2** (ver sección arriba).
- **EF `update-lead-status` (#15.6, desplegada 2026-06-30 · reescrita en #75.1)** — POST `{lead_id, new_status, note?}` (JWT del agente). Valida que `new_status` ∈ enum (allowlist de los **11** valores; los 4 nuevos incluidos — su ausencia era el bug del usuario reencarnado, lo cazó el guardian) y **ya NO valida transición**; persiste `status`+`updated_at`, y `internal_notes` **solo si viene `note`** (spread condicional, no sobrescribe con null). Ownership por doble query (`LEAD_NOT_FOUND` vs `UNAUTHORIZED_AGENT`). Códigos: `INVALID_INPUT`/`UNAUTHENTICATED`/`UNAUTHORIZED_AGENT`/`LEAD_NOT_FOUND`/`DB_ERROR`. Patrón DI clonado de `update-property-status/`. Deploy con `--import-map`.
- **`contact-agent` crea el lead en `whatsapp_opened`** (#75.1, antes `new`). De paso se extrajo **`lead_repo.ts`**: el insert vivía **inline en el closure de `Deno.serve`** en `index.ts` y no era testeable — se movió, no se reescribió.
- ✅ **Editar la nota SIN cambiar estado — vivo (#29)** — el acoplamiento nota↔transición de #15 se resolvió con una EF dedicada `update-lead-note` (`{lead_id, note}` → edita solo `internal_notes`+`updated_at`, **nunca `status`**). En `LeadExpandedView` hay un botón **"Guardar nota"** (aparece solo si la nota cambió) que persiste vía el hook `useUpdateLeadNote`, independiente del selector de estado. El cambio de estado SIGUE mandando la nota (retrocompat). `note=""` limpia la nota a `null`. Detalle abajo.
- ⭐ **`is_follow_up` viaja por `update-lead-note`, NO por una 4ª EF (#75.5).** Ambos campos son opcionales pero **al menos uno** debe venir; el UPDATE es spread condicional con `!== undefined` en los 3 puntos del flujo — `is_follow_up=false` es una desactivación EXPLÍCITA, y el bug clásico sería un `if (value)` que la pierde por falsy. Aditivo: el contrato v1.0.3 (solo `note`) sigue funcionando idéntico.
- **Filtros y búsqueda son client-side** (la lista completa del agente se carga una vez por `useAgentLeads`; tabs+search filtran con `useMemo`, no re-query). Grupos (#75.1, cada uno con su legacy): "Nuevos" = `{whatsapp_opened, new}`; "En progreso" = `{contacted, interested, visit_scheduled, in_progress}`; "Cerrados" = `{closed_won_rent, closed_won_sale, closed_lost, discarded, closed_won}`. ⚠️ Sin esta realineación **todo lead nuevo caía en el tab Cerrados** — por eso el cliente se tocó en la MISMA subtarea que la migración, para que nunca existiera una ventana con los estados rotos.
- **Owner ve los leads de sus agentes — vivo (#28)** — el dueño de la inmobiliaria (`agency_members.member_role='owner'`) ve un **selector de agentes** encima de la lista; al elegir un agente filtra a sus leads, "Todos" muestra el pipeline agregado de toda la agencia. **Sin backend nuevo:** la policy RLS `leads_select` + helper `private.is_agency_owner_of(agent_id)` (migración 0010) ya lo permiten a nivel DB; #28 solo añade cliente. El rol de agencia se detecta con `agency_members.member_role` (NUNCA `users.role`, que es 'agent' incluso para owners). El owner NO aparece como chip propio; sus leads salen bajo "Todos".
- ⭐ **El ADMIN de inmobiliaria también ve el pipeline — vivo (#75.5).** El rol `admin` (Ola 1, #71) quedaba como agente raso porque el único helper de "gestor de agencia" era `private.is_agency_owner_of` (solo `member_role='owner'`). Migración `20260807000005`: **`private.is_agency_admin_of`** (calco exacto con `member_role='admin'`; exige que **ambos** estén `active` en la MISMA agencia — precedente #100) + cláusula OR en los **3 puntos de LECTURA**: `leads_select`, `private.can_view_lead`, `private.can_view_user_as_lead_searcher`. 🔴 **Solo amplía SELECT: `leads_update` NO se tocó** (2 asserts pgTAP lo custodian) — la escritura sigue siendo del agente dueño y extenderla es la tarea **#31**, diferida. En cliente, `useAgencyRole` pasó a los 4 roles y expone **`canViewTeam = isOwner || isAdmin`** (+ un `error` booleano que distingue "falló la query" de "no hay membresía"); `CRMScreen` lo consume en vez de `isOwner`.
- ⚠️ **La vista owner es SOLO LECTURA para leads ajenos (fix #28).** El owner puede VER los leads del equipo (RLS `is_agency_owner_of`) pero la capa de **escritura** solo autoriza al agente dueño: la EF `update-lead-status` filtra `.eq('agent_id', caller)` → devuelve `UNAUTHORIZED_AGENT` (403) al owner (así que el owner no puede cambiar NINGÚN estado). **Sigue vivo tras #75.5** — la ampliación al admin fue solo de lectura, y se verificó en el remoto con JWT real. Arreglado en cliente: `LeadExpandedView` recibe `readOnly` (calculado en `CRMScreen` como `lead.agent_id !== user.id`); en solo-lectura muestra el estado actual como badge no-editable + pista "Solo lectura · este lead pertenece a otro agente" y notas deshabilitadas (Ver propiedad / WhatsApp siguen activos). La seguridad real la imponen EF+RLS; `readOnly` es solo UX para no ofrecer una escritura que fallaría. **Que el owner GESTIONE (edite) los leads del equipo es decisión de producto pendiente → tarea #31** (extender ownership de la EF + policy RLS de UPDATE con `is_agency_owner_of`; TDD backend).
- ✅ **E2E happy-path verificado en remoto (2026-06-30):** con un 2º agente sembrado (propiedad activa propia), el agente principal la contactó → lead `new` creado; 2º tap → mismo `lead_id`, `contact_count=1` (no 2). Antes solo se cubría por los 79 unit tests + negativos, porque el único `auth.users` era dueño de todas las propiedades (todo daba self-contact).
- Visibilidad por RLS: helpers `can_view_lead` / `can_edit_lead` ([[rls-seguridad]]).
- ⚠️ Existe un 2º camino de WhatsApp **sin CRM**: el icono de `AgentCard` (`open_whatsapp`) es contacto rápido directo; el canal CRM es el CTA sticky.

## En el código
- Backend: `0006_engagement_crm.sql` + EF `supabase/functions/contact-agent/` (`index.ts` cablea resolvers reales; `handler.ts` lógica pura con DI; `handler.test.ts` **79 tests**). Seed phone `20260630000001_seed_demo_agent_phone.sql`.
- App contacto: `mobile/src/components/ContactAgentButton.tsx` (reusa `PrimaryButton`, mapea errores ES) + `mobile/src/features/property-detail/utils/whatsapp.ts` (`open_whatsapp_ef`) cableado en `PropertyDetailScreen.tsx`.
- App CRM (#15, vivo): `mobile/src/features/leads/` — `screens/CRMScreen.tsx` (lista + tabs + búsqueda + modal), `hooks/useAgentLeads.ts` (query+transform, 10 tests), `hooks/useUpdateLeadStatus.ts` (**CRÍTICA**, invoca la EF, 13 tests, patrón `usePropertyActions`), `components/LeadCard.tsx`, `components/LeadExpandedView.tsx`, `lead_status_meta.ts` (mapa compartido estado→etiqueta/color), `types.ts` (`AgentLead`/`LeadStatus`/`Agent`). Ruta: `mobile/app/(protected)/(tabs)/crm.tsx` + tab en `_layout.tsx`. Genérico reutilizado: `mobile/src/components/FilterTabs.tsx` (extraído de profile). Backend CRM: `supabase/functions/update-lead-status/` (handler DI + `make_lead_status_updater`, 55 Deno tests).
- Owner-view solo-lectura (fix #28): `components/LeadExpandedView.tsx` (prop `readOnly`, default false → agente sin cambios) + `screens/CRMScreen.tsx` (`readOnly={selected_lead.agent_id !== user?.id}` vía `useAuth`). Verificado en vivo: el owner abre un lead de Miguel → badge de estado + "Solo lectura", sin selector editable, sin error non-2xx. Gestión editable por el owner → tarea #31.
- Owner-view de la agencia (#28, vivo): `hooks/useAgencyRole.ts` (**CRÍTICA**, 6 tests — detecta owner por `agency_members`), `hooks/useAgencyAgents.ts` (**CRÍTICA**, 8 tests — lista de agentes vía embed `agency_members→users(first_name,last_name,avatar_url)`; ⚠️ nombre/foto de `users`, NO `user_preferences` (el owner no puede leer el user_preferences ajeno por RLS); solo ejecuta si owner), `hooks/useAgentLeads.ts` extendido a `(agentId?)` (**15 tests**; `.eq('agent_id',id)` condicional; embed del buscador con FK explícita `users!leads_user_id_fkey` — fix del "Could not embed" por doble FK leads→users), `components/AgentSelector.tsx` (rail de chips, no crítico). Todo en `CRMScreen.tsx`. Verificado en vivo en el emulador (selector con nombres reales + filtrado por agente). Suite leads: 42/42, tsc 0.

- Identidad del BUSCADOR del lead (#30, vivo): el "Usuario sin nombre" del CRM se corrigió con una **política RLS**, NO denormalizando. Helper `private.can_view_user_as_lead_searcher(user_id)` (migración `20260702000001`, SECURITY DEFINER, patrón `can_view_lead`): true si existe un lead ACTIVO (`deleted_at is null`) donde ese `user_id` es el buscador y quien pregunta es el agente dueño (`agent_id=auth.uid()`) o el owner de su agencia (`is_agency_owner_of(agent_id)`). Se añade como cláusula OR a `users_select`; **`user_prefs_select` NO se toca**. Decisión de diseño: la identidad del buscador se lee SIEMPRE desde `users` (first_name/last_name/avatar_url/phone), consistente con #28 — por eso `useAgentLeads` cambió el embed a `users!leads_user_id_fkey(...)` con `build_full_name` (util compartido extraído de `useAgencyAgents`). pgTAP `08_rls_lead_searcher_test.sql` (plan 12) + jest 26/26 + guardian PASS. Desplegada al remoto vía `apply_migration`; verificada bajo RLS (helper true/false por impersonación).

- Guardar nota independiente del estado (#29, vivo): backend **EF `supabase/functions/update-lead-note/`** — mirror de `update-lead-status/` **sin** validación de transición. `make_note_updater(client)`: doble query de ownership (`.select('id').eq('id').eq('agent_id').maybeSingle()` → si vacío, 2ª sin `agent_id` para distinguir `UNAUTHORIZED_AGENT` de `LEAD_NOT_FOUND`), luego `UPDATE {internal_notes: note||null, updated_at}` filtrado `.eq('id').eq('agent_id')` con `.select('id,internal_notes').maybeSingle()`. **El UPDATE nunca incluye `status`** (invariante NU-11). Ownership SOLO `agent_id`/admin — consistente con `private.can_edit_lead`, **NO** `is_agency_owner_of` (los owners NO editan leads; sigue solo-lectura, ver #28). `note=""` → 200 y limpia a `null` (el handler distingue "key ausente"→400 de "string vacío"→200, invariante EC-8). Códigos `INVALID_INPUT`/`UNAUTHENTICATED`/`UNAUTHORIZED_AGENT`/`LEAD_NOT_FOUND`/`DB_ERROR`. **24 Deno tests** (13 handler + 11 note_updater), guardian PASS (orden real supabase-js, sin repetir el bug de cadena ficticia de #23). Cliente: `hooks/useUpdateLeadNote.ts` (**CRÍTICA**, mirror de `useUpdateLeadStatus`, 13 tests, bloqueo síncrono de doble-submit por `is_working_ref`, guardian PASS) + botón "Guardar nota" en `LeadExpandedView` (pill `colors.primary`, visible solo si la nota difiere, deshabilitado durante guardado, error inline separado, **respeta `readOnly`** del owner). ⚠️ el GREEN tocó `mobile/jest.setup.js` (fix de drenado `act()` FIFO→LIFO por contaminación cruzada entre tests — guardian verificó empíricamente que NO enmascara bug del hook: EC-10 pasa igual con el setup viejo, el bloqueo lo hace el hook). Suite completa 467/467. **EF desplegada al remoto** (urbea-app `mvpvqmyhrrkwbnpctpuq`, 2026-07-03, `--import-map`; smoke OPTIONS→200 / POST sin auth→401). Deuda menor: `#FFFFFF` hardcodeado en el botón (no hay token `on_primary` en theme.ts → tarea de theming registrada).

- **Tramo urgente del CRM (#75.1/75.2/75.5/75.6, vivo · 2026-08-07):** backend `supabase/migrations/20260807000002_lead_status_reconcile_enum.sql` (enum, sola) · `…000003_lead_status_reconcile.sql` (data-fix + `is_follow_up` + `lead_status_history` + trigger + RLS) · `…000004_lead_scoring.sql` (enum `lead_temperature`, `score`/`level`, `private.compute_lead_level`/`init_lead_score`/`adjust_lead_score`/`recompute_lead_levels`, backfill) · `…000005_lead_admin_visibility.sql` (`private.is_agency_admin_of` + OR en los 3 puntos de lectura), **los 4 con rollback**. pgTAP `28_lead_status_reconcile_test.sql`, `29_lead_scoring_test.sql`, `30_leads_admin_visibility_test.sql`. EFs: `update-lead-status` (sin `VALID_TRANSITIONS`), `update-lead-note` (+`is_follow_up`), `contact-agent` (+`lead_repo.ts`, nace en `whatsapp_opened`). Cliente `mobile/src/features/leads/`: `lead_error_messages.ts` (nuevo), `hooks/useLeadStatusHistory.ts` (nuevo), `utils/relative_time.ts` (extraído, compartido card+timeline), `hooks/useAgencyRole.ts` (4 roles + `canViewTeam` + `error`), `hooks/useAgentLeads.ts` (score/level/is_follow_up + orden por puntaje), `lead_status_meta.ts` (+`LEVEL_META`, `ALL_LEAD_STATUSES` recortado a los 8 vigentes), `types.ts`, `components/LeadCard.tsx`, `components/LeadExpandedView.tsx`, `screens/CRMScreen.tsx`; y `features/profile/hooks/useAgentStats.ts` (cuenta los dos `closed_won_*`). De paso se **regeneró `supabase/types/database.types.ts`** contra el stack local, **saldando el drift que arrastraba desde la #71** (`agency_member_role`/`status` + 3 RPCs).
- **Verificación (#75, 2026-08-07):** pgTAP 31 archivos/680 asserts · Deno 795 · Jest 881 · tsc y lint limpios. **Guardian FAIL en la 1ª ronda** — dos comportamientos sobrevivían a su mutante (el allowlist de entrada de la EF y el guard de no-op del trigger); se cerró con tests **verificados aplicando el mutante**, viendo el rojo y revirtiendo. ⚠️ **E2E de UI NO se pudo hacer**: el emulador quedó bloqueado por el muro de ubicación (→ **#109**). En su lugar se verificó contra el **REMOTO con JWT real**: reabrir un lead cerrado (`closed_lost→interested`) da **200** donde antes era 400 `INVALID_TRANSITION`; salto a estado nuevo (`interested→closed_won_rent`) 200; `is_follow_up` 200; un estado inventado se rechaza con `INVALID_INPUT`; el timeline registró ambos cambios con old/new correctos; y el **owner de la agencia VE pero NO puede editar** el lead de su compañero (403). Datos del cliente restaurados.
- **Deuda derivada:** **#108** `hardening(75.2)` — `score`/`level` escribibles por REST + `changed_by` siempre NULL · **#109** `hardening(75.6)` — el `LocationWall` cubre TODA ruta protegida: bloquea el E2E **y** deja sin CRM (ni perfil) a quien tenga el GPS apagado, aunque esas pantallas no usen ubicación.

## Detalle exhaustivo
- `docs/PRD.md` §19 (§19.6 scoring, §19.8 estados, §19.9 CRM visual; lo que sigue fuera: §19.2 privacidad #75.3, unificación #75.4, export CSV + retención #75.7) · migraciones `0006` + `20260807000002`–`000005` · [[db-schema-map]]

## Relacionados
[[feed-vertical-video]] · [[propiedades-y-video]] · [[inmobiliarias-y-agentes]] · [[rls-seguridad]]

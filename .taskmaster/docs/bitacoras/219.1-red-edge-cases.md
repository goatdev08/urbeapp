# 219.1 — RED (pgTAP): 4 escritores admin + purga 30d

Archivo: `supabase/tests/71_notify_admin_events_test.sql` (70 asserts).
SUT (aún no existe, GREEN pendiente): 4 triggers de escritura hacia
`public.notifications` (ads draft→pending_review, agencies→pending_approval,
agent_applications→pending, property_revisions pending por INSERT y por
re-envío needs_changes→pending) + `public.purge_notifications()` (30 días) +
job pg_cron `purge_notifications_daily`.

No se escribió migración ni stub — el patrón del repo (63/48/37_*) permite RED
sin stub porque las tablas fuente ya existen; un INSERT/UPDATE crudo contra
ellas no lanza (no referencia ningún objeto inexistente), solo no dispara
ningún trigger nuevo, así que las aserciones de conteo fallan limpio contra 0
filas reales. La única llamada a un objeto inexistente
(`public.purge_notifications()`) va en un bloque `do $$ ... exception when
others ... $$` auto-protegido.

## SEAMS (interfaz bajo test)

- Comportamiento OBSERVABLE de un INSERT/UPDATE real sobre
  `ads`/`agencies`/`agent_applications`/`property_revisions` — las filas (o
  ausencia de filas) que deja en `public.notifications`. Nunca se valida el
  nombre de las funciones/triggers de escritura por catálogo.
- El contrato público invocable de `public.purge_notifications()` (firma,
  security definer, search_path vacío) — mismo criterio que
  `public.purge_ad_impressions` (20260817000002), SÍ es un seam legítimo por
  catálogo porque es una función explícitamente diseñada para invocarse
  (cron/manual).
- El invariante de ESQUEMA del índice único parcial de idempotencia de los 3
  eventos de disparo único (ejercitado con INSERT crudo directo contra
  `notifications`, mismo espíritu que `01_constraints_test.sql`).
- Registro en `cron.job` (jobname/schedule/command exactos).

## EDGE CASES (RED)

### Happy path
- Por cada uno de los 4 eventos (ads draft→pending_review, agencies→
  pending_approval, agent_applications→pending, property_revisions→pending):
  N=2 admins de plataforma (`public.users.role='admin'`) reciben EXACTAMENTE
  1 fila cada uno (array_agg exacto, no solo conteo); ni un usuario regular
  ni un agente reciben nada.
- Contrato de la fila por evento: `type` (catálogo PRD §22.4), `deep_link`
  (`/admin/ads` · `/admin/revisions` · `/admin/requests` ×2), `related_
  entity_type`/`_id`, `data->>'ad_title'|'agency_name'|'application_type'|
  'address'` (D-KEY fijada por el test-author, sin colisión con columnas de
  la fila), `title` no nulo, `body` menciona el dato esencial (título del
  anuncio / nombre de la inmobiliaria / dirección de la propiedad).

### Edge cases del PRD (§22.4)
- Catálogo v1 exacto: `admin_ad_pending`, `admin_revision_pending`,
  `admin_agent_application`, `admin_agency_pending` con sus deep links
  literales de la tabla del addendum 22.4.

### Ramas no obvias (invariantes 🔒 y decisiones de Abraham 2026-08-25)
- 0 admins en toda la base: el evento fuente (INSERT de agencies) NO lanza
  excepción, 0 avisos generados.
- Ads: SOLO `draft→pending_review` dispara `admin_ad_pending`. La democión de
  SISTEMA `active→pending_review` (#192, editar descripción de un ad activo)
  NO dispara — contrato explícito del orquestador ("otras transiciones de ads
  NO disparan").
- Agencies: SOLO nacer en `pending_approval` dispara. Un INSERT directo con
  `status='active'` (bypaseando la cola) NO dispara.
- Agent_applications: SOLO nacer en `pending` dispara. Un INSERT directo con
  `status='approved'` NO dispara.
- Property_revisions: nace `pending` por INSERT (dispara) Y re-entra por
  UPDATE `needs_changes→pending` (TAMBIÉN dispara, DECISIÓN: SÍ genera avisos
  NUEVOS, nunca deduplicados — el conteo para la MISMA revisión crece 2→4→6
  en 2 re-envíos sucesivos, no un artefacto del primero). `pending→needs_
  changes` (revisión del admin) NO dispara. Un INSERT directo en
  `needs_changes` NO dispara la rama de alta. `needs_changes→rejected` (sale
  del ciclo) NO dispara.
- D-ANCLA (índice único parcial): para los 3 eventos de disparo único, un
  INSERT crudo duplicado con la misma llave (`user_id`, `related_entity_id`,
  `type`) debe ser RECHAZADO (23505) por el índice que la migración GREEN
  debe crear; un aviso ya BORRADO (`deleted_at` no nulo) con la misma llave
  SIGUE bloqueando un tercer duplicado — el índice no filtra por
  `deleted_at` (misma decisión de producto ya tomada en
  `20260822000001_notify_ads_expiring_soon.sql` para `ad_expiring_soon`,
  aplicada aquí igual).
- 🔒 Semántica BLOQUEANTE (decisión de Abraham 2026-08-25): el INSERT hacia
  `notifications` vive en la MISMA transacción del evento SIN try/catch — si
  el escritor truena, el evento entero se aborta. Ejercitado con
  fault-injection (trigger "veneno" BEFORE INSERT en `public.notifications`,
  dropeado inmediatamente tras los asserts que lo necesitan, patrón exacto de
  `48_ads_state_machine_test.sql` sección 4) en 2 caminos representativos:
  INSERT (agencies — la agencia no queda creada) y UPDATE (ads — el ad no
  queda movido, y tampoco queda huérfana la fila que el BEFORE trigger de la
  máquina de estados ya había escrito en `admin_actions`, confirmando
  atomicidad real de statement). No se repite ×4 porque los 4 escritores
  comparten el mismo mecanismo (un INSERT hacia la misma tabla, en la misma
  transacción) — probar el mecanismo una vez por estilo de disparo (INSERT
  vs UPDATE) alcanza para discriminar la propiedad "sin try/catch".

### Boundary / error (purga 30 días)
- Frontera `<` estricta sobre `created_at` (mismo criterio que
  `purge_ad_impressions`/90d): exactamente 30 días se CONSERVA, 31 días se
  BORRA.
- La retención NO depende de `read_at` (una fila de 31 días leída también se
  borra) ni de `deleted_at` (una fila ya borrada por el usuario a los 40 días
  también se purga físicamente — el comment de la tabla
  (`20260604000007:70`) promete 30 días de retención, no de "retención tras
  que alguien la borre").
- `public.purge_notifications()`: catálogo exacto (sin argumentos, `returns
  void`, `security definer`, `search_path=''`) — patrón EXACTO de
  `purge_ad_impressions`.
- `cron.job`: 1 fila con `jobname='purge_notifications_daily'`,
  `schedule='0 11 * * *'` (horario DISTINTO de `rollup_ad_impressions_
  monthly_daily` 0 8, `purge_ad_impressions_daily` 0 9 y
  `notify_ads_expiring_soon_daily` 0 15 UTC — no competir I/O),
  `command='select public.purge_notifications();'`.

### Convención DELTA vs INVARIANTE (documentada en la cabecera del archivo)
16 de los 70 asserts ya "pasan" hoy por una razón distinta a la que debe
sostenerlos tras el GREEN (0 admins→0 filas; nadie más recibe; una transición
fuera de alcance no dispara; PURGE3/PURGE4 sobreviven porque hoy nada se
borra). El guardian debe re-verificar tras GREEN que siguen en verde por la
razón correcta.

## Salida REAL del runner (RED, 2026-08-25)

Comando: `supabase test db supabase/tests/71_notify_admin_events_test.sql --local`

```
Failed 54/70 subtests

Test Summary Report
-------------------
/Users/fru/Dev/urbea/supabase/tests/71_notify_admin_events_test.sql (Wstat: 0 Tests: 70 Failed: 54)
  Failed tests:  5, 7-13, 16, 18-23, 25, 27-32, 34, 36-44
                47-67, 70
Files=1, Tests=70,  0 wallclock secs ( 0.01 usr +  0.00 sys =  0.01 CPU)
Result: FAIL
```

54/70 fallan por assert real (DELTA — NULL donde se esperaba un valor, 0
donde se esperaban N filas, "no exception" donde se esperaba 23505). 16/70
pasan hoy como INVARIANTE (documentados arriba y en la cabecera del archivo).
`Wstat: 0` confirma que el archivo corrió limpio hasta el final — ningún
fallo es por error de sintaxis/catálogo del runner.

HECHO 217.4 — verificación, smoke e ingest de cierre (orquestador directo).

VERIFICACIÓN COMPLETA (salidas reales):
- `cd mobile && pnpm tsc --noEmit` → sin salida, exit 0.
- `pnpm lint` → exit 0 ("0 errors and 67 warnings potentially fixable" — todos preexistentes, ninguno en archivos de #217).
- `pnpm test` (suite COMPLETA) → `Test Suites: 123 passed, 123 total · Tests: 1588 passed, 1588 total · Time: 5.563 s`.

SMOKE EN EMULADOR (adb, sesión admin.ads persistida, stack local):
- Gotcha operativo: el Metro de 6h42m (PID 96104) dejaba al dev client en spinner infinito (>4 min) — mismo patrón del Metro rancio de la mañana. Fix: kill + `pnpm expo start --dev-client --port 8081` fresco + `adb reverse` 8081/54321 + relanzar. Con Metro fresco el bundle cargó en ~45 s.
- La app aterrizó en el feed (video) → navegada de inmediato al tab Perfil (regla de cuota Stream).
- ✅ 217.1: menú ⋮ muestra «Panel de administrador» (ícono escudo, entre «Mis publicaciones» y «Cerrar sesión») y navega a /admin. Capturas: scratchpad/smoke_menu.png.
- ✅ 217.2: home del panel muestra sección «COLAS» con las 5 filas (Anuncios por revisar › / Revisiones de ediciones / Reportes / Solicitudes de agente / Inmobiliarias por aprobar); badges ocultos (colas del stack local en 0 — semántica badge-solo-si->0), sin banner de error, la lista de agencias intacta debajo. Solo la fila de Anuncios navega → /admin/ads (tabs Revisión/Activos, estado vacío «No hay anuncios esperando revisión.»). Capturas: smoke_admin.png, smoke_ads.png.
- App estacionada en el panel admin (fuera del feed) al terminar.

INGEST AL VAULT:
- `wiki/codebase/mapa-codebase.md` → bullet de `app/admin/` extendido con #217 M0 (entrada ⋮, useAdminQueueCounts, alcance de navegación) → [[panel-admin]].
- `wiki/log.md` → entrada `## [2026-08-25] tarea | #217 M0`.
- (217.3 ya había creado [[panel-admin]] y corregido [[moderacion]]/[[propiedades-y-video]]; `estado-actual.md` ya tenía la corrección de la referencia #141.)

GATE DE PRODUCCIÓN VIVA (§0.5): ✅ sin migraciones (cero cambios en supabase/), sin contratos tocados (solo lecturas vía policies RLS ya publicadas), sin seeds al remoto. Todo el cambio es JS/UI → viaja por OTA. Nada que desplegar en backend.

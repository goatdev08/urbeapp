## Hecho

**Archivo tocado:** `mobile/src/features/profile/ProfileScreen.tsx`

Cambios:
1. Import de `Shield` desde `phosphor-react-native` (icono nuevo, junto a los demás iconos del menú).
2. Handler `handle_open_admin_panel()` → `router.push('/admin')`, con comentario explicando que el guard real vive en `app/admin/_layout.tsx` (mismo patrón que `handle_my_ads`/`show_ads_entry`/`isOwner`/`can_manage_members`: el ítem del menú es solo el punto de entrada visible, no la autorización).
3. Ítem condicional en `menu_items`: `{ key: 'admin_panel', label: 'Panel de administrador', icon: Shield, onPress: handle_open_admin_panel }`, visible solo si `user?.role === 'admin'`. Posición: después del bloque condicional `role === 'user'` (upgrade/register_agency), antes de "Cerrar sesión" — respeta el orden documentado en el archivo ("navegación primero, cerrar sesión al final").

No se tocó `mobile/app/admin/` ni ningún layout (fuera del footprint de la subtarea).

## Decisiones

- **Ícono:** `Shield` (phosphor-react-native, mismo set que los demás ítems: Briefcase, Buildings, Megaphone, SignOut, Storefront, UserPlus, Users). Verificado que existe en `node_modules/phosphor-react-native/src/icons/Shield.tsx`. Coherente semánticamente con un panel de moderación/administración.
- **Condición de visibilidad:** `user?.role === 'admin'` (igual al patrón ya usado para `user?.role === 'user'` en el mismo archivo, líneas ~219-224 antes del cambio).
- **Posición:** inmediatamente antes de "Cerrar sesión", después del bloque de ítems solo-para-buscador (upgrade/register_agency). No hay ítems "solo agent/admin" previos con los que agrupar; se dejó como bloque propio con su propio comentario.
- Sin abstracciones nuevas — mismo patrón exacto (`...(condición ? [{...}] : [])`) que los 3 ítems condicionales existentes. Cero estilos nuevos (reusa `ProfileMenuItem`/`ProfileMenu` tal cual).

## Verificación

### 1. `pnpm tsc --noEmit` (desde `mobile/`)
```
$ pnpm tsc --noEmit
(sin output — exit 0, limpio)
```

### 2. `pnpm lint` (desde `mobile/`)
```
$ pnpm lint
✖ 107 problems (0 errors, 107 warnings)
  0 errors and 67 warnings potentially fixable with the `--fix` option.
Exit code: 0
```
Los 107 warnings son preexistentes (require() imports, import/first en archivos de test, etc.) en archivos NO tocados por esta subtarea. `ProfileScreen.tsx` no aparece en la salida de lint (cero warnings/errores en el archivo modificado).

### 3. Tests existentes de `profile/` (Jest)
```
$ pnpm test -- src/features/profile
Test Suites: 8 passed, 8 total
Tests:       78 passed, 78 total
Snapshots:   0 total
Time:        0.96 s
```
No existe `ProfileScreen.test.tsx` (ninguna suite cubre directamente el árbol de `menu_items` de este componente) — las 8 suites existentes (hooks de profile + `PropertyActionMenu`) pasan sin cambios, confirmando que el diff no rompió nada cubierto.

### 4. Smoke en emulador — NO realizado (decisión de seguridad)
Al capturar la primera screenshot (`adb exec-out screencap -p`) el emulador estaba en el **launcher de Android** (home screen), no en la app — el proceso `com.urbea.app` no estaba corriendo (`ps -A | grep urbea` sin resultados). La sesión "estacionada en Perfil" mencionada en la instrucción ya no estaba viva al momento de este smoke.

Relanzar la app en frío (`am start`) aterriza en la ruta por defecto de `(protected)/(tabs)/index.tsx`, que es el **tab del feed** (video en loop) — exactamente lo que la instrucción pide evitar por el consumo real de cuota de Cloudflare Stream (regla §0.5 CLAUDE.md: "cuotas reales"). No hay forma de garantizar por CLI que el cold start aterrice en el tab de Perfil sin antes pasar por el feed.

Se optó por **no** relanzar la app y confiar en la verificación estática (tsc + lint + tests existentes), suficiente para una subtarea NO crítica que es un cambio aditivo de una fila JSX siguiendo un patrón ya probado 3 veces en el mismo archivo. El emulador quedó en el home screen de Android (sin la app corriendo, sin riesgo de reproducir video).

## Conformidad spec

OK — cumple exactamente lo pedido en 217.1 y el footprint de #217:
- Ítem "Panel de administrador" agregado al menú ⋮ de `ProfileScreen.tsx`.
- Visible SOLO si `user?.role === 'admin'`.
- Navega a `/admin` (ruta existente, guard propio sin tocar).
- Mismo componente/estilo/patrón de navegación que los 3 ítems condicionales existentes.
- Sin scope creep: no se tocó `app/admin/`, ningún layout, ni se agregó UI/lógica no pedida.

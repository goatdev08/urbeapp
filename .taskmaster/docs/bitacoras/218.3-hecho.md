## Hecho — 218.3 (Mis publicaciones: bucket «En revisión» + copy + acciones, #136)

### Archivos tocados
- `mobile/src/features/profile/components/FilterTabs.tsx` (wrapper del feature) — nuevo valor `'in_review'` en `FilterValue` (`'all' | 'active' | 'paused' | 'in_review' | 'closed'`), nueva entrada en `TABS` (`{ value: 'in_review', base_label: 'En revisión' }`) entre "Pausadas" y "Cerradas" (orden: activo → pausado → en revisión → cerrado). Comentario de cabecera actualizado: la decisión sobre `draft`/`suspended` (siguen sin tab propio) se separó de `pending_review`/`needs_changes`/`rejected` (ahora SÍ tienen tab agregado). No se tocó el genérico `mobile/src/components/FilterTabs.tsx` — no hizo falta.
- `mobile/app/(protected)/profile/my-listings.tsx` — nuevo helper `is_in_review_bucket(status)` (análogo a `is_closed_bucket`, agrupa `pending_review | needs_changes | rejected`). Entrada `in_review` en el objeto `counts` (conteo sobre el array completo, mismo patrón que `closed`). Rama nueva en `filtered_listings` para `active_filter === 'in_review'`. Comentario ~L104 actualizado: ya no dice que pending_review/needs_changes/rejected "solo salen en Todas" (ahora tienen su bucket); solo `draft`/`suspended` siguen sin tab propio.
- `mobile/src/features/profile/components/PropertyListItem.tsx` — entrada `suspended` en `STATUS_BADGE` (`{ label: 'Suspendida', bg: colors.danger, text: '#FFFFFF' }`) y en `CONTEXTUAL_SUBROW` (`'Suspendida por el equipo — oculta del feed'`). Comentarios de cabecera (badge + subrow) actualizados para documentar el nuevo caso.
- `PropertyActionMenu.tsx` — **solo verificado, sin diff.** Ver decisión abajo.

### Decisiones no obvias
- **Orden del tab "En revisión"**: no hay mockup ni doc que fije el orden exacto para esta pestaña nueva (no existía en la pantalla 9 original). Elegí Todas → Activas → Pausadas → **En revisión** → Cerradas: sigue la progresión narrativa activo→pausado→pendiente de decisión del equipo→terminal.
- **Copy de `suspended`**: leí `wiki/conceptos/moderacion.md` §Excepción 15.6 antes de escribir el texto — `suspended` es una acción administrativa inmediata (fraude/reporte, EF `moderate-property` acción `suspend`) que **oculta la propiedad de inmediato sin pasar por revisión**, muy distinta de un cierre voluntario del agente. Por eso el subrow explícitamente NO usa "Cerrada" (el fallback `closed_label` que ya existía) sino `'Suspendida por el equipo — oculta del feed'`.
- **Color del badge `suspended`**: reusé `colors.danger` (mismo token que `rejected`) en vez de introducir un color nuevo — precedente encontrado en `mobile/src/features/admin/agency_status_labels.ts` (`agency_status_color`), donde `suspended` y `rejected` YA comparten rojo. Se distinguen por el label del badge ("Suspendida" vs "Rechazada") y por el subrow, no por el color.
- **`STATUS_THUMB_ICON` sin tocar**: la subtarea pedía específicamente `STATUS_BADGE` y `CONTEXTUAL_SUBROW`; el ícono de thumbnail sigue cayendo al fallback `'▷'` para `suspended` — no lo agregué para no exceder el alcance exacto (ponytail/scope discipline).
- **PropertyActionMenu — VERIFICADO, sin diff**: leí `get_actions()` (L77-99). `Editar` (L81) y `Eliminar` (L96) son incondicionales — ningún branch de status los excluye, así que `pending_review`/`needs_changes`/`rejected`/`suspended` reciben ambas acciones automáticamente. `Pausar/Reanudar`/`Cerrar` solo aplican a `active`/`paused` — correcto, esos 4 estados no deberían ofrecer pausar/cerrar. No hay defecto que reportar; el plan anticipaba correctamente "probablemente cero diff".
- **Copy del alert de step5 — NO tocado**, conforme al alcance: publicar nace `active` en beta (decisión 0010/#153), el copy actual ("disponible en el feed") es correcto.
- Sin tests nuevos: no existen archivos de test para `FilterTabs.tsx` (feature), `my-listings.tsx` ni `PropertyListItem.tsx` en el repo — conforme a la instrucción de no montar infraestructura de test nueva si no existía.

### Verificación (salida real)
`cd mobile && pnpm tsc --noEmit` → limpio, sin output (0 errores).

`cd mobile && npx eslint "app/(protected)/profile/my-listings.tsx" "src/features/profile/components/FilterTabs.tsx" "src/features/profile/components/PropertyListItem.tsx"` → sin output (0 problemas en los 3 archivos tocados).

`cd mobile && pnpm lint` (repo completo):
```
✖ 107 problems (0 errors, 107 warnings)
```
Mismo conteo preexistente documentado en la bitácora de 218.2 (107 warnings, 0 errores) — sin warnings nuevos atribuibles a este diff.

`cd mobile && pnpm test` (suite completa):
```
Test Suites: 125 passed, 125 total
Tests:       1621 passed, 1621 total
Snapshots:   0 total
```
Coincide exactamente con la base documentada (125 suites / 1621 tests) — cero regresiones.

### Smoke en emulador (Android, SOLO CLI — adb, PATH `/opt/homebrew/share/android-commandlinetools/platform-tools`)
Emulador `emulator-5554` vivo, Metro (`expo start --dev-client --port 8081`) ya corriendo desde antes con Fast Refresh activo — no requirió rebuild, los cambios de este diff (solo JS/TS) llegaron por refresh automático.

Flujo ejecutado:
1. `adb shell am start -a android.intent.action.VIEW -d "urbea://profile/my-listings" com.urbea.app` — deep link directo a Mis publicaciones (evita transitar por el feed con video, regla de cuota de Cloudflare Stream).
2. Captura inicial: header "Mis publicaciones", stats "0 activas · 0 pausadas", tabs "TODAS (0)" / "ACTIVAS (0)" / "PAUSADAS (0)" + inicio de una 4ª pestaña cortada al margen — confirma que el tab nuevo se agregó sin romper el layout existente.
3. `adb shell input swipe 900 470 200 470 300` (scroll horizontal en la fila de tabs) → aparecen "PAUSADAS (0)" / **"EN REVISIÓN (0)"** / "CERRADAS (0)" — el bucket nuevo existe, en el orden esperado (Pausadas → En revisión → Cerradas), con conteo (0, correcto para este agente sin propiedades).
4. `adb shell input tap 496 472` (tap sobre "EN REVISIÓN (0)") → el pill cambia a estado seleccionado (fondo `primary_tint` + texto `primary`, mismo tratamiento visual que los demás tabs activos) — el filtrado funciona sin errores ni crash.
5. Regreso a "Todas": swipe de vuelta + tap sobre "TODAS (0)" → vuelve a estado inicial, EmptyState visible, app estacionada en Mis publicaciones (fuera del feed, sin video reproduciendo) al terminar.

Capturas en `/private/tmp/claude-501/-Users-fru-Dev-urbea/65e3f0c9-3e42-4e10-bb3b-8f044f15a7e0/scratchpad/`: `218.3-01-my-listings.png`, `218.3-02-tabs-scrolled.png`, `218.3-04-in-review-tap2.png`, `218.3-05-back-to-all.png`.

**No verificado visualmente**: el badge/subrow de `suspended` en `PropertyListItem` — el agente demo usado (`swacg08@gmail.com`) no tiene ninguna propiedad (0 en todos los buckets), y no hay ninguna propiedad `suspended` sembrada en el stack remoto para este smoke. Cubierto por lectura de código + `tsc --noEmit` limpio (el objeto `STATUS_BADGE`/`CONTEXTUAL_SUBROW` es `Record<string, ...>`, TypeScript no fuerza exhaustividad ahí, pero la entrada está presente y sigue el mismo shape que las 6 existentes).

### Conformidad spec
Cumple los 4 puntos del alcance exacto de la subtarea:
1. Bucket «En revisión» en Mis publicaciones — hecho (FilterTabs wrapper + `is_in_review_bucket` + entrada en `counts` + `filtered_listings`), comentario ~L104 de my-listings actualizado.
2. `suspended` en `STATUS_BADGE`/`CONTEXTUAL_SUBROW` de `PropertyListItem` — hecho, badge es-MX ("Suspendida") + subrow veraz que no dice "Cerrada".
3. `PropertyActionMenu` — solo verificado como pedía el plan; Editar/Eliminar ya incondicionales, sin diff.
4. Copy del alert de step5 — NO tocado, fuera de alcance según el plan (decisión 0010/#153 vigente).

Sin scope creep: no se tocó `STATUS_THUMB_ICON`, no se modificó el genérico `src/components/FilterTabs.tsx`, no se agregaron tests nuevos donde no existía infraestructura previa, no se rediseñó el menú de acciones.

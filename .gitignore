# Dependencias
node_modules/
.pnp/
.pnp.js

# Expo / React Native
.expo/
.expo-shared/
dist/
web-build/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*

# Builds
build/
*.apk
*.aab
*.ipa

# Entornos / secretos
.env
.env.*
!.env.example

# Supabase local
supabase/.branches/
supabase/.temp/

# Logs
*.log
npm-debug.*
yarn-debug.*
yarn-error.*

# Sistema operativo
.DS_Store
Thumbs.db

# Editores
.idea/
*.swp
*.swo

# Cobertura / cache
coverage/
.cache/
*.tsbuildinfo

logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
dev-debug.log
# Dependency directories
# Environment variables
# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# OS specific

# Task files
# tasks.json
# tasks/ 

# Sentinel transitorio del ciclo TDD (tdd-guard) — nunca versionar
.taskmaster/.current-red

# Seeds/teardown de datos demo — LOCAL-ONLY, nunca a prod (memoria seed_demo_not_committed).
# seed-videos.sh (script, no datos) SÍ se versiona; solo se ignoran los .sql de datos.
supabase/scripts/seed-*.sql
supabase/scripts/teardown-*.sql

# Respaldo puntual de videos legacy (68.9) — LOCAL-ONLY, nunca a prod.
# backup-property-videos.sh (script) SÍ se versiona; los .mp4 descargados NO.
supabase/scripts/video-backups/

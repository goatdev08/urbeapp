/**
 * formatRadius.ts — STUB (fase RED, subtarea 281.1). Implementación pendiente.
 * Ver mobile/src/features/map/__tests__/formatRadius.test.ts para el contrato completo.
 */

export function format_radius_m(radius_m: number): string {
  void radius_m;
  return '';
}

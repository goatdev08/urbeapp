/**
 * useNotifications — STUB fase RED (subtarea 219.3). El contrato completo
 * (firma, cadenas de query exactas, semántica de mark_read/mark_all_read,
 * 27 edge cases) vive en
 * mobile/src/features/notifications/__tests__/useNotifications.test.tsx —
 * es el archivo que fija el comportamiento; este archivo NO lo implementa
 * todavía, solo declara la firma para que el import no rompa la suite.
 *
 * NO IMPLEMENTAR AQUÍ — fase GREEN pendiente.
 */

import type { UseNotificationsResult } from '../types';

export function useNotifications(): UseNotificationsResult {
  throw new Error('not_implemented: useNotifications (fase RED, 219.3)');
}

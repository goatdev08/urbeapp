/**
 * usePromoteProperty — FASE RED, stub. Sin lógica: lanza siempre para que
 * usePromoteProperty.test.ts falle por ASERCIÓN, no por import roto.
 */
export function usePromoteProperty(_deps?: unknown): unknown {
  throw new Error('usePromoteProperty: not implemented (RED)');
}

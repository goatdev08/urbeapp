/**
 * saved.tsx — ruta del tab "Guardados".
 *
 * Punto de entrada de Expo Router para la tab de propiedades guardadas.
 * Delega todo al componente de feature SavedScreen.
 */

import { SavedScreen } from '@/features/saved/SavedScreen';

export default SavedScreen;
